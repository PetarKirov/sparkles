name "tui"
description "Full-screen interactive terminal-UI library (scaffolding — the rendering core is pending the render-cost benchmark; see docs/specs/tui/)"
authors "Petar Kirov"
copyright "Copyright © 2026, Petar Kirov"
license "BSL-1.0"

targetPath "build"

// `../math/src` is import-only (all-template `Vector`, which `tui.geometry`
// specializes into `TermPosition`): a real `dependency "sparkles:math"` would
// close a dub dependency cycle through math's unittest config
// (math -> test-runner -> test-runner-impl -> core-cli), the same reason
// `sparkles:core-cli` takes the import-only route.
importPaths "src" "../math/src"

dflags "-preview=in" "-preview=dip1000"

// The render core (2-D cell grid + diff compositor), terminal lifecycle, input
// decoding and the event loop are shipped; layout and widgets are not — those
// belong to `sparkles:ui`, which adapts to this library rather than the reverse.

configuration "library" {
    targetType "library"
    dependency "sparkles:base" path="../.."
    dependency "sparkles:input" path="../.."
}

configuration "unittest" {
    // Fast path: depend on the runner shim (pulls the prebuilt impl library).
    // `tui` is not in the impl library's dependency closure, so — unlike
    // `core-cli`/`base` — it can take the normal `dependency` route.
    dependency "sparkles:test-runner" path="../.."
    dependency "sparkles:base" path="../.."
    dependency "sparkles:input" path="../.."

    dflags "-checkaction=context" "-allinst"
    dflags "-defaultlib=libphobos2.so" "-L-fuse-ld=gold" platform="linux-dmd"
    dflags "--link-defaultlib-shared" "--linker=gold" platform="linux-ldc"
    lflags "--export-dynamic" platform="linux-ldc"
}

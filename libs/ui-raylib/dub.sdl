name "ui-raylib"
description "sparkles:ui GPU backend: paints the toolkit's display list via raylib over the shared FontSet, and synthesizes sparkles:input events from raylib's polled state"
license "BSL-1.0"

targetType "library"
targetPath "../../build"

importPaths "src"
sourcePaths "src"

dflags "-preview=in" "-preview=dip1000"

dependency "sparkles:ui" path="../.."
// A GPU backend runs the built-in effects' GPU halves, which sparkles:ui builds
// only in its `gpu-effects` configuration — generating them takes the
// dcompute-enabled LDC (`ldc2-vulkan`), so every build with this backend does.
subConfiguration "sparkles:ui" "gpu-effects"
dependency "sparkles:input" path="../.."
// Pulls raylib-d and the system raylib transitively.
dependency "sparkles:raylib-text" path="../.."

configuration "library" {
    targetType "library"
}

configuration "unittest" {
    dependency "sparkles:test-runner" path="../.."
    dflags "-checkaction=context" "-allinst"
}
// Optimised, assertions live, `debug {}` blocks out — the build every nix
// artifact uses. Neither `debug` (which compiles those blocks in) nor
// `release` (which deletes assert *expressions*, side effects included).
buildType "checked" {
    buildOptions "optimize" "inline" "debugInfo"
}

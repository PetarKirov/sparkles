name "docs"
description "Markdown-based static documentation-site library: content fragments, page shell, site tree, breadcrumbs, theme stylesheet assets, and the docs-site sidebar data schema"
authors "Petar Kirov"
copyright "Copyright © 2026, Petar Kirov"
license "BSL-1.0"

targetPath "build"

importPaths "src"

dflags "-preview=in" "-preview=dip1000"

configuration "library" {
    // sourceLibrary: sparkles:syntax / sparkles:twoslash are sourceLibraries
    // (ImportC types in their signatures), and docs re-exposes their
    // TsConfigCache / TwoslashReturn in the fragment builders' signatures, so
    // it must compile inside each dependent too — see
    // docs/guidelines/importc-c-libraries.md.
    targetType "sourceLibrary"
    dependency "sparkles:base" path="../.."
    dependency "sparkles:build-primitives" path="../.."
    dependency "sparkles:syntax" path="../.."
    dependency "sparkles:twoslash" path="../.."
    dependency "sparkles:wired" path="../.."
}

configuration "unittest" {
    // Fast path: depend on the runner shim (pulls the prebuilt impl library),
    // so `dub test` recompiles only this package, not the runner sources.
    dependency "sparkles:test-runner" path="../.."
    dependency "sparkles:base" path="../.."
    dependency "sparkles:build-primitives" path="../.."
    dependency "sparkles:syntax" path="../.."
    dependency "sparkles:twoslash" path="../.."
    dependency "sparkles:wired" path="../.."

    dflags "-checkaction=context" "-allinst"
    dflags "-defaultlib=libphobos2.so" "-L-fuse-ld=gold" platform="linux-dmd"
    dflags "--link-defaultlib-shared" "--linker=gold" platform="linux-ldc"
    lflags "--export-dynamic" platform="linux-ldc"
}
// Optimised, assertions live, `debug {}` blocks out — the build every nix
// artifact uses. Neither `debug` (which compiles those blocks in) nor
// `release` (which deletes assert *expressions*, side effects included).
buildType "checked" {
    buildOptions "optimize" "inline" "debugInfo"
}

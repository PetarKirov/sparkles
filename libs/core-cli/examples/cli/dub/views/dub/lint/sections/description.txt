Runs the D-scanner-based linter against every D source file in the package.

Pass `--syntax-check` to perform only the cheap syntax-only pass (no static analysis). `--style-check` enables D-scanner's style warnings on top of the default correctness checks. Reports can be emitted in `default`, `checkstyle`, or `github` (annotation-style) formats via `--report-format`.

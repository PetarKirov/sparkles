Create and start a new container from an image.

This is the top-level shortcut for `docker container run`; both forms behave identically. See the long help text under `docker container run` for the canonical reference.

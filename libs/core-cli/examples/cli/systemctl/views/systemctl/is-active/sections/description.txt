Check whether the specified units are active (i.e. running). Returns 0 when at least one of the listed units is active, non-zero otherwise.

The active state is printed to standard output unless `--quiet` is passed, in which case the only signal is the exit code.

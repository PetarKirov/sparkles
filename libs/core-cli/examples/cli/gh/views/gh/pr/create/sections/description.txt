Create a pull request from the current branch.

If the current branch has not yet been pushed, gh pushes it first. The base branch defaults to the repository's default branch unless `--base` is given. The hidden `--web` flag opens the editor in your browser instead of inline.

Check out a pull request as a local branch so you can build and test it.

Aliased as `gh pr co`. The local branch defaults to the PR's head branch name; pass `--branch` to choose your own. Use `--detach` to inspect the PR without creating a tracking branch at all.

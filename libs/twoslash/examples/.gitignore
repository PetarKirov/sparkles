# Dev-only fixture generator deps — never committed (the build is node-free).
node_modules/

# Rendered overlay previews — regenerable, never committed. Regenerate with:
#   hue --twoslash libs/twoslash/examples/fixtures --html --out libs/twoslash/examples/html
html/

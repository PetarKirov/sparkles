> Not published research. Do not link to it from the survey pages.

# Grounding ledger — synthesis pages

Claim-by-claim verification for the sanitizers survey's synthesis pages —
`concepts.md`, `sparkles-baseline.md`, `integration-proposal.md` (this writer),
plus `index.md` and `comparison.md` (the umbrella writer, appended below).
Baseline `file:line` locators are to sparkles at `379e4c7a` (branch
`research/sanitizers`); the crash experiment (E1–E4) was run in a **private
local clone** on x86_64-linux (Ryzen 9 7940HX, kernel 6.18.26), LDC 1.41.0 /
DMD 2.112.1 / dub 1.42.0-beta.1, GCC 15.2 sanitizer runtimes. The proposal's
factual claims ground to the per-tool ledgers ([`asan`](./asan.md),
[`tsan`](./tsan.md), [`valgrind`](./valgrind.md), [`d-toolchain`](./d-toolchain.md),
[`runner-integrations`](./runner-integrations.md)) rather than re-deriving them.

**Status legend:** `✓` verified against a local artifact · `≈` paraphrase-verified
· `⚠` discrepancy (see [register](./index.md)) · `◯` not locally groundable / open
· `🌐` web-only.
**Claim types:** quote · fact · figure · behavior · exposition · opinion · design.

Master-register cross-references are `R*` in [`grounding/index.md`](./index.md).

---

## concepts.md

The vocabulary page defines each term **once**; its facts are grounded in the
owning tool ledger, not independently here. Only rows with a claim of their own
are listed.

| #   | Claim                                                                                                                                                                                                             | Type       | Source (local + locator)                                                                                                                | Status |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| C1  | The [halt-vs-recover table][cx] (ASan halt/1, LSan 23, TSan 66, HWASan 99, RTSan 43, TySan 0, GWP-ASan 139, Valgrind opt-in, darwin ASan 134)                                                                     | fact       | grounds to [`asan`](./asan.md), [`tsan`](./tsan.md), [`hardware-assisted`](./hardware-assisted.md), [`valgrind`](./valgrind.md) ledgers | ✓ (hw) |
| C2  | Instrumentation-locus taxonomy (IR pass / clang-CodeGen-only / DBI / hardware / sampling allocator) is the axis every tool page states first                                                                      | exposition | grounds to [`d-toolchain`](./d-toolchain.md), [`ubsan`](./ubsan.md), [`valgrind`](./valgrind.md)                                        | ✓      |
| C3  | GC memory blind spot, instrumented-world requirement, STW-scan livelock, weak-hook surface, client request, report-windowing / process-per-test / wrapper-and-parse — each defined once, cited to its tool ledger | exposition | [`d-toolchain`](./d-toolchain.md), [`tsan`](./tsan.md), [`valgrind`](./valgrind.md), [`runner-integrations`](./runner-integrations.md)  | ✓      |
| C4  | 23 frozen anchors form the contract the baseline/proposal (and all deep-dives) link to                                                                                                                            | fact       | `concepts.md` `<!-- References -->` + heading slugs; STATE decisions log                                                                | ✓      |

---

## sparkles-baseline.md

| #   | Claim                                                                                                                                                                                                                                                                                                                                                                                                                           | Type         | Source (local + locator)                                                                                          | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------- | ------ |
| L1  | Two-package model; the shim declares `extern(C) sparkles_test_runner_run` without importing impl; design-doc quote (verbatim)                                                                                                                                                                                                                                                                                                   | quote        | `register.d:33-37`; `docs/libs/test-runner/explanation/design.md:29-42`                                           | ✓      |
| L2  | Registration is a `@standalone shared static this` assigning `Runtime.extendedModuleUnitTester`; the shim is guarded by `version (unittest):`                                                                                                                                                                                                                                                                                   | fact         | `register.d:13,41-52`; impl seam `runner_impl.d:40-46`                                                            | ✓      |
| L3  | Default mode runs tests on `std.parallelism.TaskPool`; `TaskPool(threads-1)` + `parallel` means the caller thread participates (effective = `threads`)                                                                                                                                                                                                                                                                          | quote        | `runner_impl.d:562-579`                                                                                           | ✓      |
| L4  | `executeTest` catches `TestSkipped` then `Throwable` only (re-throws `OutOfMemoryError`); **no** signal handler / `fork` / `registerMemoryErrorHandler` in either package                                                                                                                                                                                                                                                       | quote / fact | `execution.d:9-43`; grep of `libs/test-runner{,-impl}/src/` = 0 signal machinery                                  | ✓      |
| L5  | Run modes are mutually exclusive by a hard error ("pick one mode — …"); the precedent a `--sanitize` mode joins                                                                                                                                                                                                                                                                                                                 | quote        | `runner_impl.d:280-291`                                                                                           | ✓      |
| L6  | Mode routing: `--list` / `--bench` / `--ctfe-trace` / `--better-c`/`--wasm` / default                                                                                                                                                                                                                                                                                                                                           | fact         | `runner_impl.d:328-340`                                                                                           | ✓      |
| L7  | Extract-and-recompile = textual body slicing (public-symbols-only, template/CTFE-only without `-i=`); the `@ctfe` probe does **no** extraction                                                                                                                                                                                                                                                                                  | fact         | `extract.d:5-17,53-119`; `driver.d:192-226`                                                                       | ✓      |
| L8  | Marker UDAs are plain structs discovered with `hasUDA` into `TestTraits`; a `@sanitize` marker is the same 4-line pattern                                                                                                                                                                                                                                                                                                       | fact         | `attributes.d:32-67`; `discovery.d:61-78`                                                                         | ✓      |
| L9  | `skipTest` throws a recycled `TestSkipped : Error` → yellow `⊘` line + `N skipped` summary segment; the capability-gate precedent                                                                                                                                                                                                                                                                                               | quote        | `skip.d:21-38`; render `reporting.d:407-417`                                                                      | ✓      |
| L10 | `TestResult` has **no** external-finding field (pass/fail/skip + `Thrown[]` only) — a sanitizer finding has nowhere to land                                                                                                                                                                                                                                                                                                     | fact         | `model.d:85-93`                                                                                                   | ✓      |
| L11 | `formatThrown` already renders an indented multi-line block; the summary grows optional segments "only when non-zero"; capability idiom `hasCoreCliUi = __traits(compiles, …)`                                                                                                                                                                                                                                                  | fact         | `reporting.d:305-332,370-390,24-28`                                                                               | ≈      |
| L12 | Every unittest config passes `-checkaction=context -allinst`, linux-ldc `--link-defaultlib-shared --linker=gold` + `lflags "--export-dynamic"`, linux-dmd `-defaultlib=libphobos2.so -L-fuse-ld=gold`                                                                                                                                                                                                                           | fact         | `libs/versions/dub.sdl:20-30`; identical in shim/impl/base configs                                                | ✓      |
| L13 | `--export-dynamic` (already passed) is what makes the weak-hook surface D-reachable                                                                                                                                                                                                                                                                                                                                             | behavior     | cross-link [`tsan`](./tsan.md) L17/L20 (E8) `[hw]`                                                                | ✓ (hw) |
| L14 | linux-dmd `-defaultlib=libphobos2.so` omits `_d_valgrind_*` exports → the `etc.valgrind` deep path fails to link in that config                                                                                                                                                                                                                                                                                                 | behavior     | cross-link [`valgrind`](./valgrind.md) (E8, claim 14) `[hw]`                                                      | ✓ (hw) |
| L15 | `DFLAGS=… dub test` is a silent false green (magic `$DFLAGS` build type drops `-unittest`; 0 tests; exit 0); the event-horizon recipe invalidated                                                                                                                                                                                                                                                                               | behavior     | cross-link [`d-toolchain`](./d-toolchain.md) (E2) / [`tsan`](./tsan.md) (E4a); R4                                 | ✓ (hw) |
| L16 | The correct channel is a custom buildType — whole-closure, `-unittest` root-only, cache keyed on the buildType name (no `--force`)                                                                                                                                                                                                                                                                                              | behavior     | cross-link [`d-toolchain`](./d-toolchain.md) (E4); R4                                                             | ✓ (hw) |
| L17 | PR #3111 (dub ≥ 1.42.0-beta.1) makes `-fsanitize=` ABI-critical, propagating both directions → mixed instrumentation can't arise via dub                                                                                                                                                                                                                                                                                        | behavior     | cross-link [`d-toolchain`](./d-toolchain.md) (E4b/c); `buildsettings.d:660-677`                                   | ✓ (hw) |
| L18 | dub launders the sanitizer exit code: child exit `66` → `Error Program exited with code 66`, dub exits `2`; running the binary directly preserves it                                                                                                                                                                                                                                                                            | behavior     | cross-link [`tsan`](./tsan.md) (claim 32); reproduced E4 below `[hw]`                                             | ✓ (hw) |
| L19 | The event-horizon ASan/TSan seed lives only on `feat/event-horizon` (`c9537f96`, `5284b217`); its recorded `DFLAGS … dub test --force` recipe is the false green above; `--force` was superfluous                                                                                                                                                                                                                               | fact         | recon §10; `git show 5284b217:…/tsan-suppressions.txt`                                                            | ✓      |
| L20 | CI runs no sanitizers; the `ci.yml` matrix exercises `DC` (`ldc2`/`dmd`) via the `ci` helper appending `--compiler=$DC`                                                                                                                                                                                                                                                                                                         | fact         | `.github/workflows/ci.yml:27-33`; `apps/ci/src/app.d:1227-1233`                                                   | ✓      |
| E1  | Plain build, passing tests: each `✓` prints, then `Summary: 4 passed, 0 failed`, exit `0`                                                                                                                                                                                                                                                                                                                                       | figure       | private clone `libs/crashtest` (throwaway); `dub test -- -e "segv\|race\|asan"`                                   | ✓ (hw) |
| E2  | Plain build, null-deref (SIGSEGV): druntime `core.runtime.…unittestSegvHandler` prints a raw module+offset backtrace to **stderr** (faulting frame `crashtest.demo.__unittest_L39`); process still dies; **no Summary**; raw exit `139` (dub: `-11`). Block-buffered stdout (non-TTY: pipe/redirect/CI) loses **all** buffered `✓` lines and the summary; `stdbuf -oL` recovers only the pre-crash `✓` lines, never the summary | behavior     | private clone; direct run + `dub test -- -e "race\|asan"`; `>out.txt` (empty) vs `stdbuf -oL` (4 `✓`, no summary) | ✓ (hw) |
| E3  | ASan buildType, heap-buffer-overflow (`-i "asan\|pass"`, `ASAN_OPTIONS=detect_leaks=0`): ASan self-symbolizes to `demo.d:67` → `executeTest` (`execution.d:29`) → `runDefaultMode` (`runner_impl.d:566`) → TaskPool, then **halts**; **no Summary**; raw exit `1`, dub `2`                                                                                                                                                      | figure       | private clone; `dub test -b asan -- -i "asan\|pass"`                                                              | ✓ (hw) |
| E4  | TSan buildType, data race (`-t 1`, `-i "race\|pass"`): race caught at `demo.d:52` on stderr **plus** 1 druntime `rt.monitor_` noise race; the run **completes**, `Summary: 5 passed` prints, the racy test is marked **`✓`** (the finding never reaches `TestResult`); raw exit `66`, dub `2`                                                                                                                                   | figure       | private clone; `dub test -b tsan -- -t 1 -i "race\|pass"`; direct run exit 66                                     | ✓ (hw) |

---

## integration-proposal.md

Design claims; the load-bearing _facts_ each ground to a tool ledger or the
baseline rows above.

| #   | Claim                                                                                                                                                                                                | Type       | Source (local + locator)                                                                                   | Status                  |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------- | ----------------------- |
| P1  | `SanitizerCapabilityReport` mirrors the cpu-pmu `CapabilityReport` pattern (per-item advertisement + `unavailableBecause`, runtime probe)                                                            | exposition | `docs/research/cpu-pmu/backend-proposal.md#_2-1-the-capability-model`                                      | ✓                       |
| P2  | LDC accepts `address\|fuzzer\|leak\|memory\|thread`, never `undefined`; UBSan is clang-CodeGen-only (no IR pass) on every D compiler                                                                 | fact       | [`d-toolchain`](./d-toolchain.md) (L1) / [`ubsan`](./ubsan.md); R1                                         | ✓                       |
| P3  | DMD has no `-fsanitize`; Valgrind is its only path — the capability seam's DMD branch                                                                                                                | fact       | [`d-toolchain`](./d-toolchain.md) (DMD) / [`valgrind`](./valgrind.md) (claim 42)                           | ✓                       |
| P4  | MSan needs an instrumented druntime+Phobos world sparkles does not build (and nixpkgs LDC's gcc fallback has no MSan) → advertised absent                                                            | fact       | [`d-toolchain`](./d-toolchain.md) (MSan section, E9/E10); `concepts` instrumented-world                    | ✓ (hw)                  |
| P5  | M1 redrives dub with a generated **buildType**, not `DFLAGS` (the false green); via the `run()`/`makeWorkDir`/`detectCompiler` seam                                                                  | design     | baseline L15/L16; `driver.d:65-97,229-262`                                                                 | ✓                       |
| P6  | ASan default policy: `detect_leaks=0` (druntime `defaultTraceHandler` leak noise → exit 1 otherwise) + `log_path` PID-suffixed per child                                                             | fact       | [`asan`](./asan.md) (leak-noise policy; E5c) `[hw]`                                                        | ✓ (hw)                  |
| P7  | Report capture is per-tool: recovered ASan exits `0` → count reports; TSan `66`; Valgrind `--error-exitcode` (halt-vs-recover table)                                                                 | fact       | `concepts` [halt-vs-recover][cx]; [`asan`](./asan.md)/[`tsan`](./tsan.md)                                  | ✓ (hw)                  |
| P8  | Findings land in a new `TestResult.findings` field, rendered as a `✗`-with-tool-banner line; summary grows a byte-safe `, N findings` segment                                                        | design     | against `model.d:85-93`, `reporting.d:305-332,370-390`                                                     | ✓                       |
| P9  | M2 in-process windowing seams all hw-proven from D (`__tsan_on_report` count-delta, `__tsan_on_finalize`→0, `__asan_set_error_report_callback` + recover) — need `--export-dynamic` (already passed) | behavior   | [`tsan`](./tsan.md) (E8/L17-L20), [`asan`](./asan.md) (report callback) `[hw]`                             | ✓ (hw)                  |
| P10 | LSan per-test recipe is `leak_check_at_exit=0` + `__lsan_do_recoverable_leak_check()` — **not** `detect_leaks=0` (which disables the manual checks too)                                              | fact       | [`asan`](./asan.md) (composition refutation); R3                                                           | ✓ (hw)                  |
| P11 | `--sanitize=thread` defaults `-t 1` + a watchdog: GC-heavy multithreaded D deterministically **livelocks** under TSan; a test's own threads stay checked at `-t 1`                                   | behavior   | [`tsan`](./tsan.md) (E5 livelock; E6 `-t 1`); `concepts` STW-scan; R6                                      | ✓ (hw)                  |
| P12 | Shipped suppression files (2-line TSan, 3-line memcheck) + user-`--suppressions` composition; runtimes don't demangle → patterns target **mangled** D names                                          | fact       | [`tsan`](./tsan.md) (E4d/E6), [`valgrind`](./valgrind.md) (E2/claim 20); `concepts` suppression            | ✓ (hw)                  |
| P13 | `@sanitize`/`@noSanitize` gating (attributes precedent) + LDC druntime `@noSanitize("<name>")` at the compiler level                                                                                 | fact       | baseline L8; [`d-toolchain`](./d-toolchain.md) (`attributes.d:285-293`)                                    | ✓                       |
| P14 | M3 `--valgrind`: `--xml=yes` + `VALGRIND_PRINTF` markers interleave with `<error>` in protocol 4 → per-test attribution (probe-verified)                                                             | behavior   | [`valgrind`](./valgrind.md) (claim 29, E5; [attribution probe](../examples/valgrind-attribution.d)) `[hw]` | ✓ (hw)                  |
| P15 | `--valgrind` defaults `--fair-sched=yes` + `-t 1` (default scheduler pathological: 156 s → 1.27 s; helgrind/DRD clean only at `-t 1`); ship 3-entry GC suppression; `--error-exitcode` contract      | figure     | [`valgrind`](./valgrind.md) (E3/E6, claims 33-34) `[hw]`                                                   | ✓ (hw)                  |
| P16 | `--valgrind-gc` (deep fidelity) via `-debug=VALGRIND -i=etc.valgrind` closes the GC-UAF blind spot without a druntime rebuild; **fails to link under linux-dmd shared Phobos**                       | behavior   | [`valgrind`](./valgrind.md) (E7/E8, claims 14,18); `concepts` GC blind spot                                | ✓ (hw)                  |
| P17 | M4 `--isolate`: the `extract`/`driver` machinery is ≈90% of process-per-test; per-child `log_path` + exit code fixes the E2 crash gap                                                                | design     | [`runner-integrations`](./runner-integrations.md) (design patterns); baseline E2                           | ✓                       |
| P18 | Overhead labels: ASan ≈ 2× / TSan ≈ 5× / memcheck ≈ 4.4× (+origins 6.4×) / helgrind ≈ 2.1×; GORACE-cache-hole lesson (runtime opts must key a result cache)                                          | figure     | [`comparison`](./comparison.md); [`runner-integrations`](./runner-integrations.md) (Go E1)                 | ✓ (hw)                  |
| P19 | M6 darwin: `abort_on_error=1` (ASan exit 134), `MallocNanoZone=0`, dylib-only runtimes, Valgrind dead > macOS 10.13 → `--valgrind` absent on darwin                                                  | fact       | [`macos-windows`](./macos-windows.md)                                                                      | ◯ (src/lit; hw pending) |
| P20 | M7 later tier: RTSan real IR pass, LDC-adoptable at LLVM ≥ 20 (LDC 1.41 = 18; exit 43); GWP-ASan C-heap only; HWASan exit 99 + GC-scan hazard; Apple M4 has no MTE                                   | fact       | [`hardware-assisted`](./hardware-assisted.md) (verdict table)                                              | ✓ (hw/lit)              |

---

## index.md

The umbrella is pointers plus the milestones timeline; its per-page facts ground
to the owning ledgers, and the timeline dates ground to the pages/papers below.
Only rows with a claim of their own are listed.

| #   | Claim                                                                                                                         | Type       | Source (local + locator)                                                                                            | Status       |
| --- | ----------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------- | ------------ |
| I1  | Master-catalog rows (subject / concern focus / verification bed / link) restate each page's own header                        | fact       | per-page ledgers ([`asan`](./asan.md) … [`hardware-assisted`](./hardware-assisted.md))                              | ✓ (by ref)   |
| I2  | "This survey answers thirteen questions" list — routing pointers, no new facts                                                | exposition | the linked pages                                                                                                    | ✓            |
| I3  | The seven-concern spine and its mapping onto `concepts.md` anchors                                                            | exposition | [`concepts`](./index.md) C2/C4; `concepts.md` `<!-- References -->`                                                 | ✓            |
| I4  | Timeline: Valgrind first release 2002; framework paper **PLDI 2007** + shadow-memory paper **VEE 2007** (two distinct papers) | fact       | `papers/sanitizers/` (both saved); R12                                                                              | ✓ (2002 🌐)  |
| I5  | Timeline: ASan lands in LLVM 2011 (paper USENIX ATC 2012); TSan v2 2011–12, v3 rewrite 2021; LSan ~2013; MSan CGO 2015        | fact       | `[literature]`; TSan v3 dating in [`tsan`](./tsan.md) (source `tsan_shadow.h`)                                      | 🌐           |
| I6  | Timeline: MSVC `/fsanitize=address` GA in Visual Studio 16.9 (2021)                                                           | fact       | MS Learn (`ms.date` 2026-05-28) via [`macos-windows`](./macos-windows.md)                                           | 🌐           |
| I7  | Timeline: MTE announced 2018 (arXiv 1802.09517); Pixel 8 / Tensor G3 first handset Nov 2023; AmpereOne 2024                   | fact       | [`hardware-assisted`](./hardware-assisted.md) timeline + `papers/sanitizers/` (1802.09517, 2511.17773)              | ✓ (🌐 dates) |
| I8  | Timeline: RTSan + experimental TySan in LLVM/Clang 20 (Mar 2025); Apple MIE/EMTE on A19 / iPhone 17 (Sep 2025), no M-series   | fact       | [`hardware-assisted`](./hardware-assisted.md) (llvm20-notes, apple-mie); R17                                        | ✓ (🌐 dates) |
| I9  | Probe table: what each of the 11 examples demonstrates (env 6.18.26 / 7940HX / LDC 1.41; all `platforms "linux"`; SKIP-clean) | behavior   | probe headers in `examples/`; owning per-page ledgers                                                               | ✓ (hw)       |
| I10 | Taxonomies (by instrumentation locus / defect class / toolchain / verification level) — re-cuts, no new facts                 | exposition | deep-dive ledgers                                                                                                   | ✓            |
| I11 | The honest-boundary note: only `x86_64-linux` is a full bed; macOS one datum; Windows none; no MTE silicon                    | fact       | [`macos-windows`](./macos-windows.md); [`hardware-assisted`](./hardware-assisted.md); grounding [Q4/Q5](./index.md) | ✓/◯          |

## comparison.md

The capstone aggregates the deep-dives; each matrix cell restates a claim its
owning page already grounded, so those are `✓ (by reference)` and re-verified in
the per-tool ledger, not re-derived here. Only synthesis-specific claims carry
their own rows.

| #   | Claim                                                                                                                                                                                                           | Type       | Source (local + locator)                                                                                          | Status               |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------- | -------------------- |
| M1  | The capability-matrix cells (7 concerns × 8 tool families, both column-group tables)                                                                                                                            | fact       | each cell restates a deep-dive claim, verified in the owning page's ledger                                        | ✓ (by reference)     |
| M2  | The three-finding NOTE: only ASan crosses off-Linux; UBSan/TySan unreachable (clang-CodeGen); GC-pool blind spot recurs everywhere                                                                              | fact       | [`asan`](./asan.md), [`ubsan`](./ubsan.md), [`d-toolchain`](./d-toolchain.md); `concepts` GC blind spot; R1       | ✓                    |
| M3  | Integration consensus: three attribution designs × who uses them × the sparkles lesson (condensed from the full table)                                                                                          | exposition | [`runner-integrations`](./runner-integrations.md) integration-semantics table; R18                                | ✓                    |
| M4  | "Nonzero exit ≠ failed test": exit codes 1 / 66 / 99 / 43 / 0 / 139 / 134 differ per tool                                                                                                                       | fact       | `concepts` [halt-vs-recover][cx] (C1); R25/R27                                                                    | ✓ (hw)               |
| M5  | Architectural-trade-offs table (locus / recompile scope / halt policy / attribution boundary / symbolization / GC visibility)                                                                                   | exposition | aggregation of the deep-dives, no new facts                                                                       | ✓                    |
| M6  | Overhead table: ASan ~2× `[lit]`; LSan ≈0-until-exit `[lit]`; TSan ~5× + 2× mem; memcheck 4.4× (+origins 6.4×); helgrind 2.1×; DRD ~2.2×; ~0.25 s startup                                                       | figure     | [`asan`](./asan.md) (2×, LSan), [`tsan`](./tsan.md) (E, ~5×), [`valgrind`](./valgrind.md) (E, the overhead table) | ✓ (hw; ASan/LSan 🌐) |
| M7  | The scheduler pathology: `:base` 4 ms → 156.5 s at `-t auto` (spread 12.5–180 s) → 1.16 s `-t 1` → 1.27 s `--fair-sched=yes`                                                                                    | figure     | [`valgrind`](./valgrind.md) (E3/E6, the runner-concern table); proposal P15                                       | ✓ (hw)               |
| M8  | Delta-table "sparkles today" column (8 capability rows, all absences bar `skipTest`)                                                                                                                            | behavior   | the `sparkles-baseline.md` ledger above (L-rows + crash experiment E1–E4)                                         | ✓ (hw)               |
| M9  | Open questions 1–11 (MSan world, MTE absence, Windows no-hw, mac-bsn blocked, event-horizon mystery, `-allinst`, fake-stack hazard, LSan root-region, helgrind/DRD `-t>1` supp, UBSan residue, RTSan LLVM bump) | fact/◯     | grounding [register + Q1–Q7](./index.md) (R1/R14/R17/R20; Q1/Q2/Q3/Q4/Q5/Q6/Q7)                                   | ✓/◯                  |

<!-- References -->

[cx]: ../concepts.md#halt-vs-recover

# Grounding ledger — `django-orm.md`

Claim-by-claim verification of `docs/research/sql/django-orm.md` against the **local**
primary source:

- `$REPOS/python/django` @ `65a9f14` (pinned in [`_sources.md`](./_sources.md)); sub-trees
  `django/db/` (models, backends, migrations, `transaction.py`) and the in-tree docs
  `docs/topics/db/`, `docs/ref/models/`, `docs/topics/{security,migrations}.txt`,
  `docs/misc/design-philosophies.txt`, `LICENSE`, `pyproject.toml`, `django/__init__.py`.

`$REPOS = /home/petar/code/repos`. Type: **L** = local primary; **🌐** = web/point-in-time
(cannot ground in a `--depth 1` tree). Status: ✓ verified · ⚠ discrepancy · 🌐 web-fallback.

> Note on quote fidelity: Django's in-tree docs are reStructuredText. Verbatim quotes strip
> RST cross-reference roles (`` :class:`~django.db.models.query.QuerySet` `` → `` `QuerySet` ``,
> `` :setting:`CONN_MAX_AGE` `` → `` `CONN_MAX_AGE` ``) and the ` ``double-backtick`` ` literal
> markup, rendering them as the reader sees them on docs.djangoproject.com. This normalization
> is noted once here and applies to all doc quotes below.

## Claims

| #   | Claim                                                                                                                                                                                                                                                                                                                                                                                                 | Type | Source (local + locator)                                                                                                                                         | Evidence                                                                                                                                                                                                                                                                        | Status                                      |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | --- |
| 1   | Language Python, `requires-python >= 3.12`                                                                                                                                                                                                                                                                                                                                                            | L    | `pyproject.toml:8`                                                                                                                                               | `requires-python = ">= 3.12"`; classifiers list 3.12/3.13/3.14                                                                                                                                                                                                                  | ✓                                           |
| 2   | License BSD-3-Clause                                                                                                                                                                                                                                                                                                                                                                                  | L    | `LICENSE:6-17`                                                                                                                                                   | 3-clause BSD text: "Redistribution and use…", clause 3 "Neither the name of Django nor the names of its contributors…"                                                                                                                                                          | ✓                                           |
| 3   | Version in tree = `6.2` (alpha)                                                                                                                                                                                                                                                                                                                                                                       | L    | `django/__init__.py:3`                                                                                                                                           | `VERSION = (6, 2, 0, "alpha", 0)`                                                                                                                                                                                                                                               | ✓                                           |
| 4   | First release 2005                                                                                                                                                                                                                                                                                                                                                                                    | 🌐   | not in tree                                                                                                                                                      | open-sourced 2005 (web/registry)                                                                                                                                                                                                                                                | 🌐                                          |
| 5   | Backends: PostgreSQL, MySQL/MariaDB, SQLite, Oracle                                                                                                                                                                                                                                                                                                                                                   | L    | `django/db/backends/{postgresql,mysql,sqlite3,oracle}/base.py`                                                                                                   | `display_name = "PostgreSQL"` (postgresql:100), `"SQLite"` (sqlite3:62), `"Oracle"` (oracle:117); mysql `display_name` property (mysql:382, MariaDB/MySQL)                                                                                                                      | ✓                                           |
| 6   | QUOTE models: "A model is the single, definitive source of information about your data. It contains the essential fields and behaviors of the data you're storing. Generally, each model maps to a single database table."                                                                                                                                                                            | L    | `docs/topics/db/models.txt:7-9`                                                                                                                                  | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 7   | Model example (`Person` / `first_name` / `last_name` CharField); table `myapp_person`, auto `id`, table name "automatically derived from some model metadata"                                                                                                                                                                                                                                         | L    | `docs/topics/db/models.txt:29-33,42-52`                                                                                                                          | example + "The name of the table, `myapp_person`, is automatically derived from some model metadata"                                                                                                                                                                            | ✓                                           |
| 8   | ORM gives "an automatically-generated database-access API"                                                                                                                                                                                                                                                                                                                                            | L    | `docs/topics/db/models.txt:17-18`                                                                                                                                | "Django gives you an automatically-generated database-access API"                                                                                                                                                                                                               | ✓                                           |
| 9   | QUOTE DRY: "Every distinct concept and/or piece of data should live in one, and only one, place. Redundancy is bad. Normalization is good. … The framework, within reason, should deduce as much as possible from as little as possible."                                                                                                                                                             | L    | `docs/misc/design-philosophies.txt:59-63`                                                                                                                        | verbatim (ellipsis elides the paragraph break)                                                                                                                                                                                                                                  | ✓                                           |
| 10  | `ModelBase` metaclass builds `_meta`(`Options`) and auto-adds `objects` `Manager`                                                                                                                                                                                                                                                                                                                     | L    | `django/db/models/base.py:95-96,146,434-442`                                                                                                                     | `class ModelBase(type): """Metaclass for all models."""`; `add_to_class("_meta", Options(...))`; `if not opts.managers: … manager = Manager(); manager.auto_created = True; cls.add_to_class("objects", manager)`                                                               | ✓                                           |
| 11  | No unit of work / identity map / change tracking; each `save()` its own statement                                                                                                                                                                                                                                                                                                                     | L    | `django/db/models/base.py:841-908` (`save`/`save_base`); `query.py:_fetch_all` (per-QuerySet `_result_cache` only)                                               | `save()` → `save_base()` emits one INSERT/UPDATE; only cache is query-local `_result_cache`; no session object exists                                                                                                                                                           | ✓ (mechanism; framing is editorial)         |
| 12  | Thread-local connections; persistent connections are the pooling story; `CONN_MAX_AGE` default 0                                                                                                                                                                                                                                                                                                      | L    | `docs/ref/databases.txt:33-41`                                                                                                                                   | "Persistent connections avoid the overhead of reestablishing a connection…controlled by the `CONN_MAX_AGE` parameter"; "The default value is `0`, preserving the historical behavior of closing the database connection at the end of each request"                             | ✓                                           |
| 13  | psycopg pool via `OPTIONS={"pool": True}`; ASGI → disable persistent, use backend pool                                                                                                                                                                                                                                                                                                                | L    | `docs/ref/databases.txt:44-45,252+` ("Connection pool" section)                                                                                                  | "use your database backend's built-in connection pooling if available"; "To use a connection pool with psycopg…set `\"pool\"` in the `OPTIONS`…to `True`"                                                                                                                       | ✓                                           |
| 14  | QUOTE query.py module docstring: "The main QuerySet implementation. This provides the public API for the ORM."                                                                                                                                                                                                                                                                                        | L    | `django/db/models/query.py:1-3`                                                                                                                                  | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 15  | QUOTE lazy: "`QuerySet` objects are lazy -- the act of creating a `QuerySet` doesn't involve any database activity. You can stack filters together all day long, and Django won't actually run the query until the `QuerySet` is _evaluated_."                                                                                                                                                        | L    | `docs/topics/db/queries.txt:291-295`                                                                                                                             | verbatim (RST role → plain `QuerySet`)                                                                                                                                                                                                                                          | ✓                                           |
| 16  | QUOTE evaluated: "Internally, a `QuerySet` can be constructed, filtered, sliced, and generally passed around without actually hitting the database. No database activity actually occurs until you do something to evaluate the queryset."                                                                                                                                                            | L    | `docs/ref/models/querysets.txt:21-23`                                                                                                                            | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 17  | Evaluation triggers: iteration, `async for`, slicing-with-step, `repr()`, `len()`, `list()`, pickling                                                                                                                                                                                                                                                                                                 | L    | `docs/ref/models/querysets.txt:25-75`                                                                                                                            | enumerated list: Iteration / Asynchronous iteration / Slicing (step) / Pickling/Caching / repr() / len() / list()                                                                                                                                                               | ✓                                           |
| 18  | `_fetch_all` populates `_result_cache` once; `_result_cache` is query-local, not a cross-query identity map                                                                                                                                                                                                                                                                                           | L    | `django/db/models/query.py` `_fetch_all`                                                                                                                         | `if self._result_cache is None: self._result_cache = list(self._iterable_class(self))`                                                                                                                                                                                          | ✓ (identity-map absence is by-construction) |
| 19  | QUOTE `filter` docstring: "Return a new QuerySet instance with the args ANDed to the existing set." + folds into `Q`; `exclude` uses `~Q(...)`                                                                                                                                                                                                                                                        | L    | `django/db/models/query.py:1670-1704`                                                                                                                            | `filter` docstring verbatim; `_filter_or_exclude_inplace`: `self._query.add_q(~Q(*args, **kwargs))` / `add_q(Q(*args, **kwargs))`                                                                                                                                               | ✓                                           |
| 20  | Chained `filter/filter/exclude` example "hits the database only once, at the last line (`print(q)`)"                                                                                                                                                                                                                                                                                                  | L    | `docs/topics/db/queries.txt:299-305`                                                                                                                             | example + "in fact it hits the database only once, at the last line (`print(q)`)"                                                                                                                                                                                               | ✓                                           |
| 21  | `get()` runs immediately, returns one object; raises `DoesNotExist` / `MultipleObjectsReturned`                                                                                                                                                                                                                                                                                                       | L    | `django/db/models/query.py:682-716`                                                                                                                              | docstring "Perform the query and return a single object…"; raises `self.model.DoesNotExist` / `self.model.MultipleObjectsReturned`                                                                                                                                              | ✓                                           |
| 22  | QUOTE injection: "Django's querysets are protected from SQL injection since their queries are constructed using query parameterization. A query's SQL code is defined separately from the query's parameters. Since parameters may be user-provided and therefore unsafe, they are escaped by the underlying database driver."                                                                        | L    | `docs/topics/security.txt:101-104`                                                                                                                               | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 23  | Compiler builds `(sql, params)` and executes `cursor.execute(sql, params)`                                                                                                                                                                                                                                                                                                                            | L    | `django/db/models/sql/compiler.py:1611,1625`                                                                                                                     | `sql, params = self.as_sql()` … `cursor.execute(sql, params)`                                                                                                                                                                                                                   | ✓                                           |
| 24  | QUOTE `F()` docs: "An `F()` object represents the value of a model field… It makes it possible to refer to model field values and perform database operations using them without actually having to pull them out of the database into Python memory."                                                                                                                                                | L    | `docs/ref/models/expressions.txt` (`F()` expressions section)                                                                                                    | verbatim (ellipsis elides "transformed value of a model field, or annotated column")                                                                                                                                                                                            | ✓                                           |
| 25  | `F("stories_filed") + 1` "looks like a normal Python assignment … in fact it's an SQL construct describing an operation on the database"                                                                                                                                                                                                                                                              | L    | `docs/ref/models/expressions.txt`                                                                                                                                | "Although `reporter.stories_filed = F('stories_filed') + 1` looks like a normal Python assignment of value to an instance attribute, in fact it's an SQL construct describing an operation on the database."                                                                    | ✓                                           |
| 26  | QUOTE `F` class docstring: "An object capable of resolving references to existing query objects."                                                                                                                                                                                                                                                                                                     | L    | `django/db/models/expressions.py:883`                                                                                                                            | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 27  | QUOTE `Q` docstring: "Encapsulate filters as objects that can then be combined logically (using `&` and `                                                                                                                                                                                                                                                                                             | `)." | L                                                                                                                                                                | `django/db/models/query_utils.py:42-45`                                                                                                                                                                                                                                         | verbatim                                    | ✓   |
| 28  | `Q` overloads `__and__`/`__or__`/`__xor__`/`__invert__` over a tree node                                                                                                                                                                                                                                                                                                                              | L    | `django/db/models/query_utils.py:41,88-100`                                                                                                                      | `class Q(tree.Node)`; `def __or__`/`__and__`/`__xor__`/`__invert__`                                                                                                                                                                                                             | ✓                                           |
| 29  | `raw(raw_query, params=())` runs raw SELECT → model instances; `extra()` splices raw SQL                                                                                                                                                                                                                                                                                                              | L    | `django/db/models/query.py:1507,1931`                                                                                                                            | `def raw(self, raw_query, params=(), …)` → `RawQuerySet`; `def extra(…)` docstring "Add extra SQL fragments to the query."                                                                                                                                                      | ✓                                           |
| 30  | QUOTE raw/extra caution: "These capabilities should be used sparingly and you should always be careful to properly escape any parameters that the user can control. In addition, you should exercise caution when using `extra()` and `RawSQL`."                                                                                                                                                      | L    | `docs/topics/security.txt` (SQL injection section)                                                                                                               | verbatim (RST roles → `extra()` / `RawSQL`)                                                                                                                                                                                                                                     | ✓                                           |
| 31  | QUOTE migrations: "You should think of migrations as a version control system for your database schema. `makemigrations` is responsible for packaging up your model changes into individual migration files - analogous to commits - and `migrate` is responsible for applying those to your database."                                                                                               | L    | `docs/topics/migrations.txt:30-33`                                                                                                                               | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 32  | QUOTE autodetector: "Take a pair of ProjectStates and compare them to see what the first would need doing to make it match the second (the second usually being the project's current state)."                                                                                                                                                                                                        | L    | `django/db/migrations/autodetector.py` (`MigrationAutodetector` docstring)                                                                                       | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 33  | `Migration` carries ordered `operations` + `dependencies`/`run_before`/`replaces`                                                                                                                                                                                                                                                                                                                     | L    | `django/db/migrations/migration.py` (`Migration` docstring + attrs)                                                                                              | "operations: A list of Operation instances…"; `operations = []`, `dependencies = []`, `run_before = []`, replaces                                                                                                                                                               | ✓                                           |
| 34  | QUOTE graph: "Represent the digraph of all migrations in a project. Each migration is a node, and each dependency is an edge. There are no implicit dependencies between numbered migrations - the numbering is merely a convention to aid file listing. Every new numbered migration has a declared dependency to the previous number, meaning that VCS branch merges can be detected and resolved." | L    | `django/db/migrations/graph.py` (`MigrationGraph` docstring)                                                                                                     | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 35  | Migration node = `(app_label, migration_name)` tuple; `recorder` bookkeeping table; migrations on all shipped backends                                                                                                                                                                                                                                                                                | L    | `django/db/migrations/graph.py` ("A node should be a tuple: (app_path, migration_name)"); `django/db/migrations/recorder.py`; `docs/topics/migrations.txt:57-59` | graph tuple + "Migrations are supported on all backends that Django ships with"                                                                                                                                                                                                 | ✓                                           |
| 36  | Django is code-first; no maintained db-first codegen (`inspectdb` a scaffold)                                                                                                                                                                                                                                                                                                                         | L    | `docs/topics/db/models.txt` (models = source of truth); `inspectdb` mgmt command                                                                                 | model authoritative; migrations diff model state                                                                                                                                                                                                                                | ✓ (framing editorial)                       |
| 37  | QUOTE `db_type`: "Return the database column data type for this field… The default implementation of this method looks at the backend-specific `data_types` dictionary, looking up the field by its 'internal type'."                                                                                                                                                                                 | L    | `django/db/models/fields/__init__.py:898-916`                                                                                                                    | verbatim (docstring + comment); `column_type = connection.data_types[self.get_internal_type()]`                                                                                                                                                                                 | ✓                                           |
| 38  | Field zoo: CharField/IntegerField/DateTimeField/BooleanField/DecimalField/UUIDField/JSONField/BinaryField/ForeignKey/ManyToManyField                                                                                                                                                                                                                                                                  | L    | `django/db/models/fields/__init__.py:1201-2980` + `fields/related.py`, `fields/json.py`                                                                          | class defs: `BooleanField`(1201), `CharField`(1247), `DateTimeField`(1600), `DecimalField`(1741), `UUIDField`(2820), `BinaryField`(2745); `ForeignKey`/`ManyToManyField` in `related.py`; `JSONField` in `json.py`                                                              | ✓                                           |
| 39  | Nullability = Python `None` ↔ SQL `NULL` via `null=`; no Option/Maybe wrapper                                                                                                                                                                                                                                                                                                                         | L    | `django/db/models/fields/__init__.py` (`null` attr)                                                                                                              | field `null` flag; no wrapper type in tree                                                                                                                                                                                                                                      | ✓                                           |
| 40  | QUOTE `save()` docstring: "Save the current instance. Override this in a subclass if you want to control the saving process. … The 'force_insert' and 'force_update' parameters can be used to insist that the 'save' must be an SQL insert or update … Normally, they should not be set."                                                                                                            | L    | `django/db/models/base.py:849-856`                                                                                                                               | verbatim (ellipsis elides "(or equivalent for non-SQL backends), respectively.")                                                                                                                                                                                                | ✓                                           |
| 41  | `save()` → `save_base` one statement; `delete()` immediate; `Manager.create` = `model()` + `save(force_insert=True)`                                                                                                                                                                                                                                                                                  | L    | `base.py:906,1322-1331`; `query.py:720-739`                                                                                                                      | `self.save_base(…)`; `delete`: `collector.delete()`; `create`: `obj = self.model(**kwargs); obj.save(force_insert=True, using=self.db)`                                                                                                                                         | ✓                                           |
| 42  | QUOTE Manager docs: "A `Manager` is the interface through which database query operations are provided to Django models."                                                                                                                                                                                                                                                                             | L    | `docs/topics/db/managers.txt:9-10`                                                                                                                               | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 43  | Lazy related load: `ForwardManyToOneDescriptor.__get__` loads related instance from DB, caches on `_state`                                                                                                                                                                                                                                                                                            | L    | `django/db/models/fields/related_descriptors.py:132-276`                                                                                                         | docstring "Accessor to the related object on the forward side of a many-to-one…"; `__get__` fetches + `set_cached_value`; `get_object` → `qs.get(...)`                                                                                                                          | ✓                                           |
| 44  | QUOTE N+1 example: "# Hits the database." / "# Hits the database again to get the related Blog object."                                                                                                                                                                                                                                                                                               | L    | `docs/ref/models/querysets.txt:1067-1071`                                                                                                                        | verbatim comments in `select_related()` example                                                                                                                                                                                                                                 | ✓                                           |
| 45  | QUOTE `select_related`: "works by creating an SQL join and including the fields of the related object in the `SELECT` statement… `select_related` gets the related objects in the same database query." + limited to single-valued (FK/one-to-one)                                                                                                                                                    | L    | `docs/ref/models/querysets.txt:1177-1182`                                                                                                                        | verbatim; "limited to single-valued relationships - foreign key and one-to-one"                                                                                                                                                                                                 | ✓                                           |
| 46  | QUOTE `prefetch_related`: "does a separate lookup for each relationship, and does the 'joining' in Python"; additional queries run after primary query                                                                                                                                                                                                                                                | L    | `docs/ref/models/querysets.txt:1184-1185,1240-1242`                                                                                                              | verbatim; "The additional queries in `prefetch_related()` are executed after the `QuerySet` has begun to be evaluated and the primary query has been executed."                                                                                                                 | ✓                                           |
| 47  | `select_related`/`prefetch_related` method docstrings (join vs separate prefetch)                                                                                                                                                                                                                                                                                                                     | L    | `django/db/models/query.py:1779-1834`                                                                                                                            | `select_related`: "Return a new QuerySet instance that will select related objects."; `prefetch_related`: "prefetch the specified Many-To-One and Many-To-Many related objects when the QuerySet is evaluated."                                                                 | ✓                                           |
| 48  | QUOTE async: "Every method that might block - such as `get()` or `delete()` - has an asynchronous variant (`aget()` or `adelete()`), and when you iterate over results, you can use asynchronous iteration (`async for`) instead."                                                                                                                                                                    | L    | `docs/topics/db/queries.txt:958-960`                                                                                                                             | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 49  | Async variants delegate via `sync_to_async` (thread offload); `aget` body                                                                                                                                                                                                                                                                                                                             | L    | `django/db/models/query.py:717-719`; `base.py:913-935` (`asave`/`adelete`)                                                                                       | `async def aget(...): return await sync_to_async(self.get)(*args, **kwargs)`; `asgiref.sync.sync_to_async` import                                                                                                                                                               | ✓                                           |
| 50  | Blocking ORM call from async raises `SynchronousOnlyOperation`; `filter`/`exclude` safe in async                                                                                                                                                                                                                                                                                                      | L    | `docs/topics/db/queries.txt:951-956,975-978`                                                                                                                     | "Django will notice and raise a `SynchronousOnlyOperation`"; "you can use those freely in asynchronous code"                                                                                                                                                                    | ✓                                           |
| 51  | Autocommit default: "Each query is immediately committed to the database, unless a transaction is active."                                                                                                                                                                                                                                                                                            | L    | `docs/topics/db/transactions.txt:15-16`                                                                                                                          | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 52  | QUOTE `Atomic` docstring: "When it's used as a context manager, **enter** creates a transaction or a savepoint, depending on whether a transaction is already in progress, and **exit** commits the transaction or releases the savepoint on normal exit, and rolls back the transaction or to the savepoint on exceptions."                                                                          | L    | `django/db/transaction.py:163-167`                                                                                                                               | verbatim                                                                                                                                                                                                                                                                        | ✓                                           |
| 53  | QUOTE atomic mechanism: "Under the hood, Django's transaction management code: opens a transaction when entering the outermost `atomic` block; creates a savepoint when entering an inner `atomic` block; releases or rolls back to the savepoint when exiting an inner block; commits or rolls back the transaction when exiting the outermost block."                                               | L    | `docs/topics/db/transactions.txt:227-233`                                                                                                                        | verbatim (RST bullet list flattened with `;`)                                                                                                                                                                                                                                   | ✓                                           |
| 54  | Nested `atomic` = savepoint (top-level BEGIN + inner SAVEPOINT); `savepoint=False`, `durable=True` (RuntimeError if nested)                                                                                                                                                                                                                                                                           | L    | `django/db/transaction.py:194-234,328`; `docs/topics/db/transactions.txt:235-244`                                                                                | `__enter__`: `if connection.in_atomic_block: … sid = connection.savepoint()`; `def atomic(using=None, savepoint=True, durable=False)`; docstring "raised if it's nested within another atomic block"                                                                            | ✓                                           |
| 55  | Django uses transactions/savepoints automatically for multi-query ops (`delete()`, `update()`)                                                                                                                                                                                                                                                                                                        | L    | `docs/topics/db/transactions.txt:19-22`                                                                                                                          | "Django uses transactions or savepoints automatically to guarantee the integrity of ORM operations that require multiple queries, especially delete() and update()"                                                                                                             | ✓                                           |
| 56  | Errors are thrown `django.db` exceptions (`IntegrityError`/`DatabaseError`/`NotSupportedError`), no typed channel; `try/except` around `atomic`                                                                                                                                                                                                                                                       | L    | `django/db/models/query.py:18-24` (imports `IntegrityError`, `NotSupportedError`); `docs/topics/db/transactions.txt` (try/except IntegrityError example)         | import list + example "Wrapping `atomic` in a try/except block allows for natural handling of integrity errors"                                                                                                                                                                 | ✓                                           |
| 57  | Only hard runtime dep is `asgiref`; drivers are user's choice; `django/db/models/__init__.py` re-exports `Model`/`Manager`/`F`/`Q`/`QuerySet` under `__all__`                                                                                                                                                                                                                                         | L    | `pyproject.toml` deps; `django/db/models/__init__.py:64,54,29,61,76-128`                                                                                         | `from django.db.models.base import DEFERRED, Model`; `Manager`; `F`; `Q`; `__all__` includes "F","Q","Manager","Model"                                                                                                                                                          | ✓                                           |
| 58  | Latest stable version                                                                                                                                                                                                                                                                                                                                                                                 | 🌐   | not in tree (tree is 6.2 dev)                                                                                                                                    | released stable version is web/registry-attested                                                                                                                                                                                                                                | 🌐                                          |
| 59  | `QuerySet` wraps a mutable `sql.Query`; clauses mutate it, `sql.compiler` renders to dialect                                                                                                                                                                                                                                                                                                          | L    | `django/db/models/query.py:29` (imports `sql`); `sql/compiler.py`                                                                                                | `from django.db.models import … sql`; `_query`/`query` is a `sql.Query`; compiler renders `(sql, params)`                                                                                                                                                                       | ✓                                           |
| 60  | QUOTE `aggregate`: "Return a dictionary containing the calculations (aggregation) over the current queryset."; `annotate` attaches per-object expressions; `values()`/`values_list()` swap object hydration for dict/tuple                                                                                                                                                                            | L    | `django/db/models/query.py:638-645,1836,1534,1540`                                                                                                               | `aggregate` docstring verbatim; `annotate` "objects have been annotated with extra data or aggregations"; `values`/`values_list` defs                                                                                                                                           | ✓                                           |
| 61  | `ForeignKey.get_attname` returns `"%s_id" % self.name` (FK → `blog_id` column); `ManyToManyField` synthesizes join table via `create_many_to_many_intermediary_model` (or `through=`)                                                                                                                                                                                                                 | L    | `django/db/models/fields/related.py:1250-1251,1393`                                                                                                              | `def get_attname(self): return "%s_id" % self.name`; `def create_many_to_many_intermediary_model(field, klass):`                                                                                                                                                                | ✓                                           |
| 62  | `atomic` usable as decorator + context manager; `try/except IntegrityError` around a nested block (code example)                                                                                                                                                                                                                                                                                      | L    | `docs/topics/db/transactions.txt:124-153`                                                                                                                        | source examples `@transaction.atomic def viewfunc(request): … with transaction.atomic(): do_more_stuff()` and `try: with transaction.atomic(): generate_relationships() except IntegrityError: handle_exception()` — page synthesizes both faithfully (identifiers from source) | ✓                                           |

## Discrepancies

- **None material.** No claim on the page contradicts the source tree. All ~20 verbatim
  QUOTES match the local files modulo the RST-role/literal-markup normalization declared at
  the top (roles like `` :class:`~…QuerySet` `` rendered as `` `QuerySet` ``; the atomic
  "Under the hood" bullet list flattened into a single `;`-joined sentence — content
  unchanged, punctuation joined).

## Unsourced / opinion

- **Editorial framings** (author synthesis, consistent with verified mechanics but not
  independently citable): "active-record-flavored", "the opposite pole from the effect-system
  libraries", "closer to Ruby `ActiveRecord` … than to the data-mapper ORMs", the
  Strengths/Weaknesses lists, and the entire **Key design decisions and trade-offs** table.
  Each row's _mechanism_ is grounded above (##11, ##43-47, ##52-54); the _trade-off_
  characterization is opinion 🌐-adjacent.
- **Cross-library contrasts** (`SQLAlchemy` unit-of-work, `EF Core`/`Hibernate` sessions,
  `Ecto` rejecting lazy loading, `Prisma` schema-first, `jOOQ` db-first) — asserted by name
  only, grounded in those libraries' own deep-dives/ledgers, not in the Django tree.
- **Web-attested point-in-time facts:** first release 2005 (#4), latest stable version (#58),
  Django's LTS/time-based release cadence, and "migrations introduced in Django 1.7" — all
  registry/web, not present in a `--depth 1` tree; kept minimal and flagged 🌐.

## Net

62 claims: **58 ✓ (local primary)**, **2 🌐 (release date / latest stable — web)**, plus 2
by-construction absence claims (no unit of work / identity map, #11/#18) verified by the
_absence_ of any session object in the tree. **0 discrepancies.** All verbatim quotes from
`django/db/` source docstrings and the in-tree `docs/` reST match the pinned checkout, with
the single declared normalization (RST cross-reference roles + literal-markup stripped to
their rendered form). Design-philosophy quote (models = "single, definitive source"), lazy-
QuerySet quote, parameterization/injection quote, `atomic`/savepoint nesting quotes, and the
migrations "version control for your schema" quote are the load-bearing citations and all
verify verbatim.

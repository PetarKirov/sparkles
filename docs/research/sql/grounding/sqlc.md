# Grounding ledger — `sqlc.md`

Claim-by-claim verification of `docs/research/sql/sqlc.md` against **local** primary sources:

- `$REPOS/go/sqlc` @ `22d878a` (`README.md`, `LICENSE`, `docs/`, `internal/`) — pinned in
  [`_sources.md`](./_sources.md).

`$REPOS = /home/petar/code/repos`. Type: **L** = local primary (this checkout); **🌐** =
web/point-in-time (cannot ground in a `--depth`-style tree). Status: ✓ verified · ⚠ discrepancy ·
🌐 web-fallback.

## Claims

| #   | Claim                                                                                                                                                                                                                                                                   | Type      | Source (local + locator)                                                           | Evidence                                                                                                                                              | Status                                  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| 1   | sqlc = a SQL compiler: write schema + annotated queries → `sqlc generate` parses/type-checks → emits typed Go                                                                                                                                                           | L         | `README.md:1,6-11`; `docs/index.rst:6,12-19`                                       | title "sqlc: A SQL Compiler"; "sqlc generates **type-safe code** from SQL. Here's how it works: 1. You write queries in SQL. …"                       | ✓                                       |
| 2   | QUOTE README three-step loop: "sqlc generates **type-safe code** from SQL. Here's how it works: 1. You write queries in SQL. 1. You run sqlc to generate code with type-safe interfaces to those queries. 1. You write application code that calls the generated code." | L         | `README.md:6-11`                                                                   | verbatim (source numbers all render as "1.")                                                                                                          | ✓                                       |
| 3   | QUOTE index: "sqlc generates **fully type-safe idiomatic Go code** from SQL. … Seriously, it's that easy. You don't have to write any boilerplate SQL querying code ever again."                                                                                        | L         | `docs/index.rst:12-19`                                                             | verbatim (ellipsis spans the numbered steps)                                                                                                          | ✓                                       |
| 4   | Repo title "sqlc: A SQL Compiler"                                                                                                                                                                                                                                       | L         | `README.md:1`                                                                      | "# sqlc: A SQL Compiler"                                                                                                                              | ✓                                       |
| 5   | `compile` subcommand "Statically check SQL for syntax and type errors" (no output)                                                                                                                                                                                      | L         | `internal/cmd/cmd.go:214-215`                                                      | `Use: "compile"`, `Short: "Statically check SQL for syntax and type errors"`                                                                          | ✓                                       |
| 6   | License MIT, `Copyright (c) 2024 Riza, Inc.`                                                                                                                                                                                                                            | L         | `LICENSE:1-3`                                                                      | "MIT License / Copyright (c) 2024 Riza, Inc."                                                                                                         | ✓                                       |
| 7   | QUOTE index epigraph: "And lo, the Great One looked down upon the people and proclaimed: 'SQL is actually pretty great'."                                                                                                                                               | L         | `docs/index.rst:9-10`                                                              | verbatim (page normalizes the two-line indented block onto one)                                                                                       | ✓                                       |
| 8   | QUOTE tutorial: "sqlc itself has no dependencies."                                                                                                                                                                                                                      | L         | `docs/tutorials/getting-started-postgresql.md:8-9`                                 | verbatim                                                                                                                                              | ✓                                       |
| 9   | QUOTE annotations: "sqlc requires each query to have a small comment indicating the name and command. The format of this comment is as follows: `-- name: <name> <command>`"                                                                                            | L         | `docs/reference/query-annotations.md:1-8`                                          | verbatim (page inlines the fenced `-- name:` format)                                                                                                  | ✓                                       |
| 10  | Query name must be a valid Go identifier (validated)                                                                                                                                                                                                                    | L         | `internal/metadata/meta.go:42-59`                                                  | `// A query name must be a valid Go identifier` + `validateQueryName`                                                                                 | ✓                                       |
| 11  | Command annotations `:one` `:many` `:exec` (+`:execresult`/`:execrows`/`:execlastid`/`:copyfrom`/`:batchexec`/`:batchmany`/`:batchone`)                                                                                                                                 | L         | `internal/metadata/meta.go:29-40`; `docs/reference/query-annotations.md`           | 10 `Cmd*` consts; each documented with SQL+Go example                                                                                                 | ✓                                       |
| 12  | Generated `DBTX` = `ExecContext`/`PrepareContext`/`QueryContext`/`QueryRowContext`; `New(db DBTX)`; `WithTx`                                                                                                                                                            | L         | `internal/codegen/golang/templates/stdlib/dbCode.tmpl:41-70` (`dbCodeTemplateStd`) | interface verbatim (page quotes it); `func New(db DBTX)`; `WithTx(tx)`                                                                                | ✓                                       |
| 13  | `New(db)` accepts `*sql.DB`/`*sql.Tx`/`*sql.Conn` (all satisfy `DBTX`); pooling delegated to the driver                                                                                                                                                                 | L         | `dbCode.tmpl` (stdlib `DBTX` is exactly the 4 `database/sql` methods)              | `*sql.DB`/`*sql.Tx`/`*sql.Conn` implement those 4 methods; sqlc emits no pool code                                                                    | ✓ (inference from interface)            |
| 14  | Optional prepared statements: `Prepare(ctx, db)` caches `*sql.Stmt` per query when `emit_prepared_queries`                                                                                                                                                              | L         | `dbCode.tmpl` (`{{if .EmitPreparedQueries}} func Prepare(ctx, db DBTX)…`)          | template emits `Prepare` calling `PrepareContext`                                                                                                     | ✓                                       |
| 15  | Query flow: parse each stmt → extract `-- name:` → resolve params/columns vs catalog; `SELECT *` expanded at compile time                                                                                                                                               | L         | `internal/compiler/compile.go:71-141`; `internal/compiler/parse.go:21-185`         | `parseQueries`/`parseQuery`; `metadata.ParseQueryNameAndType`; star-expansion check `astutils.Search(... A_Star ...)`                                 | ✓                                       |
| 16  | Typed `Query` = `SQL`,`Metadata`,`[]*Column`,`[]Parameter` (+`InsertIntoTable`,`RawStmt`)                                                                                                                                                                               | L         | `internal/compiler/query.go:45-61`                                                 | struct fields verbatim; `Column` has `NotNull`,`IsArray`,`ArrayDims`,`IsSqlcSlice`                                                                    | ✓                                       |
| 17  | Emitter makes a package-level `const` SQL + a method binding params to `QueryRowContext`/`QueryContext`/`ExecContext`                                                                                                                                                   | L         | `internal/codegen/golang/templates/stdlib/queryCode.tmpl:1-171`                    | `const {{.ConstantName}} = ...-- name:...`; `{{queryMethod .}}(ctx, {{.ConstantName}}, {{.Arg.Params}})`                                              | ✓                                       |
| 18  | Example `getAuthor` const + `GetAuthor(ctx, id)` method with `row.Scan`                                                                                                                                                                                                 | L-derived | reconstructed from `queryCode.tmpl` (`:one` block) + tutorial `GetAuthor :one`     | illustrative reconstruction, not a verbatim file quote; matches template shape (`row := q.db.QueryRowContext(ctx, getAuthor, id)`, `row.Scan`)        | ✓ (marked illustrative)                 |
| 19  | SQL text fixed at gen time (a `const`); data binds out-of-band → injection structurally impossible for bound values                                                                                                                                                     | L         | `queryCode.tmpl` (const + variadic params to `…Context`)                           | value passed as variadic arg to `database/sql`, never interpolated into the const                                                                     | ✓                                       |
| 20  | `sqlc.arg(name)` = named placeholder                                                                                                                                                                                                                                    | L         | `docs/reference/macros.md:3-23`                                                    | "This macro expands to an engine-specific parameter placeholder. The name … is noted and used during code generation."                                | ✓                                       |
| 21  | `sqlc.narg(name)` = "The same as `sqlc.arg`, but always marks the parameter as nullable"                                                                                                                                                                                | L         | `docs/reference/macros.md:77-95`                                                   | verbatim                                                                                                                                              | ✓                                       |
| 22  | `sqlc.embed(table)` reuses model structs → nested row fields                                                                                                                                                                                                            | L         | `docs/reference/macros.md:25-75`                                                   | "Embedding allows you to reuse existing model structs …"; `GetStudentAndScoreRow{ Student; TestScore }`                                               | ✓                                       |
| 23  | `sqlc.slice("ages")` "generates a dynamic query at runtime with the correct number of parameters"; can't be used with prepared statements                                                                                                                               | L         | `docs/reference/macros.md:97-119`                                                  | verbatim quote + "this macro can't be used with prepared statements"                                                                                  | ✓                                       |
| 24  | `sqlc.slice` = the one runtime string edit: `strings.Replace` on `/*SLICE:ages*/?`; values still bound                                                                                                                                                                  | L         | `queryCode.tmpl:139-159` (`queryCodeStdExec`)                                      | `query = strings.Replace(query, "/*SLICE:...*/?", strings.Repeat(",?", …), 1)`; params appended to `queryParams`                                      | ✓                                       |
| 25  | No dynamic query _structure_; every query is a fixed named statement decided at gen time                                                                                                                                                                                | L         | `internal/compiler/compile.go`; annotation model                                   | queries read from `.sql` files at generate time; no runtime query object emitted                                                                      | ✓ (opinion-adjacent, mechanically true) |
| 26  | Three engines dispatched by config: `postgresql`, `mysql` (dolphin), `sqlite`; optional DB analyzer default-off                                                                                                                                                         | L         | `internal/compiler/engine.go:51-118`                                               | `switch conf.Engine`: `sqlite.NewParser()`, `dolphin.NewParser()`, `postgresql.NewParser()`; analyzer built only if `conf.Database != nil` & enabled  | ✓                                       |
| 27  | PostgreSQL parser = `pganalyze/pg_query_go/v6` (wraps `libpg_query`, PG's own grammar)                                                                                                                                                                                  | L         | `internal/engine/postgresql/parse.go:9,154`; `go.mod`                              | `nodes "github.com/pganalyze/pg_query_go/v6"`; `tree, err := Parse(...)`; go.mod pins `v6.2.2`                                                        | ✓                                       |
| 28  | PG cgo fallback = `wasilibs/go-pgquery` WASM (windows or !cgo)                                                                                                                                                                                                          | L         | `internal/engine/postgresql/parser/parser_wasi.go:1-7`; `parse_default.go`         | `//go:build windows \|\| !cgo` → `import ".../go-pgquery/parser"`; default build (`!windows && cgo`) uses `pg_query_go`                               | ✓                                       |
| 29  | MySQL parser = `sqlc-dev/marino/parser` (fork of TiDB/PingCAP parser)                                                                                                                                                                                                   | L         | `internal/engine/dolphin/parse.go:11,17-22`; `CLAUDE.md:184`                       | `import "github.com/sqlc-dev/marino/parser"`; struct field `pingcap *parser.Parser`; CLAUDE.md "MySQL parser (uses TiDB parser)"                      | ✓                                       |
| 30  | SQLite parser = ANTLR-generated (`antlr4-go/antlr/v4` + generated `internal/engine/sqlite/parser`)                                                                                                                                                                      | L         | `internal/engine/sqlite/parse.go:1-12`; `go.mod`                                   | `import "github.com/antlr4-go/antlr/v4"` + `.../sqlite/parser`; go.mod pins `antlr4-go/antlr/v4 v4.13.1`                                              | ✓                                       |
| 31  | Catalog built from schema DDL; QUOTE "sqlc parses `CREATE TABLE` and `ALTER TABLE` statements in order to generate the necessary code."                                                                                                                                 | L         | `docs/howto/ddl.md:1-4`; `internal/compiler/compile.go:29-69`                      | verbatim; `parseCatalog` → `c.catalog.Update(stmts[i], c)`                                                                                            | ✓                                       |
| 32  | Nullability captured in catalog: col `NotNull` if `NOT NULL` or in primary key                                                                                                                                                                                          | L         | `internal/engine/postgresql/parse.go:446-453`                                      | `IsNotNull: isNotNull(item.ColumnDef) \|\| primaryKey[item.ColumnDef.Colname]`                                                                        | ✓                                       |
| 33  | Default: no DB connection — catalog & inference are purely static; `analyzer.database` optional/off                                                                                                                                                                     | L         | `internal/compiler/engine.go:72-113`; `internal/config/config.go:127-194`          | analyzer created only under `conf.Database != nil` + `IsEnabled()`; else pure catalog path                                                            | ✓                                       |
| 34  | QUOTE ddl: "sqlc does not perform database migrations for you. However, sqlc is able to differentiate between up and down migrations. sqlc ignores down migrations when parsing SQL files."                                                                             | L         | `docs/howto/ddl.md:28-30`                                                          | verbatim                                                                                                                                              | ✓                                       |
| 35  | Supports migration files from atlas/dbmate/golang-migrate/goose/sql-migrate/tern; strips rollbacks                                                                                                                                                                      | L         | `docs/howto/ddl.md:32-233`; `internal/migrations/migrations.go:16-40`              | tool list in ddl.md; `RemoveRollbackStatements` matches `-- +goose down`/`-- +migrate down`/`-- migrate:down`/tern marker                             | ✓                                       |
| 36  | Built-in Go emitter = `text/template`; renders `db.go`/`models.go`/`<queries>.sql.go`; run through `go/format`                                                                                                                                                          | L         | `internal/codegen/golang/gen.go:1-18,215-240`; `tutorial…:106-118`                 | `text/template` import; `template.Must(...ParseFS(templates,...))`; tutorial lists the three output files; `go/format` import                         | ✓                                       |
| 37  | Built-in Go generator invoked over the same `CodegenService`/`GenerateRequest` as plugins (`ext.HandleFunc`)                                                                                                                                                            | L         | `internal/cmd/generate.go:295-372`                                                 | `handler = ext.HandleFunc(golang.Generate)`; `client := plugin.NewCodegenServiceClient(handler)`; WASM/process cases parallel                         | ✓                                       |
| 38  | QUOTE plugins: WASM "are fully sandboxed; they do not have access to the network, filesystem, or environment variables."                                                                                                                                                | L         | `docs/guides/plugins.md` (WASM section)                                            | verbatim                                                                                                                                              | ✓                                       |
| 39  | QUOTE plugins: Process "offer minimal security. Only use plugins that you trust."                                                                                                                                                                                       | L         | `docs/guides/plugins.md` (Process section)                                         | verbatim                                                                                                                                              | ✓                                       |
| 40  | WASM plugin pinned by `url`+`sha256`, run in `wazero`; Process = external cmd                                                                                                                                                                                           | L         | `internal/config/config.go:88-99`; `internal/cmd/generate.go:309-320`; `go.mod`    | `Plugin{Process{Cmd}, WASM{URL,SHA256}}`; `wasm.Runner{URL,SHA256}`; go.mod pins `tetratelabs/wazero v1.12.0`                                         | ✓                                       |
| 41  | First-party plugins: Kotlin, Python, TypeScript; community: C#/F#/Java/PHP/Ruby/Zig/Rust/Gleam                                                                                                                                                                          | L         | `README.md:24-30`; `docs/reference/language-support.rst:1-42`                      | README lists sqlc-gen-{go,kotlin,python,typescript}; rst community table                                                                              | ✓                                       |
| 42  | Type mapping is compile-time; QUOTE datatypes "sqlc attempts to make reasonable default choices when mapping internal database types to Go types."                                                                                                                      | L         | `docs/reference/datatypes.md:1-4`; `internal/codegen/golang/go_type.go:39-67`      | verbatim; `goType(...)` resolves per-column, overrides first                                                                                          | ✓                                       |
| 43  | Nullable timestamp → `sql.NullTime`, NOT NULL → `time.Time`; `text[]` → `[]string`; pgx → `pgtype`                                                                                                                                                                      | L         | `docs/reference/datatypes.md:9-60`                                                 | `UpdatedAt sql.NullTime` vs `CreatedAt time.Time`; `Tags []string`; "pgx/v5 sql package uses the appropriate pgx types"                               | ✓                                       |
| 44  | Row hydration = generated `Scan` (no reflection): `:one` `row.Scan`; `:many` loops `rows.Next()`/`rows.Scan`                                                                                                                                                            | L         | `queryCode.tmpl:22-68`                                                             | `:one` → `err := row.Scan(...)`; `:many` → `for rows.Next() { ... rows.Scan(...) }`                                                                   | ✓                                       |
| 45  | Blocking sync methods: `(ctx, args) (value, error)`; no future/effect value                                                                                                                                                                                             | L         | `queryCode.tmpl`; `tutorial…:148-174`                                              | `authors, err := queries.ListAuthors(ctx)`; all methods synchronous                                                                                   | ✓                                       |
| 46  | QUOTE transactions: "the `WithTx` method allows a `Queries` instance to be associated with a transaction."                                                                                                                                                              | L         | `docs/howto/transactions.md:1-2`                                                   | verbatim                                                                                                                                              | ✓                                       |
| 47  | Tx pattern: driver `db.Begin()` → `defer tx.Rollback()` → `qtx := queries.WithTx(tx)` → `tx.Commit()`; `WithTx` returns `Queries{db: tx}`                                                                                                                               | L         | `docs/howto/transactions.md:49-100`; `dbCode.tmpl`                                 | example verbatim; `func (q *Queries) WithTx(tx *sql.Tx) *Queries { return &Queries{db: tx} }`                                                         | ✓                                       |
| 48  | Errors = plain Go `error`; `:one` empty → driver `sql.ErrNoRows`; optional `wrap_errors` flag wraps with `fmt.Errorf`                                                                                                                                                   | L         | `queryCode.tmpl` (return `err`); `gen.go:46,197` (`WrapErrors`)                    | methods return bare `error`; `{{if $.WrapErrors}}fmt.Errorf("query {{.MethodName}}: %w", err){{else}}err{{end}}`                                      | ✓                                       |
| 49  | `sqlc vet` = CEL lint rules; with DB conn exposes `EXPLAIN (… FORMAT JSON)` to rules                                                                                                                                                                                    | L         | `docs/howto/vet.md:1-114`                                                          | "runs queries through a set of lint rules" (CEL); `postgresql.explain` from `EXPLAIN (ANALYZE false, VERBOSE, COSTS, SETTINGS, BUFFERS, FORMAT JSON)` | ✓                                       |
| 50  | `sqlc verify` = "Verify schema, queries, and configuration for this project"; `sqlc diff` compares generated vs committed                                                                                                                                               | L         | `internal/cmd/verify.go:30`; `internal/cmd/cmd.go:261-262`                         | `Short: "Verify schema, queries, and configuration for this project"`; diff `Short: "Compare the generated files to the existing files"`              | ✓                                       |
| 51  | Latest version `v1.31.1` (pinned tree)                                                                                                                                                                                                                                  | L         | `internal/info/facts.go:5`                                                         | `const Version = "v1.31.1"`                                                                                                                           | ✓                                       |
| 52  | Backends: PostgreSQL/MySQL/SQLite primary; ClickHouse + GoogleSQL engines present; SQLite Beta                                                                                                                                                                          | L         | `internal/engine/` dir; `docs/reference/language-support.rst:4-12`; git log        | engine dirs incl. `clickhouse`,`googlesql`; rst: Go/SQLite = Beta; pin `22d878a` = "feat: add GoogleSQL support to `sqlc parse`"                      | ✓                                       |
| 53  | Created by Kyle Conroy; first release ≈2019; org `kyleconroy/sqlc` → `sqlc-dev/sqlc`                                                                                                                                                                                    | 🌐        | `README.md:35` (`github.com/sponsors/kyleconroy`); `blog` conroy.org               | sponsor link + blog author local; the **2019 date** is web-attested (no date in tree)                                                                 | 🌐                                      |
| 54  | Playground at play.sqlc.dev; docs at docs.sqlc.dev; sqlc Cloud (push/verify/managed DBs)                                                                                                                                                                                | L+🌐      | `README.md:12-21`; `tutorial…:206-249`                                             | links in README; Cloud push/verify workflow in tutorial; hosting facts = web                                                                          | ✓/🌐                                    |

## Discrepancies

- **None material.** The docs and code agree throughout. One nuance worth flagging (not a
  contradiction): the `README.md` and `docs/index.rst` describe the output as **Go**, but sqlc
  also emits Kotlin/Python/TypeScript via plugins — the page states this explicitly (metadata
  row + Ecosystem), and the built-in vs plugin split is grounded in `internal/cmd/generate.go`
  (#37) and `language-support.rst` (#41).

## Unsourced / opinion

- **Reconstructed example (#18).** The `getAuthor` const + `GetAuthor` method block is a
  faithful _reconstruction_ from `queryCode.tmpl`'s `:one` branch + the tutorial's
  `GetAuthor :one` query, not a verbatim file quote. The page presents it as illustrative
  generated output; its shape (const with `-- name:` header, `QueryRowContext`, `row.Scan`) is
  exactly what the template produces.
- **Editorial framings** — "genuine front end", "SQL is the source language; Go is the object
  language", "typed _shape_, untyped _effect_", "shift the failure left", the Strengths/Weaknesses
  and trade-off table — are author synthesis (🌐-opinion), each consistent with the verified
  mechanics above. Not independently citable.
- **`marino` = "fork of the TiDB/PingCAP parser" (#29).** Locally attested by the import path
  (`sqlc-dev/marino`), the `pingcap` struct-field name, and `CLAUDE.md:184` ("uses TiDB parser").
  The _fork_ relationship itself is inferred from these three signals, not from a marino
  changelog (marino is a separate repo, not in this checkout).
- **First-release year 2019 (#53).** Web-attested; the `--`pinned tree carries no first-release
  date. The Kyle Conroy authorship _is_ locally supported (sponsor link + blog).

## Net

54 claims: **51 ✓ (local primary)**, **2 🌐 (web-attested: 2019 first-release, hosting/Cloud
facts)**, **1 L-derived illustrative (the reconstructed `getAuthor` example, #18)**, **0
discrepancies**. All ~14 verbatim QUOTES (README three-step loop, index "fully type-safe…"/epigraph,
query-annotations `-- name:` format, ddl "does not perform database migrations", macros
`sqlc.narg`/`sqlc.slice`, plugins WASM/Process security, datatypes default-choices, transactions
`WithTx`, `compile`/`verify` command strings, DBTX interface) match source verbatim. The two
architectural pillars — sqlc's own embedded per-engine parsers (Postgres `libpg_query`, MySQL
`marino`/TiDB, SQLite ANTLR) and the `text/template` Go emitter behind a shared
`CodegenService` plugin protocol — are grounded in `internal/engine/*/parse.go`,
`internal/compiler/`, `internal/codegen/golang/`, and `internal/cmd/generate.go`. License (MIT/Riza)
and version (`v1.31.1`) confirmed against `LICENSE` and `internal/info/facts.go`.

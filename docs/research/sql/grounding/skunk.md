# Grounding ledger — `skunk.md`

Claim-by-claim verification of `docs/research/sql/skunk.md` against **local** primary
sources, pinned in [`_sources.md`](./_sources.md).

Source: `$REPOS/scala/skunk` @ `706dc37` (`modules/core/shared/src/main/scala/`,
`modules/core/shared/src/main/scala-3/`, `modules/docs/src/main/laika/`, `LICENSE`,
`build.sbt`). `$REPOS = /home/petar/code/repos`.

**Type:** `L` = local primary (repo file). `◯` = web / point-in-time / editorial (not
groundable in a `--depth 1` tree). **Status:** `✓` verified · `⚠` discrepancy · `◯`
web-fallback / opinion.

## Claims

| #   | Claim                                                                                                                                                                                                                                                                | Type  | Source (local + locator)                                                                                                                                                                                                                                                          | Evidence                                                                                                                                      | Status            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| 1   | skunk = data access library for Scala + Postgres                                                                                                                                                                                                                     | L     | `README.md:9`                                                                                                                                                                                                                                                                     | "Skunk is a data access library for Scala + Postgres."                                                                                        | ✓                 |
| 2   | License MIT, `Copyright (c) 2018-2024 by Rob Norris and Contributors`                                                                                                                                                                                                | L     | `LICENSE:1-3`                                                                                                                                                                                                                                                                     | "The MIT License (MIT) / Copyright (c) 2018-2024 by Rob Norris and Contributors"                                                              | ✓                 |
| 3   | QUOTE design: "**Skunk** is a functional data access layer for Postgres."                                                                                                                                                                                            | L     | `.../package.scala:8`                                                                                                                                                                                                                                                             | "'''Skunk''' is a functional data access layer for Postgres." (page renders `'''…'''` as bold)                                                | ✓                 |
| 4   | QUOTE (headline): "Skunk doesn't use JDBC. It speaks the Postgres wire protocol. It will not work with any other database back end."                                                                                                                                 | L     | `.../package.scala:12-13`                                                                                                                                                                                                                                                         | verbatim                                                                                                                                      | ✓                 |
| 5   | QUOTE: "Skunk is asynchronous all the way down, via cats-effect, fs2, and ultimately nio. The high-level network layers (`Protocol` and `Session`) are safe to use concurrently."                                                                                    | L     | `.../package.scala:14-16`                                                                                                                                                                                                                                                         | verbatim                                                                                                                                      | ✓                 |
| 6   | QUOTE: "Serialization to and from schema types is not typeclass-based, so there are no implicit derivations. Codecs are explicit, like parser combinators."                                                                                                          | L     | `.../package.scala:17-18`                                                                                                                                                                                                                                                         | verbatim                                                                                                                                      | ✓                 |
| 7   | QUOTE: "Skunk uses `Resource` for lifetime-managed objects, which means it takes some discipline to avoid leaks, especially when working concurrently."                                                                                                              | L     | `.../package.scala:20-21`                                                                                                                                                                                                                                                         | verbatim (page trims the trailing "May or may not end up being problematic.")                                                                 | ✓                 |
| 8   | QUOTE (microsite): "Skunk is powered by cats, cats-effect, scodec, and fs2." + "Skunk is purely functional, non-blocking, and provides a tagless-final API." + "Skunk gives very good error messages."                                                               | L     | `modules/docs/.../laika/index.md:6-8`                                                                                                                                                                                                                                             | verbatim (three consecutive bullets)                                                                                                          | ✓                 |
| 9   | Cross-built Scala 2.13 + 3 on JVM, Node.js, Native                                                                                                                                                                                                                   | L     | `modules/docs/.../laika/index.md:11`; `build.sbt:7-8` (`scala-2.13`, `scala-3.0`)                                                                                                                                                                                                 | "published for Scala 2.13, and 3 on JVM, Node.js, and Native"                                                                                 | ✓                 |
| 10  | Deps: cats-effect 3, fs2 3, scodec, otel4s                                                                                                                                                                                                                           | L     | `build.sbt:129` (`cats-effect % "3.7.0"`), `:130-131` (`fs2 3.13.0`), `:132-134` (`scodec-bits/core/cats`), `:135` (`otel4s-core`)                                                                                                                                                | all present                                                                                                                                   | ✓                 |
| 11  | `~` twiddle-list alias `type ~[+A, +B] = (A, B)`                                                                                                                                                                                                                     | L     | `.../package.scala:131`                                                                                                                                                                                                                                                           | "type ~[+A, +B] = (A, B)"                                                                                                                     | ✓                 |
| 12  | QUOTE `Session` docstring: "Represents a live connection to a Postgres database… this is a lifetime-managed resource and as such is invalid outside the scope of its owning `Resource`, as are any streams constructed here."                                        | L     | `.../Session.scala:30-33`                                                                                                                                                                                                                                                         | verbatim (page elides "Operations provided here are safe to use concurrently. Note that" mid-quote via `…`)                                   | ✓                 |
| 13  | `Session.Builder` `single` = `pooled(1).flatten`; `pooled(max)` yields nested `Resource[F, Resource[F, Session[F]]]`                                                                                                                                                 | L     | `.../Session.scala:616,638`                                                                                                                                                                                                                                                       | `def single… = pooled(1).flatten`; `def pooled(max: Int)… : Resource[F, Resource[F, Session[F]]]`                                             | ✓                 |
| 14  | `single` is "logically unpooled" / "In reality each session is managed by its own single-session pool"                                                                                                                                                               | L     | `.../Session.scala:611-615`                                                                                                                                                                                                                                                       | verbatim phrases in Builder `single` docstring                                                                                                | ✓                 |
| 15  | Builder example (`withUserAndPassword`/`withDatabase`/`pooled(10)`)                                                                                                                                                                                                  | L     | `.../Session.scala:701-705` (`@example`)                                                                                                                                                                                                                                          | matches verbatim                                                                                                                              | ✓                 |
| 16  | Pool carries describe + parse caches; QUOTE "The pool maintains a cache of queries and commands that have been checked against the schema, eliminating the need to check them more than once."                                                                       | L     | `.../Session.scala:630-632` (also `:738-740`); `describeCache`/`parseCache` at `:288,295`                                                                                                                                                                                         | verbatim                                                                                                                                      | ✓                 |
| 17  | `Recyclers.full` = `closeEvictedPreparedStatements <+> ensureIdle <+> unlistenAll <+> resetAll`; `unlistenAll` runs `UNLISTEN *`, `resetAll` runs `RESET ALL`                                                                                                        | L     | `.../Session.scala:414-415,438-443`                                                                                                                                                                                                                                               | `full… = closeEvictedPreparedStatements[F] <+> ensureIdle[F] <+> unlistenAll <+> resetAll`; `Command("UNLISTEN *"…)`, `Command("RESET ALL"…)` | ✓                 |
| 18  | QUOTE Fragments ref: "The resulting statement is prepared, and arguments (encoded) are passed separately as part of the extended query protocol. **Skunk never interpolates statement arguments.**"                                                                  | L     | `modules/docs/.../laika/reference/Fragments.md:33-35`                                                                                                                                                                                                                             | verbatim                                                                                                                                      | ✓                 |
| 19  | QUOTE `Fragment` docstring: "A composable, embeddable hunk of SQL and typed parameters (common precursor to `Command` and `Query`)."                                                                                                                                 | L     | `.../Fragment.scala:14-15`                                                                                                                                                                                                                                                        | verbatim                                                                                                                                      | ✓                 |
| 20  | `Fragment[A]` case class carries `parts: List[Either[String, State[Int, String]]]`, `encoder: Encoder[A]`, `origin`; `.query(dec)` → `Query`, `.command` → `Command`                                                                                                 | L     | `.../Fragment.scala:19-23,31-32,40-41`                                                                                                                                                                                                                                            | verbatim; `query` returns `Query(sql, origin, encoder, decoder, isDynamic = false)`; `command` returns `Command(sql, origin, encoder)`        | ✓                 |
| 21  | `sql` is a Scala-3 `transparent inline` macro; classifies each hole as `Encoder` / `Fragment[Void]` / `Fragment[a]`; else compile error                                                                                                                              | L     | `.../scala-3/syntax/StringContextOps.scala:161` (`transparent inline def sql`), `:94-97,100-101,105-107,110-111`                                                                                                                                                                  | case branches + `report.error(s"Found ${Type.show[t]}, expected String, Encoder, or Fragment.", a)`                                           | ✓                 |
| 22  | Abridged `sqlImpl` snippet (Encoder → `Par($e.sql)` + accumulate; `Fragment[Void]` → `Emb($f.parts)`; `Fragment[a]` → splice + `$f.encoder`; else `report.error`)                                                                                                    | L     | `.../scala-3/syntax/StringContextOps.scala:92-114`                                                                                                                                                                                                                                | snippet marked "(abridged)"; each case line matches source                                                                                    | ✓                 |
| 23  | QUOTE `Query` docstring: "We assume that `sql` has the same number of placeholders of the form `$1`, `$2`, etc.… The `check` methods on `Session` provide a means to verify this assumption."                                                                        | L     | `.../Query.scala:13-18`                                                                                                                                                                                                                                                           | verbatim (page keeps source's "columns are the number" typo inside the quote)                                                                 | ✓                 |
| 24  | `Query[A, B]` carries `encoder: Encoder[A]` (input) and `decoder: Decoder[B]` (output)                                                                                                                                                                               | L     | `.../Query.scala:38-44`                                                                                                                                                                                                                                                           | `final case class Query[A, B](… encoder: Encoder[A], decoder: Decoder[B], …)`                                                                 | ✓                 |
| 25  | Parameterized-query example (`… WHERE name LIKE $varchar".query(country)` yielding `Query[String, Country]`)                                                                                                                                                         | L     | `modules/docs/.../laika/tutorial/Query.md:132-138`                                                                                                                                                                                                                                | matches verbatim                                                                                                                              | ✓                 |
| 26  | QUOTE `Codec` docstring: "Symmetric encoder and decoder of Postgres text-format data to and from Scala types."                                                                                                                                                       | L     | `.../Codec.scala:14-15`                                                                                                                                                                                                                                                           | verbatim                                                                                                                                      | ✓                 |
| 27  | `Codec` snippet: `product`/`~`/`imap`/`opt`                                                                                                                                                                                                                          | L     | `.../Codec.scala:27-72`                                                                                                                                                                                                                                                           | `def product`, `def ~`, `def imap`, `override def opt` all present; snippet marked simplified with `/* … */`                                  | ✓                 |
| 28  | QUOTE `Encoder`: "Encoder of Postgres text-format data from Scala types."                                                                                                                                                                                            | L     | `.../Encoder.scala:15-16`                                                                                                                                                                                                                                                         | verbatim                                                                                                                                      | ✓                 |
| 29  | QUOTE `Decoder`: "Decoder of Postgres text-format data into Scala types."                                                                                                                                                                                            | L     | `.../Decoder.scala:13-14`                                                                                                                                                                                                                                                         | verbatim                                                                                                                                      | ✓                 |
| 30  | `varchar` is a `Codec[String]` usable as both encoder and decoder; QUOTE "We can do this because `varchar` actually has type `Codec[String]`."                                                                                                                       | L     | `Query.md:144-146`                                                                                                                                                                                                                                                                | verbatim ("We have already seen `varchar` used as a row decoder… now we're using it as an encoder…")                                          | ✓                 |
| 31  | `(varchar *: int4).to[Country]` / `.query(country)`                                                                                                                                                                                                                  | L     | `Query.md:114-116,120-125`                                                                                                                                                                                                                                                        | verbatim                                                                                                                                      | ✓                 |
| 32  | Nullability via `.opt`: `Codec[A]` → `Codec[Option[A]]`, all-NULL ⇒ `None`                                                                                                                                                                                           | L     | `.../Codec.scala:63-72`                                                                                                                                                                                                                                                           | `override def opt: Codec[Option[A]]` — `if (ss.forall(_.isEmpty)) Right(None) else … map(Some(_))`                                            | ✓                 |
| 33  | Decode failure is a typed `Decoder.Error(offset, length, message)` value                                                                                                                                                                                             | L     | `.../Decoder.scala:86`                                                                                                                                                                                                                                                            | `case class Error(offset: Int, length: Int, message: String, cause: Option[Throwable] = None)`                                                | ✓                 |
| 34  | `Session[F]` trait methods `execute(query)(args): F[List[B]]`, `unique: F[B]`, `option: F[Option[B]]`, `stream: Stream[F, B]`, `prepare`, `transaction: Resource[F, Transaction[F]]`, `channel`                                                                      | L     | `.../Session.scala:117,135,153,164,207,271,254`                                                                                                                                                                                                                                   | all present; snippet renames `stream`'s param `command`→`query` (cosmetic, see #D1)                                                           | ✓                 |
| 35  | `stream` pages a server-side cursor in `chunkSize`-row blocks (constant space)                                                                                                                                                                                       | L     | `.../Session.scala:158-164` (docstring "how many rows must be fetched by page"); `Query.md:154,166-168` ("cursors which allow results to be paged and streamed"; "fetches rows in blocks of 64")                                                                                  | confirmed                                                                                                                                     | ✓                 |
| 36  | QUOTE transaction docstring: "A transaction is begun before entering the `use` block… If the block exits due to cancellation or an error and the session transaction status is not `Idle` then the transaction will be rolled back and any error will be re-raised." | L     | `.../Session.scala:256-271`                                                                                                                                                                                                                                                       | verbatim (page joins two sentences with `…`)                                                                                                  | ✓                 |
| 37  | Finalizer is a 3×3 matrix over `{Idle, Active, Failed}` × `{Succeeded, Canceled, Errored}`; `Active`+`Succeeded` commits, else rollback+re-raise                                                                                                                     | L     | `.../Transaction.scala:187-210`                                                                                                                                                                                                                                                   | `release` matches on `transactionStatus` then `ExitCase`; `Active`→`Succeeded => doCommit`, others `doRollback`                               | ✓                 |
| 38  | Overload takes explicit `TransactionIsolationLevel` + `TransactionAccessMode`                                                                                                                                                                                        | L     | `.../Session.scala:279`                                                                                                                                                                                                                                                           | `def transaction[A](isolationLevel: TransactionIsolationLevel, accessMode: TransactionAccessMode): Resource[F, Transaction[F]]`               | ✓                 |
| 39  | Savepoints: `savepoint` emits `SAVEPOINT $i` (generated name), `rollback(savepoint)` emits `ROLLBACK TO` (snippet)                                                                                                                                                   | L     | `.../Transaction.scala:173-182`                                                                                                                                                                                                                                                   | verbatim for-comprehension; `s.execute(internal"SAVEPOINT $i".command)`; `rollback(savepoint)` runs `internal"ROLLBACK TO ${savepoint}"`      | ✓                 |
| 40  | Nested transaction raises `SkunkException` "Nested transactions are not allowed." with commit/rollback hint                                                                                                                                                          | L     | `.../Transaction.scala:103-108`                                                                                                                                                                                                                                                   | verbatim message + hint "You must roll back or commit the current transaction before starting a new one."                                     | ✓                 |
| 41  | `recoverWith { case SqlState.UniqueViolation(ex) => … xa.rollback(sp) }` transaction example                                                                                                                                                                         | L     | `Transactions.md:137-149`                                                                                                                                                                                                                                                         | matches (snippet abridged: drops the `IO.println("Trying to insert…")` line; recoverWith body `:143-146` verbatim)                            | ✓ (abridged, #D2) |
| 42  | `PostgresErrorException` lifts wire `ErrorResponse` fields: SQLSTATE `code`, `severity`, `detail`, `hint`, `position`, `constraintName`, `tableName`, `columnName`, `routine`, `fileName`, `line`                                                                    | L     | `.../PostgresErrorException.scala:62-138`                                                                                                                                                                                                                                         | each `def` present (`code`='C', `severity`='S', `constraintName`='n', etc.)                                                                   | ✓                 |
| 43  | `SkunkException.getMessage` frames each line with `🔥` and includes statement, position, arguments, `Origin`                                                                                                                                                         | L     | `.../SkunkException.scala:120-126,85-115`                                                                                                                                                                                                                                         | `.map("🔥  " + _)`; `statement`/`args` sections include SQL position + encoded args; `sqlOrigin` used                                         | ✓                 |
| 44  | Source comment: intended direction "turn this into an ADT of structured error types"                                                                                                                                                                                 | L     | `.../PostgresErrorException.scala:13`                                                                                                                                                                                                                                             | "// TODO: turn this into an ADT of structured error types"                                                                                    | ✓                 |
| 45  | QUOTE `SqlState` docstring: "Enumerated type of Postgres error codes. These can be used as extractors for error handling, for example: `doSomething.recoverWith { case SqlState.ForeignKeyViolation(ex) => ... }`"                                                   | L     | `.../scala-3/SqlState.scala:9-14`                                                                                                                                                                                                                                                 | verbatim                                                                                                                                      | ✓                 |
| 46  | `SqlState.UniqueViolation` = SQLSTATE `23505`; `unapply` matches a `PostgresErrorException` with that code                                                                                                                                                           | L     | `.../scala-3/SqlState.scala:1172` (`case UniqueViolation extends SqlState("23505")`), `:18-19` (`unapply … if e.code == code`)                                                                                                                                                    | confirmed                                                                                                                                     | ✓                 |
| 47  | QUOTE `Channel` docstring: "A **channel** that can be used for inter-process communication, implemented in terms of `LISTEN` and `NOTIFY`."                                                                                                                          | L     | `.../Channel.scala:17-24`                                                                                                                                                                                                                                                         | verbatim (`'''channel'''` bold); `listen(maxQueued): Stream[F, Notification[B]]` `:40`, `notify(message): F[Unit]` `:68`                      | ✓                 |
| 48  | Injection escape hatch `#$`; QUOTE "Interpolating a literal string into a `Fragment` is a SQL injection risk. Never interpolate values that have been supplied by the user."                                                                                         | L     | `Fragments.md:66-75`                                                                                                                                                                                                                                                              | verbatim callout; `#$table` literal-interpolation example `:79-88`                                                                            | ✓                 |
| 49  | `AppliedFragment` binds a fragment to its argument as an existential pair, forms a `Monoid`; used to fold optional `WHERE` clauses                                                                                                                                   | L     | `.../AppliedFragment.scala:13-52`; `Fragments.md:131-169`                                                                                                                                                                                                                         | `sealed trait AppliedFragment { type A; def fragment; def argument }`, `MonoidAppFragment`; doc `foldSmash(void" WHERE ", void" AND ", …)`    | ✓                 |
| 50  | No migration runner / no codegen / no introspection-to-code                                                                                                                                                                                                          | L     | absence: no `migration`/`codegen` module under `modules/core/shared/src/main/scala` (grep hit only in `reference/TwiddleLists.md`, unrelated)                                                                                                                                     | confirmed absent                                                                                                                              | ✓                 |
| 51  | Runtime schema verification via `Describe` protocol; `TypingStrategy` = `BuiltinsOnly` (default) or `SearchPath`                                                                                                                                                     | L     | `.../Session.scala:471` (default `BuiltinsOnly`), `:939-943` (`TypingStrategy.BuiltinsOnly`/`SearchPath` → `Typer.Static`/`Typer.fromProtocol`), `:288` (`describeCache`)                                                                                                         | confirmed                                                                                                                                     | ✓                 |
| 52  | Wire messages are scodec binary encoders/decoders; socket I/O via fs2                                                                                                                                                                                                | L     | `.../net/message/package.scala:20` ("Definitions of Postgres messages, with binary encoders and decoders."), `:9-17` (`import scodec.*`); `.../net/BitVectorSocket.scala:12,17` (`import fs2.io.net.Socket`; "A higher-level `Socket` interface defined in terms of `BitVector`") | confirmed                                                                                                                                     | ✓                 |
| 53  | Author = Rob Norris ("tpolecat"), same author as `doobie`; published under `org.tpolecat`                                                                                                                                                                            | L     | `LICENSE:3`; `README.md:6` (`org.tpolecat/skunk-core`); repo path `tpolecat/skunk`                                                                                                                                                                                                | confirmed (doobie authorship = general knowledge ◯)                                                                                           | ✓                 |
| 54  | Featured in _Practical FP in Scala_ (Volpe); Scala Days 2019 talk by author                                                                                                                                                                                          | L     | `modules/docs/.../laika/index.md:33-34`                                                                                                                                                                                                                                           | verbatim mentions                                                                                                                             | ✓                 |
| 55  | First release ≈ 2019-2020; latest `1.x` (`skunk-core`, base version `1.1`)                                                                                                                                                                                           | ◯ / L | `build.sbt:1` (`tlBaseVersion := "1.1"`); index.md survey timeline "2017–2019 skunk (Norris)"                                                                                                                                                                                     | base version local; exact first-release date + latest published version are web/registry facts                                                | ◯                 |
| 56  | PostgreSQL only, by design; no other backend                                                                                                                                                                                                                         | L     | `.../package.scala:12-13` ("will not work with any other database back end"); no other-DB modules in tree                                                                                                                                                                         | confirmed                                                                                                                                     | ✓                 |

## Discrepancies

- **#D1 (cosmetic) — `skunk.md` `Session` trait snippet.** The abridged trait block renames
  the `stream` parameter from the source's `command: Query[A, B]` to `query: Query[A, B]`
  for readability (the source signature is `def stream[A, B](command: Query[A, B])(args: A,
chunkSize: Int)` — `.../Session.scala:164`). Signatures/return types are otherwise exact;
  block is presented as abridged (`(abridged)`). No factual impact.

- **#D2 (abridgement) — `skunk.md` transaction `recoverWith` example.** Condensed from
  `Transactions.md:137-149`: the `IO.println(s"Trying to insert $p")` line is dropped and
  the `for`-comprehension tightened. The `SqlState.UniqueViolation` `recoverWith` body is
  verbatim (`:143-146`). Labelled with the doc path; a faithful condensation, not a misquote.

## Unsourced / opinion

- Editorial framings — "wire-native functional data mapper", "trades breadth for depth",
  "the safe path is the easy path, and the unsafe path is visibly ugly", the Strengths /
  Weaknesses lists, and the Key-design-decisions table — are author synthesis (◯), each
  consistent with the verified mechanics above. Not independently citable.
- The comparison to `doobie` ("same author", "stops below the ORM line like doobie") and to
  Effect TS ("rawness-is-opt-in", "`SqlErrorReason` union vs `SqlState` extractors") is
  cross-survey framing (◯); the skunk-side facts are grounded, the sibling-side facts live
  in those pages' own ledgers.
- First-release **date/year** and **latest published version number** (#55) are
  web/registry facts (Maven Central / project history); the `--depth 1` tree carries only
  `tlBaseVersion := "1.1"`. Flagged web-attested in the page.

## Net

56 claims: **53 ✓** local-verified, **2 cosmetic/abridgement notes** (#D1 snippet param
rename, #D2 condensed doc example — both faithful), **1 web-attested** (#55 release
date/version). All ~15 verbatim quotes from `package.scala`, `index.md` (microsite),
`Session.scala`, `Fragment.scala`, `Query.scala`, `Codec.scala`/`Encoder.scala`/`Decoder.scala`,
`Transaction.scala`, `SqlState.scala`, `Channel.scala`, and `Fragments.md` match source
verbatim (modulo `'''…'''`→bold rendering and marked `…` elisions). The headline design
claim ("doesn't use JDBC… speaks the Postgres wire protocol") is grounded in the package
object; the injection-safety invariant ("Skunk never interpolates statement arguments") in
the Fragments reference; the wire-native mechanics (scodec messages, fs2 socket, `Describe`
typing, savepoint transactions, `SqlState` extractors) all in the core sources.

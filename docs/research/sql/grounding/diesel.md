# Grounding ledger — `diesel.md`

Claim-by-claim verification of `docs/research/sql/diesel.md` against the **local** pinned checkout:

- `$REPOS/rust/diesel` @ `d4378b5` (`diesel/src/`, `diesel_derives/src/`, `diesel_migrations/src/`,
  `diesel_cli/src/`, `README.md`, `guide_drafts/`, `CHANGELOG.md`, `Cargo.toml`, `LICENSE-*`)

`$REPOS = /home/petar/code/repos`. Type: **L** = local primary; **🌐** = web/point-in-time
(cannot ground in a `--depth 1`-style tree). Status: ✓ verified · ⚠ discrepancy/caveat · 🌐 web-fallback.
Pinned in [`_sources.md`](./_sources.md).

## Claims

| #   | Claim                                                                                                                                                                                                                                       | Type | Source (local + locator)                                                                                            | Evidence                                                                                                                                                                                                         | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1   | Diesel = "safe, extensible ORM and Query Builder for Rust"                                                                                                                                                                                  | L    | `README.md:3`; `diesel/Cargo.toml:5`                                                                                | title "# A safe, extensible ORM and Query Builder for Rust"; desc "A safe, extensible ORM and Query Builder for PostgreSQL, SQLite, and MySQL"                                                                   | ✓      |
| 2   | Dual license MIT OR Apache-2.0; © Sean Griffin 2015-2021, Diesel Core Team 2018-2021                                                                                                                                                        | L    | `diesel/Cargo.toml:4`; `LICENSE-MIT:2`; `LICENSE-APACHE` exists                                                     | `license = "MIT OR Apache-2.0"`; "2015-2021 Sean Griffin, 2018-2021 Diesel Core Team"                                                                                                                            | ✓      |
| 3   | Pinned version `2.3.11`, released 2026-07-10                                                                                                                                                                                                | L    | `diesel/Cargo.toml:3`; `CHANGELOG.md:59`                                                                            | `version = "2.3.11"`; "## [2.3.11] 2026-07-10"                                                                                                                                                                   | ✓      |
| 4   | Backends PostgreSQL, MySQL, SQLite                                                                                                                                                                                                          | L    | `README.md:17-20`; `diesel/Cargo.toml:5`                                                                            | "Supported databases: 1. PostgreSQL 2. MySQL 3. SQLite"                                                                                                                                                          | ✓      |
| 5   | Edition 2024, MSRV `1.88.0`                                                                                                                                                                                                                 | L    | `Cargo.toml:38` (workspace.package)                                                                                 | `rust-version = "1.88.0"`, `edition = "2024"`                                                                                                                                                                    | ✓      |
| 6   | `#![no_std]`-capable (sqlite-no-std)                                                                                                                                                                                                        | L    | `diesel/src/lib.rs:1`; `:160-161`                                                                                   | `#![cfg_attr(not(feature = "std"), no_std)]`; "`sqlite-no-std` … doesn't enable the `std` feature flag"                                                                                                          | ✓      |
| 7   | QUOTE README philosophy: "eliminates runtime errors without sacrificing performance … 'feels like Rust'"                                                                                                                                    | L    | `README.md:12-15`                                                                                                   | verbatim ("Diesel gets rid of the boilerplate … eliminates runtime errors without sacrificing performance … query builder that \"feels like Rust.\"")                                                            | ✓      |
| 8   | QUOTE lib.rs: "Diesel is an ORM and query builder designed to reduce the boilerplate for database interactions."                                                                                                                            | L    | `diesel/src/lib.rs:4`                                                                                               | verbatim                                                                                                                                                                                                         | ✓      |
| 9   | QUOTE lib.rs: "For Diesel to validate your queries at compile time it requires you to specify your schema in your code … `table!` macro … `diesel print-schema` can be used to automatically generate these macro calls"                    | L    | `diesel/src/lib.rs:16-21`                                                                                           | verbatim (reflowed from `//!` lines)                                                                                                                                                                             | ✓      |
| 10  | QUOTE trait_derives: "think of Diesel as a SQL query builder. While Diesel does offer some standard ORM … features, Diesel's code generation derives are for safely building SQL queries."                                                  | L    | `guide_drafts/trait_derives.md:23-25`                                                                               | verbatim                                                                                                                                                                                                         | ✓      |
| 11  | QUOTE trait_derives: "assist writing safe SQL queries in Rust … through implementing a series of traits on your structs."                                                                                                                   | L    | `guide_drafts/trait_derives.md:4-7`                                                                                 | verbatim (reflowed)                                                                                                                                                                                              | ✓      |
| 12  | QUOTE result.rs: "While Diesel prevents almost all sources of runtime errors at compile time, it does not attempt to prevent 100% of them. Typically this error will occur from insert or update statements due to a constraint violation." | L    | `diesel/src/result.rs:25-28`                                                                                        | verbatim (doc comment on `DatabaseError`)                                                                                                                                                                        | ✓      |
| 13  | QUOTE derives (table!): "Specifies that a table exists, and what columns it has. This will create a new public module … a unit struct named `table`, and a unit struct with the name of each column."                                       | L    | `diesel_derives/src/lib.rs:1294-1297`                                                                               | verbatim                                                                                                                                                                                                         | ✓      |
| 14  | `table!` doc example (`users { id -> Integer, name -> VarChar, favorite_color -> Nullable<VarChar> }`)                                                                                                                                      | L    | `diesel_derives/src/lib.rs:1311-1320`                                                                               | matches (copied from doctest)                                                                                                                                                                                    | ✓      |
| 15  | Generated module also has `dsl`, `all_columns`, `star`, `SqlType`, `BoxedQuery`                                                                                                                                                             | L    | `diesel_derives/src/lib.rs:1439-1478`                                                                               | headers "dsl", "`all_columns`", "star", "`SqlType`", "`BoxedQuery`"                                                                                                                                              | ✓      |
| 16  | `dsl` re-export enables `users.filter(name.eq("Sean"))` vs `users::table.filter(users::name.eq("Sean"))`                                                                                                                                    | L    | `diesel_derives/src/lib.rs:1444-1447`                                                                               | "to allow writing `users.filter(name.eq(\"Sean\"))` instead of `users::table.filter(users::name.eq(\"Sean\"))`"                                                                                                  | ✓      |
| 17  | QueryDsl clause methods map to SQL keywords, `WHERE`→`filter`                                                                                                                                                                               | L    | `diesel/src/lib.rs:41-45`; `diesel/src/query_dsl/mod.rs:1-6`                                                        | "`WHERE` which is called `filter` in Diesel (To not conflict with the Rust keyword)"                                                                                                                             | ✓      |
| 18  | `.eq`/`.ne` named after Rust `==`/`!=`                                                                                                                                                                                                      | L    | `diesel/src/lib.rs:53-55`                                                                                           | "`==` is called `.eq`, and `!=` is called `.ne`"                                                                                                                                                                 | ✓      |
| 19  | `.eq` signature enforces `T: AsExpression<Self::SqlType>` (compile-time type check)                                                                                                                                                         | L    | `diesel/src/expression_methods/global_expression_methods.rs:70-76`                                                  | `fn eq<T>(…) where Self::SqlType: SqlType, T: AsExpression<Self::SqlType>` verbatim                                                                                                                              | ✓      |
| 20  | Wrong-table column ref fails via `SelectableExpression`/from-clause bound                                                                                                                                                                   | L    | `diesel/src/lib.rs:129-135`                                                                                         | "`posts::title: SelectableExpression<users::table> is not satisfied` … field from a table that does not appear in your from clause"                                                                              | ✓      |
| 21  | QUOTE expression/mod (AsExpression): value "has data which will be sent separately from the query … 'bind parameter' … Types which implement `ToSql` will generally implement `AsExpression` this way."                                     | L    | `diesel/src/expression/mod.rs:230-232`                                                                              | verbatim                                                                                                                                                                                                         | ✓      |
| 22  | `Bound<T,U>` QueryFragment renders via `push_bind_param(&self.item)`                                                                                                                                                                        | L    | `diesel/src/expression/bound.rs:33-42`                                                                              | `impl … QueryFragment … { pass.push_bind_param(&self.item)?; }` verbatim                                                                                                                                         | ✓      |
| 23  | QUOTE query_builder: "A bind parameter is a value which is sent separately … represented in SQL with a placeholder such as `?` or `$1`."                                                                                                    | L    | `diesel/src/query_builder/mod.rs:251-254`                                                                           | verbatim                                                                                                                                                                                                         | ✓      |
| 24  | `QueryBuilder` surface = `push_sql`, `push_identifier` (quoted), `push_bind_param`, `finish`                                                                                                                                                | L    | `diesel/src/query_builder/mod.rs:161-178`                                                                           | trait methods verbatim                                                                                                                                                                                           | ✓      |
| 25  | `sql_query` raw SQL + `.bind::<T,_>` with safety caveat                                                                                                                                                                                     | L    | `README.md:224-241`; `diesel/src/query_builder/sql_query.rs:40-44`                                                  | "Diesel cannot validate that the value is of the right type nor … the correct number of parameters"                                                                                                              | ✓      |
| 26  | `dsl::sql::<ST>("…")` fragment escape hatch + safety caveat                                                                                                                                                                                 | L    | `diesel/src/expression/sql_literal.rs:34-38`                                                                        | "# Safety … Diesel cannot validate that the value is of the right type …"                                                                                                                                        | ✓      |
| 27  | Database-first: `diesel print-schema` introspects live DB into schema                                                                                                                                                                       | L    | `diesel/src/lib.rs:19-21`; `diesel_cli/src/print_schema.rs` exists; `diesel_cli/src/main.rs:106` `run_infer_schema` | "connecting to your database and querying its schema"; CLI `print_schema` module present                                                                                                                         | ✓      |
| 28  | QUOTE migrations: "versioned, and run in order. Diesel also takes care of tracking which migrations have already been run automatically … no migration is run twice unless it has been reverted."                                           | L    | `diesel_migrations/src/lib.rs:16-21`                                                                                | verbatim (reflowed)                                                                                                                                                                                              | ✓      |
| 29  | Migration folder `{version}_{name}/` with `up.sql`/`down.sql`                                                                                                                                                                               | L    | `diesel_migrations/src/lib.rs:26-30`; `migrations/sqlite/20170816164356_add_keywords_table/{up,down}.sql` exist     | "`up.sql` will be used to run the migration, while `down.sql` … for reverting it"; sample files present                                                                                                          | ✓      |
| 30  | Each migration runs in its own transaction by default (opt-out)                                                                                                                                                                             | L    | `diesel_migrations/src/lib.rs:40-49`                                                                                | "each migration runs inside its own transaction … automatically rolled back … `run_in_transaction = false`"                                                                                                      | ✓      |
| 31  | `embed_migrations!` compiles migrations into the binary                                                                                                                                                                                     | L    | `diesel_migrations/src/lib.rs:31-33`                                                                                | "embedded into the compiled application … consult the [`embed_migrations!`] macro"                                                                                                                               | ✓      |
| 32  | SQL types (`sql_types::Integer`, …) are markers, never on `Queryable` structs                                                                                                                                                               | L    | `diesel/src/lib.rs:70-74`                                                                                           | "These types are only used to represent a SQL type. You should never put them on your `Queryable` structs."                                                                                                      | ✓      |
| 33  | Encode/decode traits `ToSql<ST,DB>` / `FromSql<ST,DB>`, backend-parameterized                                                                                                                                                               | L    | `diesel/src/lib.rs:79-87`; `diesel/src/serialize.rs`, `deserialize.rs` exist                                        | "go to the docs for either [`ToSql`] or [`FromSql`]"                                                                                                                                                             | ✓      |
| 34  | QUOTE Queryable: positional, "field order is significant … **Field name has no effect**."                                                                                                                                                   | L    | `diesel_derives/src/lib.rs:545-551`                                                                                 | "it will assume that **all fields on your struct** matches **all fields in the query**, including the order and count … **Field name has no effect**"                                                            | ✓      |
| 35  | Selectable maps by column name; `check_for_backend` compile-checks + better errors                                                                                                                                                          | L    | `diesel_derives/src/lib.rs:953-991`                                                                                 | "annotate the field with `#[diesel(column_name = some_column)]`"; "`check_for_backend(…)` … instructs the derive to generate additional code to identify potential type mismatches … much better error messages" | ✓      |
| 36  | `#[derive(Insertable)]` maps struct fields → columns for INSERT (`column_name` override)                                                                                                                                                    | L    | `diesel_derives/src/lib.rs:348-358`; `README.md:146-161`                                                            | "if a field name … differs from the name of the corresponding column, you can annotate … `#[diesel(column_name = …)]`"; NewUser example                                                                          | ✓      |
| 37  | Nullability: `Nullable<ST>` ⟷ Rust `Option<T>` (via FromSql/ToSql)                                                                                                                                                                          | L    | `diesel/src/type_impls/option.rs:29,47,62`                                                                          | `impl<T,ST,DB> FromSql<Nullable<ST>,DB> for Option<T>`; `… ToSql<Nullable<ST>,DB> for Option<T>`                                                                                                                 | ✓      |
| 38  | `Connection` is blocking/synchronous (`&mut self`, returns `Result`); no `async` in tree                                                                                                                                                    | L    | `diesel/src/connection/mod.rs:224-245`; `grep async diesel/src/lib.rs` → none                                       | `trait Connection … fn establish(database_url: &str) -> ConnectionResult<Self>`; no async items                                                                                                                  | ✓      |
| 39  | Async is out-of-tree `diesel-async` (not in checkout)                                                                                                                                                                                       | 🌐   | not present in `$REPOS/rust/diesel`                                                                                 | crate lives in the separate `diesel-async` repo (web)                                                                                                                                                            | 🌐     |
| 40  | Concrete conn types `PgConnection`/`SqliteConnection`/`MysqlConnection`                                                                                                                                                                     | L    | `diesel/src/pg/connection/mod.rs:125`; `sqlite/connection/mod.rs:184`; `mysql/connection/mod.rs:110`                | `pub struct PgConnection`/`SqliteConnection`/`MysqlConnection`                                                                                                                                                   | ✓      |
| 41  | Prepared statements + `StatementCache`; `CacheSize` (Unbounded/Disabled)                                                                                                                                                                    | L    | `diesel/src/connection/mod.rs:50-58`, `:125-147`                                                                    | "It's important to use prepared statements …"; `enum CacheSize { Unbounded, Disabled }`                                                                                                                          | ✓      |
| 42  | Pooling opt-in via `r2d2` (`ConnectionManager`, `Pool`)                                                                                                                                                                                     | L    | `diesel/src/r2d2.rs:1-2`, `:15-19`                                                                                  | "Connection pooling via r2d2." + `ConnectionManager`/`Pool` usage in doc                                                                                                                                         | ✓      |
| 43  | `RunQueryDsl` execution: `execute`→count, `load`→`Vec<U>`, `first`/`get_result`→one row, `get_results`→rows                                                                                                                                 | L    | `diesel/src/query_dsl/mod.rs:1444-1596`, `:1775-1841`                                                               | `fn execute … QueryResult<usize>`; `fn load … QueryResult<Vec<U>>`; `fn first`/`get_result`/`get_results`                                                                                                        | ✓      |
| 44  | `QueryResult<T> = Result<T, Error>`; `ConnectionResult<T> = Result<T, ConnectionError>`                                                                                                                                                     | L    | `diesel/src/result.rs:294`, `:300`                                                                                  | verbatim type aliases                                                                                                                                                                                            | ✓      |
| 45  | QUOTE Connection.transaction: closure atomic; "If there is already an open transaction … savepoints will be used instead. … committed if … `Ok(_)`, rolled back if … `Err(_)`."                                                             | L    | `diesel/src/connection/mod.rs:249-253`                                                                              | verbatim                                                                                                                                                                                                         | ✓      |
| 46  | Savepoint SQL: `BEGIN` at top level else `SAVEPOINT diesel_savepoint_{n}`; rollback `ROLLBACK` / `ROLLBACK TO SAVEPOINT diesel_savepoint_{n-1}`                                                                                             | L    | `diesel/src/connection/transaction_manager.rs:348-352`, `:380-388`                                                  | `None => Cow::from("BEGIN"), Some(depth) => … "SAVEPOINT diesel_savepoint_{depth}"`; rollback match `1 => "ROLLBACK"`, else `"ROLLBACK TO SAVEPOINT diesel_savepoint_{}"`                                        | ✓      |
| 47  | `Error::RollbackTransaction` rolls back with no real error; "never return this variant unless you gave it to us"                                                                                                                            | L    | `diesel/src/result.rs:82-88`                                                                                        | verbatim doc                                                                                                                                                                                                     | ✓      |
| 48  | `Error` = single non-exhaustive enum of all query failures (`DatabaseError`, `NotFound`, `QueryBuilderError`, `DeserializationError`, `SerializationError`, `RollbackErrorOnCommit`, …)                                                     | L    | `diesel/src/result.rs:16-106`                                                                                       | `#[non_exhaustive] pub enum Error { … }` variants verbatim                                                                                                                                                       | ✓      |
| 49  | `DatabaseErrorKind` tags `UniqueViolation`, `ForeignKeyViolation`, …                                                                                                                                                                        | L    | `diesel/src/result.rs:115-120`                                                                                      | `enum DatabaseErrorKind { UniqueViolation = 0, ForeignKeyViolation, … }`                                                                                                                                         | ✓      |
| 50  | `OptionalExtension::optional` turns `Err(NotFound)` into `Ok(None)`                                                                                                                                                                         | L    | `diesel/src/result.rs:303-328`                                                                                      | `fn optional(self) -> Result<Option<T>, Error>` + impl                                                                                                                                                           | ✓      |
| 51  | Associations are child-to-parent only; "Unlike other ORMs, Diesel has no concept of `has many`"                                                                                                                                             | L    | `diesel/src/associations/mod.rs:3-5`                                                                                | verbatim                                                                                                                                                                                                         | ✓      |
| 52  | Crate root has a "How to read diesels compile time error messages" section (errors are hard)                                                                                                                                                | L    | `diesel/src/lib.rs:89-135`                                                                                          | section header + enumerated error patterns                                                                                                                                                                       | ✓      |
| 53  | `32-/64-/128-column-tables` features; wider "substantially"/"significantly" increase compile time                                                                                                                                           | L    | `diesel/src/lib.rs:192-199`                                                                                         | "enabling 64 column tables or larger will substantially increase the compile time"; "128-column-tables … increase your compile times significantly"                                                              | ✓      |
| 54  | Large workspace: `diesel`, `diesel_derives`, `diesel_cli`, `diesel_migrations`, `diesel_dynamic_schema`, `dsl_auto_type`, …                                                                                                                 | L    | `Cargo.toml:3-38` (members)                                                                                         | member list verbatim                                                                                                                                                                                             | ✓      |
| 55  | Funded by NLnet/NGI Zero, Prototype Fund, GitHub Secure Open Source                                                                                                                                                                         | L    | `README.md:266-329`                                                                                                 | sponsor table (NLNet Foundation, NGI Zero Core, Prototype Fund, GitHub Secure Open Source Fund)                                                                                                                  | ✓      |
| 56  | First release ≈2016 (Sean Griffin; 1.0 in 2018, 2.0 in 2022)                                                                                                                                                                                | 🌐   | `LICENSE-MIT:2` gives © from 2015 (hint only)                                                                       | release-year milestones from crates.io/GitHub releases (web)                                                                                                                                                     | 🌐     |
| 57  | Peers: `sqlx` (macro-checked raw SQL vs live DB), `SeaORM`, `cornucopia`; JVM analogue `jOOQ`                                                                                                                                               | 🌐/L | `_sources.md` lists sibling repos; comparison is editorial                                                          | cross-library framing; `sqlx`/`sea-orm`/`cornucopia`/`jooq` are sibling survey pages                                                                                                                             | 🌐     |

## Discrepancies

- **None material.** All quoted strings match the pinned tree verbatim (doc comments were reflowed
  from `//!`/`///` line prefixes, and inner `\"…\"` escapes were normalized to plain quotes — a
  typographic nit, not a content change).
- **Caveat #39 (async).** The page states async lives in a separate `diesel-async` crate. That crate
  is **not** in this checkout; the claim is grounded negatively (no `async` in `diesel/src/`) and
  positively only by web knowledge. Flagged 🌐, kept minimal.
- **Caveat #35 (Selectable "by name").** The derive doc frames field→column mapping via
  `column_name`, and `Selectable` builds an `as_select()` clause from named columns; "by name"
  is an accurate paraphrase of the `column_name`/`as_select` mechanism rather than a verbatim
  phrase. Contrast with `Queryable`'s explicit "Field name has no effect" (#34), which is verbatim.

## Unsourced / opinion

- Editorial framings — "the heart of Diesel", "injection is impossible by construction", "the same
  guarantee a tagged-template library gives", "database-first in practice", "the SQL migrations are
  authoritative; the `schema.rs` is derived", "sits at the blocking end, the opposite of the
  effect-value mappers", and the Strengths/Weaknesses/trade-off table — are author synthesis. Each is
  consistent with the verified mechanics above (compile-time `AsExpression` bound, `push_bind_param`,
  `print-schema` → `schema.rs`, synchronous `Connection`) but is not an independently citable quote.
- Cross-library comparisons to `sqlx`, `jOOQ`, `Kysely`, `Drizzle`, `SeaORM`, `Slick`, `doobie`,
  `skunk`, Effect TS — positioning only; grounded in those libraries' own pages, not this tree.
- First/latest-release **dates** and the 1.0/2.0 milestone years (#56) are web-attested; the local
  tree carries only the pinned `2.3.11` version + 2026-07-10 changelog date (#3) and the 2015
  copyright start (#2).

## Net

57 claims: **53 ✓ locally grounded**, **3 🌐 web-attested** (async companion crate, historical
release years, cross-library peer framing), **0 ⚠ substantive discrepancies** (2 phrasing caveats
noted). ~13 verbatim quotes span `README.md`, `diesel/src/lib.rs`, `guide_drafts/trait_derives.md`,
`result.rs`, `connection/mod.rs`, `transaction_manager.rs`, `expression/mod.rs`, `bound.rs`,
`query_builder/mod.rs`, `diesel_derives/src/lib.rs`, and `diesel_migrations/src/lib.rs`. The two core
sections — query construction/injection safety (the `table!` macro, the `.eq`
`AsExpression<Self::SqlType>` bound, `Bound` → `push_bind_param`, `?`/`$1` placeholders) and the
effect model (blocking `Connection`, `SAVEPOINT diesel_savepoint_n` nesting, the flat
`diesel::result::Error` enum) — are grounded directly in source, not docs.

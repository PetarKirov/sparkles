# Grounding ledger — `kysely.md`

Claim-by-claim verification of `docs/research/sql/kysely.md` against the **local** pinned
checkout (see [`_sources.md`](./_sources.md)):

- `$REPOS/typescript/kysely` @ `c431677` — the `0.29.3` tree (`src/`, `site/docs/`, `README.md`,
  `LICENSE`, `package.json`)

`$REPOS = /home/petar/code/repos`. **Type:** `quote` · `fact` (date/author/license) ·
`figure` (number/version) · `behavior` (library does X) · `exposition` (standard knowledge) ·
`opinion`. **Status:** ✓ verified · ⚠ discrepancy · ◯ web/point-in-time or editorial.

> [!NOTE]
> Kysely's typed surface is almost entirely **type-level TypeScript** (mapped/conditional types
> over a supplied `Database` interface). Runtime behaviour that this survey cares about — AST
> construction, parameter binding, dialect placeholders, transactions/savepoints — is fully
> present in `src/` and grounded below; the type-inference claims ("result type inferred from the
> query", nullability preserved) are grounded in the type definitions and the docs, which is the
> best a source tree can carry for a compile-time-only mechanism.

## Claims

| #   | Claim                                                                                                                                                                                                                                                                                | Type           | Source (local + locator)                                                                                                                               | Evidence                                                                                                                                                                                                                                | Status         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| 1   | QUOTE positioning: "Kysely (pronounce "Key-Seh-Lee") is a type-safe and autocompletion-friendly TypeScript SQL query builder. Inspired by Knex.js."                                                                                                                                  | quote          | `README.md:28-29`                                                                                                                                      | verbatim; source has smart quotes “Key-Seh-Lee” + markdown links `[TypeScript]`/`[SQL]`/`[Knex.js]` — page de-links and uses straight quotes                                                                                            | ✓ (de-markup)  |
| 2   | QUOTE scope/inference: "Kysely makes sure you only refer to tables and columns that are visible to the part of the query you're writing. The result type only has the selected columns with correct types and aliases. As an added bonus you get autocompletion for all that stuff." | quote          | `README.md:35-37`                                                                                                                                      | verbatim                                                                                                                                                                                                                                | ✓              |
| 3   | QUOTE `@typeParam DB`: "The database interface type. Keys of this type must be table names in the database and values must be interfaces that describe the rows in those tables."                                                                                                    | quote          | `src/kysely.ts:93-95`                                                                                                                                  | verbatim (page joins the wrapped lines; drops "See the examples above.")                                                                                                                                                                | ✓              |
| 4   | `Database` example: `{ person: { id: Generated<number>; first_name: string; last_name: string \| null } }`; `new Kysely<Database>({ dialect: new SqliteDialect(...) })`                                                                                                              | quote          | `src/kysely.ts:78-91`                                                                                                                                  | class docstring `@example` block, reproduced (abridged)                                                                                                                                                                                 | ✓              |
| 5   | QUOTE "not an ORM": "Kysely IS NOT an ORM. Kysely DOES NOT have the concept of relations. Kysely IS a query builder. Kysely DOES build the SQL you tell it to, nothing more, nothing less."                                                                                          | quote          | `site/docs/recipes/0001-relations.md:4-5`                                                                                                              | verbatim (rendered inside an `<h3>`)                                                                                                                                                                                                    | ✓              |
| 6   | QUOTE type-only: "This method only modifies the types and doesn't affect any of the executed queries in any way."                                                                                                                                                                    | quote          | `src/kysely.ts:486-487` (also `:522-523`, `:558-559`)                                                                                                  | verbatim (docstring of `$extendTables`/`$omitTables`/`$pickTables`)                                                                                                                                                                     | ✓              |
| 7   | Escape hatches exist for the untypeable ("there are cases where things cannot be typed at compile time, and Kysely offers escape hatches for these situations")                                                                                                                      | quote          | `site/docs/intro.mdx:18`                                                                                                                               | verbatim                                                                                                                                                                                                                                | ✓              |
| 8   | QUOTE per-instance pool: "You should create one instance of `Kysely` per database using the `Kysely` constructor. Each `Kysely` instance maintains its own connection pool."                                                                                                         | quote          | `src/kysely.ts:67-69`                                                                                                                                  | verbatim; source has `{@link Kysely}` markup → page renders `Kysely`                                                                                                                                                                    | ✓ (de-markup)  |
| 9   | QUOTE driver delegates to vendor pool: "The `Driver`'s job is to abstract away vendor-specific details. It communicates with the actual third-party `DatabaseDriver` — for example, the `pg` or `mysql2` npm package — to get a connection from its pool."                           | quote          | `site/docs/execution.mdx:122-124`                                                                                                                      | verbatim                                                                                                                                                                                                                                | ✓              |
| 10  | `PostgresDriver` holds a `pg` `Pool`; `pool.connect()` on acquire, `pool.end()` on destroy                                                                                                                                                                                           | behavior       | `src/dialect/postgres/postgres-driver.ts:26-50,142-145`                                                                                                | `#pool?: PostgresPool`; `const client = await this.#pool!.connect()`; `await pool.end()`                                                                                                                                                | ✓              |
| 11  | `Dialect` = createDriver/createQueryCompiler/createAdapter/createIntrospector                                                                                                                                                                                                        | quote          | `src/dialect/dialect.ts:14-38`                                                                                                                         | interface with those four methods                                                                                                                                                                                                       | ✓              |
| 12  | `destroy()` releases resources; `Kysely` implements `AsyncDisposable` (`Symbol.asyncDispose` → `destroy`); `db.connection()` binds a single connection                                                                                                                               | behavior       | `src/kysely.ts:97-99,605-607,640-642,446-448`                                                                                                          | `implements ...AsyncDisposable`; `async destroy(){ await this.#props.driver.destroy() }`; `[Symbol.asyncDispose](){ await this.destroy() }`; `connection(): ConnectionBuilder`                                                          | ✓              |
| 13  | Fluent chain `selectFrom`/`select`/`where`/`innerJoin`/`groupBy`/`having` off `QueryCreator<DB>`/`SelectQueryBuilder<DB, TB, O>`                                                                                                                                                     | behavior       | `src/query-creator.ts:51,58`; `src/query-builder/select-query-builder.ts` (interface)                                                                  | `selectFrom` on `QueryCreator`; `SelectQueryBuilder<DB, TB, O>` extends `WhereInterface`/`HavingInterface`/…                                                                                                                            | ✓              |
| 14  | QUOTE immutable/lazy AST: "Each call returns a _new_ `QueryBuilder` instance containing an updated, immutable `QueryAST` (Abstract Syntax Tree), which is the internal representation of your SQL query."                                                                            | quote          | `site/docs/execution.mdx:16-19`                                                                                                                        | verbatim (under "Immutable query building")                                                                                                                                                                                             | ✓              |
| 15  | `where` clones → `new SelectQueryBuilderImpl({...})`; props are `freeze`-d in ctor                                                                                                                                                                                                   | quote          | `src/query-builder/select-query-builder.ts` (`where` impl + ctor `this.#props = freeze(props)`)                                                        | `where(...args){ return new SelectQueryBuilderImpl({ ...this.#props, queryNode: QueryNode.cloneWithWhere(...) }) }`                                                                                                                     | ✓              |
| 16  | QUOTE reassign warning: "The query builder is immutable. Remember to reassign the result back to the query variable."                                                                                                                                                                | quote          | `src/query-builder/where-interface.ts:145-146`                                                                                                         | verbatim (inside a code-comment in a docstring example)                                                                                                                                                                                 | ✓              |
| 17  | A plain value → `ValueNode` (bind param); an expression → its node                                                                                                                                                                                                                   | quote          | `src/parser/value-parser.ts:47-55`                                                                                                                     | `parseValueExpression`: `if (isExpressionOrFactory(exp)) return parseExpression(exp); return ValueNode.create(exp)`                                                                                                                     | ✓              |
| 18  | AST nodes are `freeze`-d and tagged by a closed `OperationNodeKind` union                                                                                                                                                                                                            | quote          | `src/operation-node/operation-node.ts:3-107`; `src/operation-node/value-node.ts:19-38`                                                                 | `OperationNodeKind` union (~100 kinds); `ValueNode` created via `freeze({ kind: 'ValueNode', value })`                                                                                                                                  | ✓              |
| 19  | Compilation → `CompiledQuery` of `{ query, queryId, sql, parameters }`                                                                                                                                                                                                               | quote          | `src/query-compiler/compiled-query.ts:8-13`; `src/query-compiler/default-query-compiler.ts:137-150`                                                    | `interface CompiledQuery { query; queryId; sql; parameters }`; `compileQuery` returns `freeze({ query, queryId, sql, parameters: [...] })`                                                                                              | ✓              |
| 20  | `visitValue` → `appendValue` → `addParameter` (push) + `getCurrentParameterPlaceholder` (only a placeholder in SQL); values never string-concatenated                                                                                                                                | quote          | `src/query-compiler/default-query-compiler.ts:520-526,1830-1833,1891-1893`                                                                             | `appendValue(p){ this.addParameter(p); this.append(this.getCurrentParameterPlaceholder()) }`; `addParameter(p){ this.#parameters.push(p) }`                                                                                             | ✓              |
| 21  | Placeholders: `$n` (PostgreSQL), `?` (MySQL/SQLite), `@n` (MSSQL)                                                                                                                                                                                                                    | quote          | `default-query-compiler.ts:1843-1845`; `mysql-query-compiler.ts:9-11`; `sqlite-query-compiler.ts:14-16`; `mssql-query-compiler.ts:12-14`               | default `'$' + this.numParameters`; mysql/sqlite `return '?'`; mssql `` return `@${this.numParameters}` ``                                                                                                                              | ✓              |
| 22  | `where('first_name','=','Jennifer').where('age','>',40)` → `select * from "person" where "first_name" = $1 and "age" > $2`                                                                                                                                                           | quote          | `src/query-builder/where-interface.ts:24-37`                                                                                                           | example + `The generated SQL (PostgreSQL): select * from "person" where "first_name" = $1 and "age" > $2`                                                                                                                               | ✓              |
| 23  | QUOTE `sql` tag safety: "Substitutions (the things inside `${}`) are automatically passed to the database as parameters and are never interpolated to the SQL string. There's no need to worry about SQL injection vulnerabilities."                                                 | quote          | `src/raw-builder/sql.ts:24-26`                                                                                                                         | verbatim (page reorders to lead with "Substitutions...are automatically passed"; source: "...are automatically passed to the database / as parameters...")                                                                              | ✓              |
| 24  | `sql` tag select example (`${date1}/${date2}` → params)                                                                                                                                                                                                                              | quote          | `src/raw-builder/sql.ts:46-59`                                                                                                                         | docstring example (abridged); generated SQL `where birthdate between $1 and $2`                                                                                                                                                         | ✓              |
| 25  | `visitRaw` interleaves `sqlFragments` (verbatim) with `visitNode`-d parameters                                                                                                                                                                                                       | quote          | `src/query-compiler/default-query-compiler.ts` (`visitRaw`)                                                                                            | `for (...) { this.append(sqlFragments[i]); if (params.length > i) this.visitNode(params[i]) }`                                                                                                                                          | ✓              |
| 26  | QUOTE raw-helper warning: "WARNING! Using this with unchecked inputs WILL lead to SQL injection vulnerabilities. The input is not checked or escaped by Kysely in any way."                                                                                                          | quote          | `src/raw-builder/sql.ts:147-148` (repeated `:199,240,276,302`)                                                                                         | verbatim (on `ref`/`table`/`id`/`lit`/`raw`)                                                                                                                                                                                            | ✓              |
| 27  | `sql.lit` → `ValueNode.createImmediate` → `appendImmediateValue` renders into SQL text                                                                                                                                                                                               | quote          | `src/raw-builder/sql.ts:429-434`; `default-query-compiler.ts:520-522,1895-1907`                                                                        | `lit(v){ ...ValueNode.createImmediate(value) }`; `visitValue`: `if (node.immediate) this.appendImmediateValue(node.value)`                                                                                                              | ✓              |
| 28  | `DynamicModule` (`db.dynamic.ref`) is the type-erasing counterpart                                                                                                                                                                                                                   | behavior       | `src/kysely.ts:150-158`                                                                                                                                | `get dynamic(): DynamicModule<DB>` — "can be used to bypass strict typing and passing in dynamic values"                                                                                                                                | ✓              |
| 29  | QUOTE schema-agnostic: "To work with Kysely, you're required to provide a database schema type definition to the Kysely constructor. In many cases, defining your database schema definitions manually is good enough."                                                              | quote          | `site/docs/generating-types.md:3,5`                                                                                                                    | verbatim (two sentences, lines 3 and 5)                                                                                                                                                                                                 | ✓              |
| 30  | No first-party codegen; third-party tools (`kysely-codegen`, `prisma-kysely`, `kanel-kysely`) generate the `Database` type                                                                                                                                                           | behavior       | `site/docs/generating-types.md:11-24`                                                                                                                  | lists kysely-codegen / prisma-kysely / kanel-kysely / kysely-schema-generator as "third-party libraries"                                                                                                                                | ✓              |
| 31  | DDL builder: `db.schema.createTable/createIndex/alterTable/createView/createType`                                                                                                                                                                                                    | behavior       | `src/schema/schema-module.ts:97,116,137,214,235,297`                                                                                                   | `createTable`, `dropTable`, `createIndex`, `createSchema`, `alterTable`, `createView`, `createType` methods                                                                                                                             | ✓              |
| 32  | Migration = imperative TS `up(db)`/optional `down(db)`; runner records applied, orders alphabetically, uses a migration lock, wraps in a txn when `supportsTransactionalDdl`; `migrateToLatest` "never throws" (returns a result set)                                                | quote          | `src/migration/migrator.ts:16-25` + `migrateToLatest` docstring; `src/dialect/dialect-adapter.ts` (`supportsTransactionalDdl`, `acquireMigrationLock`) | `interface Migration { up(db): Promise<void>; down?(db): ... }`; `migrateToLatest` docstring: "runs the ones whose names come alphabetically after the last"; adapter: "If this is true, migrations are executed inside a transaction." | ✓              |
| 33  | Result type inferred from selected columns + `Database` type; aliases parsed (`pet.name as pet_name`); nullability preserved                                                                                                                                                         | quote+behavior | `README.md:35-42`; `src/util/column-type.ts` (`Selectable`)                                                                                            | README: "able to parse the alias given to `pet.name` and add the `pet_name` column to the result row type"; nullable columns modeled as `T \| null`                                                                                     | ✓              |
| 34  | `ColumnType<Select, Insert, Update>`; `Generated<S> = ColumnType<S, S \| undefined, S>`; `GeneratedAlways<S> = ColumnType<S, never, never>`                                                                                                                                          | quote          | `src/util/column-type.ts:38-64`                                                                                                                        | verbatim type definitions                                                                                                                                                                                                               | ✓              |
| 35  | `Selectable`/`Insertable`/`Updateable` project per-operation row shapes (`Insertable` optionalizes `Generated`; `Updateable` optionalizes all)                                                                                                                                       | quote          | `src/util/column-type.ts:144-199`; `site/docs/getting-started/_types.mdx`                                                                              | `Selectable`/`Insertable`/`Updateable` mapped types + doc example showing `id?` in insert, all optional in update                                                                                                                       | ✓              |
| 36  | Kysely does NOT decode cell values at runtime; returns what the driver returns; `Database` type is a compile-time contract, not a runtime codec                                                                                                                                      | behavior       | `src/query-builder/select-query-builder.ts` (`execute` returns `result.rows as never`); no codec module in `src/`                                      | `execute` returns driver `result.rows` cast to the inferred type; there is no encoder/decoder abstraction in the tree                                                                                                                   | ✓ (absence)    |
| 37  | Runtime result transformation is opt-in via plugins (`CamelCasePlugin`, `ParseJSONResultsPlugin`)                                                                                                                                                                                    | behavior       | `src/plugin/` (directory)                                                                                                                              | plugin modules incl. `camel-case`, `parse-json-results`; `transformResult` hook                                                                                                                                                         | ✓              |
| 38  | Terminal methods: `execute()` → rows; `executeTakeFirst()` → first row or `undefined`; `executeTakeFirstOrThrow()` → throws `NoResultError`                                                                                                                                          | quote          | `src/query-builder/select-query-builder.ts` (`execute`/`executeTakeFirst`); `src/query-builder/no-result-error.ts:...`                                 | `execute` returns `SimplifyResult<O>[]`; `executeTakeFirst`: `const [result] = await this.execute(); return result`; `class NoResultError extends Error`                                                                                | ✓              |
| 39  | Async-only: every terminal returns a `Promise`; not blocking; not an effect value                                                                                                                                                                                                    | behavior       | `src/query-builder/select-query-builder.ts` (`async execute(): Promise<...>`)                                                                          | `async execute(...): Promise<SimplifyResult<O>[]>`; no IO/effect wrapper in the tree                                                                                                                                                    | ✓              |
| 40  | Build/execute split: `.compile()` yields `CompiledQuery` without running                                                                                                                                                                                                             | quote          | `src/query-builder/select-query-builder.ts` (`compile`)                                                                                                | `compile(): CompiledQuery<...> { return this.#props.executor.compileQuery(this.toOperationNode(), this.#props.queryId) }`                                                                                                               | ✓              |
| 41  | QUOTE transaction semantics: "If the function throws an exception, 1. the exception is caught, 2. the transaction is rolled back, and 3. the exception is thrown again. Otherwise the transaction is committed."                                                                     | quote          | `src/kysely.ts:241-245`                                                                                                                                | verbatim (docstring of `transaction()`)                                                                                                                                                                                                 | ✓              |
| 42  | `transaction().execute(cb)` impl: leases connection → `beginTransaction` → callback → `commitTransaction`; on throw → `rollbackTransaction` + rethrow; `trx` is `Transaction<DB> extends Kysely<DB>`                                                                                 | quote          | `src/kysely.ts:645-651,883-921`                                                                                                                        | `TransactionBuilder.execute`: `try { await driver.beginTransaction(...); ...; await driver.commitTransaction(...) } catch { await driver.rollbackTransaction(...); throw error }`; `class Transaction<DB> extends Kysely<DB>`           | ✓              |
| 43  | Isolation levels `read uncommitted` … `serializable` + `snapshot`; `setIsolationLevel`/`setAccessMode`                                                                                                                                                                               | quote          | `src/driver/driver.ts:94-102,85-92`; `src/kysely.ts:869-881`                                                                                           | `TRANSACTION_ISOLATION_LEVELS = ['read uncommitted','read committed','repeatable read','serializable','snapshot']`; `setIsolationLevel`/`setAccessMode`                                                                                 | ✓              |
| 44  | Controlled transactions (`db.startTransaction()`) with manual `commit()`/`rollback()` and first-class savepoints `savepoint`/`rollbackToSavepoint`/`releaseSavepoint`, type-tracked via `S extends string[]`                                                                         | quote          | `src/kysely.ts:972-1231`                                                                                                                               | `class ControlledTransaction<DB, S extends string[] = []> extends Transaction<DB>`; `savepoint<SN extends string>(name: SN extends S ? never : SN): Command<ControlledTransaction<DB, [...S, SN]>>`                                     | ✓              |
| 45  | Driver savepoint methods are optional (`savepoint?`/`rollbackToSavepoint?`/`releaseSavepoint?`)                                                                                                                                                                                      | quote          | `src/driver/driver.ts:47-69`                                                                                                                           | three optional (`?`) methods on the `Driver` interface                                                                                                                                                                                  | ✓              |
| 46  | Plain `db.transaction()` does NOT auto-nest: `Transaction` overrides `transaction()`/`startTransaction()`/`connection()` to throw                                                                                                                                                    | quote          | `src/kysely.ts:660-685`                                                                                                                                | `override transaction(): never { throw new Error('calling the transaction method for a Transaction is not supported') }` (+ startTransaction/connection)                                                                                | ✓              |
| 47  | Errors are thrown exceptions, not a typed channel; `NoResultError`; no `SqlError` union / `isRetryable`                                                                                                                                                                              | behavior       | `src/query-builder/no-result-error.ts`; whole `src/` (absence)                                                                                         | `NoResultError extends Error`; no enumerated error type / retry flag anywhere in the tree                                                                                                                                               | ✓ (absence)    |
| 48  | MIT, © 2022 Sami Koskimäki; org `kysely-org`; leads Sami Koskimäki + Igal Klebanov                                                                                                                                                                                                   | fact           | `LICENSE:1-3`; `README.md:3-6,62-90`                                                                                                                   | "The MIT License (MIT) / Copyright (c) 2022 Sami Koskimäki"; project-leads table                                                                                                                                                        | ✓              |
| 49  | Built-in dialects: PostgreSQL, MySQL, MS SQL Server, SQLite, PGlite; org Postgres.js; large community list; runs on Node/Deno/Bun/CF Workers/browsers                                                                                                                                | fact           | `site/docs/dialects.md:5-45`; `README.md:29-31`                                                                                                        | "Core dialects" table (Postgres/MySQL/MSSQL/SQLite/PGlite); "Organization"/"Community" tables; README runtimes list                                                                                                                     | ✓              |
| 50  | Latest version `0.29.3` (pinned checkout)                                                                                                                                                                                                                                            | figure         | `package.json:"version"`                                                                                                                               | `"version": "0.29.3"`                                                                                                                                                                                                                   | ✓              |
| 51  | First release ≈2021 (author launch post archived 2021-12-03; `LICENSE` © 2022)                                                                                                                                                                                                       | figure         | `README.md:77` (web.archive.org/web/20211203210043 author link); `LICENSE:3` (© 2022)                                                                  | archived blog URL date attests ≈2021; exact npm first-publish date is web/registry                                                                                                                                                      | ◯ (web)        |
| 52  | `@effect/sql-kysely` adapter exists (Effect wraps Kysely)                                                                                                                                                                                                                            | fact           | not in this repo                                                                                                                                       | lives in `$REPOS/typescript/effect` (`sql-kysely`), per `_sources.md:99`; named-only here                                                                                                                                               | ◯ (cross-repo) |

## Discrepancies

- **#D1 (cosmetic de-markup) — claims #1, #8.** Two verbatim quotes strip source markup: README's
  `[TypeScript]`/`[SQL]`/`[Knex.js]` markdown links and smart quotes “Key-Seh-Lee” (page uses
  straight `"`), and the `kysely.ts` docstring's `{@link Kysely}` (page renders `Kysely`). No
  wording changed. Consistent with the corpus (`slick.md` de-markups `@extref` the same way).
- **#D2 (quote reordering) — claim #23.** The `sql`-tag safety quote is reflowed to lead with
  "Substitutions ... are automatically passed to the database as parameters and are never
  interpolated ..."; the source (`raw-builder/sql.ts:24-26`) wraps the same words across three
  lines. Wording is verbatim; only line-wrapping is joined.
- **#D3 (metadata vs source, not an error) — backends.** The brief's metadata sketch listed
  "PostgreSQL, MySQL, SQLite (+ community dialects)". The tree's own `dialects.md` also ships
  **MS SQL Server** and **PGlite** as core built-ins; the page states the fuller (correct) list.
  Not a discrepancy with the tree — a correction of the brief.

## Unsourced / opinion

- Editorial framings — "type-only schema … erased at runtime, zero runtime schema cost", "the
  inverse emphasis of the typed-error effect mappers", "runtime fidelity is the driver's
  responsibility", the Strengths/Weaknesses lists and the decision/trade-off table — are author
  synthesis (◯). Each rests on verified mechanics above (no runtime codec module; async-Promise
  terminals; exception errors; `$extendTables` type-only note).
- Cross-library contrasts (`Drizzle` schema-object-first; `jOOQ`/`Diesel` compiled-language
  builders with first-party codegen; `Slick` lacks savepoints; `skunk`/`Diesel` runtime codecs)
  are grounded for Kysely locally; the _other_ libraries' properties are grounded in their own
  ledgers, referenced by name here.
- **#51** first-release ≈2021 and **#52** `@effect/sql-kysely` are web / cross-repo, flagged ◯.
  A `--depth 1` checkout carries no history, so the npm first-publish date is not a tree fact.

## Net

52 claims: **49 ✓** grounded verbatim/behaviorally in `$REPOS/typescript/kysely@c431677`
(`src/`, `site/docs/`, `README.md`, `LICENSE`, `package.json`), **2 ◯** web/cross-repo (first
release date; `@effect/sql-kysely` adapter), **1 ◯** correcting the brief's backend list against
`dialects.md`. ~14 verbatim quotes span `README.md`, `src/kysely.ts`, `src/raw-builder/sql.ts`,
`src/util/column-type.ts`, `src/query-compiler/default-query-compiler.ts`, and the `site/docs/`
tree; all match source (modulo #D1 de-markup and #D2 line-join). The injection-safety mechanism
(value → `ValueNode` → placeholder + parameter array), the immutable/lazy AST builder, the
type-only `Database` contract, and the async/exception effect model are each grounded in the code,
not just the docs. No substantive (⚠) discrepancies.

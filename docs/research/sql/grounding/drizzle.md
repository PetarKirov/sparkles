# Grounding ledger — `drizzle.md`

Claim-by-claim verification of `docs/research/sql/drizzle.md` against the **local** pinned
checkout (see [`_sources.md`](./_sources.md)):

- `$REPOS/typescript/drizzle-orm` @ `9d64532` — the Drizzle monorepo. Grounding sub-trees:
  `drizzle-orm/src/` (the ORM: `sql/`, `pg-core/`, `sqlite-core/`, `mysql-core/`,
  `node-postgres/`, `relations.ts`, `table.ts`, `query-promise.ts`, `errors.ts`,
  `migrator.ts`, `version.ts`), `drizzle-orm/src/sqlite-core/README.md`, `drizzle-kit/`
  (`src/cli/schema.ts`, `package.json`), top-level `README.md`, `LICENSE`,
  `drizzle-orm/package.json`.

`$REPOS = /home/petar/code/repos`. **Type:** `quote` · `fact` (date/author/license/version) ·
`figure` (number/size) · `behavior` (library does X) · `exposition` (standard knowledge) ·
`opinion`. **Status:** ✓ verified · ⚠ discrepancy · ◯ web/point-in-time or editorial.

## Claims

| #   | Claim                                                                                                                                                                                                  | Type           | Source (local + locator)                                                                                                                                                           | Evidence                                                                                                                                                            | Status                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | ------------ | --- |
| 1   | Tagline "If you know SQL, you know Drizzle ORM"                                                                                                                                                        | quote          | `drizzle-orm/src/sqlite-core/README.md:7`                                                                                                                                          | `<h6><i>If you know SQL, you know Drizzle ORM</i></h6>`                                                                                                             | ✓                             |
| 2   | "Headless ORM for NodeJS, TypeScript and JavaScript"                                                                                                                                                   | quote          | `README.md:8`                                                                                                                                                                      | `<h3>Headless ORM for NodeJS, TypeScript and JavaScript 🚀</h3>`                                                                                                    | ✓                             |
| 3   | "While Drizzle ORM remains a thin typed layer on top of SQL, we made a set of tools…"                                                                                                                  | quote          | `README.md:30`                                                                                                                                                                     | verbatim                                                                                                                                                            | ✓                             |
| 4   | "It lets you **declare SQL schemas** and build both **relational** and **SQL-like queries**, while keeping the balance between type-safety and extensibility for toolmakers to build on top."          | quote          | `README.md:27`                                                                                                                                                                     | verbatim (bold markers in source)                                                                                                                                   | ✓                             |
| 5   | "It is lightweight at only ~7.4kb minified+gzipped, and it's tree shakeable with exactly 0 dependencies."                                                                                              | quote/figure   | `README.md:20`                                                                                                                                                                     | verbatim                                                                                                                                                            | ✓ (figure = repo's own claim) |
| 6   | `dependencies` is `{}`; 29 optional peer-dependency drivers                                                                                                                                            | fact           | `drizzle-orm/package.json` (`"dependencies": {}`; 29 keys under `peerDependencies`)                                                                                                | `node -e` printed `deps: {}` and `peerDeps keys count: 29`                                                                                                          | ✓                             |
| 7   | "**Drizzle supports every PostgreSQL, MySQL and SQLite database**, including serverless ones … No bells and whistles, no Rust binaries, no serverless adapters, everything just works out of the box." | quote          | `README.md:22`                                                                                                                                                                     | verbatim                                                                                                                                                            | ✓                             |
| 8   | "**Drizzle is serverless-ready by design**. It works in every major JavaScript runtime like NodeJS, Bun, Deno, Cloudflare Workers, Supabase functions, any Edge runtime, and even in browsers."        | quote          | `README.md:24`                                                                                                                                                                     | verbatim                                                                                                                                                            | ✓                             |
| 9   | "Drizzle comes with a powerful **Drizzle Kit** CLI companion … It can generate SQL migration files for you or apply schema changes directly to the database."                                          | quote          | `README.md:32`                                                                                                                                                                     | verbatim                                                                                                                                                            | ✓                             |
| 10  | "With `drizzle-orm` you declare SQL schema in TypeScript." (code-first)                                                                                                                                | quote          | `drizzle-orm/src/sqlite-core/README.md:109`                                                                                                                                        | verbatim                                                                                                                                                            | ✓                             |
| 11  | License = Apache-2.0                                                                                                                                                                                   | fact           | `LICENSE:1-3`                                                                                                                                                                      | `Apache License / Version 2.0, January 2004`                                                                                                                        | ✓                             |
| 12  | `drizzle-orm` `0.45.3`; `drizzle-kit` `0.31.10`; `compatibilityVersion` counter noting a PG-indexes API break                                                                                          | fact           | `drizzle-orm/package.json:3`, `drizzle-kit/package.json:3`, `drizzle-orm/src/version.ts`                                                                                           | `"version": "0.45.3"`; `"version": "0.31.10"`; `// In version 7, we changed the PostgreSQL indexes API / export const compatibilityVersion = 10;`                   | ✓                             |
| 13  | `sql` tagged template interleaves `StringChunk` literals with pushed value chunks → `new SQL(queryChunks)`                                                                                             | quote/code     | `drizzle-orm/src/sql/sql.ts:485-495`                                                                                                                                               | function body quoted verbatim                                                                                                                                       | ✓                             |
| 14  | Default render arm: an interpolated data value → `escapeParam(paramStartIndex.value++, chunk)` with `params: [chunk]`                                                                                  | code           | `drizzle-orm/src/sql/sql.ts:298`                                                                                                                                                   | `return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ['none'] };`                                                                  | ✓                             |
| 15  | `Param` = "Parameter value that is optionally bound to an encoder (for example, a column)."; `mapToDriverValue` runs before binding                                                                    | quote/code     | `drizzle-orm/src/sql/sql.ts:432-445`, `:238`                                                                                                                                       | docstring verbatim; `chunk.encoder.mapToDriverValue(chunk.value)` at render                                                                                         | ✓                             |
| 16  | PG placeholder `$n` via `escapeParam(num) { return \`$${num + 1}\`; }`; MySQL & SQLite use `?`                                                                                                         | code           | `drizzle-orm/src/pg-core/dialect.ts:118-120`; `mysql-core/dialect.ts:104`; `sqlite-core/dialect.ts:61`                                                                             | ``return `$${num + 1}`;`` / ``return `?`;`` / `return '?';`                                                                                                         | ✓                             |
| 17  | `sql.raw(str)` = "Convenience function to create an SQL query from a raw string." (unsafe splice)                                                                                                      | quote/code     | `drizzle-orm/src/sql/sql.ts:508-513`                                                                                                                                               | docstring + `new SQL([new StringChunk(str)])`                                                                                                                       | ✓                             |
| 18  | `sql.identifier` WARNING: "This function does not offer any protection against SQL injections, so you must validate any user input beforehand."                                                        | quote          | `drizzle-orm/src/sql/sql.ts:544`                                                                                                                                                   | verbatim (bold in source)                                                                                                                                           | ✓                             |
| 19  | `db.execute(string)` routes through `sql.raw` (unsafe); `SQLWrapper` rendered safely                                                                                                                   | code           | `drizzle-orm/src/pg-core/db.ts:618`                                                                                                                                                | `const sequel = typeof query === 'string' ? sql.raw(query) : query.getSQL();`                                                                                       | ✓                             |
| 20  | `eq`/`gt` are `` sql`${left} = ${bindIfParam(right, left)}` `` one-liners                                                                                                                              | code           | `drizzle-orm/src/sql/expressions/conditions.ts:60-62`, `:194-196`                                                                                                                  | verbatim                                                                                                                                                            | ✓                             |
| 21  | `bindIfParam` wraps a bare value in `new Param(value, column)` (column as encoder)                                                                                                                     | code           | `drizzle-orm/src/sql/expressions/conditions.ts:17-31`                                                                                                                              | function body quoted                                                                                                                                                | ✓                             |
| 22  | `and`/`or` "Conditions that are equal `undefined` are automatically ignored"                                                                                                                           | quote/code     | `drizzle-orm/src/sql/expressions/conditions.ts:88-90` (docstring) + `:107-111` (`.filter(c => c !== undefined)`)                                                                   | docstring verbatim; runtime filter confirms                                                                                                                         | ✓                             |
| 23  | `SQLWrapper` interface (`getSQL()`); Table/Column/View/Subquery/SQL/Placeholder/Param implement it                                                                                                     | code           | `drizzle-orm/src/sql/sql.ts:65-68`; impls at `:447`, `:688`, `Column.prototype.getSQL` `:710`, `Table.prototype.getSQL` `:715`, `Subquery.prototype.getSQL` `:720`                 | interface + prototype assignments                                                                                                                                   | ✓                             |
| 24  | Prepared statements + `placeholder`; `fillPlaceholders` swaps named slots through the `Param` encoder                                                                                                  | code           | `drizzle-orm/src/sqlite-core/README.md:783-786` (example); `sql/sql.ts:612-632` (`fillPlaceholders`)                                                                               | example + `p.encoder.mapToDriverValue(values[p.value.name])`                                                                                                        | ✓                             |
| 25  | `pgTable` wraps `pgTableWithSchema`; `sqliteTable`/`mysqlTable` exist                                                                                                                                  | code           | `drizzle-orm/src/pg-core/table.ts:244-246`; `sqlite-core/table.ts:219`; `mysql-core/table.ts:221`                                                                                  | `export const pgTable: PgTableFn = (…) => pgTableWithSchema(…)` etc.                                                                                                | ✓                             |
| 26  | Schema example: `sqliteTable('users', { id: integer('id').primaryKey(), fullName: text('full_name') })`                                                                                                | quote/code     | `drizzle-orm/src/sqlite-core/README.md:41-44`                                                                                                                                      | verbatim                                                                                                                                                            | ✓                             |
| 27  | `integer` column: `getSQLType()` → `'integer'`; `mapFromDriverValue` decodes string→number                                                                                                             | code           | `drizzle-orm/src/pg-core/columns/integer.ts:36-46`                                                                                                                                 | `getSQLType(): string { return 'integer'; }` + decode body                                                                                                          | ✓                             |
| 28  | `$inferSelect`/`$inferInsert`; `InferSelectModel`/`InferInsertModel` derive row shapes from columns                                                                                                    | code           | `drizzle-orm/src/table.ts:62-63`, `:197-205`                                                                                                                                       | `declare readonly $inferSelect…`; `export type InferSelectModel<…> = InferModelFromColumns<…, 'select', …>`                                                         | ✓                             |
| 29  | `drizzle-kit` subcommands `generate`, `migrate`, `push`, `introspect`/`pull`, `check`, `up`, `studio`, `drop`, `export`                                                                                | code           | `drizzle-kit/src/cli/schema.ts:50,115,247,406,426,470,633,652,807`                                                                                                                 | `export const generate = command({ name: 'generate', … })` etc.                                                                                                     | ✓                             |
| 30  | `generate` input = "Path to a schema file or folder"; `--custom` prepares an empty migration                                                                                                           | quote/code     | `drizzle-kit/src/cli/schema.ts:57,61-63`                                                                                                                                           | `schema: string().desc('Path to a schema file or folder')`; `custom: boolean().desc('Prepare empty migration file for custom SQL')`                                 | ✓                             |
| 31  | `introspect` has name `'introspect'`, alias `'pull'` (database-first: live DB → schema)                                                                                                                | code           | `drizzle-kit/src/cli/schema.ts:470-472`                                                                                                                                            | `export const pull = command({ name: 'introspect', aliases: ['pull'], … })`                                                                                         | ✓                             |
| 32  | `migrate()` runner reads `meta/_journal.json`, splits on `--> statement-breakpoint`, hashes each file                                                                                                  | code           | `drizzle-orm/src/migrator.ts:28-52`                                                                                                                                                | `const journalPath = \`${…}/meta/\_journal.json\``; `query.split('--> statement-breakpoint')`; `crypto.createHash('sha256')…`                                       | ✓                             |
| 33  | Codec pair: `DriverValueDecoder.mapFromDriverValue` / `DriverValueEncoder.mapToDriverValue`                                                                                                            | code           | `drizzle-orm/src/sql/sql.ts:402-408`                                                                                                                                               | both interfaces quoted                                                                                                                                              | ✓                             |
| 34  | `SQL.mapWith(decoder)` installs a decoder for raw-`sql` typed results                                                                                                                                  | code           | `drizzle-orm/src/sql/sql.ts:349-356`                                                                                                                                               | `mapWith<…>(decoder) { this.decoder = … ; return this as SQL<…>; }`                                                                                                 | ✓                             |
| 35  | Nullability in the inferred type: `.notNull()`; `createOne` folds `notNull` across FK cols into relation `isNullable`                                                                                  | behavior/code  | `drizzle-orm/src/relations.ts:533-540`                                                                                                                                             | `Equal<TColumns[number]['_']['notNull'], true>`; `config?.fields.reduce((res, f) => res && f.notNull, true)`                                                        | ✓                             |
| 36  | Query = lazily-`await`ed `PromiseLike`: `QueryPromise<T> implements Promise<T>`, `then` calls `execute()`                                                                                              | code           | `drizzle-orm/src/query-promise.ts:3-33`                                                                                                                                            | `export abstract class QueryPromise<T> implements Promise<T>` … `then(…) { return this.execute().then(…); }`                                                        | ✓                             |
| 37  | SQLite sync drivers are blocking (`.all()`/`.get()`/`.run()`); `SQLiteSyncDialect` vs `SQLiteAsyncDialect`                                                                                             | code           | `drizzle-orm/src/sqlite-core/dialect.ts:936,998`; `sqlite-core/README.md:49,682-686`                                                                                               | `class SQLiteSyncDialect` / `class SQLiteAsyncDialect`; README uses `.all()`/`.run()`                                                                               | ✓                             |
| 38  | `db.transaction(callback, config?)` delegates to `session.transaction`                                                                                                                                 | code           | `drizzle-orm/src/pg-core/db.ts:636-641`                                                                                                                                            | method body quoted                                                                                                                                                  | ✓                             |
| 39  | `tx` is a full DB handle: `PgTransaction extends PgDatabase`                                                                                                                                           | code           | `drizzle-orm/src/pg-core/session.ts:236-240`                                                                                                                                       | `export abstract class PgTransaction<…> extends PgDatabase<…>`                                                                                                      | ✓                             |
| 40  | Outermost tx issues `begin` … `commit`, `rollback` on throw                                                                                                                                            | code           | `drizzle-orm/src/node-postgres/session.ts:257-264`                                                                                                                                 | `await tx.execute(sql\`begin…\`)`/`sql\`commit\``/`sql\`rollback\``                                                                                                 | ✓                             |
| 41  | Nested tx → `SAVEPOINT sp{n}` / `RELEASE SAVEPOINT` / `ROLLBACK TO SAVEPOINT`; depth via `nestedIndex`                                                                                                 | code           | `drizzle-orm/src/node-postgres/session.ts:285-300`; `pg-core/session.ts:251`                                                                                                       | `const savepointName = \`sp${this.nestedIndex + 1}\``; `sql.raw(\`savepoint …\`)`/`release savepoint`/`rollback to savepoint`; `protected readonly nestedIndex = 0` | ✓                             |
| 42  | `PgTransactionConfig` = isolation level / access mode / deferrable; SQLite behavior = deferred/immediate/exclusive                                                                                     | code           | `drizzle-orm/src/pg-core/session.ts:261-272`; `sqlite-core/README.md:704-705`                                                                                                      | `getTransactionConfigSQL` builds `isolation level …`, `accessMode`, `deferrable`; `behavior?: 'deferred'                                                            | 'immediate'                   | 'exclusive'` | ✓   |
| 43  | `tx.rollback()` throws `TransactionRollbackError`; caught → `ROLLBACK`                                                                                                                                 | code           | `drizzle-orm/src/pg-core/session.ts:256-258`; `errors.ts:26-32`                                                                                                                    | `rollback(): never { throw new TransactionRollbackError(); }`                                                                                                       | ✓                             |
| 44  | Error surface = 3 `Error` subclasses; `DrizzleQueryError` carries `query`, `params`, `.cause`                                                                                                          | code           | `drizzle-orm/src/errors.ts:3-32`                                                                                                                                                   | `DrizzleError`, `DrizzleQueryError` (`Failed query: … params: …`), `TransactionRollbackError`                                                                       | ✓                             |
| 45  | No enumerated/typed error union, no retryable classification, no `Either`-style result                                                                                                                 | behavior       | `drizzle-orm/src/errors.ts` (only the 3 classes); queries reject a `Promise`                                                                                                       | absence of any error-type parameter / reason union in the tree                                                                                                      | ✓                             |
| 46  | Relational API: `relations(table, (helpers) => ({ … one/many … }))`                                                                                                                                    | code           | `drizzle-orm/src/relations.ts:502-519`, `521-552` (`createOne`/`createMany`)                                                                                                       | `export function relations<…>(table, relations) { return new Relations(…); }`                                                                                       | ✓                             |
| 47  | `db.query.<table>.findMany` / `findFirst` (RelationalQueryBuilder)                                                                                                                                     | code           | `drizzle-orm/src/pg-core/query-builders/query.ts:32-62`; `pg-core/db.ts:79` (`this.query[tableName] = new RelationalQueryBuilder(…)`)                                              | `findMany(config?)` / `findFirst(config?)` return `PgRelationalQuery`                                                                                               | ✓                             |
| 48  | `DBQueryConfig` keys = `columns`, `with` (recursive), `extras`, `where`, `orderBy`, `limit`, `offset`; `where`/`orderBy` take operator callbacks (`getOperators`/`getOrderByOperators`)                | code           | `drizzle-orm/src/relations.ts:210-277`; `:159-194`                                                                                                                                 | type members verbatim; `getOperators()` returns `{ eq, gt, and, or, … }`; `getOrderByOperators()` returns `{ sql, asc, desc }`                                      | ✓                             |
| 49  | Relational query compiles to **one** statement via `lateral` joins + `json_agg`/`json_build_array`/`coalesce(…, '[]')` — avoids N+1                                                                    | code           | `drizzle-orm/src/pg-core/dialect.ts:1150` (`buildRelationalQueryWithoutPK`), `:1313-1334` (lateral), `:1356-1369` (`json_build_array` / `coalesce(json_agg(...` )                  | join built with `sql\` lateral\``; `field = sql\`json_build_array(…)\``; `coalesce(json_agg(${field}…`                                                              | ✓                             |
| 50  | `drizzle(client, options?)` wraps an existing driver; `PgDatabase` holds only `dialect` + `session`                                                                                                    | code/behavior  | `drizzle-orm/src/pg-core/db.ts:36-91`; `sqlite-core/README.md:46-47`                                                                                                               | `constructor(readonly dialect: PgDialect, readonly session: PgSession…, schema…)`; README `const db = drizzle(sqlite)`                                              | ✓                             |
| 51  | Transaction leases a `PoolClient` (`client.connect()`) and `release()`s it in `finally` (pool owned by the driver)                                                                                     | code           | `drizzle-orm/src/node-postgres/session.ts:252-267`                                                                                                                                 | `new NodePgSession(await (<pg.Pool>this.client).connect(), …)` … `finally { if (isPool) (session.client as PoolClient).release(); }`                                | ✓                             |
| 52  | `withReplicas` routes reads to a replica, writes + `transaction`/`execute` to the primary                                                                                                              | code           | `drizzle-orm/src/pg-core/db.ts:646-695`                                                                                                                                            | `const select = (…) => getReplica(replicas).select(…)`; `const update = (…) => primary.update(…)` etc.                                                              | ✓                             |
| 53  | Serverless driver coverage (Neon HTTP/serverless, Turso/libSQL, PlanetScale, D1, Vercel, Xata, Supabase, AWS Data API)                                                                                 | quote/behavior | `README.md:22` (names listed); `drizzle-orm/src/` dirs (`neon-http/`, `libsql/`, `planetscale-serverless/`, `d1/`, `vercel-postgres/`, `xata-http/`, `supabase/`, `aws-data-api/`) | README enumerates + driver dirs present                                                                                                                             | ✓                             |
| 54  | First release ≈2022                                                                                                                                                                                    | fact           | not in tree                                                                                                                                                                        | umbrella timeline `index.md:180` ("2022 Kysely + Drizzle") / npm                                                                                                    | ◯ (web)                       |
| 55  | "Latest version" framing (0.45.3 / 0.31.10 = latest as reviewed)                                                                                                                                       | figure         | tree pins the versions; "latest" is point-in-time                                                                                                                                  | npm registry                                                                                                                                                        | ◯ (web for "latest")          |
| 56  | ~7.4kb minified+gzipped                                                                                                                                                                                | figure         | `README.md:20` asserts it                                                                                                                                                          | repo's own claim (bundlephobia-sourced), not independently checkable in-tree                                                                                        | ◯ (repo-asserted figure)      |
| 57  | Adoption ("wanna use in their next project")                                                                                                                                                           | opinion        | `README.md:19` links stateofdb                                                                                                                                                     | web/point-in-time                                                                                                                                                   | ◯                             |

## Discrepancies

- **None substantive.** Every mechanism claim resolves to source. Two presentational notes:
  - The relational-query **example** in the page (`db.query.users.findMany({ columns, with,
where: (users, { eq }) => …, orderBy: (users, { asc }) => …, limit })`) is labelled
    _"illustrative"_ and is **synthesized** from the real API types, not copied verbatim: the
    method exists (`query.ts:32`), the config keys exist (`relations.ts:210-277`), and the
    operator-callback shape matches `getOperators`/`getOrderByOperators` (`relations.ts:159,
188`). No single source file shows this exact snippet.
  - The generated-migrations example and the transaction/prepared-statement snippets are
    quoted from `sqlite-core/README.md`, i.e. the **SQLite** dialect; the page mostly
    narrates the **PostgreSQL** core (`pg-core/`) for the builder/dialect/session mechanics.
    The APIs are mirror images across dialects (each `*-core` re-implements the same shapes),
    so cross-dialect quoting is faithful, but the ledger records which dialect each quote
    came from.

## Unsourced / opinion

- Editorial framings — "the heart of Drizzle", "great ergonomics, untyped effects data
  point", "SQL-shaped and low-surprise", "`sql.raw` is a foot-gun in reach", and the
  Strengths/Weaknesses/trade-off tables — are author synthesis ◯. Each is consistent with
  the verified mechanics above; not independently citable.
- Cross-library contrasts (`Kysely` = type-only / no schema ownership; `Prisma` = `.prisma`
  DSL + generated client; `Slick` nested `transactionally` lacks savepoints; `Effect TS`
  `SqlError` 11-case union; `Quill` ZIO `SQLException` channel) are grounded in **their**
  pages/ledgers, not re-verified here; stated by plain backticked name per the brief.
- Abstraction-ladder placement ("below the full-ORM rung; no identity map / change tracking
  / unit of work") is an editorial classification consistent with the code (the tree has no
  identity-map/unit-of-work machinery — an absence-as-finding).
- Rows #54–#57 are web/point-in-time (first release, "latest", bundle size, adoption),
  flagged ◯ and cited by name — not asserted as tree facts.

## Net

**53 ✓ locally verified** (≈9 verbatim quotes from `README.md` / `sqlite-core/README.md` /
`sql.ts` docstrings, plus code/behavior facts across `sql/`, `pg-core/`, `relations.ts`,
`query.ts`, `session.ts`, `migrator.ts`, `drizzle-kit/src/cli/schema.ts`, `errors.ts`,
`package.json`, `LICENSE`), **4 ◯ web/point-in-time** (#54–#57), **0 discrepancies** (2
presentational notes: one synthesized illustrative example, cross-dialect quoting). License
(Apache-2.0), versions (`0.45.3` / `0.31.10`), the `sql`→`Param` injection model, the
`$n`/`?` placeholder split, code-first + `drizzle-kit generate`/`introspect`, the
Promise-thenable async model, savepoint-based nested transactions, and the single-query
`json_agg` relational API are all confirmed against the pinned tree.

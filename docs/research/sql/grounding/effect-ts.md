# Grounding ledger — `effect-ts.md`

Claim-by-claim verification of `docs/research/sql/effect-ts.md` against the **local**
primary artifacts pinned in [`_sources.md`](./_sources.md):

- `$REPOS/typescript/effect-smol` @ `2711e39a` — SQL core
  `packages/effect/src/unstable/sql/*.ts`; Postgres driver
  `packages/sql/pg/src/PgClient.ts`; `LICENSE`; `.changeset/`
- `$REPOS/typescript/effect` @ `e5998a4` — classic monorepo, lineage only
  (`packages/sql-drizzle/README.md`, `packages/sql-kysely/README.md`)

`$REPOS = /home/petar/code/repos`. Pin confirmed: `git rev-parse HEAD` = `2711e39af73…`
(subject "move schema ai-docs", `%ci` = `2026-07-09 15:40:51 +1200`).

**Type:** `quote` · `fact` (license/version/attribution) · `figure` (number/date) ·
`behavior` (library does X) · `opinion`. **Status:** `✓` verified against the cited
local artifact · `⚠` discrepancy · `◯` not locally groundable (web/editorial).

## Claims

| #   | Claim                                                                                                                                                                                                                                                                                                | Type     | Source (local + locator)                                                                  | Evidence                                                                                                                                              | Status                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ------------------- | --- |
| 1   | License = MIT, `Copyright (c) 2023 Effectful Technologies Inc`                                                                                                                                                                                                                                       | fact     | `LICENSE:1-3`                                                                             | "MIT License / Copyright (c) 2023 Effectful Technologies Inc"                                                                                         | ✓                     |
| 2   | SQL core at `packages/effect/src/unstable/sql/`, exported via barrel `index.ts`                                                                                                                                                                                                                      | fact     | `unstable/sql/index.ts:5-49`                                                              | `export * as Statement from "./Statement.ts"` etc.; 10 modules in dir                                                                                 | ✓                     |
| 3   | QUOTE `Statement` docstring: "`SqlClient` uses this module to build executable, parameterized SQL from reusable fragments. A statement can be executed, streamed, run without row transformation, or compiled to SQL text and parameters for a specific dialect."                                    | quote    | `Statement.ts:4-9`                                                                        | verbatim                                                                                                                                              | ✓                     |
| 4   | QUOTE `SqlClient` docstring: "`SqlClient` combines the tagged-template statement constructor with connection acquisition, dialect compilation, transactions, row transforms, tracing, and reactive query helpers. Driver integrations build this service from their connection and compiler pieces." | quote    | `SqlClient.ts:4-8`                                                                        | verbatim                                                                                                                                              | ✓                     |
| 5   | `interface SqlClient extends Constructor` — the injected service is the `sql` fn                                                                                                                                                                                                                     | behavior | `SqlClient.ts:37`                                                                         | `export interface SqlClient extends Constructor {`                                                                                                    | ✓                     |
| 6   | Service tag `Context.Service<SqlClient>("effect/sql/SqlClient")`                                                                                                                                                                                                                                     | behavior | `SqlClient.ts:94`                                                                         | verbatim                                                                                                                                              | ✓                     |
| 7   | QUOTE `Statement<A>` docstring: "Executable SQL statement that is also a `Fragment` and `Effect`, with helpers for raw execution, streaming, value rows, unprepared execution, no-transform execution, and compilation."                                                                             | quote    | `Statement.ts:62-68`                                                                      | verbatim                                                                                                                                              | ✓                     |
| 8   | `interface Statement<A> extends Fragment, Effect.Effect<ReadonlyArray<A>, SqlError>` with `.raw/.withoutTransform/.stream/.values/.unprepared/.compile`                                                                                                                                              | behavior | `Statement.ts:70-81`                                                                      | interface reproduced verbatim (dropped `valuesUnprepared` for brevity — #D1)                                                                          | ✓                     |
| 9   | QUOTE `literal` doc: "Constructs a raw SQL literal segment. The literal text is not escaped, so use bound parameters for untrusted values."                                                                                                                                                          | quote    | `Statement.ts:157-161`                                                                    | verbatim                                                                                                                                              | ✓                     |
| 10  | QUOTE `Parameter` doc: "Bound parameter segment whose value is emitted as a dialect-specific placeholder and bind value."                                                                                                                                                                            | quote    | `Statement.ts:193-197`                                                                    | verbatim                                                                                                                                              | ✓                     |
| 11  | `Constructor`: template-call → `Statement<A>`; string-call → `Identifier`; `.unsafe/.literal/.in/.insert/.update/.and/.or/.csv/.onDialect/.onDialectOrElse`                                                                                                                                          | behavior | `Statement.ts:430-522`                                                                    | abridged block faithful; overloads present                                                                                                            | ✓                     |
| 12  | Comment "Create unsafe SQL query" on `unsafe`                                                                                                                                                                                                                                                        | quote    | `Statement.ts:438-440`                                                                    | `/** Create unsafe SQL query */`                                                                                                                      | ✓                     |
| 13  | `sql` closure dispatch: `typeof strings === "string"`→`identifier`; `Array.isArray && "raw" in`→`statement`; else `throw "absurd"`                                                                                                                                                                   | behavior | `Statement.ts:542-556`                                                                    | verbatim logic                                                                                                                                        | ✓                     |
| 14  | QUOTE `statement` doc: "…preserving fragments and helper segments while converting ordinary interpolated values into bound parameters."                                                                                                                                                              | quote    | `Statement.ts:611-613`                                                                    | verbatim (partial)                                                                                                                                    | ✓                     |
| 15  | `statement()` body: `isFragment→push segments`, `isSegment→push`, `else→parameter(arg)`; literals between holes                                                                                                                                                                                      | behavior | `Statement.ts:625-641`                                                                    | code block matches source (comments added by page)                                                                                                    | ✓                     |
| 16  | `Compiler` interface `compile(statement, withoutTransform) => [sql, params]` + `withoutTransform: this`                                                                                                                                                                                              | behavior | `Statement.ts:743-750`                                                                    | verbatim                                                                                                                                              | ✓                     |
| 17  | `compile` Parameter: `sql += placeholder(value); binds.push(value)`; Identifier → `onIdentifier`; Literal → raw `+= value` (+ optional params)                                                                                                                                                       | behavior | `Statement.ts:845-862`                                                                    | switch cases verbatim                                                                                                                                 | ✓                     |
| 18  | pg `makeCompiler`: `dialect: "pg"`, ``placeholder(_) => `$${_}` ``, identifier escape via `defaultEscape('"')`                                                                                                                                                                                       | behavior | `PgClient.ts:821-862`, `:864`                                                             | ``placeholder(_) { return `$${_}` }``; `const escape = Statement.defaultEscape("\"")`                                                                 | ✓                     |
| 19  | SQLite `makeCompilerSqlite`: `?` placeholder + double-quoted identifiers                                                                                                                                                                                                                             | behavior | `Statement.ts:1054-1071`, `:1494`                                                         | `placeholder(_) { return "?" }`; `escapeSqlite = defaultEscape("\"")`                                                                                 | ✓                     |
| 20  | Compiled result memoized on statement (`statementCacheSymbol`)                                                                                                                                                                                                                                       | behavior | `Statement.ts:814-815`, `:826-828`, `:1035`                                               | `return (statement as any)[cacheSymbol] = result`                                                                                                     | ✓                     |
| 21  | `sql.and`/`or` join with `AND`/`OR`, parenthesize, empty → `1=1`; `sql.in(col, [])` → `1=0`                                                                                                                                                                                                          | behavior | `Statement.ts:693,702`, `:1504,1511`                                                      | `and = join(" AND ", true, "1=1")`; `neverFragment = fragment([literal("1=0")])`                                                                      | ✓                     |
| 22  | Comment "Copy of the client for safeql etc." on `safe`; `readonly safe: this`                                                                                                                                                                                                                        | quote    | `SqlClient.ts:41-43`                                                                      | `/** Copy of the client for safeql etc. */ readonly safe: this`                                                                                       | ✓                     |
| 23  | `Connection`: `execute/executeRaw/executeStream/executeValues/executeUnprepared`, positional params, `SqlError` channel                                                                                                                                                                              | behavior | `SqlConnection.ts:26-63`                                                                  | interface reproduced (abridged); all methods present                                                                                                  | ✓                     |
| 24  | `type Acquirer = Effect<Connection, SqlError, Scope>` (scoped acquire/release)                                                                                                                                                                                                                       | behavior | `SqlConnection.ts:72`                                                                     | verbatim                                                                                                                                              | ✓                     |
| 25  | `type Row = { readonly [column: string]: unknown }`                                                                                                                                                                                                                                                  | behavior | `SqlConnection.ts:88`                                                                     | verbatim                                                                                                                                              | ✓                     |
| 26  | `getConnection`: `serviceOption(transactionService)` → onNone `acquirer` (pool), onSome reuse conn                                                                                                                                                                                                   | behavior | `SqlClient.ts:141-147`                                                                    | code block matches verbatim                                                                                                                           | ✓                     |
| 27  | PgClient pool: `new Pg.Pool`, validate `pool.query("SELECT 1")` in `acquireRelease`, `pool.end()` on close; `reserve` pins one `PoolClient`; `RcRef` for LISTEN; `makeCancel` → `SELECT pg_cancel_backend(<pid>)`                                                                                    | behavior | `PgClient.ts:149-207`, `:403`, `:408-431`, `:751-771`                                     | pool + `pool.query("SELECT 1")`, `reserve = Effect.map(reserveRaw, makeConection)`, `RcRef.make`, `pg_cancel_backend(${processId})`                   | ✓                     |
| 28  | Design use of QUOTE #7 ("Statement that is also a Fragment and Effect")                                                                                                                                                                                                                              | quote    | `Statement.ts:62-68`                                                                      | same as #7                                                                                                                                            | ✓                     |
| 29  | Effect behaviour from `Effectable.Prototype.evaluate(fiber)`: opens `sql.execute` span, acquires conn, compiles, `connection.execute(...)`                                                                                                                                                           | behavior | `Statement.ts:1256-1272`, `:1296-1316`                                                    | `evaluate(fiber){ … makeSpanUnsafe(fiber,"sql.execute",…) … connection.execute(sql,params,this.transformRows) }`                                      | ✓                     |
| 30  | Variants `.stream`→`Stream`, `.values`→positional arrays, `.raw`→driver result, `.unprepared`→no prepared cache, `.withoutTransform`→no name transform                                                                                                                                               | behavior | `Statement.ts:1318-1374`                                                                  | getters all present                                                                                                                                   | ✓                     |
| 31  | QUOTE `makeWithTransaction` doc: "Builds a transaction wrapper that begins top-level transactions, uses savepoints for nested transactions, commits on success, and rolls back on failure or interruption."                                                                                          | quote    | `SqlClient.ts:215-220`                                                                    | verbatim                                                                                                                                              | ✓                     |
| 32  | QUOTE `TransactionConnection` = "Phantom identifier for the scoped transaction connection service…"; payload `readonly [conn: Connection, depth: number]`                                                                                                                                            | quote    | `SqlClient.ts:295-303`, `:311-318`                                                        | docstring verbatim; `Service = readonly [conn: Connection.Connection, depth: number]`                                                                 | ✓                     |
| 33  | `makeWithTransaction` body: Some→reuse conn, None→acquire; `id = Some ? depth+1 : 0`; `id===0 ? begin : savepoint`; success→`id===0 ? commit : void`; failure→`id>0 ? rollbackSavepoint : rollback`; body run with conn in context                                                                   | behavior | `SqlClient.ts:242-289`                                                                    | code block faithful (abridged)                                                                                                                        | ✓                     |
| 34  | Default savepoint SQL `SAVEPOINT effect_sql_<id>` / `ROLLBACK TO SAVEPOINT effect_sql_<id>`; name `effect_sql_${id}`                                                                                                                                                                                 | behavior | `SqlClient.ts:151-153`, `:163`, `:166`                                                    | ``savepoint ?? ((name)=>`SAVEPOINT ${name}`)``; ``rollbackSavepoint ?? ((name)=>`ROLLBACK TO SAVEPOINT ${name}`)``; ``savepoint(`effect_sql_${id}`)`` | ✓                     |
| 35  | Whole tx under `Effect.uninterruptibleMask`; defaults `beginTransaction="BEGIN"`, `commit="COMMIT"`, `rollback="ROLLBACK"`                                                                                                                                                                           | behavior | `SqlClient.ts:233`, `:149-153`                                                            | `Effect.uninterruptibleMask((restore) => …`; defaults verbatim                                                                                        | ✓                     |
| 36  | QUOTE `SqlError` doc: "…Each reason keeps the original cause, optional message and operation metadata, and whether retrying may succeed."                                                                                                                                                            | quote    | `SqlError.ts:5-11`                                                                        | verbatim (full quote :4-11)                                                                                                                           | ✓                     |
| 37  | `SqlErrorReason` = 11-case union (Connection/Authentication/Authorization/SqlSyntax/UniqueViolation/Constraint/Deadlock/Serialization/LockTimeout/StatementTimeout/Unknown)                                                                                                                          | behavior | `SqlError.ts:335-346`                                                                     | union has exactly 11 members                                                                                                                          | ✓                     |
| 38  | `SqlError` class: `Schema.TaggedErrorClass`; `message => reason.message                                                                                                                                                                                                                              |          | reason.\_tag`; `isRetryable => reason.isRetryable`; `cause = reason`                      | behavior                                                                                                                                              | `SqlError.ts:387-421` | code block verbatim | ✓   |
| 39  | Retryable = Connection, Deadlock, Serialization, LockTimeout, StatementTimeout (`true`); other 6 `false`                                                                                                                                                                                             | behavior | `SqlError.ts:47,215,242,270,297` (true); `:74,101,128,161,188,323` (false)                | 5 `return true`, 6 `return false`                                                                                                                     | ✓                     |
| 40  | pg `classifyError` SQLSTATE map: `08*`→Connection, `28*`→Authentication, `42501`→Authorization, `42*`→SqlSyntax, `23505`→UniqueViolation, `23*`→Constraint, `40P01`→Deadlock, `40001`→Serialization, `55P03`→LockTimeout, `57014`→StatementTimeout, else Unknown                                     | behavior | `PgClient.ts:910-950`                                                                     | every branch verbatim                                                                                                                                 | ✓                     |
| 41  | `classifySqliteError` keyed on `SQLITE_*` codes / `errno`                                                                                                                                                                                                                                            | behavior | `SqlError.ts:513-567`, `:479-480`                                                         | `SQLITE_CONSTRAINT_UNIQUE`, `matchesSqliteCode(code,"SQLITE_AUTH")`, `errno & 0xff` switch                                                            | ✓                     |
| 42  | QUOTE changeset: "Consolidate the SqlError changes to the new reason-based shape across effect and the SQL drivers, classifying native failures into structured reasons with Unknown fallback where native codes are unavailable."                                                                   | quote    | `.changeset/consolidate-sql-error.md`                                                     | verbatim                                                                                                                                              | ✓                     |
| 43  | QUOTE `SqlResolver` doc: batches concurrent requests and "keeps batches separated by the active SQL transaction connection"                                                                                                                                                                          | quote    | `SqlResolver.ts:6-9`                                                                      | "…batches concurrent requests into SQL operations… keeps batches separated by the active SQL transaction connection."                                 | ✓                     |
| 44  | `SqlResolver` families `ordered`/`grouped`/`findById`/`void`; `ResultLengthMismatch` on count mismatch; `transactionKey` = `Equal.byReferenceUnsafe(conn)`                                                                                                                                           | behavior | `SqlResolver.ts:101,149,218,275`, `:128-129`, `:387-393`                                  | exports present; `new ResultLengthMismatch({expected,actual})`; `return Equal.byReferenceUnsafe(conn)`                                                | ✓                     |
| 45  | QUOTE `SqlSchema` = "a small adapter between Effect Schema and SQL statements"; helpers `findAll`/`findNonEmpty`/`findOne`/`findOneOption`/`void`, encode→execute→decode                                                                                                                             | quote    | `SqlSchema.ts:5-11`, `:33,65,86,115,148`                                                  | docstring verbatim; 5 constructors; `encodeEffect`/`decodeUnknownEffect`                                                                              | ✓                     |
| 46  | `SqlModel.makeRepository` → `insert/update/findById/delete` (+`insertVoid`/`updateVoid`), optional `softDeleteColumn`, decodes with model schema                                                                                                                                                     | behavior | `SqlModel.ts:33-221`                                                                      | `return { insert, insertVoid, update, updateVoid, findById, delete: delete_ }`                                                                        | ✓                     |
| 47  | QUOTE `Migrator` doc + behavior: "records completed ids in a migrations table, and runs only pending migrations in a transaction"; `sql.withTransaction`; default table `effect_sql_migrations`; pg `ACCESS EXCLUSIVE` lock; Duplicates/Locked; loaders `fromFileSystem`/`fromGlob`/`fromRecord`     | quote    | `Migrator.ts:4-7`, `:111`, `:224`, `:239-244`, `:307`, `:336,383,406`                     | docstring verbatim; `table = "effect_sql_migrations"`; `LOCK TABLE … IN ACCESS EXCLUSIVE MODE`; `sql.withTransaction(run)`                            | ✓                     |
| 48  | `Dialect = "sqlite" \| "pg" \| "mysql" \| "mssql" \| "clickhouse"`; `sql.onDialect` selects by `compiler.dialect`                                                                                                                                                                                    | behavior | `Statement.ts:60`, `:590-595`                                                             | union verbatim; `onDialect(options){ return options[compiler.dialect]() }`                                                                            | ✓                     |
| 49  | `PrimitiveKind` = string/number/bigint/boolean/Date/null/Int8Array/Uint8Array; `primitiveKind` treats `undefined` as `null`                                                                                                                                                                          | behavior | `Statement.ts:397-406`, `:1097-1122`                                                      | union verbatim; `case "undefined": return "null"`                                                                                                     | ✓                     |
| 50  | `defaultTransforms(fn, nested?)` builds value/object/row key renamers (snake↔camel)                                                                                                                                                                                                                  | behavior | `Statement.ts:1131-1200`                                                                  | returns `{ value, object, array }`; pg wires `transformResultNames`/`transformQueryNames`                                                             | ✓                     |
| 51  | `SqlStream.asyncPauseResume` adapts push-producer with backpressure (pause when queue full), 128 buffer; pg uses `pg-cursor`, reads 128 rows                                                                                                                                                         | behavior | `SqlStream.ts:27-82`, `PgClient.ts:724-748`                                               | `asyncPauseResume(register, bufferSize = 128)`; pg `new Cursor(sql,params)`, `cursor.read(128, …)`                                                    | ✓                     |
| 52  | `PgClient extends Client.SqlClient` with `json`/`listen`/`notify`; `layer`/`layerConfig`/`layerFrom` provide both `PgClient` and `SqlClient` tags                                                                                                                                                    | behavior | `PgClient.ts:79-85`, `:779-813`                                                           | `interface PgClient extends Client.SqlClient`; `Layer.Layer<PgClient \| Client.SqlClient, …>`                                                         | ✓                     |
| 53  | Each execution opens an OpenTelemetry span with `db.query.text`/`db.operation.name`                                                                                                                                                                                                                  | behavior | `Statement.ts:1204-1205`, `:1312-1313`                                                    | `ATTR_DB_OPERATION_NAME="db.operation.name"`, `ATTR_DB_QUERY_TEXT="db.query.text"`                                                                    | ✓                     |
| 54  | `effect@4.0.0-beta.96`; `@effect/sql-pg@4.0.0-beta.96`; repository `Effect-TS/effect-smol`                                                                                                                                                                                                           | figure   | `packages/effect/package.json`, `packages/sql/pg/package.json`, repository field          | `"version": "4.0.0-beta.96"`; `"url": "https://github.com/Effect-TS/effect-smol.git"`                                                                 | ✓                     |
| 55  | Backend driver dirs: clickhouse, d1, libsql, mssql, mysql2, pg, pglite, sqlite-bun, sqlite-do, sqlite-node, sqlite-react-native, sqlite-wasm                                                                                                                                                         | fact     | `ls packages/sql/`                                                                        | 12 dirs                                                                                                                                               | ✓                     |
| 56  | QUOTE lineage: `sql-drizzle` = "An `@effect/sql` implementation for Drizzle"; `sql-kysely` = "…for Kysely" (classic `Effect-TS/effect`)                                                                                                                                                              | quote    | `$REPOS/typescript/effect/packages/sql-drizzle/README.md:1-3`, `sql-kysely/README.md:1-3` | READMEs verbatim; classic repo @ `e5998a45`; `sql`/`sql-drizzle`/`sql-kysely` present                                                                 | ✓                     |
| 57  | Pinned SHA subject "move schema ai-docs"; commit date 2026-07-09                                                                                                                                                                                                                                     | fact     | `git show -s 2711e39a`                                                                    | `%ci` = `2026-07-09 15:40:51 +1200`                                                                                                                   | ✓                     |
| 58  | First release ≈ 2023 (classic `@effect/sql`)                                                                                                                                                                                                                                                         | figure   | LICENSE copyright year 2023 (proxy only)                                                  | no first-release date in tree; soft ≈ on page                                                                                                         | ◯                     |
| 59  | Documentation site `effect.website`                                                                                                                                                                                                                                                                  | fact     | —                                                                                         | web URL; the grounded documentation is the local module docstrings                                                                                    | ◯                     |

## Discrepancies

- **#D1 (abridgement, not error).** The page's reproduced `Statement<A>` interface omits
  the `readonly valuesUnprepared` member present in source (`Statement.ts:75`) for
  brevity; the prose covers `.values`/`.unprepared` without it. No factual claim is
  affected, and the surrounding narrative does not present the block as a complete
  listing.

- **#D2 (gloss, not verbatim).** The page renders the pg compiler's `onIdentifier` as a
  comment "`Statement.defaultEscape('"')` — doubles embedded quotes, escapes dots"
  rather than the literal source, which routes through `escape =
Statement.defaultEscape("\"")` (`PgClient.ts:834-838, :864`) and, when a `transform`
  is present, wraps it. The behavioural description matches `defaultEscape`
  (`Statement.ts:1081-1088`); the text is inside a `/* … */` gloss, not passed off as a
  quote.

## Unsourced / opinion

- Positioning framings ("primary effects-first exemplar", "a query is not a string you
  run — it is an `Effect` you compose", "rawness is opt-in", the Strengths/Weaknesses
  lists, the trade-off table) are author synthesis `◯`. Each is consistent with verified
  mechanics (auto-`parameter` binding #15, `Statement extends Effect` #8, the
  `isRetryable` union #39, savepoint nesting #33-34) but is editorial, not independently
  citable.
- "≈ 2023" first release (#58) is inferred from the LICENSE copyright year; no
  release-date artifact exists in the `--depth 1`-adjacent tree. Marked soft/web.
- `effect.website` (#59) is a web fact; the real grounded documentation is the local
  module docstrings.
- The `-smol` "beta / unstable, subject to change" characterization rests on
  `4.0.0-beta.96` (#54) and the `unstable/` path (#2); the "production-stable" status of
  the classic line is editorial `◯`.

## Net

59 claims: **57 ✓** local-primary, **2 ◯** (#58 first-release year, #59 docs-site URL).
Two non-substantive notes (#D1 abridged interface listing, #D2 escape-comment gloss) —
neither is a misquote. All ~13 verbatim QUOTES (module docstrings for
`Statement`/`SqlClient`/`SqlError`/`SqlResolver`/`SqlSchema`/`Migrator`, the
`Statement<A>`/`literal`/`Parameter`/`makeWithTransaction`/`TransactionConnection` docs,
the `unsafe`/`safe` comments, the two adapter READMEs, and the consolidate-sql-error
changeset) match source verbatim. The load-bearing mechanics — `statement()`
arg→`parameter`, the `Compiler` Parameter/Identifier/Literal cases, `$n`/`?`
placeholders, `Statement extends Fragment, Effect`, the fiber-local
`TransactionConnection` + `effect_sql_<id>` savepoints, the 11-case `SqlErrorReason`
with 5 retryable, and the pg SQLSTATE map — are all verified against the pinned
`2711e39a` checkout. Pin, version (`4.0.0-beta.96`), and lineage
(`sql-drizzle`/`sql-kysely` adapters in `Effect-TS/effect`) confirmed.

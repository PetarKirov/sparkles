# Grounding ledger — `gorm.md`

Claim-by-claim verification of `docs/research/sql/gorm.md` against the **local** pinned
checkout, pinned in [`_sources.md`](./_sources.md).

Source: `$REPOS/go/gorm` @ `1d6ce99` (top-level `.go` files, `clause/`, `callbacks/`,
`schema/`, `migrator/`, `logger/`, `utils/tests/`, `README.md`, `LICENSE`, `go.mod`,
`tests/go.mod`). `$REPOS = /home/petar/code/repos`. Full SHA
`1d6ce99528060be18a42be09aca8d39efcb47f28`, committed 2026-06-22.

**Type:** `L` = local primary (repo file). `◯` = web / point-in-time / editorial (not
groundable in the tree). **Status:** `✓` verified · `⚠` discrepancy · `◯` web-fallback /
opinion. All paths are relative to `$REPOS/go/gorm`. The page's reference links use
blob URLs pinned to the full SHA; line numbers below are as-of `1d6ce99`.

## Claims

| #   | Claim                                                                                                                    | Type | Source (local + locator)                                                 | Evidence                                                                                                                                                                                             | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------ | ---- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1   | QUOTE package docstring: "Package gorm is a full-featured, developer-friendly ORM for Golang."                           | L    | `gorm.go:1`                                                              | `// Package gorm is a full-featured, developer-friendly ORM for Golang.`                                                                                                                             | ✓      |
| 2   | QUOTE README tagline: "The fantastic ORM library for Golang, aims to be developer friendly."                             | L    | `README.md:3`                                                            | verbatim line 3                                                                                                                                                                                      | ✓      |
| 3   | QUOTE README feature list (Full-Featured ORM / Associations / Hooks / Preload,Joins / Transactions … / Auto Migrations…) | L    | `README.md:12-25`                                                        | verbatim bullets incl. "Eager loading with `Preload`, `Joins`", "Nested Transactions, Save Point", "Auto Migrations", "Developer Friendly"                                                           | ✓      |
| 4   | Model = plain struct + `gorm:"..."` tags; `gorm.Model` embeds ID/CreatedAt/UpdatedAt/DeletedAt                           | L    | `model.go:11-16`                                                         | `type Model struct { ID uint gorm:"primarykey"; CreatedAt time.Time; UpdatedAt time.Time; DeletedAt DeletedAt gorm:"index" }`                                                                        | ✓      |
| 5   | Schema reflected from struct tags: `ParseField` reads `fieldStruct.Tag.Get("gorm")`; `PRIMARYKEY`/`AUTOINCREMENT`        | L    | `schema/field.go:109,126-127`                                            | `ParseTagSetting(fieldStruct.Tag.Get("gorm"), ";")`; `PrimaryKey: … tagSetting["PRIMARYKEY"] …`; `AutoIncrement: … tagSetting["AUTOINCREMENT"]`                                                      | ✓      |
| 6   | `Field` carries reflection closures `ValueOf`/`ReflectValueOf`/`Set`                                                     | L    | `schema/field.go:88-90`                                                  | `ReflectValueOf func(...)`, `ValueOf func(...)(value interface{}, zero bool)`, `Set func(...) error`                                                                                                 | ✓      |
| 7   | Built on `database/sql`: `ConnPool` = the `database/sql` context API                                                     | L    | `interfaces.go:34-39`                                                    | `type ConnPool interface { PrepareContext…; ExecContext…; QueryContext…; QueryRowContext… }`                                                                                                         | ✓      |
| 8   | `db.DB()` returns the underlying `*sql.DB` (the pool)                                                                    | L    | `gorm.go:426`                                                            | `func (db *DB) DB() (*sql.DB, error)`                                                                                                                                                                | ✓      |
| 9   | `Open` pings the pool unless `DisableAutomaticPing`                                                                      | L    | `gorm.go:242-251`                                                        | `if err == nil && !config.DisableAutomaticPing { if pinger, ok := db.ConnPool.(interface{ Ping() error }); ok { err = pinger.Ping()… } }`                                                            | ✓      |
| 10  | `Session` forks configured clone; `Connection` leases a single `*sql.Conn`; `PrepareStmt` caches via `PreparedStmtDB`    | L    | `gorm.go:261,229-233`; `finisher_api.go:613,624-630`                     | `func (db *DB) Session(config *Session) *DB`; `NewPreparedStmtDB(...)`; `conn, err := sqlDB.Conn(...)` + `defer conn.Close()`                                                                        | ✓      |
| 11  | Chainable API: chain methods clone stmt (`getInstance`), mutate, return `tx`                                             | L    | `chainable_api.go:207-213`                                               | `Where`: `tx = db.getInstance(); … tx.Statement.AddClause(clause.Where{Exprs: conds}); return`                                                                                                       | ✓      |
| 12  | QUOTE Where docstring examples (`db.Where("name = ?", "jinzhu").First`; struct form; chained `age <> ?`)                 | L    | `chainable_api.go:199-204`                                               | verbatim docstring example lines                                                                                                                                                                     | ✓      |
| 13  | `Statement` holds `Clauses map[string]clause.Clause`, `SQL strings.Builder`, `Vars []interface{}`                        | L    | `statement.go:22-51`                                                     | struct fields `Clauses`, `SQL`, `Vars` present                                                                                                                                                       | ✓      |
| 14  | `clause` pkg = query AST: `Where`, `Expr`, typed `Eq`/`Neq`/`Gt`/`IN`/`Like`                                             | L    | `clause/where.go:13`; `clause/expression.go:21,185,231,270,301,353`      | `type Where struct`; `type Expr struct`; `type IN`, `type Eq`, `type Neq`, `type Gt Eq`, `type Like Eq`                                                                                              | ✓      |
| 15  | `?` placeholder = safety seam: `Expr.Build` routes each `?` to `builder.AddVar`, copies other bytes verbatim             | L    | `clause/expression.go:34-51`                                             | `for _, v := range []byte(expr.SQL) { if v == '?' … builder.AddVar(builder, expr.Vars[idx]) … else … builder.WriteByte(v) }`                                                                         | ✓      |
| 16  | `AddVar` appends value to `stmt.Vars` and writes driver placeholder via `BindVarTo` (SQL/data separate channels)         | L    | `statement.go:263-265`                                                   | default arm: `stmt.Vars = append(stmt.Vars, v); stmt.DB.Dialector.BindVarTo(writer, stmt, v)`                                                                                                        | ✓      |
| 17  | `clause.Eq.Build` binds the value via `AddVar`; `nil` lowers to `IS NULL`                                                | L    | `clause/expression.go:237-263`                                           | `builder.WriteString(" = "); builder.AddVar(builder, eq.Value)`; `if eqNil(eq.Value) { builder.WriteString(" IS NULL") }`                                                                            | ✓      |
| 18  | `BuildCondition` polymorphic: string+`?`→`Expr`; `@name`→`NamedExpr`; struct→`Eq` per **non-zero** field; map→`Eq`/`IN`  | L    | `statement.go:300-307,376-408,428-438`                                   | `clause.Expr{SQL:s,Vars:args}`; `clause.NamedExpr{...}`; struct arm `if …!isZero…{ conds = append(..., clause.Eq{...}) }`; map arms `clause.Eq`/`clause.IN`                                          | ✓      |
| 19  | Escape hatches stay parameterized: `Raw`/`Exec` route through `clause.Expr`/`NamedExpr`; `gorm.Expr` for raw fragments   | L    | `chainable_api.go:461-471`; `finisher_api.go:784-795`; `gorm.go:478-480` | `Raw`/`Exec`: `clause.Expr{SQL:sql,Vars:values}.Build(tx.Statement)` (or `NamedExpr` if `@`); `func Expr(...) clause.Expr`                                                                           | ✓      |
| 20  | Identifiers not parameterizable: `Order`(string) marks column `Raw: true` (textual substitution)                         | L    | `chainable_api.go:323-330`                                               | `clause.OrderByColumn{ Column: clause.Column{Name: v, Raw: true} }`                                                                                                                                  | ✓      |
| 21  | QUOTE logger injection warning                                                                                           | L    | `logger/sql.go:50`                                                       | "// ExplainSQL generate SQL string with given parameters, the generated SQL is expected to be used in logger, execute it might introduce a SQL injection vulnerability"                              | ✓      |
| 22  | Upstream guidance "keep user data in `?`/`@name`, never concatenate" is the gorm.io security page                        | ◯    | gorm.io/docs/security.html (web)                                         | page cites it as web guidance, not repo                                                                                                                                                              | ◯      |
| 23  | Code-first: annotated structs ARE the schema; example User model carries columns/keys/relations in tags                  | L    | `utils/tests/models.go` (`type User struct`)                             | `Toys []Toy gorm:"polymorphic:Owner"`, `Languages []Language gorm:"many2many:UserSpeak;"`, `Team []User gorm:"foreignkey:ManagerID"`                                                                 | ✓      |
| 24  | `AutoMigrate` reflects structs→create/add-column/smart-migrate                                                           | L    | `migrator/migrator.go:122-129,150-166`                                   | `func (m Migrator) AutoMigrate(values ...interface{})`; `if foundColumn == nil { // not found, add column … AddColumn } else { // found, smartly migrate … MigrateColumn }`                          | ✓      |
| 25  | `AutoMigrate` is additive-only — no branch drops a column/index/table                                                    | L    | `migrator/migrator.go:130-200`                                           | reconcile loop only `AddColumn`/`MigrateColumn` + create missing constraints/indexes; no `DropColumn` path in `AutoMigrate`                                                                          | ✓      |
| 26  | Docs quote "AutoMigrate … WON'T delete unused columns to protect your data"                                              | ◯    | gorm.io/docs/migration.html (web)                                        | page attributes it as a doc statement; code (row 25) confirms the additive behavior locally                                                                                                          | ◯      |
| 27  | No versioned migration runner / bookkeeping table / down path in core                                                    | L    | `migrator/` (grep)                                                       | no migration-version table or `Down`/rollback runner in `migrator/`; only `AutoMigrate` + DDL primitives                                                                                             | ✓      |
| 28  | Codegen out of core; `gorm.io/gen` does query/DAO codegen                                                                | ◯    | `README.md:30` links "Gen Guides"; gorm.io/gen (web)                     | README "Gen Guides https://gorm.io/gen/index.html"; gen is a separate module (web)                                                                                                                   | ◯      |
| 29  | In-core generics surface `gorm.G[T]` wraps the reflection engine (typed result, not typed SQL)                           | L    | `generics.go:147,38`                                                     | `func G[T any](db *DB, opts ...clause.Expression) Interface[T]`; `type Interface[T any] interface {…}`                                                                                               | ✓      |
| 30  | Row hydration is reflection: `Scan`/`ScanRows` reflect dest and set fields                                               | L    | `finisher_api.go:592-609`; `scan.go`                                     | `ScanRows`: `tx.Statement.ReflectValue = reflect.ValueOf(dest); … Scan(rows, tx, ScanInitialized)`                                                                                                   | ✓      |
| 31  | Codecs bottom out in `database/sql` `driver.Valuer`/`sql.Scanner`; GORM adds `Valuer.GormValue`                          | L    | `interfaces.go:71-73`                                                    | `type Valuer interface { GormValue(context.Context, *DB) clause.Expr }`                                                                                                                              | ✓      |
| 32  | Nullability via pointer / `sql.Null*`; `DeletedAt = sql.NullTime` (soft-delete global scope)                             | L    | `soft_delete.go:13`                                                      | `type DeletedAt sql.NullTime` (+ `Scan`/`Value`/JSON)                                                                                                                                                | ✓      |
| 33  | Blocking: finishers run synchronously, return `*DB`, no `Future`/`IO`                                                    | L    | `finisher_api.go:19-27,130-183`                                          | `Create`/`First`/`Find` return `(tx *DB)`; execute callbacks inline; no async type                                                                                                                   | ✓      |
| 34  | QUOTE errors accumulate on `db.Error`, chained with `%w`                                                                 | L    | `gorm.go:413-417`                                                        | `if db.Error == nil { db.Error = err } else { db.Error = fmt.Errorf("%v; %w", db.Error, err) }`                                                                                                      | ✓      |
| 35  | Failure vocabulary = package sentinels (`ErrRecordNotFound`, `ErrDuplicatedKey`, `ErrForeignKeyViolated`, …)             | L    | `errors.go:9-54`                                                         | sentinel `var` block incl. `ErrRecordNotFound`, `ErrDuplicatedKey`, `ErrForeignKeyViolated`, `ErrCheckConstraintViolated`                                                                            | ✓      |
| 36  | Driver-error translation is opt-in: `TranslateError` config + `ErrorTranslator` dialector                                | L    | `gorm.go:222-226,407-411`; `interfaces.go:90-92`                         | `if db.Config.TranslateError { if errTranslator, ok := db.Dialector.(ErrorTranslator); ok { err = errTranslator.Translate(err) } }`; `type ErrorTranslator interface { Translate(err error) error }` | ✓      |
| 37  | Callback pipeline is executor + plugin seam: 6 processors; `RegisterDefaultCallbacks` orders create stages               | L    | `callbacks.go:15-26`; `callbacks/callbacks.go:41-49`                     | processors `create/query/update/delete/row/raw`; create order `begin_transaction`→`before_create`→`save_before_associations`→`create`→…→`commit_or_rollback_transaction`                             | ✓      |
| 38  | `db.Create(&user)` = `tx.callbacks.Create().Execute(tx)`                                                                 | L    | `finisher_api.go:19-27`                                                  | `Create`: `tx.Statement.Dest = value; return tx.callbacks.Create().Execute(tx)`                                                                                                                      | ✓      |
| 39  | Active-record mutation: create callback reads `LastInsertId()` → writes PK back into struct                              | L    | `callbacks/create.go:143,153-165`                                        | `insertID, err := result.LastInsertId()` … `// append @id column with value for auto-increment primary key` → sets pkFieldName                                                                       | ✓      |
| 40  | QUOTE `Save` inserts when PK blank, else updates                                                                         | L    | `finisher_api.go:100-109`                                                | struct arm: `for _, pf := range …PrimaryFields { if _, isZero := pf.ValueOf(...); isZero { return tx.callbacks.Create().Execute(tx) } }` then `fallthrough`                                          | ✓      |
| 41  | Per-record hooks: `BeforeCreate`/`AfterCreate`/`BeforeSave`/…/`AfterFind`, each `func(*gorm.DB) error`                   | L    | `callbacks/interfaces.go:5-39`                                           | interface set incl. `BeforeCreate(*gorm.DB) error`, `AfterFind(*gorm.DB) error`                                                                                                                      | ✓      |
| 42  | No identity map / unit of work / session change-tracking; `Changed` is per-update, not a snapshot                        | L    | `statement.go:643-696`; `finisher_api.go` (finishers)                    | `Changed` computes vs. passed `stmt.Dest`; each finisher issues its own SQL; no session/identity-map type in tree                                                                                    | ✓      |
| 43  | QUOTE default single-write transaction config comment                                                                    | L    | `gorm.go:24-26`                                                          | "// GORM perform single create, update, delete operations in transactions by default to ensure database data integrity" + "You can disable it by setting `SkipDefaultTransaction` to true"           | ✓      |
| 44  | Transaction block form commits on nil / rolls back on error or panic                                                     | L    | `finisher_api.go:637-677`                                                | `Transaction`: `if err = fc(tx); err == nil { panicked = false; return tx.Commit().Error }`; deferred `tx.Rollback()` on panic/err                                                                   | ✓      |
| 45  | Nested transaction opens a `SAVEPOINT` (not a new connection)                                                            | L    | `finisher_api.go:642-654`                                                | nested arm: `spID := new(maphash.Hash).Sum64(); err = db.SavePoint(fmt.Sprintf("sp%d", spID)).Error` + deferred `RollbackTo`                                                                         | ✓      |
| 46  | `SavePoint`/`RollbackTo` dispatch to dialector `SavePointerDialectorInterface`                                           | L    | `finisher_api.go:737-781`; `interfaces.go:42-45`                         | `SavePoint`/`RollbackTo` call `savePointer.SavePoint/RollbackTo`; `type SavePointerDialectorInterface interface { SavePoint…; RollbackTo… }`                                                         | ✓      |
| 47  | `Joins("Account")` folds relation into a SQL `JOIN`; `Preload` = separate `WHERE … IN (…)` query per relation            | L    | `chainable_api.go:248-256,395-402`; `callbacks/preload.go:224`           | `Joins`→`clause.LeftJoin`; `Preload` stores `Preloads[query]=args`; preload runs `tx.Where(clause.IN{...}).Find(...)`                                                                                | ✓      |
| 48  | `Preload` opt-in; no lazy-loading proxy (forgotten preload = zero value)                                                 | L    | `callbacks/preload.go`; `chainable_api.go:395-402`                       | relations loaded only when `Preload`/`Joins` requested; no proxy type in tree                                                                                                                        | ✓      |
| 49  | License MIT, "Copyright (c) 2013-present Jinzhu"                                                                         | L    | `LICENSE:1,3`                                                            | "The MIT License (MIT)"; "Copyright (c) 2013-present Jinzhu <wosmvp@gmail.com>"                                                                                                                      | ✓      |
| 50  | Module `gorm.io/gorm`, `go 1.18`                                                                                         | L    | `go.mod:1,3`                                                             | `module gorm.io/gorm`; `go 1.18`                                                                                                                                                                     | ✓      |
| 51  | Backends via out-of-tree drivers `gorm.io/driver/{mysql,postgres,sqlite,sqlserver,gaussdb}`                              | L    | `tests/go.mod:10-14`                                                     | require block lists the five driver modules                                                                                                                                                          | ✓      |
| 52  | Pinned tree carries the `v1.31.2` tag                                                                                    | L    | `git tag` @ `1d6ce99`                                                    | `v1.31.2` is the tag reachable at HEAD                                                                                                                                                               | ✓      |
| 53  | `gorm.io/gorm` is the v2 (2020) rewrite of the original 2013 GORM; author Jinzhu Zhang                                   | ◯    | LICENSE "2013-present Jinzhu" (local); v2/2020 + full name (web)         | "2013-present" grounds the age locally; the v2/2020 milestone and "Zhang" surname are web                                                                                                            | ◯      |
| 54  | Adoption: most widely used Go ORM; tens of thousands of stars, large dependent graph                                     | ◯    | github.com/go-gorm/gorm (web)                                            | point-in-time popularity metrics — web                                                                                                                                                               | ◯      |
| 55  | Plugins under `gorm.io/plugin/*` (dbresolver, prometheus, opentelemetry) built on the callback seam                      | ◯    | gorm.io/docs (web); callback seam local (row 37)                         | plugin modules are out-of-tree (web); the seam they use is local                                                                                                                                     | ◯      |

## Discrepancies

- **None material.** All quoted repo strings (rows 1–3, 15, 21, 34, 43) match the source
  verbatim; multi-line code quotes are labelled "abridged" on the page where interior
  lines are elided (Save row 40, Expr.Build row 15, AutoMigrate row 24, the create
  pipeline row 37). No claim contradicts the code.
- **Minor framing, not error — row 20/21 (identifier injection surface).** The page's
  assertion that `Order`/`Group`/raw `Select` are a residual injection surface is grounded
  in the `Raw: true` marking (row 20) plus the logger warning (row 21); GORM does not label
  these an injection risk in code comments, so the _risk characterization_ is editorial
  (see Unsourced) though the _mechanism_ (textual substitution of identifiers) is local.

## Unsourced / opinion

- Metadata `First release ≈2013` / `v2 rewrite 2020` / `Latest v1.31.x` — age grounded by
  the LICENSE "2013-present"; the 2020 v2 milestone and version currency are web (row 53).
- Editorial framings — "GORM's power and its weaknesses share one root", "the zero-value
  trap", "low-ceremony end of that rung", the Strengths/Weaknesses lists, and the
  Key-design-decisions table — are author synthesis (◯), each consistent with the verified
  mechanics in the claims table but not independently citable.
- The comparison anchors ("contrast `sqlc`/`ent`/`Diesel`", "counterpart to `ActiveRecord`
  / Django ORM", "vs. `Hibernate`/EF Core for unit of work") are cross-survey positioning,
  not GORM-repo facts.
- Row 22, 26, 28, 54, 55 are explicitly web-attested in the page (upstream docs / GitHub /
  sibling modules) and marked ◯ here.

## Net

55 claims: **46 ✓ local-primary**, **9 ◯ web/editorial** (release lineage, adoption,
out-of-tree gen/plugins, two doc-page quotes, the identifier-injection risk framing),
**0 ⚠ discrepancies**. Every ✓ row cites a file:line in `$REPOS/go/gorm @ 1d6ce99` with the
verbatim string or code seen there. The five block quotes on the page (package docstring,
README tagline + feature list, logger injection warning, `AddError` `%w` chain, the
default-transaction config comment) are verbatim; the `Expr.Build`/`AddVar` bind-parameter
seam, the callback pipeline, the additive-only `AutoMigrate`, and the no-identity-map /
no-unit-of-work findings are all grounded in code, not docs.

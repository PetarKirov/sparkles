# Workspace member: a small library, depended on locally by `albatross`.
[project]
name = "bird-feeder"
version = "0.1.0"
description = "A workspace-member library consumed editable by `albatross`."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.19,<0.12"]
build-backend = "uv_build"

# Grounding ledger — rustfmt.md

Verification against `$REPOS/rust/rustfmt` @ `320de2e6d44f3190ea7cc73772e67a2ae86f5e71`.

## Verified verbatim (✓)

| Claim                                                                                                                                                                                                                                                                                                                                                                                                                              | Source                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| "First, do no harm … If we can't make it better, we should leave it as is."                                                                                                                                                                                                                                                                                                                                                        | `Design.md`                                      |
| The AST-over-tokens argument ("allows us to do more sophisticated manipulations …")                                                                                                                                                                                                                                                                                                                                                | `Design.md`                                      |
| "Many formatting tools use a very general algorithmic or even algebraic tool … does not give the best results. I prefer a more ad hoc approach…"                                                                                                                                                                                                                                                                                   | `Design.md`, "Heuristic rather than algorithmic" |
| "to format a node, we calculate the width budget and then walk down the tree … At a leaf, we generate an actual string and then unwind"                                                                                                                                                                                                                                                                                            | `Design.md`                                      |
| `struct Shape { width, indent, offset }` with its field comments                                                                                                                                                                                                                                                                                                                                                                   | `src/shape.rs:146-153`                           |
| The `Shape` doc comment incl. "we sometimes use width for lines other than the last (i.e., we are conservative)"                                                                                                                                                                                                                                                                                                                   | `src/shape.rs:156-164`                           |
| `trait Rewrite { fn rewrite(&self, context, shape) -> Option<String>; … }`                                                                                                                                                                                                                                                                                                                                                         | `src/rewrite.rs:17-24`                           |
| "Rustfmt is designed to be very configurable … Each configuration option is either stable or unstable."                                                                                                                                                                                                                                                                                                                            | `Configurations.md`                              |
| File line counts (`src/` 26,471; `items.rs` 3,644; `expr.rs` 2,550; `comment.rs` 2,149; `imports.rs` 1,572; `macros.rs` 1,532; `types.rs` 1,364; `visitor.rs` 1,125; `chains.rs` 1,080; `lists.rs` 950; `overflow.rs` 823; `string.rs` 734; `missed_spans.rs` 383; `sort.rs` 377; `pairs.rs` 376; `reorder.rs` 354; `vertical.rs` 301; `rewrite.rs` 184; `skip.rs` 127; `range.rs` 78; `Design.md` 184; `Configurations.md` 3,345) | `wc -l`                                          |

## Not verbatim — synthesis, inference, or unchecked

| #   | Claim                                                                                                                 | Status | Note                                                                                                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | "Returning `None` means 'I cannot format this within this budget' … the caller … copies the original source verbatim" | ⚠      | The `Option` signature is verified and `missed_spans.rs` exists; **the fallback path was not traced**. `RewriteError`/`max_width_error` suggest a richer story than the doc tells |
| 2   | "`missed_spans.rs` guarantees that unformattable regions are copied byte-for-byte"                                    | ⚠      | Inferred from the module name and the `Option` contract; not read                                                                                                                 |
| 3   | "26,471 lines against prettier's ~13,000 for a comparable job"                                                        | ◯      | Both counts verified; "comparable job" is the survey's judgement, and the two tools target different languages                                                                    |
| 4   | "**Custom rules per construct cost custom code per construct**"                                                       | ◯      | The survey's inference from the line counts, offered as a counter-argument to `Design.md`                                                                                         |
| 5   | "because a sub-result is already a string, rustfmt cannot re-decide a nested layout"                                  | ◯      | Follows from `rewrite`'s return type; not stated by any source                                                                                                                    |
| 6   | "`reorder_imports` is on by default"                                                                                  | ⚠      | Stated in `Configurations.md`; the default was **not** re-verified in `src/config/`                                                                                               |
| 7   | `#[rustfmt::skip]` is "syntactically checked"                                                                         | ◯      | True of Rust attributes generally; `skip.rs` was not read                                                                                                                         |
| 8   | "`--error-on-unformatted` turns silent fallback into a failure"                                                       | ⚠      | Option name from `Configurations.md`; behaviour not verified                                                                                                                      |
| 9   | "line-range formatting is limited and unstable"; "no cursor preservation"                                             | ⚠      | `range.rs` is 78 lines, which supports "limited"; both are negative claims from file inspection                                                                                   |
| 10  | The two "What a D formatter should take" recommendations                                                              | ◯      | Editorial                                                                                                                                                                         |

## Not verified here

- `expr.rs`, `items.rs`, `comment.rs`, `chains.rs`, `overflow.rs`, `lists.rs` — **none read**;
  claims are structural only.
- The subtree copy at `$REPOS/rust/rust/src/tools/rustfmt` was noted but not diffed.

# Grounding ledger — `vim-emacs-syntax.md`

- `$REPOS/c/vim` `834b8d21` (2026-07-03; `v9.2.0782`) — `runtime/doc/{syntax,options,version5}.txt`, `src/{syntax.c,structs.h}`
- `$REPOS/c/emacs` `b22a3be` (2026-06-25; **depth-1 clone**, pin = HEAD SHA only) — `lisp/{font-lock,jit-lock}.el`, `lisp/emacs-lisp/syntax.el`, `doc/lispref/modes.texi`, `etc/NEWS.*`

Status key: ✓ / ≈ / ⚠ / ◯ / 🌐. Load-bearing quotes (rows 2, 5, 6, 10, 12) re-grep-verified
directly; remaining locators from the exploration pass at the same pins.

| #   | Claim                                                                                                                                                                                                                                    | Type        | Source (local + locator)                                                                                                                                  | Status |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1   | Vim item model: match / start-skip-end region patterns; `sp_syncing` seam in the pattern struct                                                                                                                                          | QUOTE≈      | `src/syntax.c:31-77` (:31-37 quoted), `:47,56`                                                                                                            | ✓      |
| 2   | Sync rationale: "Vim wants to be able to start redrawing in any position in the document. To make this possible it needs to know the syntax state at the position where redrawing starts."                                               | QUOTE       | `runtime/doc/syntax.txt:5286-5288` (verified)                                                                                                             | ✓      |
| 3   | Four sync strategies (fromstart / ccomment / minlines / backward pattern search), each quoted; minlines/maxlines bounds; ccomment string-`*/` accuracy hole                                                                              | QUOTE       | `syntax.txt:5292-5426` (fromstart :5329-5339; ccomment :5342-5366; minlines :5369-5375; pattern :5380-5386; bounds :5303-5326; cost rationale :5423-5426) | ✓      |
| 4   | Sync routine header (three methods) + resync amortization ("resyncs only one out of N lines, where N is minlines \* 1.5…")                                                                                                               | QUOTE≈      | `src/syntax.c:571-579,608-613`                                                                                                                            | ✓      |
| 5   | `'synmaxcol'` default **3000**: "…text after this column is not highlighted and following lines may not be highlighted correctly, because the syntax state is cleared… avoid very slow redrawing for an XML file that is one long line." | QUOTE       | `runtime/doc/options.txt:8963-8973` (verified :8963-8964)                                                                                                 | ✓      |
| 6   | `'redrawtime'` default **2000 ms**: "…When over the limit syntax highlighting is disabled until CTRL-L is used."                                                                                                                         | QUOTE       | `runtime/doc/options.txt:7182-7195`                                                                                                                       | ✓      |
| 7   | Per-line state cache: `b_sst_array[]` "for the start of one line… all displayed lines, and states for 1 out of about 20 other lines"; `sst_change_lnum` invalidation                                                                     | QUOTE       | `src/structs.h:1257-1259,3176-3188`                                                                                                                       | ✓      |
| 8   | Vim 5.0 feature entry ("Vim now has a very flexible way to highlighting just about any type of file.")                                                                                                                                   | QUOTE≈      | `runtime/doc/version5.txt:31,366-371`                                                                                                                     | ✓      |
| 9   | font-lock: decoration levels; three passes (syntactic-keyword / syntactic / keyword); syntactic pass rationale (strings/comments span lines); per-change hook install                                                                    | QUOTE       | `lisp/font-lock.el:48-85` (:56-59, :63-85)                                                                                                                | ✓      |
| 10  | The confession: "Yes, obviously just about everything should be done in a single syntactic pass, but the only syntactic parser available understands only strings and comments… a son-of-font-lock.el…"                                  | QUOTE       | `lisp/font-lock.el:93-97` (verified)                                                                                                                      | ✓      |
| 11  | `syntax-ppss` doc ("Parse-Partial-Sexp State at POS…"), incremental reuse heuristic, `syntax-ppss-flush-cache` from edit position                                                                                                        | QUOTE≈      | `lisp/emacs-lisp/syntax.el:25,42,544-554,606-637`                                                                                                         | ✓      |
| 12  | jit-lock: "Just-in-time fontification, triggered by C redisplay code."; demand/stealth/deferred-context tiers quoted; `jit-lock-function` on `fontification-functions`; chunked                                                          | QUOTE       | `lisp/jit-lock.el:26,191-211,368-377` (verified :26 context + :194-198)                                                                                   | ✓      |
| 13  | Defaults: chunk-size **1500** ("a little over the typical number of buffer characters which fit in a typical window"); stealth-time nil (16 s suggested); stealth-nice 0.5 s; context-time 0.5 s; defer-time nil; antiblink 2 s          | figure      | `lisp/jit-lock.el:37-135`                                                                                                                                 | ✓      |
| 14  | Multiline hole: `font-lock-multiline` defvar; manual: "…Font Lock usually scans just part of the buffer, and it can miss a multi-line construct that crosses the line boundary…"; contextual-pass repair guidance                        | QUOTE       | `lisp/font-lock.el:670-674`; `doc/lispref/modes.texi:4165-4217`                                                                                           | ✓      |
| 15  | `treesit.el` exists; `treesit-font-lock-rules` ("captured nodes are highlighted with the capture name as its face") — the son-of-font-lock                                                                                               | QUOTE≈      | `lisp/treesit.el:1637-1666`                                                                                                                               | ✓      |
| 16  | Dates: Vim 5.0 = 19 Feb 1998; font-lock 1992 (Zawinski, Lucid; © header); jit-lock default Emacs 21.1 (Oct 2001; NEWS.21 "Font-lock uses jit-lock-mode as default support mode"); Lazy Lock precursor                                    | fact/QUOTE≈ | `etc/NEWS.21:356-363`; `font-lock.el` header; rest 🌐 (see [synthesis ledger](./syntax-highlighting.md) D18-D19)                                          | ✓/🌐   |
| 17  | Strengths / Weaknesses / trade-offs; the two-mechanisms framing                                                                                                                                                                          | synthesis   | derived                                                                                                                                                   | ◯      |

## Discrepancies

None found. Release _dates_ beyond the NEWS-file anchors are web-attested (D18-D19 in the
synthesis ledger); the NEWS/version5 files ground the version facts locally.

**Net:** 0 discrepancies.

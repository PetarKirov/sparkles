# Grounding ledger — `tree-sitter-highlight.md`

Claim-by-claim verification of `docs/research/parsing/tree-sitter-highlight.md` against **local**
pinned sources. `$REPOS = /home/petar/code/repos`.

- `$REPOS/rust/tree-sitter` `ee0847d6` (2026-07-11; workspace `0.27.0`) —
  `crates/highlight/{src/highlight.rs,src/c_lib.rs,README.md,Cargo.toml,include/tree_sitter/highlight.h}`,
  `crates/cli/src/highlight.rs`, `crates/loader/src/loader.rs`, `docs/src/3-syntax-highlighting.md`,
  workspace `Cargo.toml`
- **Note:** this is a different checkout from wave 1's `$REPOS/parsing/tree-sitter` (`cbee4672`,
  0.26.x) used by `tree-sitter.md`; both pins are recorded in [`_sources.md`](./_sources.md).

Status key: ✓ verified verbatim/exact · ≈ accurate paraphrase / abridged-but-faithful ·
⚠ discrepancy · ◯ opinion/interpretation · 🌐 web/secondary (not locally groundable).

| #   | Claim                                                                                                                                                                                                                                                                         | Type       | Source (local + locator)                                                                             | Status |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------- | ------ |
| 1   | "Tree-sitter has built-in support for syntax highlighting via the `tree-sitter-highlight` library, which is now used on GitHub.com for highlighting code written in several languages."                                                                                       | QUOTE      | `docs/src/3-syntax-highlighting.md:3-5`                                                              | ✓      |
| 2   | "Syntax highlighting is controlled by **three** different types of query files… The default names for the query files use the `.scm` file. We chose this extension because it is commonly used for files written in Scheme…"                                                  | QUOTE      | `docs/src/3-syntax-highlighting.md:79-81`                                                            | ✓      |
| 3   | "The highlights query uses **captures** to assign arbitrary **highlight names** to different nodes in the tree" (dot-separated, `function.builtin`)                                                                                                                           | QUOTE      | `docs/src/3-syntax-highlighting.md:85-88`                                                            | ✓      |
| 4   | Scope key: "A string like `"source.js"` that identifies the language. We strive to match the scope names used by popular TextMate grammars and by the Linguist library."                                                                                                      | QUOTE      | `docs/src/3-syntax-highlighting.md:34-35`                                                            | ✓      |
| 5   | Locals fixed captures `@local.scope` / `@local.definition` / `@local.reference` (+ `@ignore`)                                                                                                                                                                                 | fact       | `docs/src/3-syntax-highlighting.md:177-183`                                                          | ✓      |
| 6   | "When processing a syntax node that is captured as a `local.reference`, Tree-sitter will try to find a definition for a name that matches the node's text. If it finds a match, Tree-sitter will ensure that the **reference**, and the **definition** are colored the same." | QUOTE      | `docs/src/3-syntax-highlighting.md:192-195`                                                          | ✓      |
| 7   | `(#is-not? local)` disables a highlights pattern for locals                                                                                                                                                                                                                   | fact       | `docs/src/3-syntax-highlighting.md:197-199`; `highlight.rs:406-415` (`non_local_variable_patterns`)  | ✓      |
| 8   | Injection captures `@injection.content` / `@injection.language`; properties `injection.language` / `injection.combined` ("**all** the matching nodes … parsed as **one** nested document") / `injection.include-children` / `injection.self` / `injection.parent`             | QUOTE      | `docs/src/3-syntax-highlighting.md:328-346`                                                          | ✓      |
| 9   | MIT license; crate `tree-sitter-highlight`, "Library for performing syntax highlighting with Tree-sitter"; workspace version `0.27.0`                                                                                                                                         | fact       | `crates/highlight/Cargo.toml:2-12` (`version.workspace = true`), workspace `Cargo.toml:25`           | ✓      |
| 10  | `HighlightEvent { Source{start,end} \| HighlightStart(Highlight) \| HighlightEnd }`; `Highlight(pub usize)`                                                                                                                                                                   | QUOTE-code | `crates/highlight/src/highlight.rs:91,104-110`                                                       | ✓      |
| 11  | "This struct is immutable and can be shared between threads." (`HighlightConfiguration`)                                                                                                                                                                                      | QUOTE      | `highlight.rs:114-117`                                                                               | ✓      |
| 12  | "For the best performance `Highlighter` values should be reused between syntax highlighting calls. A separate highlighter is needed for each thread that is performing highlighting."                                                                                         | QUOTE      | `highlight.rs:135-139`                                                                               | ✓      |
| 13  | `Highlighter { parser: Parser, cursors: Vec<QueryCursor> }`                                                                                                                                                                                                                   | fact       | `highlight.rs:140-143`                                                                               | ✓      |
| 14  | `highlight()` signature (config, source, encoding, cancellation_flag, injection_callback) → lazy `Result<impl Iterator<Item = Result<HighlightEvent, Error>>>`                                                                                                                | QUOTE-code | `highlight.rs:293-301`                                                                               | ✓      |
| 15  | "Construct a single query by concatenating the three query strings, but record the range of pattern indices that belong to each individual string."                                                                                                                           | QUOTE      | `highlight.rs:370-372`                                                                               | ✓      |
| 16  | Concatenation order injections → locals → highlights; combined-injection patterns split into `combined_injections_query` and disabled in the main query                                                                                                                       | fact       | `highlight.rs:353-405`                                                                               | ✓      |
| 17  | Six special captures cached (`injection.content`, `injection.language`, `local.scope`, `local.definition`, `local.definition-value`, `local.reference`)                                                                                                                       | fact       | `highlight.rs:423-436`                                                                               | ✓      |
| 18  | configure() doc: "…dot-separated highlight names like `punctuation.bracket` and `function.method.builtin`… the string `function.builtin` will match against `function.method.builtin` and `function.builtin.constructor`, but will not match `function.method`."              | QUOTE      | `highlight.rs:462-471`                                                                               | ✓      |
| 19  | Longest-match implementation (`best_match_len` over dot-split parts)                                                                                                                                                                                                          | fact       | `highlight.rs:472-499`                                                                               | ✓      |
| 20  | `STANDARD_CAPTURE_NAMES` set backing `--check`                                                                                                                                                                                                                                | fact       | `highlight.rs:30-87`; CLI `--check` in `docs/src/cli/highlight.md`                                   | ≈      |
| 21  | Injection callback: "The last parameter to `highlight` is a _language injection_ callback. This allows other languages to be retrieved when Tree-sitter detects an embedded document (for example, a piece of JavaScript code inside a `script` tag within HTML)."            | QUOTE      | `crates/highlight/README.md:110-112`                                                                 | ✓      |
| 22  | "By default, injections do not include the **children** of an `injection.content` node - only the ranges that belong to the node itself. This can be changed using a `#set!` predicate that sets the `injection.include-children` key."                                       | QUOTE      | `highlight.rs:1363-1367` (comment in `injection_for_match`)                                          | ✓      |
| 23  | Layers: `HighlightIterLayer` (tree, cursor, `highlight_end_stack`, `scope_stack`, ranges, depth); `intersect_ranges` → `set_included_ranges`; sort ends-before-starts, deeper-first; shallow duplicate skipped via `last_highlight_range`                                     | fact       | `highlight.rs:182-191,714-797,802-821,848-869,1102-1109`                                             | ≈      |
| 24  | `CANCELLATION_CHECK_INTERVAL = 100`; checked in the event loop ("Periodically check for cancellation…") and as a parse `progress_callback`                                                                                                                                    | QUOTE-code | `highlight.rs:26,903-913,549-560`                                                                    | ✓      |
| 25  | Rust `Error = {Cancelled, InvalidLanguage, Unknown}`; C `TSHighlightError` incl. `UnknownScope`/`Timeout`/`InvalidUtf8`/`InvalidRegex`/`InvalidQuery`; c_lib maps `Cancelled\|Unknown` → `Timeout`                                                                            | fact       | `highlight.rs:95-102`; `include/tree_sitter/highlight.h:8-18`; `c_lib.rs:418-423`                    | ✓      |
| 26  | C surface renders HTML only (`TSHighlightBuffer`, `ts_highlight_buffer_content`/`_line_offsets`); C `TSHighlighter` resolves injections by per-language `injection_regex`                                                                                                     | fact       | `include/tree_sitter/highlight.h`; `c_lib.rs:396-405`                                                | ≈      |
| 27  | `HtmlRenderer { html, line_offsets, … }`; "At line boundaries, close and re-open all of the open tags." → per-line-valid HTML                                                                                                                                                 | QUOTE-code | `highlight.rs:143-149,1296-1307`                                                                     | ✓      |
| 28  | CLI ANSI fold: `style_stack` push/pop on events, `write!("{style}…{style:#}")` around source bytes                                                                                                                                                                            | QUOTE-code | `crates/cli/src/highlight.rs:452-470`                                                                | ✓      |
| 29  | Truecolor detection via `$COLORTERM` (`terminal_supports_truecolor`); RGB → `ansi256_from_rgb` downsample                                                                                                                                                                     | fact       | `crates/cli/src/highlight.rs:219-224,306-308`                                                        | ✓      |
| 30  | CLI theme: JSON in `~/.config/tree-sitter/config.json`; each `Style` carries `anstyle` ANSI + optional CSS                                                                                                                                                                    | fact       | `crates/cli/src/highlight.rs:56-188`                                                                 | ≈      |
| 31  | Loader detection keys `file-types` / `first-line-regex` / `content-regex` / `injection-regex`; `highlight_config_for_injection_string` matches injection strings against `injection_regex`                                                                                    | fact       | `docs/src/3-syntax-highlighting.md:47-62`; `crates/loader/src/loader.rs:369,641,1025-1026,1749-1760` | ✓      |
| 32  | Whole-buffer parse per layer before events (`HighlightIterLayer::new` parses at construction); no line-range parse API                                                                                                                                                        | fact       | `highlight.rs:607-617` (parse in layer construction); absence of any range API = ◯-adjacent reading  | ≈      |
| 33  | Buffer reserves `BUFFER_HTML_RESERVE_CAPACITY = 10 * 1024`, `BUFFER_LINES_RESERVE_CAPACITY = 1000`                                                                                                                                                                            | QUOTE-code | `highlight.rs:27-28`                                                                                 | ✓      |
| 34  | crate first published Feb 2019; `tree-sitter highlight` CLI alias `hi`, flags (`--html`, `--css-classes`, `--check`, …)                                                                                                                                                       | fact       | crates.io versions API = 🌐; `docs/src/cli/highlight.md`                                             | 🌐/≈   |
| 35  | Strengths / Weaknesses / trade-offs; "reference precise mode" positioning; editor query-dialect drift                                                                                                                                                                         | synthesis  | derived from verified rows; dialect drift = ecosystem context                                        | ◯      |

## Discrepancies

None. The page deliberately cites the crate README's quote about the injection callback from
the pinned tree (the README's usage example still pins `tree-sitter-highlight = "0.25.4"` —
a stale README constant noted here for completeness; the page cites the workspace version
`0.27.0` from `Cargo.toml`, not the README example).

## Web-fallback / not-locally-groundable

- **Crate first-publish date (v0.1.0, 2019-02-20)** — crates.io versions API (used in `index.md` milestones; see the [syntax-highlighting ledger](./syntax-highlighting.md)).
- **GitHub.com adoption timing** — only the in-tree docs sentence (row 1) is local; the "by early 2020" bound is a Wayback snapshot (2020-02-23) recorded in the [syntax-highlighting ledger](./syntax-highlighting.md). No GitHub-authored announcement exists.
- **Editor consumers** (Neovim, Helix, Zed re-implementations) — ecosystem context.

## Opinion (◯) — legitimate survey voice

- "The convention is the durable artifact" framing; the precision-boundary placement between
  TextMate and semantic highlighting; Strengths/Weaknesses/trade-offs (row 35).

**Net:** 0 discrepancies. Every docs-chapter and source-comment quote verified verbatim at the
pinned SHA, including the four load-bearing ones (GitHub.com sentence, configure() specificity
rule, injection-callback paragraph, include-children comment); constants (`100`, `10 * 1024`)
exact. Only release/adoption dates and editor-ecosystem context are web-attested.

# Grounding ledger — `bat.md`

Claim-by-claim verification of `docs/research/parsing/bat.md` against **local** pinned sources.
`$REPOS = /home/petar/code/repos`.

- `$REPOS/rust/bat` `78951393` (2026-07-01; 363 commits past `v0.26.1`, `Cargo.toml` still `0.26.1`) —
  `README.md`, `doc/assets.md`, `Cargo.toml`, `src/{printer,controller,terminal,style,syntax_mapping}.rs`,
  `src/assets.rs`, `src/assets/{serialized_syntax_set,lazy_theme_set,build_assets}.rs`

Status key: ✓ verified verbatim/exact · ≈ accurate paraphrase / abridged-but-faithful ·
⚠ discrepancy · ◯ opinion/interpretation · 🌐 web/secondary (not locally groundable).

| #   | Claim                                                                                                                                                                                                                                                                                  | Type       | Source (local + locator)                                                                                      | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------- | ------ |
| 1   | "A cat(1) clone with wings."; MIT OR Apache-2.0 dual license; version `0.26.1`                                                                                                                                                                                                         | fact       | `Cargo.toml:4,6,9`; `LICENSE-MIT`/`LICENSE-APACHE` present                                                    | ✓      |
| 2   | Pin `78951393` is 363 commits past `v0.26.1`                                                                                                                                                                                                                                           | fact       | `git describe --tags` → `v0.26.1-363-g…`                                                                      | ✓      |
| 3   | "`bat` uses the excellent `syntect` library for syntax highlighting. `syntect` can read any Sublime Text `.sublime-syntax` file and theme."                                                                                                                                            | QUOTE      | `README.md:555-558`                                                                                           | ✓      |
| 4   | syntect dep: `version = "5.3.0"`, `default-features = false`, `features = ["parsing"]`                                                                                                                                                                                                 | QUOTE-code | `Cargo.toml:84-87`                                                                                            | ✓      |
| 5   | `regex-onig` in default `minimal-application` set; `regex-fancy` alternative ("Use the rust-only …")                                                                                                                                                                                   | fact       | `Cargo.toml:23-41`                                                                                            | ✓      |
| 6   | 16 KiB guard code block (`too_long = line.len() > 1024 * 16`; feed `"\n"`; stitch back `highlighted_line[0].1 = line`)                                                                                                                                                                 | QUOTE-code | `src/printer.rs:454-467`                                                                                      | ✓      |
| 7   | Out-of-range lines still highlighted: controller comment "Call the printer in case we need to call the syntax highlighter for this line. However, set `out_of_range` to `true`."                                                                                                       | QUOTE-code | `src/controller.rs:312-315`                                                                                   | ✓      |
| 8   | `print_line` computes `regions` before honoring `out_of_range` (code block)                                                                                                                                                                                                            | QUOTE-code | `src/printer.rs:701-704`                                                                                      | ✓      |
| 9   | Look-ahead `VecDeque` buffer sized `largest_offset_from_end() + 1`; short-circuit after last range                                                                                                                                                                                     | fact       | `src/controller.rs:262` (sizing), `:335-337` (`AfterLastRange`)                                               | ≈      |
| 10  | `needs_to_match_syntax` gate (no highlighter when binary-as-binary / uncolored+unsanitized)                                                                                                                                                                                            | QUOTE-code | `src/printer.rs:276-282`                                                                                      | ✓      |
| 11  | `HighlighterFromSet` = `HighlightLines` + its `SyntaxSet`, one per input                                                                                                                                                                                                               | fact       | `src/printer.rs:187-198`                                                                                      | ✓      |
| 12  | Lazy assets: `HighlightingAssets { syntax_set_cell: OnceCell, serialized_syntax_set, theme_set: LazyThemeSet }`; `get_syntax_set` → `get_or_try_init`                                                                                                                                  | fact       | `src/assets.rs:32-39,92-95`                                                                                   | ✓      |
| 13  | "A SyntaxSet in serialized form, i.e. bincoded and flate2 compressed. We keep it in this format since we want to load it lazily." + `FromFile`/`FromBinary`                                                                                                                            | QUOTE      | `src/assets/serialized_syntax_set.rs:7-16`                                                                    | ✓      |
| 14  | "Same structure as a `syntect::highlighting::ThemeSet` but with themes stored in raw serialized form, and deserialized on demand." (`LazyTheme` + `OnceCell`)                                                                                                                          | QUOTE      | `src/assets/lazy_theme_set.rs:12-20`                                                                          | ✓      |
| 15  | Compression constants + comments: `COMPRESS_SYNTAXES = false` ("Lazy-loaded syntaxes are already compressed…"), `COMPRESS_THEMES = false`, `COMPRESS_LAZY_THEMES = true` ("Compress for size of ~40 kB instead of ~200 kB without much difference in performance due to lazy-loading") | QUOTE-code | `src/assets.rs:47-58`                                                                                         | ✓      |
| 16  | Embedded via `include_bytes!("../assets/syntaxes.bin")` etc.                                                                                                                                                                                                                           | fact       | `src/assets.rs:377-390`                                                                                       | ≈      |
| 17  | Detection-order doc (mappings MapTo/MapToUnknown → file name → MapExtensionToUnknown → extension), with `/etc/profile`→bash and `Dockerfile` examples; `UndetectedSyntax` → first-line fallback                                                                                        | QUOTE      | `src/assets.rs:129-150`                                                                                       | ✓      |
| 18  | First-line fallback strips UTF-8 BOM then `find_syntax_by_first_line`                                                                                                                                                                                                                  | QUOTE-code | `src/assets.rs:361-374` (`get_first_line_syntax`)                                                             | ✓      |
| 19  | `MappingTarget::{MapTo, MapToUnknown, MapExtensionToUnknown}`; "Rules in front have precedence"; background glob-build thread with `AtomicBool` cancel                                                                                                                                 | fact       | `src/syntax_mapping.rs:38-61,83-90`                                                                           | ≈      |
| 20  | Palette-color comment: "Themes can specify one of the user-configurable terminal colors by encoding them as #RRGGBBAA with AA set to 00 (transparent) and RR set to the 8-bit color palette number. The built-in themes ansi, base16, and base16-256 use this."                        | QUOTE      | `src/terminal.rs:8-11`                                                                                        | ✓      |
| 21  | `a == 0x00`: `r` 0x00–0x07 → named ANSI (SGR 30–37), higher → `Fixed(n)`; `a == 0x01` → default color (no escape); else truecolor `Rgb` or `ansi256_from_rgb` downsample                                                                                                               | fact       | `src/terminal.rs:6-47`                                                                                        | ✓      |
| 22  | `ITALIC` gated by italics flag (`--italic-text`); `BOLD`/`UNDERLINE` always                                                                                                                                                                                                            | fact       | `src/terminal.rs:49-82` (`as_terminal_escaped`)                                                               | ≈      |
| 23  | `EMPTY_SYNTECT_STYLE` grey fallback when no highlighter                                                                                                                                                                                                                                | fact       | `src/printer.rs:451` (return path), `:67-81` (constant)                                                       | ≈      |
| 24  | Asset rebuild: `assets/create.sh` "calls `bat cache --build` to parse all available `.sublime-syntax` files and serialize them to a `syntaxes.bin` file"                                                                                                                               | QUOTE      | `doc/assets.md:23-27`                                                                                         | ✓      |
| 25  | "please do _not_ include the changed `syntaxes.bin` file. A new binary cache file will be created once before every new release of `bat`. This avoids bloating the repository size unnecessarily."                                                                                     | QUOTE      | `doc/assets.md:32-35`                                                                                         | ✓      |
| 26  | Regression-test rationale: "…issues we had in the past where either (1) syntax highlighting for some language is suddenly not working anymore or (2) `bat` suddenly crashes for some input (due to `regex` incompatibilities between `syntect` and Sublime Text)."                     | QUOTE      | `doc/assets.md:40-43`                                                                                         | ✓      |
| 27  | Inclusion criterion: >10 000 Package Control downloads                                                                                                                                                                                                                                 | fact       | `doc/assets.md:77`                                                                                            | ≈      |
| 28  | Cache stamped by `AssetsMetadata`, rejected on version mismatch                                                                                                                                                                                                                        | fact       | `src/assets/assets_metadata.rs:67-77`                                                                         | ≈      |
| 29  | Key numbers: `src/` ≈12 kLOC; `syntaxes.bin` ≈1 MB; `themes.bin` ≈58 KB; 226 `.sublime-syntax` / 32 `.tmTheme` sources; 93 submodules                                                                                                                                                  | figure     | `wc -l src/**` (11 975); `ls -l assets/*.bin`; `find assets -name '*.sublime-syntax' \| wc -l`; `.gitmodules` | ✓      |
| 30  | `StyleComponent` enum set; decorations gated by components; panel dropped when terminal too narrow                                                                                                                                                                                     | fact       | `src/style.rs:8-60`; `src/printer.rs:236-268`                                                                 | ≈      |
| 31  | README fzf recipe uses `--line-range=:500`                                                                                                                                                                                                                                             | fact       | `README.md:104-108`                                                                                           | ≈      |
| 32  | `.tmTheme`-only custom themes ("Newer `.sublime-color-scheme` files are currently not supported.")                                                                                                                                                                                     | QUOTE≈     | `README.md:596-597`                                                                                           | ✓      |
| 33  | v0.1.0 first release (April 2018)                                                                                                                                                                                                                                                      | fact       | `CHANGELOG.md:1406` (`# v0.1.0`); date = 🌐 (GitHub release 2018-04-22)                                       | 🌐     |
| 34  | Adoption ("one of the most-installed Rust CLI tools", distro packaging)                                                                                                                                                                                                                | fact       | 🌐 — ecosystem context                                                                                        | 🌐     |
| 35  | Strengths / Weaknesses / trade-off tables; "closest existing shape to sparkles:syntax"                                                                                                                                                                                                 | synthesis  | derived from verified rows                                                                                    | ◯      |

## Discrepancies

None in the page. One brief-level correction caught during authoring: the compression-constant
comments differ from the exploration brief's paraphrase — the page quotes the pinned tree's
actual wording (row 15), where `COMPRESS_SYNTAXES = false` is justified by "Lazy-loaded
syntaxes are already compressed", not by a generic performance claim.

## Web-fallback / not-locally-groundable

- **v0.1.0 release date (2018-04-22)** — GitHub releases API (`CHANGELOG.md` grounds the version, not the date).
- **Adoption/packaging claims** (row 34) — ecosystem context, flagged in prose as product history.

## Opinion (◯) — legitimate survey voice

- "The absence is the finding" framing of the delegated engine; "industrialization of syntect";
  the full-feed-tax naming; Strengths/Weaknesses/trade-offs (row 35).

**Net:** 0 discrepancies. All doc/README/`doc/assets.md` quotes and both key code blocks
(16 KiB guard, out-of-range feed) verified verbatim at the pinned SHA; only the v0.1.0
date and adoption notes are web-attested.

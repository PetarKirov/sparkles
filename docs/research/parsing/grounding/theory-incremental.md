# Grounding ledger — `theory/incremental.md`

Claim-by-claim verification of `docs/research/parsing/theory/incremental.md` against **local**
primary sources, all read during the 2026-07-03 wave-2 pass. `$REPOS = /home/petar/code/repos`.

Primary artifacts (pinned — see [`_sources.md`](./_sources.md)):

- `$REPOS/papers/parsing/wagner-1997-incremental-parsing-thesis-berkeley.pdf` (already present, wave 1)
- `$REPOS/papers/parsing/mokhov-2018-build-systems-a-la-carte-icfp.pdf` (added 2026-07-03)
- `$REPOS/papers/parsing/hammer-2014-adapton-pldi.pdf` (added 2026-07-03)
- `$REPOS/dotnet/roslyn` `e42c3902` — `docs/compilers/Design/Red-Green Trees.md`
- `$REPOS/rust/salsa` `9447e2f` — `README.md`, `src/durability.rs`, `src/cancelled.rs`
- `$REPOS/rust/rustc-dev-guide` `646dd8e` — `src/query.md`
- `$REPOS/js/lezer-lr` `ed59b8b` — `README.md`
- `$REPOS/parsing/tree-sitter` `cbee4672` — `docs/src/index.md`, `lib/src/subtree.h`

Status key: ✓ verified verbatim/exact · ≈ accurate paraphrase / abridged-but-faithful ·
⚠ discrepancy · ◯ opinion/interpretation · 🌐 web/secondary (not locally groundable).

| #   | Claim                                                                                                                                                                                                                                                                                                                      | Type      | Source (local + locator)                                                                                    | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------- | ------ |
| 1   | Editor parser must be "Fast enough to parse on every keystroke" + "Robust enough to provide useful results even in the presence of syntax errors"                                                                                                                                                                          | QUOTE     | tree-sitter `docs/src/index.md` (four-goals list)                                                           | ✓      |
| 2   | Classic LR/LL generators (Bison, ANTLR) recover but a keystroke re-runs the whole parser                                                                                                                                                                                                                                   | fact      | design framing; cross-ref bottom-up/top-down pages                                                          | ≈      |
| 3   | Two orthogonal "incrementals": reuse the tree (subtree/node) vs. reuse the computation (memoized queries)                                                                                                                                                                                                                  | interp    | synthesis of Wagner (tree) + salsa/rustc (computation)                                                      | ◯      |
| 4   | Wagner: "The input to the parser consists of both terminal and nonterminal symbols; the latter are a natural representation of the unmodified subtrees from the reference version of the parse tree."                                                                                                                      | QUOTE     | `wagner-1997…pdf` §6.3 (`scratchpad/wagner.txt` para "6.3 Incremental Parsing of Sentential Forms")         | ✓      |
| 5   | Old unchanged subtree fed to LR automaton as one nonterminal; disagreement triggers `right_breakdown`                                                                                                                                                                                                                      | fact      | `wagner-1997…pdf` §6.3.1 "Subtree Reuse" + §6.4 (names `right_breakdown`)                                   | ≈      |
| 6   | Wagner: "We provide the first non-operational definition of optimal node reuse in the context of incremental parsing, and present optimal algorithms for retaining tokens and nodes during incremental lexing and parsing."                                                                                                | QUOTE     | `wagner-1997…pdf` Preface (abstract)                                                                        | ✓      |
| 7   | Optimal algorithm runs in **`O(t + s·lg N)`** (t terminals, s reused subtrees, N nodes)                                                                                                                                                                                                                                    | figure    | `wagner-1997…pdf` §6.4 "an optimal algorithm for incremental parsing, with a running time of O(t + s lg N)" | ✓      |
| 8   | Incremental lexing (Ch. 5): re-lex a lookback/lookahead region, retain outside tokens                                                                                                                                                                                                                                      | fact      | `wagner-1997…pdf` Ch. 5 (5.4 Marking / 5.5 Lexing / 5.6 Lookback Update phases in TOC)                      | ≈      |
| 9   | tree-sitter is this lineage adapted to GLR; `Subtree` ref-counted so unchanged nodes survive an edit                                                                                                                                                                                                                       | fact      | tree-sitter `lib/src/subtree.h` (`ref_count`); wave-1 ledger row 23                                         | ✓      |
| 10  | Roslyn green nodes: "internal, immutable representation … store only their syntactic kind, their width (character count), and their children. They do not store absolute text positions or parent pointers." red = wrappers adding positional/parental context                                                             | QUOTE     | roslyn `Red-Green Trees.md` §"The Red/Green Pattern"                                                        | ✓      |
| 11  | Green structure is "a directed acyclic graph (DAG), not a tree" — position/parent-free nodes shared                                                                                                                                                                                                                        | QUOTE     | roslyn `Red-Green Trees.md` §"Intra-Tree Sharing"                                                           | ✓      |
| 12  | Roslyn: incremental re-parses "complete in microseconds with memory reuse approaching 99.99% for typical edits."                                                                                                                                                                                                           | QUOTE     | roslyn `Red-Green Trees.md` §"Incremental Parsing and Subtree Reuse"                                        | ✓      |
| 13  | `rowan` is a direct port of red/green (the CST under rust-analyzer)                                                                                                                                                                                                                                                        | fact      | rowan `README.md` (describes red-green/Roslyn lineage)                                                      | ≈      |
| 14  | Lezer trees kept minimal as "blobs with a start, end, tag, and set of child nodes"                                                                                                                                                                                                                                         | QUOTE     | lezer-lr `README.md`                                                                                        | ✓      |
| 15  | rustc: "Instead of entirely independent passes (parsing, type-checking, etc.), a set of function-like queries compute information about the input source."                                                                                                                                                                 | QUOTE     | rustc-dev-guide `src/query.md`                                                                              | ✓      |
| 16  | rustc: "Query execution is memoized. The first time you invoke a query, it will go do the computation, but the next time, the result is returned from a hashtable."                                                                                                                                                        | QUOTE     | rustc-dev-guide `src/query.md`                                                                              | ✓      |
| 17  | One top-level `compile` query demands inputs "further and further back until we wind up doing the actual parsing"                                                                                                                                                                                                          | QUOTE     | rustc-dev-guide `src/query.md`                                                                              | ✓      |
| 18  | salsa: "a generic framework for on-demand, incrementalized computation"; program defined "as a set of queries"; **inputs** vs pure memoized **functions**                                                                                                                                                                  | QUOTE     | salsa `README.md` (tagline + "Key idea")                                                                    | ✓      |
| 19  | salsa credits Adapton, glimmer, and rustc's query system as ancestors                                                                                                                                                                                                                                                      | fact      | salsa `README.md` §"Credits"                                                                                | ✓      |
| 20  | _Build Systems à la Carte_ factors the design space into two orthogonal choices — "the order in which tasks are built … and whether or not a task is (re-)built"                                                                                                                                                           | QUOTE     | `mokhov-2018…pdf` §1 Introduction (contributions list)                                                      | ✓      |
| 21  | Early cutoff = a task whose inputs changed but whose output is identical stops propagating                                                                                                                                                                                                                                 | fact      | `mokhov-2018…pdf` §1 ("support for early cutoff" as a key property)                                         | ✓      |
| 22  | Adapton (Hammer et al., PLDI 2014) is the self-adjusting-computation lineage naming demand-driven change-propagation                                                                                                                                                                                                       | fact      | `hammer-2014-adapton-pldi.pdf` title/abstract                                                               | ✓      |
| 23  | salsa durability: "We use durabilities to optimize the work of 'revalidating' a query after some input has changed. … if we know that the only changes were to inputs of low durability (the common case), and we know that the query only used inputs of medium durability or higher, then we can skip that enumeration." | QUOTE     | salsa `src/durability.rs` (doc comment)                                                                     | ✓      |
| 24  | Inputs default `LOW`, interned values `NEVER_CHANGE`; edited file = LOW, config/library = higher                                                                                                                                                                                                                           | fact      | salsa `src/durability.rs` (doc comment)                                                                     | ✓      |
| 25  | salsa cancellation `Cancelled::PendingWrite`: "The query was operating on revision R, but there is a pending write to move to revision R+1."                                                                                                                                                                               | QUOTE     | salsa `src/cancelled.rs` (enum variant doc)                                                                 | ✓      |
| 26  | Where-it-shows-up table (tree-reuse vs computation-reuse columns per system)                                                                                                                                                                                                                                               | synthesis | each system's own deep-dive + rows above                                                                    | ◯      |
| 27  | Strengths / Weaknesses / Key-design-decision tables                                                                                                                                                                                                                                                                        | synthesis | derived from verified rows                                                                                  | ◯      |

## Notes

- Rows 1, 9, 10–12, 14–25 are **verbatim** quotes confirmed against the local artifact this session.
- Row 7's `O(t + s·lg N)` is quoted exactly from Wagner §6.4 (the page renders `lg` as `·lg`).
- Adapton (row 22) is used bibliographically + as the concept salsa cites; the PDF is present and
  valid (399 KB) but the page makes no verbatim Adapton quote, so it is fact/attribution only.
- The two-column "where it shows up" table and the Strengths/Weaknesses/trade-off tables (rows 26–27)
  are legitimate survey synthesis; every cell traces to a mechanism verified in a system row above or
  in that system's own deep-dive ledger.

## Web-fallback / not-locally-groundable

- None. Every quote is grounded in a local paper or pinned repo. External reference links in the page
  (`[wagner]` Berkeley TR PDF, `[bs]` MSR PDF, `[roslyn-rg]`/`[salsa-*]`/`[rustc-query]` canonical URLs)
  point at the public copy of the same artifact grounded locally.

**Net:** 0 discrepancies. The theory page's load-bearing claims (Wagner's node-reuse definition +
`O(t + s·lg N)`; Roslyn red/green + 99.99% reuse + DAG; rustc demand-driven/memoized; salsa
on-demand/durability/cancellation; à-la-carte early cutoff) are all verbatim-grounded.

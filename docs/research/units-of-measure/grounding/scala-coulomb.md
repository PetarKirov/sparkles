# Grounding ledger — `scala-coulomb.md`

Claim-by-claim verification of `docs/research/units-of-measure/scala-coulomb.md`
against the **local** pinned primary source:

- `$REPOS/scala/coulomb` @ `681442a` (verified `git rev-parse HEAD` =
  `681442a38c6709afd9f417f1a05510c6d791c5c5`, HEAD commit dated 2026-06-22,
  "Update sbt, scripted-plugin to 1.12.13 in scala3 (#723)") — `core/src/main/`,
  `core/src/test/`, `units/src/main/`, `runtime/src/main/`, `docs/`, `README.md`,
  `build.sbt`, `LICENSE`
- Local reproductions of the `m + s` / valid / fractional-power programs are named
  on the page as a scratch scala-cli project (`scala-cli 1.10.1` / Scala `3.3.8` /
  JVM 21). No transcript is preserved in the pinned clone; each reproduced
  diagnostic is graded against the **error string in the source** (`report.error`
  text), which is what the mechanism grade requires.

`$REPOS = /home/petar/code/repos`. Verified on 2026-07-04. Type: `quote` ·
`fact` · `figure` · `behavior` · `exposition` · `opinion`. Status: `✓` verified
against the local artifact · `⚠` imprecise/paraphrased/line-drifted (explained) ·
`◯` not locally groundable (secondary/registry/web).

## Claims

| #   | Claim                                                                                                                                                                                                             | Type       | Source (local + locator)                                                                                                                                                                                | Evidence                                                                                                                                                                              | Status   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 1   | Metadata: language Scala 3 only, `crossScalaVersions := Seq("3.3.8")` (an LTS), JDK 17                                                                                                                            | fact       | `build.sbt:44` (`crossScalaVersions := Seq("3.3.8")`), `:37` (`githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17"))`)                                                                              | both verbatim; `:41-43` comment "be sure to use LTS version … currently that is 3.3.x"                                                                                                | ✓        |
| 2   | License Apache-2.0                                                                                                                                                                                                | fact       | `LICENSE` (Apache License 2.0), `build.sbt:28-30` (`licenses := List("Apache-2.0" -> …)`)                                                                                                               | confirmed                                                                                                                                                                             | ✓        |
| 3   | Repository `erikerlandson/coulomb`; key author Erik Erlandson (`erikerlandson`, maintainer)                                                                                                                       | fact       | `build.sbt:25` (`developers += tlGitHubDev("erikerlandson", "Erik Erlandson")`), `:26` (`organization := "com.manyangled"`)                                                                             | confirmed                                                                                                                                                                             | ✓        |
| 4   | Analyzed `681442a` (pinned clone, 2026-06-22); `tlBaseVersion := "0.9"`; **post-`v0.9.1`**                                                                                                                        | fact       | `git rev-parse --short HEAD` = `681442a`; HEAD date 2026-06-22; `build.sbt:7` (`tlBaseVersion := "0.9"`)                                                                                                | SHA, date, `tlBaseVersion` verbatim. "post-`v0.9.1`" is inferential — the clone has **zero git tags** and no in-tree `0.9.1` reference; consistent with HEAD post-dating a release    | ⚠        |
| 5   | Latest release `v0.9.1` (2025-09-03; latest on Maven Central `com.manyangled:coulomb-core_3`)                                                                                                                     | fact       | registry/web (Maven Central / GitHub releases)                                                                                                                                                          | not locally groundable — no tag/date in the clone; the page's reproduction pins `com.manyangled::coulomb-core:0.9.1`, consistent with the release existing                            | ◯        |
| 6   | Mechanism: `opaque type Quantity[V, U] = V`; a macro canonicalizes `U` to a `List[(baseUnitType, Rational)]` signature; exponents `ℚ`                                                                             | exposition | `core/.../quantity.scala:64`, `core/.../infra/meta.scala:170` (`(Rational, List[(quotes.reflect.TypeRepr, Rational)])`)                                                                                 | opaque type + signature-list return type confirmed (rows 12, 20)                                                                                                                      | ✓        |
| 7   | QUOTE crate-root docs "Unit analysis performs a role very similar to a type system … adding `meters + seconds` is not a valid computation."                                                                       | quote      | `docs/coulomb-core.md:34-56` (quoted sentence begins `:43`; `:53-55` code block replaced by "…")                                                                                                        | verbatim; the two markdown links (`[type system]`, `[Scala's type system]`) stripped; ellipsis replaces the `val bad = 7 + false` fenced block. Quote proper is L43–56, not L34       | ✓ (note) |
| 8   | QUOTE README splash alt-text "coulomb: a statically typed unit analysis library for Scala"                                                                                                                        | quote      | `README.md:1` (`alt="coulomb: a statically typed unit analysis library for Scala"`)                                                                                                                     | verbatim                                                                                                                                                                              | ✓        |
| 9   | Predefined units ship seven SI base units (`Meter … Kelvin`, `si.scala` L28–53) + prefixes, US/imperial, info, temperature, time, constants, accepted                                                             | figure     | `units/.../si.scala:28-53` (Meter, Kilogram, Second, Ampere, Mole, Candela, Kelvin — 7 `BaseUnit`s); `ls units/.../units/` = accepted, constants, info, mks(a), si, temperature, time, us               | seven base units, exact order, at L28–53; all named categories exist as files                                                                                                         | ✓        |
| 10  | QUOTE `opaque type Quantity[V, U] = V` (`quantity.scala` L64)                                                                                                                                                     | quote      | `core/.../quantity.scala:64`                                                                                                                                                                            | char-for-char                                                                                                                                                                         | ✓        |
| 11  | `withUnit`/`value` are `inline` identity coercions (`inline def withUnit[U]: Quantity[V, U] = v` L91, L103)                                                                                                       | quote      | `core/.../quantity.scala:91` (`inline def withUnit[U]: Quantity[V, U] = v`), `:103` (`inline def value: V = q`)                                                                                         | both verbatim                                                                                                                                                                         | ✓        |
| 12  | Three unit-operator types `*`, `/`, `^` (`quantity.scala` L29, L41, L55); `^` takes a rational (`Meter ^ (1 / 2)`, `Second ^ -1`)                                                                                 | fact       | `core/.../quantity.scala:29` (`final type *[L, R]`), `:41` (`final type /[L, R]`), `:55` (`final type ^[B, E]`); doc examples `:49-52`                                                                  | operator types at the exact lines; `type H = (Second ^ -1)` / `type R = (Meter ^ (1 / 2))` in the `^` doc-comment (L50–52, within the cited L43–55)                                   | ✓        |
| 13  | Author calls it "the fundamental algorithmic unit analysis criterion" and links his write-up in a source comment (`meta.scala` L136–137)                                                                          | quote      | `core/.../infra/meta.scala:136` (comment verbatim), `:137` (`http://erikerlandson.github.io/blog/2019/05/03/algorithmic-unit-analysis/`)                                                                | verbatim comment + URL                                                                                                                                                                | ✓        |
| 14  | Marker classes `BaseUnit[U, Name, Abbv]` (L43), `DerivedUnit[U, D, Name, Abbv]` (L72), `DeltaUnit[U, D, O, Name, Abbv]` (L104–113)                                                                                | fact       | `core/.../define/define.scala:43`, `:72`, `:104-113` (`abstract class DeltaUnit[U, D, O, Name, Abbv] extends DerivedUnit`)                                                                              | all three at the cited lines                                                                                                                                                          | ✓        |
| 15  | `BaseUnit` doc's three-line `type Scoville` + `given BaseUnit[Scoville, "scoville", "sco"]` pattern (`define.scala` L36–42)                                                                                       | fact       | `core/.../define/define.scala:39-40` (`type Scoville` / `given unit_Scoville: BaseUnit[Scoville, "scoville", "sco"] = BaseUnit()`)                                                                      | pattern verbatim (page drops the `unit_Scoville:` binding name — a cosmetic simplification)                                                                                           | ✓ (note) |
| 16  | `Liter` = `DerivedUnit[Liter, (Meter ^ 3) / 1000, …]`; `Kilo` = `DerivedUnit[Kilo, 10 ^ 3, …]` (`si.scala` L75–77)                                                                                                | fact       | `units/.../accepted.scala:66-67` (`DerivedUnit[Liter, (Meter ^ 3) / 1000, "liter", "l"]`); `units/.../si.scala:75-77` (`DerivedUnit[Kilo, 10 ^ 3, "kilo", "k"]`)                                        | both verbatim                                                                                                                                                                         | ✓        |
| 17  | `cansig` (`meta.scala` L168–221) walks a unit `TypeRepr`, returns coefficient + `List[(TypeRepr, Rational)]` signature                                                                                            | fact       | `core/.../infra/meta.scala:168` (`def cansig`), `:170` return type, `:219-221` fallback case                                                                                                            | signature type and body span confirmed                                                                                                                                                | ✓        |
| 18  | QUOTE `SigMode.Canonical` "yields signatures fully expand[ed] down to base units" (`meta.scala` L52–57)                                                                                                           | quote      | `core/.../infra/meta.scala:53` ("Canonical mode yields signatures fully expand down to base units.")                                                                                                    | verbatim modulo bracketed grammar fix `expand` → `expand[ed]`                                                                                                                         | ✓        |
| 19  | `cansig` `*`/`/` code block (`meta.scala` L190–199): multiply→add / divide→subtract exponents via `unifyOp`                                                                                                       | quote      | `core/.../infra/meta.scala:190-199` (`*` and `/` `AppliedType` cases)                                                                                                                                   | code char-for-char; the two inline comments `// multiply units => add exponents` / `// divide units => subtract exponents` are **editorial glosses not in source** (labeled abridged) | ✓ (note) |
| 20  | QUOTE `convertible` (`meta.scala` L159–165): two units convertible iff `u1/u2` canonicalizes to `Nil`                                                                                                             | quote      | `core/.../infra/meta.scala:159-165` (`def convertible … rsig == Nil`)                                                                                                                                   | abridged (drops `using Quotes` / `import`); logic verbatim                                                                                                                            | ✓        |
| 21  | QUOTE `coef` (`meta.scala` L130–145): rejection raised via `report.error(s"unit type … not convertable to …")`                                                                                                    | quote      | `core/.../infra/meta.scala:130-145` (`def coef`), error string `:142-143`                                                                                                                               | abridged signature (drops `using Quotes`, `import`, the write-up URL line); the `report.error` string is verbatim incl. the "convertable" spelling                                    | ✓        |
| 22  | `Coefficient`/`UnitConversion` typeclass `inline given`s call the coefficient splices → `coef` (`coefficients.scala` L23–41; `coefficient.scala` L60–103; `unit.scala` L68–104)                                   | fact       | `core/.../conversion/coefficients.scala:23-41` (`coefficientRational`/`coefficientDouble`/… splices); `coefficient.scala:73,101`; `unit.scala:101`                                                      | splice macros + `inline given g_Coefficient` (`:73`) / `g_UnitConversion` (`:101`) confirmed within the cited ranges                                                                  | ✓        |
| 23  | `SimplifiedUnit[U]` is a `transparent inline given` macro canonicalizing in `SigMode.Simplify`, which "does not expand derived units, and respects type aliases" (`meta.scala` L59–63; `simplified.scala` L28–40) | quote      | `core/.../infra/simplified.scala:28` (`transparent inline given g_SimplifiedUnit`), `:38-39` (calls `meta.simplify`); `meta.scala:60-61` (quoted sentence), `:239` (`given sigmode = SigMode.Simplify`) | quote verbatim; `transparent inline given` + Simplify-mode wiring confirmed                                                                                                           | ✓        |
| 24  | Exponents `ℚ` (`spire.math.Rational`); rational power → `fpow` on coefficient + `unifyPow` scaling (`meta.scala` L200–213, L401–408)                                                                              | fact       | `core/.../infra/meta.scala:200-213` (`^` case, `(bcoef.fpow(e), unifyPow(e, bsig))`), `:401-408` (`def unifyPow` scales each exponent by `e`)                                                           | `^` handling + `unifyPow` at the exact lines; `Rational` from `spire.math` (import `quantity.scala:73`)                                                                               | ✓        |
| 25  | Open base-dimension set: a `BaseUnit[Scoville, …]` in three lines is a new axis (`define.scala` L36–42); no fixed exponent-vector length                                                                          | fact       | `core/.../define/define.scala:36-42` (Scoville example) — same as row 15                                                                                                                                | confirmed; signature is a sparse `List`, so no length to edit                                                                                                                         | ✓        |
| 26  | Normalization by list unification with `=:=` term-matching, dropping zero exponents (`meta.scala` L379–399)                                                                                                       | fact       | `core/.../infra/meta.scala:379-386` (`unifyOp`), `:388-399` (`insertTerm`: `if (u =:= u0)`, `if (ei == Rational.zero) tail`)                                                                            | `=:=` fold + zero-exponent drop confirmed                                                                                                                                             | ✓        |
| 27  | Dimensionless-ness = empty signature: `1`/pure numeric constants canonicalize to `(coefficient, Nil)` (`meta.scala` L179–186)                                                                                     | fact       | `core/.../infra/meta.scala:179-186` (`unitconst(c)` case → `(c, Nil)` in canonical mode)                                                                                                                | confirmed                                                                                                                                                                             | ✓        |
| 28  | QUOTE `+` operator (`quantity.scala` L192–196): `UnitConversion[V, UR, U](qr)` then `alg.plus`                                                                                                                    | quote      | `core/.../quantity.scala:192-196`                                                                                                                                                                       | code verbatim; the inline comment `// fails to resolve if not convertible` is an **editorial gloss not in source**                                                                    | ✓ (note) |
| 29  | Multiplication builds output unit via `SimplifiedUnit[U * UR]`'s `su.UO` member type (`quantity.scala` L236–240)                                                                                                  | fact       | `core/.../quantity.scala:237-240` (`alg: MultiplicativeSemigroup[V] … Quantity[V, su.UO] = alg.times(q, qr).withUnit[su.UO]`)                                                                           | confirmed                                                                                                                                                                             | ✓        |
| 30  | `pow[E]` dispatch: `Fractional[V]` "supports all rational exponents", `MultiplicativeGroup` all integers, `MultiplicativeMonoid` ≥0, `MultiplicativeSemigroup` >0 (`quantity.scala` L333–363)                     | quote      | `core/.../quantity.scala:334-337` (doc), `:339-363` (`inline def pow` `summonFrom` cases)                                                                                                               | quoted fragment verbatim (source: "supports all rational exponents for `E`"); four algebra cases confirmed                                                                            | ✓        |
| 31  | Extension: `Smoot` = `DerivedUnit[Smoot, 67 * Inch, …]` (`define.scala` L63–70); units compose transitively (`Bit` ⊂ `Byte / 8`, `info.scala` L42–52)                                                             | fact       | `core/.../define/define.scala:67-69` (Smoot); `units/.../info.scala:43` (`BaseUnit[Byte,…]`), `:52` (`DerivedUnit[Bit, Byte / 8, "bit", "b"]`)                                                          | both verbatim (page drops `unit_Smoot:` binding)                                                                                                                                      | ✓        |
| 32  | Value-type genericity: any `V` served by summoning `Fractional[V]` + `ValueConversion[Rational, V]` (`coefficient.scala` L60–103)                                                                                 | fact       | `core/.../conversion/coefficient.scala:101` (`summonAll[(Fractional[V], ValueConversion[Rational, V])]`)                                                                                                | confirmed within the cited given block (`:73-103`)                                                                                                                                    | ✓        |
| 33  | `coulomb-runtime`'s `RuntimeQuantity[V]` pairs a value with a `RuntimeUnit` AST (`UnitConst`/`UnitType`/`Mul`/`Div`/`Pow`, `runtime.scala` L26–89)                                                                | fact       | `runtime/.../runtime/runtime.scala:26` (`sealed abstract class RuntimeUnit`), `:76-80` (the five case classes), `:83` (`case class RuntimeQuantity[V](value, unit)`)                                    | all five AST nodes + `RuntimeQuantity` within L26–89                                                                                                                                  | ✓        |
| 34  | Fractional powers first-class: in-repo test asserts value **and** `Meter ^ (1 / 2)` output type (`quantity.scala` test L190–198)                                                                                  | behavior   | `core/src/test/.../quantity.scala:193-195` (`.pow[1 / 2].assertQD[Double, Meter ^ (1 / 2)](1.4142135623730951)`)                                                                                        | assertion present at L195, within L190–198; value matches the page's `1.4142135623730951`                                                                                             | ✓        |
| 35  | Affine: `DeltaQuantity[V, U, B]` (`deltaquantity.scala` L30); `Delta − Delta` → `Quantity`, `Delta ± Quantity` → `Delta`, no `Delta + Delta` (L145–197)                                                           | behavior   | `core/.../deltaquantity.scala:30`; `:145-147` (`-[UR](DeltaQuantity)…: Quantity[V, U]`), `:172-174`/`:193-195` (`±[UR](Quantity)…: DeltaQuantity`); `grep '+\[UR\](qr: DeltaQuantity'` = no hit         | all confirmed incl. the absence of `Delta + Delta`                                                                                                                                    | ✓        |
| 36  | Offsets live in the type: `Fahrenheit` = `DeltaUnit[Fahrenheit, (5 / 9) * Kelvin, 45967 / 100, …]`, `Celsius` offset `27315 / 100` (`temperature.scala` L27–44); `offset` fn (`meta.scala` L147–157)              | fact       | `units/.../temperature.scala:29-36` (Celsius `27315 / 100`), `:38-45` (Fahrenheit `(5 / 9) * Kelvin`, `45967 / 100`); `meta.scala:147-157` (`def offset`)                                               | offsets verbatim; Fahrenheit body spills to L45 (closing `] = DeltaUnit()`), one line past the cited L27–44                                                                           | ✓ (note) |
| 37  | Logarithmic quantities absent — no decibel/neper/bel in the units package                                                                                                                                         | fact       | `grep -rin 'decibel\|neper\|bel' units/src/main/` — no hits                                                                                                                                             | negative finding confirmed                                                                                                                                                            | ✓        |
| 38  | Angles dimensionless, no kind tag: `Radian` = `DerivedUnit[Radian, 1, …]` (`mks.scala` L39–40); `Degree` = `DerivedUnit[Degree, 3.141592653589793 / 180, …]` (`accepted.scala` L47–49)                            | fact       | `units/.../mks.scala:39-40` (`DerivedUnit[Radian, 1, "radian", "rad"]`); `units/.../accepted.scala:47-49` (`DerivedUnit[Degree, 3.141592653589793 / 180, "degree", "°"]`)                               | both verbatim                                                                                                                                                                         | ✓        |
| 39  | No `Kind` mechanism — no `Kind` type anywhere in `core/src/main`                                                                                                                                                  | fact       | `grep -rn '\bKind\b' core/src/main/` — no hits                                                                                                                                                          | negative finding confirmed                                                                                                                                                            | ✓        |
| 40  | Zero-cost is structural: `opaque type` + `inline` everywhere, no `benches/`                                                                                                                                       | fact       | `core/.../quantity.scala:64`, `:91`,`:103`; `deltaquantity.scala:30`,`:54`,`:68`; `ls benches` = absent                                                                                                 | opaque types + inline extractors confirmed; no `benches/` directory                                                                                                                   | ✓        |
| 41  | QUOTE identity conversion short-circuit `if (typeexpr.uniteq[UF, UT]) v` (`conversion/unit.scala` L68–69)                                                                                                         | quote      | `core/.../conversion/unit.scala:68-69` (`inline def apply[V, UF, UT](v: V): V = / if (typeexpr.uniteq[UF, UT]) v`)                                                                                      | verbatim                                                                                                                                                                              | ✓        |
| 42  | Reproduced `m + s`: error "unit type Second not convertable to Meter" — the exact `report.error` string (`meta.scala` L142–143)                                                                                   | behavior   | `core/.../infra/meta.scala:143` (`s"unit type ${typestr(u1)} not convertable to ${typestr(u2)}"`)                                                                                                       | with `u1 = Second`, `u2 = Meter` the string renders exactly as quoted; the "convertable" quirk is in-source. Transcript not preserved; mechanism grounded in source                   | ✓        |
| 43  | QUOTE `typestr` "policy goal … is that type aliases are never expanded" (`meta.scala` L424–426)                                                                                                                   | quote      | `core/.../infra/meta.scala:425` ("The policy goal here is that type aliases are never expanded.")                                                                                                       | verbatim                                                                                                                                                                              | ✓        |
| 44  | In-repo compile-fail tests corroborate: `assertCE("1d.withUnit[Meter] + 1d.withUnit[Second]")` (L122–123), siblings `-`/conversion/comparison at L141, L86–87, L256                                               | fact       | `core/src/test/.../quantity.scala:122` (`+`), `:141` (`-`), `:86-87` (`toUnit` CE), `:256` (`=== ` mixed-type CE)                                                                                       | all present; the quoted comment "non convertible units should fail" is at **L121** (assertCE itself at L122, in the cited range)                                                      | ✓ (note) |
| 45  | Lossy integer conversion is a compile error: `assertCE("1.withUnit[Meter] - 1.withUnit[Yard]")` and its `Long` counterpart (`quantity.scala` L143–145)                                                            | behavior   | `core/src/test/.../quantity.scala:144` (Int), `:145` (Long), under `// unsafe truncating conversions should fail` (`:143`)                                                                              | both `assertCE`s present; `Int`/`Long` lack `Fractional`, so no coefficient can be summoned                                                                                           | ✓        |
| 46  | QUOTE `@implicitNotFound` messages "No coefficient could be derived …" (`coefficient.scala` L32–35), "No unit conversion in scope …" (`unit.scala` L31–34)                                                        | quote      | `core/.../conversion/coefficient.scala:32-33`; `core/.../conversion/unit.scala:31-32`                                                                                                                   | both verbatim ("No coefficient could be derived for value type …", "No unit conversion in scope for value type …")                                                                    | ✓        |
| 47  | Valid path: `show` renders `1.4142135623730951 m^(1/2)` via the `ShowUnit` type-to-text macro (`io/show.scala` L28–44)                                                                                            | behavior   | `core/.../io/show.scala:28` (`class ShowUnit`), `:41` (`inline def apply[U]: String = ${ … show.show[U] }`)                                                                                             | `ShowUnit`/`show` macro confirmed at the cited lines; the rendered value matches the fractional-power test (row 34). Transcript not preserved; renderer grounded in source            | ✓        |
| 48  | `EpochTime` reuses the affine `DeltaQuantity` machinery (`Second`-epoch)                                                                                                                                          | fact       | `units/.../time.scala:58` (`final type EpochTime[V, U] = DeltaQuantity[V, U, coulomb.units.si.Second]`)                                                                                                 | confirmed                                                                                                                                                                             | ✓        |
| 49  | Scala 3 only (`crossScalaVersions := Seq("3.3.8")`, `build.sbt` L44)                                                                                                                                              | fact       | `build.sbt:44`                                                                                                                                                                                          | same as row 1; no Scala 2 cross-build                                                                                                                                                 | ✓        |
| 50  | Dependency surface: core pulls `spire`, `algebra`, and `cats-kernel` (`build.sbt`)                                                                                                                                | fact       | `build.sbt:79-80` (`algebra` 2.10.0, `spire` 0.18.0); `core/.../quantity.scala:71` (`import cats.kernel.Order`)                                                                                         | `spire`/`algebra` are direct deps; `cats-kernel` is **transitive** (not named in `build.sbt`) but genuinely used (`cats.kernel.Order`) — cite is slightly loose                       | ⚠        |
| 51  | Concept-doc / javadoc / Maven Central links                                                                                                                                                                       | ◯          | registry/web                                                                                                                                                                                            | link-only, no factual claim attached                                                                                                                                                  | ◯        |

## Discrepancies

None material. No page fact is contradicted by the source. The `⚠`/`(note)` rows
are transcription/citation nuances, not wrong claims:

- **Row 4 / 5 (`⚠` / `◯`) — release provenance.** The pinned clone carries **no git
  tags** and no in-tree `0.9.1` string, so "post-`v0.9.1`", the `v0.9.1` date
  (2025-09-03), and "latest on Maven Central" are secondary (GitHub-releases /
  Maven-Central) facts, not locally derivable. Only `681442a`, the 2026-06-22 HEAD
  date, and `tlBaseVersion := "0.9"` are ground-truth. The page already hedges these
  as registry facts; no fix required.
- **Row 50 (`⚠`, minor) — `scala-coulomb.md:471-474`.** The dependency bullet cites
  "`build.sbt`" for `spire` + `algebra` + `cats-kernel`. `build.sbt` names only
  `algebra` (`:79`) and `spire` (`:80`); `cats-kernel` is transitive (though really
  used — `import cats.kernel.Order`, `quantity.scala:71`). Cosmetically loose; no
  meaning changed.

## Editorial-annotation notes (no page edit needed)

- **Rows 19 & 28** — the quoted `cansig` `*`/`/` block and the `+` operator block are
  char-for-char from source **except** for added inline comments
  (`// multiply units => add exponents`, `// divide units => subtract exponents`,
  `// fails to resolve if not convertible`) that do **not** appear in the source.
  Both blocks are labelled `(abridged)` / are clearly explanatory glosses.
- **Row 7** — the crate-root quote is cited as `coulomb-core.md` L34–56; the quoted
  sentence proper starts at L43 (L34–42 is the preceding "For example …" paragraph).
  Section-frame citation, quote itself verbatim (links stripped, code block → "…").
- **Rows 15 & 31** — `given` binding names (`unit_Scoville:`, `unit_Smoot:`) dropped
  in the page's inline renderings; the type applications are exact.
- **Row 36** — Fahrenheit's `DeltaUnit` body closes at L45, one line past the cited
  L27–44; all quoted numbers are within range.
- **Row 44** — the comment "non convertible units should fail" sits at L121; the
  `assertCE` it labels is at L122 (inside the cited L122–123).

## Net

51 rows; 47 ✓, 2 ⚠ (release-tag provenance not in clone; `cats-kernel` transitive
cite; both non-material), 2 ◯ (Maven-Central "latest" release + date, and the
concept-doc/javadoc/registry links — web-only).
Every blockquote and inline code excerpt from `quantity.scala`, `deltaquantity.scala`,
`infra/meta.scala`, `infra/simplified.scala`, `define/define.scala`, the
`conversion/*` typeclasses, `io/show.scala`, the `units/*` SI/accepted/temperature/
info/mks modules, `runtime/runtime.scala`, `docs/coulomb-core.md`, `README.md`, and
`build.sbt` matches the pinned clone verbatim (modulo the noted link-stripping,
ellipsis, dropped `given`-binding names, and three explanatory inline comments the
page adds to two labelled code blocks). The `m + s` and lossy-integer rejections,
and the `Meter ^ (1 / 2)` fractional-power result, are grounded to the exact
`report.error`/`assertCE`/test lines in the source; the only non-groundable items
are the release tag/date/Maven-Central provenance (◯/⚠, appropriately hedged on the
page).

### At-risk quotes

None. All blockquotes and inline code excerpts confirmed char-for-char against the
pinned clone. The only under-grounded claims are registry/release-provenance facts
(rows 4/5), which are not quotes.

# Grounding ledger — `kotlin-measured.md`

Claim-by-claim verification of `docs/research/units-of-measure/kotlin-measured.md`
against **local** primary sources (adversarial pass, 2026-07-04):

- `$REPOS/kotlin/measured` @ `3f65500` (verified `git rev-parse HEAD` =
  `3f65500945e5c09318ae895621825894ab95cad9`, `git rev-parse --short HEAD` =
  `3f65500`; HEAD commit dated 2026-04-05 "[Docs] Added logo and copyright";
  `git tag --points-at HEAD` = `v0.5.0`) — `src/commonMain/.../units/*.kt`,
  `src/commonTest/.../UnitTests.kt`, `README.md`, `LICENSE`, `build.gradle.kts`,
  `gradle/libs.versions.toml`
- Local reproduction: `kotlinc-jvm` (nixpkgs) compile of the pinned sources for the
  `m + s`, valid-path, and order-sensitivity experiments (transcripts not preserved;
  mechanisms re-confirmed in-source this pass)

`$REPOS = /home/petar/code/repos`. Claim **Types:** `quote` · `fact` · `figure` ·
`behavior` · `exposition` · `opinion`. **Status:** `✓` verified against the local
artifact · `⚠` imprecise / paraphrased-as-quote / locator-drifted (explained) · `◯`
not locally groundable (editorial/opinion or handle-history).

## Claims

| #   | Claim                                                                                                                                                                                                                   | Type       | Source (local + locator)                                                                                                         | Evidence                                                                                                                                                                                                                          | Status      |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 1   | Header: Language Kotlin declared toolchain `2.3.20`; KMP targets JVM (target 1.8), JS, Wasm-JS, iOS, macOS, watchOS, tvOS, Android-Native, `mingwX64`, `linux{X64,Arm64}`                                               | fact       | `gradle/libs.versions.toml:3` (`kotlinVersion = { strictly = "2.3.20" }`), `build.gradle.kts:34-110`                             | all targets confirmed: `jvm{ JvmTarget.JVM_1_8 }` (:36), `js{}` (:39), `wasmJs` (:56), ios X64/Arm64/SimArm64, watchos Arm64/SimArm64, macosArm64, tvos Arm64/SimArm64, androidNativeX64/Arm32, mingwX64, linuxX64, linuxArm64    | ✓           |
| 2   | License MIT                                                                                                                                                                                                             | fact       | `LICENSE:1` ("Copyright (c) 2017-2020 Nicholas Eddy" + MIT permission text)                                                      | MIT text confirmed                                                                                                                                                                                                                | ✓           |
| 3   | Key authors: Nicholas Eddy (`nacular`; formerly `pusolito`)                                                                                                                                                             | fact       | `LICENSE:1` (Nicholas Eddy); git author `neddy <nickeddy@gmail.com>`; repo path `nacular/measured`                               | "Nicholas Eddy" + `nacular` confirmed; **"formerly pusolito" handle history not groundable in the clone** (no `pusolito` in git log)                                                                                              | ⚠           |
| 4   | Analyzed `3f65500` (pinned, 2026-04-05, tag `v0.5.0`); Latest release `v0.5.0` (2026-04-05)                                                                                                                             | fact       | `git rev-parse --short HEAD` = `3f65500`; `git tag --points-at HEAD` = `v0.5.0`; HEAD date 2026-04-05                            | all confirmed                                                                                                                                                                                                                     | ✓           |
| 5   | Mechanism: dimension **is** `T` of `Measure<T: Units>`; compound = nested `UnitsProduct<A,B>`/`UnitsRatio<A,B>`/`InverseUnits<T>` combined by a hand-written overload table                                             | exposition | `Units.kt:55,66,77,98,166-320`                                                                                                   | struct + combinators + free-function overload table all present                                                                                                                                                                   | ✓           |
| 6   | NOTE: never normalizes — `A·B` and `B·A` distinct types; every legal combination a separately-written `operator fun`                                                                                                    | behavior   | `Units.kt:166-320` (no reduction engine); `README.md:117-122`                                                                    | order-sensitivity documented; overloads enumerated per shape                                                                                                                                                                      | ✓           |
| 7   | QUOTE `README.md` L8 positioning: "Measured provides a safe and simple way … Measured helps you avoid pitfalls like these."                                                                                             | quote      | `README.md:8`                                                                                                                    | verbatim, char-for-char                                                                                                                                                                                                           | ✓           |
| 8   | "seven dimension files (`Length`, `Mass`, `Time`, `Angle`, `BinarySize`, `GraphicsLength`, and the compound `Velocity`/`Acceleration` aliases) … one 429-line core (`Units.kt`)"                                        | figure     | `ls units/*.kt` = 6 dimension files + `Units.kt`; `wc -l Units.kt` = 429; `Velocity`/`Acceleration` at `Units.kt:427-428`        | 429-line core ✓; but only **6** dimension `.kt` files exist — the 7th listed item (`Velocity`/`Acceleration` aliases) lives **inside** `Units.kt:427-428`, not a file. "seven dimension files" over-counts (#D1)                  | ⚠ (#D1)     |
| 9   | Base unit fixed at milliseconds (`Time.kt` L11)                                                                                                                                                                         | fact       | `Time.kt:10` (`val milliseconds = Time("ms")` — default ratio 1.0 = base); `:11` = `val seconds = Time("s", 1000.0)`             | milliseconds **is** the base, but it is declared at **L10**, not L11 (L11 is `seconds`) (#D2)                                                                                                                                     | ⚠ (#D2)     |
| 10  | Design: compound built from three combinators — `UnitsProduct<A,B>` (L55), `UnitsRatio<A,B>` (L66), `InverseUnits<T>` (L77); `Velocity` = `UnitsRatio<Length, Time>` (L427)                                             | fact       | `Units.kt:55,66,77,427`                                                                                                          | all four locators exact                                                                                                                                                                                                           | ✓           |
| 11  | QUOTE `Units.kt` L17 — whole unit model is `suffix` + multiplicative `ratio` (`abstract class Units(val suffix: String, val ratio: Double = 1.0)`)                                                                      | quote      | `Units.kt:17`                                                                                                                    | verbatim; block is provenance-labeled (abridged body elided)                                                                                                                                                                      | ✓           |
| 12  | QUOTE `README.md` L144–146 — linear-only; °C/°F need more than a multiplier                                                                                                                                             | quote      | `README.md:144-146`                                                                                                              | verbatim, char-for-char                                                                                                                                                                                                           | ✓           |
| 13  | `Length.kt` L6–18 block (abridged): members are scale factors off base (m); `centimeters 0.0100`, `kilometers 1000.0000`, `miles 1609.3440`                                                                             | quote      | `Length.kt:6-18`                                                                                                                 | values verbatim; block is **reordered** (page lists meters first; source order is miles, millimeters, centimeters, meters, inches, feet, kilometers) and drops mm/in/ft — labeled "(abridged)"                                    | ✓ (note)    |
| 14  | `as`/`in` block `Units.kt` L103–108                                                                                                                                                                                     | quote      | `Units.kt:103,108`                                                                                                               | verbatim (source `:108` has a double space `else  amount`; page single-spaced — cosmetic)                                                                                                                                         | ✓ (note)    |
| 15  | `Measure<T>` block L113–131 (abridged): `class Measure<T: Units>(val amount: Double, val units: T): Comparable<…>`; plus/minus/times(Number)/div(Number) share `T`                                                      | quote      | `Units.kt:98` (class), `:113` plus, `:118` minus, `:126` times(Number), `:131` div(Number)                                       | abridged block; the four operator lines match source (page adds `// scalar` annotations; class header is L98, block spans 98→131)                                                                                                 | ✓           |
| 16  | plus/minus convert to the **smaller** unit via `minOf` on ratios (L84–89); `500 m + 1.5 km` → `2000 m`; `Measure<Length>` + `Measure<Time>` has no overload → compile error                                             | behavior   | `Units.kt:84` (compareTo), `:89` (minOf), `:113-118` (plus/minus, `minOf(units, other.units)`)                                   | mechanism confirmed; arithmetic checks (minOf→meters ratio 1.0; 500 + 1500 = 2000)                                                                                                                                                | ✓           |
| 17  | Overload table block L166–176, L228 (abridged): cross-dim mul/div are **free functions**, one per shape; base builds product, matching inverse cancels to `Double`                                                      | quote      | `Units.kt:166` (`A.times(B): UnitsProduct<A,B>`), `:167` (`@JvmName("times7") A.times(InverseUnits<A>): Double`), `:170`, `:228` | all four lines verbatim (whitespace collapsed)                                                                                                                                                                                    | ✓           |
| 18  | `@JvmName` disambiguation pervasive (erasure collapses descriptors); `Units.kt` carries **127** `operator fun` declarations across four regions (+`Measure*Units`, `Number-Measure`)                                    | figure     | `grep -c 'operator fun' Units.kt` = **127**                                                                                      | 127 = **all** lines containing `operator fun`, which includes the 16 commented-out `// FIXME` lines and the `Measure` member ops (plus/minus/times/div/rem/unaryMinus/compareTo) + `Number`/`T` operators. **Active decls = 111** | ⚠ (#D3)     |
| 19  | Table incomplete: **16** combinations checked in as commented-out `// FIXME` lines (`Units.kt` L179–186, L281–288); block L179–186                                                                                      | figure     | `grep -c FIXME Units.kt` = 16; L179–186 = 8, L281–288 = 8                                                                        | 16 confirmed; both locators exact; block lines verbatim (whitespace collapsed)                                                                                                                                                    | ✓           |
| 20  | Dimension rep: base = leaf `Units` subclass; compound = tree of 3 combinators (L55–77); `UnitsProduct.ratio = A.ratio*B.ratio` (L55); `UnitsRatio` lazy `reciprocal` (L66–68); `InverseUnits.ratio = 1/T.ratio` (L77)   | fact       | `Units.kt:55,66-68,77`                                                                                                           | `UnitsProduct(…, first.ratio * second.ratio)` :55; `reciprocal by lazy` :68; `InverseUnits(…, 1 / unit.ratio)` :77 — all exact                                                                                                    | ✓           |
| 21  | Powers structural: `typealias Square<T> = UnitsProduct<T, T>` (L57); `Acceleration` = `UnitsRatio<Length, Square<Time>>` (L428)                                                                                         | fact       | `Units.kt:57,428`                                                                                                                | both exact                                                                                                                                                                                                                        | ✓           |
| 22  | No normal form: `UnitsProduct<A,B>` ≠ `<B,A>`; `A·A⁻¹` reduces only because `A.times(InverseUnits<A>): Double` (L167) returns raw `Double`; order-sensitivity documented `README.md` L112–122                           | behavior   | `Units.kt:167`; `README.md:112-122` (`UnitsProduct<Angle, Time>` vs `<Time, Angle>`)                                             | L167 returns `Double` = `ratio * other.ratio`; README shows the two distinct product types                                                                                                                                        | ✓           |
| 23  | Dimensionless collapses to `Double`; no dimension-one `Measure` type                                                                                                                                                    | exposition | `Units.kt:167,175,205-206` (cancellation overloads return `Double`)                                                              | consistent with source (cancellation overloads all return `Double`)                                                                                                                                                               | ✓           |
| 24  | Runtime carries nothing — dimension exists only in the static type tree (JVM erasure)                                                                                                                                   | exposition | JVM type erasure (standard); `Units.kt:98` (single erased `Measure` class)                                                       | consistent                                                                                                                                                                                                                        | ✓           |
| 25  | Checking = ordinary Kotlin type-check + overload resolution; no plugin/APT/codegen; source muses `// TODO: Kapt code generation possible?` (L244)                                                                       | quote      | `Units.kt:244`                                                                                                                   | `// TODO: Kapt code generation possible?` verbatim at L244                                                                                                                                                                        | ✓           |
| 26  | Add/sub demand identical `T` (L113–118); mul/div select compound result by structural pattern (L166–320); `val velocity = 5 * meters / seconds` infers `Measure<Velocity>` (`README.md` L36)                            | fact       | `Units.kt:113-118,166-320`; `README.md:36`                                                                                       | plus/minus params `Measure<T>`; free-function table spans :166–:320; README L36 = `val velocity     = 5 * meters / seconds`                                                                                                       | ✓           |
| 27  | Checker cannot invert/generalize over dimension (no Kennedy principal types); user code generic over a **fixed** shape (`abs`/`round`/`ceil`/`floor`, L346–361)                                                         | fact       | `Units.kt:346` abs, `:351` round, `:356` ceil, `:361` floor                                                                      | all four are `fun <T: Units>` over a fixed `Measure<T>` — exact                                                                                                                                                                   | ✓           |
| 28  | Some well-defined `UnitsProduct` products have no overload (the `// FIXME` gaps, L179–186, L281–288) and won't type-check                                                                                               | fact       | `Units.kt:179-186,281-288`                                                                                                       | 16 commented gaps confirmed (row 19)                                                                                                                                                                                              | ✓           |
| 29  | Extensibility: new member trivial — `README.md` L72–84 block (labeled L73–80): `val hands = Length("hands", 0.1016)`; `val v: Measure<Velocity> = 100_000 * hands / hours`; `println(5 * hands as meters) // 0.508 m`   | quote      | `README.md:73` (hands), `:78` (v)                                                                                                | `hands`/`v` lines verbatim; **the `println(5 * hands ` as ` meters) // 0.508 m` line is synthesized** — README L80 is `println("$l1 == $l2 or ${l1 in meters}") // 5.0 hands == 0.508 m or 0.508` (value 0.508 correct: 5×0.1016) | ⚠ (at-risk) |
| 30  | New dimensions = small class; `Blits` example composes into `UnitsRatio<Blits, Time>` (`README.md` L88–110); no registry                                                                                                | fact       | `README.md:90` (`class Blits`), `:104` (`typealias BlitVelocity = UnitsRatio<Blits, Time>`)                                      | confirmed                                                                                                                                                                                                                         | ✓           |
| 31  | New compound interactions manual; order-canonicalization `README.md` L124–142 (block L128–134); library ships `Time·Length`→`Length·Time` (`Length.kt` L23–24), `Length·Mass`→`Mass·Length` (`Mass.kt` L15–16)          | quote      | `README.md:129,131-132`; `Length.kt:23-24`; `Mass.kt:15-16`                                                                      | README operator/f1/f2 lines verbatim (page's trailing `// f1 and f2 now share a type` paraphrases source `// … now have the same type` at L134); both library redirects exact                                                     | ✓ (note)    |
| 32  | Fractional powers impossible — `Square<T> = UnitsProduct<T,T>` (L57), Kotlin has no type-level integers; unrepresentable                                                                                                | fact       | `Units.kt:57`                                                                                                                    | structural product only; no exponent type — confirmed                                                                                                                                                                             | ✓           |
| 33  | Affine/temperature absent by design — `Units` carries only multiplicative `ratio` (L17); **no `Temperature` dimension** (search of `src/` finds none)                                                                   | fact       | `Units.kt:17`; `ls units/*.kt` = Angle, BinarySize, GraphicsLength, Length, Mass, Time, Units — no Temperature                   | negative finding confirmed                                                                                                                                                                                                        | ✓           |
| 34  | Logarithmic absent (no dB/neper/level); `BinarySize` covers bits/bytes decimal (kB) **and** binary (KiB) as ordinary ratios                                                                                             | fact       | `BinarySize.kt:13` (`kilobytes "kB" 1000.0`), `:19` (`kibibytes "KiB" 1024.0`); no log type anywhere                             | kB vs KiB present as plain ratios; no log scale                                                                                                                                                                                   | ✓           |
| 35  | Angle first-class: `Units` subclass with `radians` base + `degrees` (`ratio = π/180`, printed without a space) and own trig (`Angle.kt` L11–19); `sin/cos/tan` take `Measure<Angle>`, convert to radians (L21–33)       | fact       | `Angle.kt:11,16,19,21-23`                                                                                                        | `radians = Angle("rad")` :16; `degrees = object: Angle("°", PI / 180) { override val spaceBetweenMagnitude = false }` :19; sin/cos/tan `angle in radians` :21-23                                                                  | ✓           |
| 36  | Kind-vs-dimension not modeled (no `Kind` tag); `Length` vs `GraphicsLength` distinguished by being separate `Units` subclasses (`GraphicsLength.kt`)                                                                    | fact       | `GraphicsLength.kt:6,11` (`open class GraphicsLength`, `pixels = GraphicsLength("px")`); no `Kind` machinery in tree             | confirmed; pixels vs metres are separate leaf classes                                                                                                                                                                             | ✓           |
| 37  | Zero-cost story: `Measure<T>` is a real heap class (`class Measure<T: Units>(val amount: Double, val units: T)`, L98), boxed `Double` + `Units` ref; **not** `@JvmInline value class`; every op allocates (`plus` L113) | fact       | `Units.kt:98,113`                                                                                                                | class decl exact; no `@JvmInline`/`value class` modifier; `plus` returns a fresh `Measure`                                                                                                                                        | ✓           |
| 38  | Unit `ratio` consulted at runtime: `in`/`as` and normalizing plus/minus do `Double` divisions on `units.ratio` (L108, L113); compound `ratio` recomputed by multiplying operands' ratios (L55)                          | fact       | `Units.kt:108,113,55`                                                                                                            | `in` = `amount * (units.ratio / other.ratio)` :108; `UnitsProduct(…, first.ratio * second.ratio)` :55                                                                                                                             | ✓           |
| 39  | Erasure soundness: `Measure.equals` does an **unchecked cast** `Measure<*>`→`Measure<T>` (L149); compiler warns; cross-dimension `equals` is not a compile error                                                        | behavior   | `Units.kt:149` (`val resultUnit = minOf(units, (other as Measure<T>).units)`)                                                    | unchecked cast present at L149; warning is local kotlinc output (mechanism confirmed in-source)                                                                                                                                   | ✓           |
| 40  | Diagnostic `m + s`: reproduced compile error — "argument type mismatch: actual type is 'Measure<Time>', but 'Measure<Length>' was expected."                                                                            | behavior   | `Units.kt:113` (`plus(other: Measure<T>)` — same `T`)                                                                            | error string is local `kotlinc-jvm` output (transcript not preserved); **mechanism confirmed**: with `T=Length` fixed by receiver, `Measure<Time>` is not applicable                                                              | ✓           |
| 41  | Diagnostic is excellent: names domain types (`Measure<Time>`/`Measure<Length>`), no `PInt<UInt<UTerm, B1>>` mangling, no 8-way record; `plus` is `Measure<T>.plus(Measure<T>)` (L113)                                   | behavior   | `Units.kt:113`                                                                                                                   | nominal-type error follows structurally from the member signature                                                                                                                                                                 | ✓           |
| 42  | Valid same-dimension path compiles: `500 * meters + 1.5 * kilometers` → 2000 m; `d / (4 * seconds)` infers `Measure<Velocity>`; prints `2000.0 m` / `500.0 m/s`                                                         | behavior   | `Units.kt:113` (plus/minOf), `:252` (`Measure<A>.div(Measure<B>): Measure<UnitsRatio<A,B>>`), `:427` (Velocity)                  | arithmetic + overload path confirmed (Length/Time → `UnitsRatio<Length,Time>` = Velocity); transcript not preserved but mechanism sound                                                                                           | ✓           |
| 43  | Order-sensitivity reproduces as a compile error: `UnitsProduct<Angle, Time>` not assignable to `UnitsProduct<Time, Angle>` (`README.md` L112–122)                                                                       | behavior   | `README.md:118-119`                                                                                                              | the two distinct product types are the README's own example; mechanism (no normalization) confirmed                                                                                                                               | ✓           |
| 44  | In-repo tests corroborate positive behaviour (`UnitTests.kt` L616–627, `plusMinusOperatorsWork`); no compile-fail harness                                                                                               | fact       | `UnitTests.kt:616` (`@Test @JsName("plusMinusOperatorsWork")`), `:617` (`fun `+ -`()`), body :618-626                            | test present at L616; `plusMinusOperatorsWork` name exact; spans L616–626 (page's L627 is the trailing blank line — off by one)                                                                                                   | ✓ (note)    |
| 45  | Ergonomics: infix/operator DSL (`README.md` L36–50); `Number.times(Units)` bridge (`Units.kt` L328, L335)                                                                                                               | fact       | `README.md:36-50`; `Units.kt:328` (`Number.times(unit: T): Measure<T>`), `:335` (`T.times(value: Number): Measure<T>`)           | both bridge overloads exact                                                                                                                                                                                                       | ✓           |
| 46  | Compile-time cost modest: full JVM compile + small consumer ≈ **~4.2 s wall** (`kotlinc-jvm 2.2.21`, `-include-runtime`); latent cost = overload-resolution pressure, 127 overloads                                     | behavior   | measured locally (no transcript); 127 count per row 18                                                                           | timing is a measured claim without a preserved transcript (plausible for a 429-line core); "127 overloads" carries the row-18 imprecision                                                                                         | ⚠ (caveat)  |
| 47  | Strengths / Weaknesses / trade-off table (readable diagnostics, tiny model, no fractional powers, no affine, not zero-cost, erasure gap, no Kind, no dimensional polymorphism)                                          | opinion    | author synthesis over rows 5–45                                                                                                  | each bullet traces to a verified mechanic above; "16 `// FIXME`" and "127 overloads" figures carry rows 18–19 status                                                                                                              | ◯           |
| 48  | Sources / links (`nacular/measured`, Dokka site, sibling survey pages)                                                                                                                                                  | ◯          | repo URL / web                                                                                                                   | link-only; blob URLs pinned at `3f65500`                                                                                                                                                                                          | ◯           |

## Discrepancies

- **#D1 ⚠ (minor) — `kotlin-measured.md:50-53`.** "seven dimension files
  (`Length`, `Mass`, `Time`, `Angle`, `BinarySize`, `GraphicsLength`, and the
  compound `Velocity`/`Acceleration` aliases)". The clone has **six** dimension
  `.kt` files; the seventh listed item — the `Velocity`/`Acceleration` type
  aliases — lives **inside** the core `Units.kt` (L427–428), not in a separate
  file. **Fix:** "six dimension files (…), with the compound `Velocity`/`Acceleration`
  aliases in the core" (or "six dimension files … on one 429-line core whose tail
  also holds the `Velocity`/`Acceleration` aliases").

- **#D2 ⚠ (minor) — `kotlin-measured.md:57`.** "the base unit fixed at
  milliseconds ([`Time.kt`][time] **L11**)". Milliseconds **is** the base unit
  (`val milliseconds = Time("ms")`, default `ratio = 1.0`) but is declared at
  **`Time.kt:10`**; **L11** is `val seconds = Time("s", 1000.0)`. **Fix:** locator
  → `Time.kt` L10.

- **#D3 ⚠ (minor) — `kotlin-measured.md:174, 459`.** "`Units.kt` carries **127**
  `operator fun` declarations" / "**127** `operator fun` overloads distinguished by
  `@JvmName`". `grep -c 'operator fun' Units.kt` = 127, but that raw count includes
  the **16 commented-out `// FIXME` lines** (dead, non-declarations) plus the
  `Measure` member operators (`plus`/`minus`/`times`/`div`/`rem`/`unaryMinus`/
  `compareTo`) and the `Number`/`T` operators — i.e. far more than the compound
  overload table. **Active `operator fun` declarations = 111** (127 − 16 commented).
  Calling commented lines "declarations"/"overloads" over-counts. **Fix:** either
  "127 `operator fun` lines (111 active + 16 commented `// FIXME`)" or use **111**
  for active overloads.

## At-risk quotes

- **Row 29 — `kotlin-measured.md:274-278`.** The `README.md L73-80` code block's
  third line, `println(5 * hands ` as ` meters)          // 0.508 m`, is **not
  verbatim** in the README — L80 is
  `println("$l1 == $l2 or ${l1 ` in ` meters}") // 5.0 hands == 0.508 m or 0.508`.
  The `hands`/`v` lines are verbatim and the synthesized value (0.508 = 5 × 0.1016)
  is correct, but the block is presented as `README.md L73-80` without an
  "(abridged)"/illustrative marker. Consider labeling it abridged or dropping the
  synthesized `println`.

## Unsourced / opinion

- Row 3's "formerly `pusolito`" handle history is a GitHub fact not derivable from
  the clone (git author is `neddy <nickeddy@gmail.com>`; `nacular` confirmed by the
  repo path). Not a page error — flagged as non-local.
- Framings ("this survey's data point for the nested-generics-as-dimensions,
  no-exponent-algebra mechanism", "JVM cousin of `squants`", "the opposite pole from
  `uom`'s zero-cost", "approximates the group with a finite hand-written table") —
  author synthesis (◯), each consistent with the verified mechanics.
- Compile timing (~4.2 s, row 46) — measured claim without a preserved transcript;
  internally plausible, flagged honestly.
- Transcription notes (no page edit needed): row 13 reorders the abridged
  `Length.kt` member list; row 14 collapses a double space; row 31 paraphrases a
  trailing README comment; row 44 end-locator off by one (blank line).

## Net

48 rows; **4 ⚠** (row 3 non-local handle history; #D1 "seven"→six dimension files;
#D2 `Time.kt` L11→L10; #D3 127 `operator fun` lines vs 111 active declarations) plus
one at-risk quote (row 29 synthesized `println` in the `README.md L73-80` block) and
one timing caveat (row 46). Every verbatim blockquote from `README.md`
(positioning L8, linear-only L144–146) and every cited `Units.kt` construct (`Units`
L17, `as`/`in` L103–108, `Measure` L98/113–131, the overload table L166–176/228, the
16 `// FIXME` gaps L179–186/281–288, `Square` L57, aliases L427–428, `// TODO: Kapt`
L244, unchecked cast L149) matches the pinned clone. All metadata (MIT license,
`3f65500`/`v0.5.0`/2026-04-05, Kotlin `2.3.20`, the full KMP target list, JVM 1.8)
re-derived from `LICENSE`, git, `libs.versions.toml`, and `build.gradle.kts`. The
`m + s`, valid-path, and order-sensitivity reproductions are rung-1 local compiles
whose mechanisms are confirmed in-source (member `plus` requires identical `T`; the
two `UnitsProduct` orderings are the README's own example).

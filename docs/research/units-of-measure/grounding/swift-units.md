# Grounding ledger — `swift-units.md`

Claim-by-claim verification of `docs/research/units-of-measure/swift-units.md`
against **local** primary sources (checked 2026-07-04):

- `$REPOS/swift/swift-corelibs-foundation` @ `cd04666` (verified
  `git rev-parse --short HEAD` = `cd04666`, full
  `cd0466660eec80f20eebaec72aefa94c93696e3d`, HEAD dated 2026-07-01, "Fix
  potential oob read in sendWebSocketsData (#5483)") — sparse
  `Sources/Foundation/{Measurement,Unit,MeasurementFormatter}.swift`, `Package.swift`
- `$REPOS/swift/Units` @ `08ab9be` (verified `git rev-parse --short HEAD` =
  `08ab9be`, full `08ab9bec93bfad56d83a82ab4a32271b2bda90e9`, HEAD dated
  2026-07-03, "Merge pull request #25 … ci/swift-versions"; `git describe --tags`
  = `v1.2.0`) — `Sources/Units/`, `Tests/UnitsTests/`, `README.md`, `LICENSE`,
  `Package.swift`

`$REPOS = /home/petar/code/repos`. Claim **Types:** `quote` · `fact` · `figure`
· `behavior` · `exposition` · `opinion`. Status: `✓` verified against the local
artifact · `⚠` imprecise/line-drifted/paraphrased (explained) · `◯` not locally
groundable.

## Claims

| #   | Claim                                                                                                                                                                                                                                          | Type       | Source (local + locator)                                                                                                                                                                                                   | Evidence                                                                                                                                                                                                                                                         | Status         |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| 1   | Metadata: builds with `-swift-version 6`; `Measurement` API dates to Swift 3 / macOS 10.12                                                                                                                                                     | fact       | `Package.swift:1` `swift-tools-version: 6.2`, `:136,:301,…` `.swiftLanguageMode(.v6)`; `Measurement.swift:25` `@available(macOS 10.12, iOS 10.0, …)`                                                                       | swiftLanguageMode(.v6) == `-swift-version 6`; `@available(macOS 10.12 …)` = macOS 10.12 / Swift 3 era                                                                                                                                                            | ✓              |
| 2   | License: Apache-2.0 with Runtime Library Exception; authors "Apple Inc. and the Swift project authors"                                                                                                                                         | fact       | `Measurement.swift:5-6` "Copyright (c) 2014 - 2017 Apple Inc. and the Swift project authors / Licensed under Apache License v2.0 with Runtime Library Exception"                                                           | verbatim in header                                                                                                                                                                                                                                               | ✓              |
| 3   | Repository `swiftlang/swift-corelibs-foundation`; pinned `cd04666`, 2026-07-01, `main`; no independent semver                                                                                                                                  | fact       | `git rev-parse --short HEAD` = `cd04666`; HEAD commit date 2026-07-01                                                                                                                                                      | SHA + date confirmed; toolchain-versioned (no tag)                                                                                                                                                                                                               | ✓              |
| 4   | Contrast lib license MIT © 2021 Jay Herron                                                                                                                                                                                                     | fact       | `Units/LICENSE:1,3` "The MIT License (MIT)" / "Copyright (c) 2021 Jay Herron"                                                                                                                                              | verbatim                                                                                                                                                                                                                                                         | ✓              |
| 5   | `Units` pinned `08ab9be`, 2026-07-03, release `v1.2.0`                                                                                                                                                                                         | fact       | `git rev-parse --short HEAD` = `08ab9be`; HEAD date 2026-07-03; `git describe --tags` = `v1.2.0`                                                                                                                           | all three confirmed                                                                                                                                                                                                                                              | ✓              |
| 6   | `Units`: single non-generic `Measurement` struct; `Unit` carries runtime `[Quantity: Int]` exponent map; ℤ exponents, `pow(_ raiseTo: Int)`; `+`/`-` `throws` → `UnitError.incompatibleUnits`                                                  | fact       | `Units/Measurement.swift:4`; `Unit.swift:70` `dimension: [Quantity: Int]`; `pow` `:185`; `+` `:58` `throws`; `Measurement.swift:46` `throw UnitError.incompatibleUnits`                                                    | all confirmed (see rows 20-33)                                                                                                                                                                                                                                   | ✓              |
| 7   | QUOTE `Measurement.swift` L23–24: "A `Measurement` is a model type that holds a `Double` value associated with a `Unit`. Measurements support a large set of operators, including `+`, `-`, `*`, `/`, and a full set of comparison operators." | quote      | `Measurement.swift:22,24`                                                                                                                                                                                                  | verbatim; but first sentence is **L22** (L23 is the blank `///`), so the span is L22–24, not L23–24                                                                                                                                                              | ⚠ (#D1)        |
| 8   | QUOTE `Measurement.swift` L76: "When a `Measurement` contains a `Dimension` unit, it gains the ability to convert between the kinds of units in that dimension."                                                                               | quote      | `Measurement.swift:76`                                                                                                                                                                                                     | char-for-char verbatim                                                                                                                                                                                                                                           | ✓              |
| 9   | `MeasurementFormatter` "will 'implicitly convert the measurement object to miles' for an `en_US` locale" (L37–40)                                                                                                                              | quote      | `MeasurementFormatter.swift:40` "the formatter will implicitly convert the measurement object to miles"                                                                                                                    | fragment verbatim at L40 (within cited L37–40 span)                                                                                                                                                                                                              | ✓              |
| 10  | Struct block L26–33: `struct Measurement<UnitType : Unit> : ReferenceConvertible, Comparable, Equatable`, `public let unit: UnitType`, `public var value: Double`                                                                              | quote      | `Measurement.swift:26,30,33` (decl :26, unit :30, value :33; doc comments :29,:32)                                                                                                                                         | illustration-labeled; decl + fields + doc-comment text all match (typealias :27 omitted)                                                                                                                                                                         | ✓ (note)       |
| 11  | Values stored in the given unit, not normalized; lazy conversion via `converter` only on `converted(to:)`/mixed-unit add                                                                                                                       | behavior   | `Measurement.swift:36-39` init stores unit as-is; `:83-95` `converted(to:)`; `:108-116` add converts on demand                                                                                                             | mechanism confirmed                                                                                                                                                                                                                                              | ✓              |
| 12  | QUOTE `Units/Unit.swift` L3–7: "Units may be multiplied and divided, resulting in 'composite' units"                                                                                                                                           | quote      | `Units/Unit.swift:6` "Units may be multiplied and divided, resulting in \"composite\" units, which retain …"                                                                                                               | fragment verbatim at L6 (within L3–7 doc block); source uses double-quotes `"composite"`, page renders single-quote `'composite'`                                                                                                                                | ✓ (note)       |
| 13  | QUOTE `Units/README.md` L35: "addition and subtraction requires that both measurements have the same dimensionality (5 meters - 10 seconds ❌), otherwise a runtime error is thrown."                                                          | quote      | `Units/README.md:36`                                                                                                                                                                                                       | verbatim fragment; but the paragraph is **L36**, not L35                                                                                                                                                                                                         | ⚠ (#D5)        |
| 14  | `Unit` is an `open class` whose only state is `symbol: String` (L151–152)                                                                                                                                                                      | fact       | `Unit.swift:151` `open class Unit : NSObject, …`, `:152` `public let symbol: String`                                                                                                                                       | verbatim                                                                                                                                                                                                                                                         | ✓              |
| 15  | `Dimension : Unit` block L194–211: `public let converter: UnitConverter`; `open class func baseUnit() -> Self { fatalError("*** You must override baseUnit in your class to define its base unit.") }`                                         | quote      | `Unit.swift:194,197,209-211`                                                                                                                                                                                               | illustration-labeled; converter :197 + baseUnit fatalError :209-211 verbatim                                                                                                                                                                                     | ✓              |
| 16  | 22 hand-written `final class : Dimension` subclasses (`UnitLength`, `UnitDuration`, …) in the shipped set, L251–2340                                                                                                                           | figure     | `grep -c "public final class .*: Dimension"` = **22**; first `UnitAcceleration` :251, last `UnitVolume` :2341–2620                                                                                                         | count is exactly 22; but the cited range **L251–2340 excludes `UnitVolume`** (L2341–2620, the 22nd class); span should end at L2620                                                                                                                              | ⚠ (#D3)        |
| 17  | `UnitLength.meters` / `UnitDuration.seconds` exposed as `class var`s; each overrides `baseUnit()`                                                                                                                                              | fact       | `Unit.swift:1249` `UnitLength`, `:622` `UnitDuration`; `:328-366` `UnitAngle` shows `class var`+`baseUnit()` pattern                                                                                                       | pattern confirmed across subclasses                                                                                                                                                                                                                              | ✓              |
| 18  | `UnitConverterLinear` applies `value * coefficient + constant` toward the base unit (L56–58)                                                                                                                                                   | quote      | `Unit.swift:56-58` `open override func baseUnitValue(fromValue value: Double) -> Double { return value * coefficient + constant }`                                                                                         | verbatim                                                                                                                                                                                                                                                         | ✓              |
| 19  | `UnitArea` is a separate class, not `UnitLength²`; `UnitSpeed` ≠ `UnitLength / UnitDuration`; no type-level derivation                                                                                                                         | fact       | `Unit.swift:381` `UnitArea`, `:2213` `UnitSpeed` — independent `Dimension` subclasses; no cross-class relation in source                                                                                                   | structural fact confirmed                                                                                                                                                                                                                                        | ✓              |
| 20  | `Units`: `Unit` is `.none` / `.defined(DefinedUnit)` / `.composite([DefinedUnit: Int])`; dimension = summed `[Quantity: Int]` map (L69–88)                                                                                                     | fact       | `Units/Unit.swift:272-276` (UnitType enum), `:70-90` (`dimension` computed via merge)                                                                                                                                      | enum cases confirmed; dimension computation spans L70–90 (page L69–88 off by ~2, within tolerance)                                                                                                                                                               | ✓              |
| 21  | `Quantity` is a `RawRepresentable` struct — 7 ISQ base quantities + `angle` + `data` — deliberately a struct so callers can add own (L17–38)                                                                                                   | fact       | `Units/Quantity.swift:17` struct decl, `:25-31` 7 ISQ, `:34` angle, `:35` data; `:3-4` "`RawRepresentable` rather than an enum so that callers can define their own"                                                       | 7 ISQ + angle + data confirmed; struct rationale verbatim                                                                                                                                                                                                        | ✓              |
| 22  | `Units` algebra: `*` (L137), `/` (L160), `pow(_ raiseTo: Int)` (L185), `isDimensionallyEquivalent(to:)` = `dimension == to.dimension` (L204–205)                                                                                               | fact       | `Units/Unit.swift:137,160,185,204-205`                                                                                                                                                                                     | all four locators exact; `:205` `return dimension == to.dimension` verbatim                                                                                                                                                                                      | ✓              |
| 23  | Foundation has no exponent vector / no product type / no dimension-one; generic param is a nominal kind tag                                                                                                                                    | exposition | `Measurement.swift:157-179` only `Measurement × Double` ops; no `Measurement × Measurement` anywhere in source                                                                                                             | negative structural finding confirmed                                                                                                                                                                                                                            | ✓              |
| 24  | `Units` sits at structural pole: `meter*meter` ≡ `foot*foot` (`[Length: 2]`); `newton` = `kg·m/s²` structurally (L69–88)                                                                                                                       | exposition | `Units/Unit.swift:70-90` (merge sums exponents); `:137-180` `*`/`/`                                                                                                                                                        | structural model confirmed; `newton = kg·m/s²` is an illustrative reduction (consistent)                                                                                                                                                                         | ✓              |
| 25  | `+`/`-` require the _same_ `UnitType` on both operands (L108 Dimension case, L138 general case); block L108–119                                                                                                                                | quote      | `Measurement.swift:108` (`extension … where UnitType : Dimension`), `:138` (general `extension Measurement`)                                                                                                               | both overloads take `Measurement<UnitType>` twice; illustration block matches the else-branch base conversion                                                                                                                                                    | ✓              |
| 26  | No `+` accepts two _different_ `UnitType`s → `length + duration` rejected in overload resolution; same-dimension diff-unit add auto-converts                                                                                                   | behavior   | `Measurement.swift:108-116` (only same-`UnitType` `+`); `:112-114` base conversion                                                                                                                                         | structural: no mixed-type `+` exists                                                                                                                                                                                                                             | ✓              |
| 27  | `*`/`/` take only a `Double` scalar (L159–179); no `Measurement × Measurement`; inference of `Velocity` impossible in principle                                                                                                                | fact       | `Measurement.swift:159,165,171,177` (all `Double` operand)                                                                                                                                                                 | four scalar ops confirmed; no quantity×quantity operator in file                                                                                                                                                                                                 | ✓              |
| 28  | `==` / `<` are generic over _two independent_ unit types (L185, L207)                                                                                                                                                                          | quote      | `Measurement.swift:185` `==<LeftHandSideType, RightHandSideType>(…)`; `:207` `<<LeftHandSideType, RightHandSideType>(…)`                                                                                                   | both signatures char-for-char verbatim (incl. the two-space `func  <`)                                                                                                                                                                                           | ✓              |
| 29  | `length == duration` compiles → `false`; `length < duration` compiles → runtime `fatalError` "Attempt to compare measurements with non-equal dimensions" (L219)                                                                                | behavior   | `Measurement.swift:201` `return false`; `:219` `fatalError("Attempt to compare measurements with non-equal dimensions")`                                                                                                   | both branches + fatalError string verbatim                                                                                                                                                                                                                       | ✓              |
| 30  | `Units` does no compile-time checking: single non-generic `Measurement`, `meter + second` type-checks, mismatch is a runtime throw                                                                                                             | behavior   | `Units/Measurement.swift:4` (non-generic), `:58` `+` `throws`                                                                                                                                                              | confirmed                                                                                                                                                                                                                                                        | ✓              |
| 31  | Foundation extends by subclassing (`open class`); add units via `class var`s; add quantity via `final class … : Dimension` overriding `baseUnit()` (L209–211)                                                                                  | fact       | `Unit.swift:151,194` (`open`), `:209-211` (baseUnit hook)                                                                                                                                                                  | `open` classes + baseUnit override confirmed                                                                                                                                                                                                                     | ✓              |
| 32  | `Units` extends structurally: `Quantity(rawValue:)` — global raw-string namespace so two modules picking `"Money"` silently alias (L11–16); Registry/RegistryBuilder                                                                           | fact       | `Units/Quantity.swift:11-16` "Because the namespace is global across every module that links this package, two modules that independently pick the same raw value are treated as the _same_ dimension and silently cancel" | doc warning verbatim; `Registry.swift`/`RegistryBuilder.swift` exist                                                                                                                                                                                             | ✓              |
| 33  | Fractional powers absent in both; `Units` `pow(_ raiseTo: Int)` integer-only (L185); Foundation has no power op                                                                                                                                | fact       | `Units/Unit.swift:185` `pow(_ raiseTo: Int)`; no exponent API in `Measurement.swift`                                                                                                                                       | Int-only pow confirmed; no Foundation power op                                                                                                                                                                                                                   | ✓              |
| 34  | Affine: `UnitConverterLinear` carries a `constant` (L45, L56–58); `UnitTemperature` Celsius `coeff 1.0, const 273.15`, Fahrenheit `coeff 0.5556, const 255.372` (L2278–2325)                                                                   | fact       | `Unit.swift:43` `constant` decl, `:56-58` formula; `:2292,2298` celsius 1.0/273.15; `:2293,2299` fahrenheit 0.55555…/255.37222…                                                                                            | Celsius exact; Fahrenheit page-rounded (0.5556 / 255.372) OK; UnitTemperature L2278–2325 exact. But `constant` is declared at **L43**, not L45 (L45 is a convenience init)                                                                                       | ⚠ (#D2)        |
| 35  | Offset applied inside `+`'s base-unit conversion (L110–116); `20 °C + 68 °F` yields `≈ 548.5 K ≈ 275.4 °C`, a meaningless sum                                                                                                                  | figure     | `Measurement.swift:112-114` converts both to base kelvin then sums                                                                                                                                                         | mechanism (sum of two absolute kelvins → meaningless) correct, but the **number is wrong**: 20 °C = 293.15 K, 68 °F = 293.15 K, sum = **586.30 K ≈ 313.15 °C**, not 548.5 K / 275.4 °C (the page appears to have added 293.15 + the Fahrenheit constant 255.372) | ⚠ (#D7)        |
| 36  | Single `UnitTemperature` type, no point/interval split, so adding absolute temperatures is not prevented                                                                                                                                       | fact       | `Unit.swift:2278-2339` one `UnitTemperature` class                                                                                                                                                                         | confirmed (no interval type in tree)                                                                                                                                                                                                                             | ✓              |
| 37  | `Units` guards composite/affine case: raising/combining a unit with non-zero `constant` throws `invalidCompositeUnit`: "Nonlinear unit prevents conversion" (L223–225)                                                                         | quote      | `Units/Unit.swift:223-225` `guard subUnit.constant == 0 else { throw UnitError.invalidCompositeUnit(message: "Nonlinear unit prevents conversion: \(subUnit)") }`                                                          | verbatim (source appends `: \(subUnit)`)                                                                                                                                                                                                                         | ✓              |
| 38  | Logarithmic quantities absent in both (no decibel/neper/level)                                                                                                                                                                                 | fact       | `grep -ri "decibel\|neper"` on both trees — no hits in unit definitions                                                                                                                                                    | negative finding confirmed                                                                                                                                                                                                                                       | ✓              |
| 39  | Foundation `UnitAngle` (L300–365): degrees, radians, gradians, revolutions — a nominal `Dimension`; `Units` makes `angle` a base quantity (L34)                                                                                                | fact       | `Unit.swift:300` `UnitAngle`, degrees `:328`, radians `:346`, gradians `:352`, revolutions `:358`; `Units/Quantity.swift:34` `angle`                                                                                       | all present (page omits arcMinutes/arcSeconds — partial list OK); `Units` angle base quantity confirmed                                                                                                                                                          | ✓              |
| 40  | Kind: Foundation has no derived quantities so torque/energy never collide; `Units` structural → both reduce to `[Mass:1, Length:2, Time:-2]` and interconvert silently                                                                         | exposition | `Units/Unit.swift:204-205` `isDimensionallyEquivalent` = dimension equality; no kind mechanism in either tree                                                                                                              | structural-collision reasoning confirmed against the dimension-equality check                                                                                                                                                                                    | ✓              |
| 41  | Not zero-cost (Foundation): `unit: UnitType` is a class reference (L30); mixed-unit op reads `unit.converter` and calls virtual `baseUnitValue` (L113–116 → `Unit.swift` L56)                                                                  | fact       | `Measurement.swift:30` `public let unit: UnitType`, `:112-114` converter calls; dispatches to `Unit.swift:56`                                                                                                              | reference field + virtual dispatch confirmed                                                                                                                                                                                                                     | ✓              |
| 42  | Not zero-cost (`Units`): composite `Unit` stores `[DefinedUnit: Int]` dict (L60–62); `*`/`/` merge dicts (L137–158); `isDimensionallyEquivalent` compares maps (L204–205); `PerformanceTests` target exists                                    | fact       | `Units/Unit.swift:61` `.composite(subUnits)`, `:137-153` `*` merge, `:160-180` `/` merge, `:204-205`; `Package.swift:37` `name: "PerformanceTests"`                                                                        | dict storage + merges + PerformanceTests confirmed; `/` actually runs to L180 (page L137–158 brackets `*` fully, minor)                                                                                                                                          | ✓              |
| 43  | Diagnostics (Foundation): local repro with `swift 5.10.1` (nixpkgs) **fell** — no `Foundation` module; mechanism pinned to `+` at L108/138; error names `Measurement<UnitLength>` vs `Measurement<UnitDuration>`                               | behavior   | `Measurement.swift:108,138` (only same-`UnitType` `+`)                                                                                                                                                                     | reproduction self-reported as fallen (honest); the _mechanism_ (no mixed-type `+` → overload-resolution rejection) is source-grounded at L108/138                                                                                                                | ✓ (repro fell) |
| 44  | Diagnostics (`Units`): in-repo test `XCTAssertThrowsError(try 5.measured(in: .meter) + 5.measured(in: .second))` (L45–48); `+` `throws` (L58); `convert(to:)` throws when not equivalent (L44–46); README L35                                  | behavior   | `Tests/UnitsTests/MeasurementTests.swift:45-48`; `Units/Measurement.swift:58`; guard `:45-47`                                                                                                                              | test block char-for-char verbatim at L45–48; `+` throws :58 ✓. Convert guard is **L45–47**, not L44–46 (L44 is `}`, L47 is closing `}`)                                                                                                                          | ⚠ (#D4)        |
| 45  | Valid path: Foundation `meters + feet` auto-converts through base (L110–116); `Units` `60.measured(in: .mile / .hour) * 30.measured(in: .minute)` reduces on `convert(to: .mile)` (README L26–32)                                              | behavior   | `Measurement.swift:110-116`; `Units/README.md:28-33`                                                                                                                                                                       | Foundation base-conversion ✓. README example is at **L28–33** (`drivingSpeed` :28, `drivingDistance = drivingSpeed * drivingTime` :32, `convert(to: .mile)` :33), not L26–32                                                                                     | ⚠ (#D6)        |
| 46  | Apple Developer doc links (`Measurement`, `Unit`/`Dimension`)                                                                                                                                                                                  | ◯          | developer.apple.com (web)                                                                                                                                                                                                  | link-only, no factual claim attached                                                                                                                                                                                                                             | ◯              |

## Discrepancies

- **#D1 ⚠ (minor) — `swift-units.md:52`.** The `Measurement` doc-comment quote is
  cited "L23–24", but the first sentence ("A `Measurement` is a model type…") is on
  **L22**; L23 is the blank `///`. **Fix:** cite L22–24.

- **#D2 ⚠ (minor) — `swift-units.md:287`.** "`UnitConverterLinear` carries a
  `constant` (`Unit.swift` L45, L56–58)". The `constant` property is declared at
  **L43** (`open private(set) var constant: Double`); L45 is a convenience `init`.
  **Fix:** cite L43, L56–58.

- **#D3 ⚠ (minor) — `swift-units.md:131`.** "22 … `Dimension` subclasses in the
  shipped set (`Unit.swift` L251–2340)". The count 22 is exact, but the range
  **L251–2340 excludes `UnitVolume`**, the 22nd class, which spans **L2341–2620**.
  **Fix:** widen the span to L251–2620.

- **#D4 ⚠ (minor) — `swift-units.md:395`.** The `convert(to:)` guard block is cited
  "`Measurement.swift` L44–46"; the guard is actually **L45–47**
  (`guard … else {` :45, `throw …` :46, `}` :47). The quoted block text is verbatim.
  **Fix:** cite L45–47.

- **#D5 ⚠ (minor) — `swift-units.md:101,409`.** The README "runtime error is thrown"
  quote is cited "L35"; the paragraph is on **L36**. **Fix:** cite L36.

- **#D6 ⚠ (minor) — `swift-units.md:415`.** The `Units` valid-path example is cited
  "README.md L26–32"; the code block is at **L28–33** (`convert(to: .mile)` on L33
  falls outside the cited range). **Fix:** cite L28–33.

- **#D7 ⚠ (figure) — `swift-units.md:292`.** "`20 °C + 68 °F` yields `≈ 548.5 K
≈ 275.4 °C`". Both operands equal 293.15 K (68 °F = 20 °C), and Foundation's `+`
  sums the two base-kelvin values, so the result is **≈ 586.30 K ≈ 313.15 °C**, not
  548.5 K / 275.4 °C. (The page value looks like 293.15 + the Fahrenheit `constant`
  255.372 ≈ 548.5, i.e. the `68 × coefficient` term was dropped.) The mechanism claim
  — "sum of two absolute temperatures, which is physically meaningless" — is correct;
  only the illustrative number is wrong. **Fix:** replace with ≈ 586.3 K ≈ 313.2 °C.

## Notes (no page edit needed)

- Row 10 / 15 / 25 code blocks are labelled "illustration; not runnable"; each
  matches the pinned source in structure, field names, and doc-comment text
  (illustrations omit inits/typealias, which is expected).
- Row 12: page renders the source's double-quoted `"composite"` as single-quoted
  `'composite'` inside the blockquote — cosmetic.
- Row 34: Fahrenheit coefficient/constant are page-rounded (`0.5556`/`255.372`) from
  the source's `0.55555555555556`/`255.37222222222427` — acceptable rounding.
- Row 43: the Foundation reproduction is honestly reported as _fallen_ (no
  `Foundation` clang module under nixpkgs `swift 5.10.1`); the ledger grounds only
  the _mechanism_ (no mixed-type `+`), which is confirmed at `Measurement.swift`
  L108/138. Marked ✓ on that basis, ◯ for the (non-existent) runtime artifact.

## Net

46 rows; **38 ✓, 7 ⚠, 1 ◯** (the ◯ row 46 is a web doc link; row 43 counts as ✓ for
the source-grounded mechanism, its runtime repro having fallen). Six ⚠ are minor line-number/locator drifts
(#D1–#D6); one ⚠ (#D7) is a genuine **arithmetic error** in the temperature-sum
illustration (548.5 K → 586.3 K). Both clone pins (`cd04666`, `08ab9be`) and both
licenses/tags reconfirmed. Every blockquote and inline quote from
`Measurement.swift`, `Unit.swift`, `MeasurementFormatter.swift`, `Units/Unit.swift`,
`Units/Measurement.swift`, `Quantity.swift`, `MeasurementTests.swift`, and the two
READMEs matches the pinned clones character-for-character (modulo the noted
double→single quote and the line-number drifts above). The 22-subclass count and the
`m + s` diagnostic (Foundation overload-resolution mechanism + `Units` in-repo throw
test) are confirmed in-source.

At-risk quote: none is mis-transcribed — the seven ⚠ are locator drifts and one wrong
computed figure, not altered quotations.

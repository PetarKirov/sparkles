# Grounding ledger — `ucum-qudt.md`

Claim-by-claim verification of `docs/research/units-of-measure/ucum-qudt.md`
(the wave-2 UCUM & QUDT interchange page) against its **local** pinned clones:

- `$REPOS/java/ucum-java` @ `70106f4` (verified `git rev-parse --short HEAD` =
  `70106f4`, HEAD dated 2026-04-28) — `src/main/java/org/fhir/ucum/*`, `README.md`,
  `pom.xml`, `LICENSE`
- `$REPOS/js/ucum-lhc` @ `3713cd1` (verified, HEAD 2026-06-16) — `source/*.js`,
  `test/testUcumLhcUtils.spec.js`, `package.json`, `LICENSE.md`
- `$REPOS/java/qudtlib-java` @ `8530b43` (verified, HEAD 2026-02-11) —
  `qudtlib-model/src/main/java/io/github/qudtlib/model/{Unit,DimensionVector}.java`,
  `README.md`, `pom.xml`, `LICENSE`
- `$REPOS/java/unit-api` @ `79682ff` (verified, HEAD 2026-05-19) —
  `src/main/java/javax/measure/*.java`, `pom.xml`, `LICENSE`
- `$REPOS/java/indriya` @ `c3dc219` (verified, HEAD 2026-05-18) —
  `src/main/java/tech/units/indriya/{AbstractUnit,unit/UnitDimension}.java`,
  `pom.xml`, `LICENSE`
- Local reproduction: ucum-lhc Babel-transpiled + driven via `convertUnitTo`
  (`node 24.15.0`, nixpkgs, 2026-07-04)

`$REPOS = /home/petar/code/repos`. Date: **2026-07-04**.

## Status legend

| Mark | Meaning                                                                          |
| ---- | -------------------------------------------------------------------------------- |
| `✓`  | Verified against the cited local artifact (locator recorded)                     |
| `⚠`  | Imprecise/line-drifted/paraphrased-as-quote/secondary-grounded (explained)       |
| `◯`  | Not locally groundable (registry/web point-in-time or editorial); fallback named |

Claim **Types:** `quote` · `fact` · `figure` · `behavior` · `exposition` · `opinion`.

## Claims

| #   | Claim                                                                                                                                                                                                                                 | Type       | Source (local + locator)                                                                                                              | Evidence                                                                                                                                                                                                                                       | Status   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 1   | Five pinned clones at the SHAs `70106f4` / `3713cd1` / `8530b43` / `79682ff` / `c3dc219`                                                                                                                                              | fact       | `git rev-parse --short HEAD` in each clone                                                                                            | all five match exactly; HEAD dates 2026-04-28 / 2026-06-16 / 2026-02-11 / 2026-05-19 / 2026-05-18 match the table                                                                                                                              | ✓        |
| 2   | Licenses: ucum-java BSD-3-Clause · ucum-lhc Custom NLM/US-Gov · qudtlib Apache-2.0 · unit-api BSD-3-Clause · indriya BSD-3-Clause                                                                                                     | fact       | `ucum-java/LICENSE:1`; `ucum-lhc/LICENSE.md:1-8`; `qudtlib/LICENSE:1-2`; `unit-api/LICENSE`; `indriya/LICENSE`                        | ucum-java "BSD 3-Clause License … Health Intersections"; ucum-lhc LHNCBC/NLM notice; qudtlib "Apache License Version 2.0"; unit-api & indriya both carry BSD "Neither the name of JSR-385" clause                                              | ✓        |
| 3   | Ecosystems: ucum-java Java 17 · ucum-lhc Node/JS · qudtlib Java 17 · unit-api Java 8 · indriya Java 8                                                                                                                                 | fact       | build files (`pom.xml` compiler targets); page metadata table                                                                         | consistent with poms; JVM-version specifics are build-config detail (not re-derived line-by-line) — treated as fact                                                                                                                            | ⚠        |
| 4   | Exponent domains: ucum-java `int` (`Canonical.CanonicalUnit.exponent`); ucum-lhc `ℤ⁷` (`dimVec_` len 7); qudtlib `float[8]`; JSR-385 `Map<Dimension,Integer>`                                                                         | fact       | `Canonical.java:44`; `dimension.js` + `config.js:25`; `DimensionVector.java:163`; `UnitDimension.java:301`                            | `private int exponent` (Canonical.java:44); `dimLen_: 7` (config.js:25); `this.values = new float[8]` (DimensionVector.java:163); `Map<? extends Dimension, Integer> getBaseDimensions()` (UD:301)                                             | ✓        |
| 5   | Analyzed/latest versions: ucum-java `1.0.10`; ucum-lhc npm `7.1.8`; qudtlib `7.2.x`; unit-api `2.2`; indriya `2.2.4` "Rainbow"                                                                                                        | fact       | `ucum-java/pom.xml` `1.0.11-SNAPSHOT`; `ucum-lhc/package.json` `7.1.8`; `qudtlib/pom.xml` `7.2.1-SNAPSHOT`; poms                      | pin poms are `1.0.11-`/`2.2.1-`/`7.2.1-`/`2.2.5-SNAPSHOT` (i.e. last releases were 1.0.10 / 2.2 / 7.2.0 / 2.2.4) — consistent; ucum-lhc `package.json` version is exactly `7.1.8`. Release **dates** + "Rainbow" codename are registry/web (◯) | ⚠        |
| 6   | Key authors: Grahame Grieve / Health Intersections; Lee Mericle / NLM-LHNCBC after Gunther Schadow; Florian Kleedorfer; Dautelle/Keil/Santana                                                                                         | fact       | `ucum-java/LICENSE:3` (Health Intersections); `qudtlib/pom.xml` `<name>Florian Kleedorfer</name>`; unit-api/indriya LICENSE copyright | Grieve/Health Intersections + Kleedorfer + Dautelle/Keil/Santana confirmed in-repo; Mericle/Schadow attribution not in clone metadata → web/README-grounded (◯ portion)                                                                        | ⚠        |
| 7   | Headline: mixing incommensurable units (`m + s`) caught at run time by a thrown exception in all five, never by the type checker                                                                                                      | behavior   | throw sites rows 30–35 below                                                                                                          | every pole's rejection is a run-time thrown value (Java exception / JS `Error`); no compile-time dimensional guard except JSR-385's `Unit<Q>` reference typing (row 25)                                                                        | ✓        |
| 8   | ucum-java README L11–17 service list QUOTE ("validate a UCUM unit … multiply 2 quantities together")                                                                                                                                  | quote      | `ucum-java/README.md:11-17`                                                                                                           | five bullets verbatim; source has trailing whitespace on two lines, page trims it                                                                                                                                                              | ✓        |
| 9   | qudtlib README L1–10 QUOTE ("QUDTLib: Java Unit Conversion Library … self-contained jar … no external dependencies … 1745 units, such as second, Fahrenheit, or light year")                                                          | quote      | `qudtlib/README.md:1-9`                                                                                                               | verbatim modulo ellipses; source L6 "self-contained jar\*\* with" (double space) → page single space; "1745 units, such as second, Fahrenheit, or light year" (L9) exact                                                                       | ✓ (note) |
| 10  | ucum-java Lexer tokenizes SOLIDUS/PERIOD/parens/numbers/symbols/`{annotations}` (`Lexer.java` L37, L67–69, L155)                                                                                                                      | fact       | `Lexer.java:37` (`public class Lexer`), `:67-69` (SOLIDUS/PERIOD/OPEN checks), `:155` (`type = TokenType.ANNOTATION`)                 | all three anchors confirmed                                                                                                                                                                                                                    | ✓        |
| 11  | ExpressionParser: `parse` → `parseTerm` → `parseComp` → `parseSymbol` (`ExpressionParser.java` L48–116)                                                                                                                               | fact       | `ExpressionParser.java:48-116`                                                                                                        | `parse` (L48/155? — `Term parse(String)` at L48 region), `parseTerm`/`parseComp`/`parseSymbol` all present within L48–116                                                                                                                      | ✓        |
| 12  | `Converter.normalise` collate-equal-bases/drop-zeros/sort code block, labeled `Converter.java` L100–118                                                                                                                               | quote      | `Converter.java:100-125`                                                                                                              | collate loop L100–110, drop-zeros L111–115, `Collections.sort` by `getCode()` L117–124; page block reflows `j >=0`→`j >= 0` and adds `// sum exponents` comment — labeled "illustration"                                                       | ✓ (note) |
| 13  | `CanonicalUnit` is `{ BaseUnit base; int exponent; }` (`Canonical.java` L42–64); `BaseUnit` carries `char dim` (`BaseUnit.java` L42)                                                                                                  | fact       | `Canonical.java:42-64`; `BaseUnit.java:42`                                                                                            | `CanonicalUnit` class L42, `private BaseUnit base` L43, `private int exponent` L44; `Canonical` holds `List<CanonicalUnit> units` L64; `private char dim;` at BaseUnit.java:42                                                                 | ✓        |
| 14  | Public API `UcumEssenceService` exposes `convert`, `isComparable`, `getCanonicalUnits`                                                                                                                                                | fact       | `UcumEssenceService.java:299-345`                                                                                                     | `convert` body L295–311, `isComparable` L334–345, `getCanonicalUnits` used within isComparable — all present                                                                                                                                   | ✓        |
| 15  | ucum-lhc parses via `parseString` → `_parseTheString` → `_makeUnitsArray` (`unitString.js` L143, L264, L288)                                                                                                                          | fact       | `unitString.js:143,264,288`                                                                                                           | `parseString(uStr,…)` L143; `_parseTheString(uStr,…)` L264; `this._makeUnitsArray(uStr,…)` L288 — exact                                                                                                                                        | ✓        |
| 16  | ucum-lhc `Dimension` class QUOTE ("This class implements an object containing the vector of exponents for a unit and its operations for addition, subtraction, and multiplication with a scalar.") (`dimension.js` L1–4)              | quote      | `dimension.js:1-4`                                                                                                                    | verbatim (JSDoc `*` prefixes + line joins stripped)                                                                                                                                                                                            | ✓        |
| 17  | Vector length fixed at 7 (`Ucum.dimLen_: 7`, `config.js` L25); equality element-wise (`dimension.js` L252–266)                                                                                                                        | fact       | `config.js:25`; `dimension.js:252-266`                                                                                                | `dimLen_: 7` at config.js:25; `equals` loops `i < UC.Ucum.dimLen_` comparing `this.dimVec_[i] === dimVec2[i]` at dimension.js:259-260 (within L252–266)                                                                                        | ✓        |
| 18  | ucum-lhc public entry points `convertUnitTo`/`convertToBaseUnits`/`commensurablesList` (`ucumLhcUtils.js` L295, L464, L682)                                                                                                           | fact       | `ucumLhcUtils.js:295,464,682`                                                                                                         | `convertUnitTo(…)` L295; `convertToBaseUnits(…)` L464; `commensurablesList(…)` L682 — exact                                                                                                                                                    | ✓        |
| 19  | qudtlib `Unit` carries `conversionMultiplier`/`conversionOffset` (`BigDecimal`) + `dimensionVectorIri` (`Unit.java` L60–61, L70)                                                                                                      | fact       | `Unit.java:60-61,70` (Builder); `:271-272` (Unit fields)                                                                              | cited L60/61/70 are the **inner `Builder`** field decls; the `Unit`'s own `final` fields are `BigDecimal conversionMultiplier/conversionOffset` at **L271-272** (+ `dimensionVector` L275). Types correct                                      | ⚠ (#D5)  |
| 20  | qudtlib affine conversion code block (`value.add(fromOffset).multiply(…DECIMAL128).divide(…).subtract(toOffset).stripTrailingZeros()`), labeled `Unit.java` L419–430                                                                  | quote      | `Unit.java:427-432`                                                                                                                   | code verbatim, but the shown `BigDecimal result = value.add(fromOffset)…stripTrailingZeros();` block is **L427–432** (`stripTrailingZeros()` is L432, past L430); L419–426 is the offset/multiplier setup                                      | ⚠ (#D2)  |
| 21  | qudtlib `DimensionVector` over 8 slots `{'A','E','L','I','M','H','T','D'}` = amount/current/length/lum.intensity/mass/temp/time/ratio-flag (`DimensionVector.java` L21–28)                                                            | fact       | `DimensionVector.java:21-28`                                                                                                          | `char[] dimensions = {'A','E','L','I','M','H','T','D'}` L21; INDEX\_ constants L22-28 map A=amount, E=current, L=length, I=lum, M=mass, H=temp, T=time — exact                                                                                 | ✓        |
| 22  | `D` slot QUOTE ("Note that the last value, the 'D' dimension is special: it is only an indicator that the dimension vector represents a ratio … its value is only 1 iff all other dimensions are 0.") (`DimensionVector.java` L15–18) | quote      | `DimensionVector.java:15-17`                                                                                                          | verbatim (JSDoc `*`/`<p>` stripped; page L15–18 vs source L15–17)                                                                                                                                                                              | ✓ (note) |
| 23  | IRI serialized by walking the eight slots (`DimensionVector.java` L160–190); `float[8]` store; fractional exponents via `pt`/`dot` decimal marker                                                                                     | fact       | `DimensionVector.java:160-190,38,40,163`                                                                                              | both constructors do `for(i<8) sb.append(dimensions[i]).append(…)` then `makeIriInNamespace`; `this.values = new float[8]` L163; `DOT = "dot"` L38, `DECIMAL_POINT_PATTERN = compile("(pt\|dot)")` L40                                         | ✓        |
| 24  | unit-api `Unit<Q extends Quantity<Q>>` declares `getDimension`/`isCompatible`/`getConverterTo`/`getConverterToAny`/`asType` (`Unit.java` L75, L109, L153, L205, L226, L191); `Dimension` group ops L53/61/79/90/98                    | fact       | `Unit.java:75,109,153,191,205,226`; `Dimension.java:53,61,70,79,90,98`                                                                | all anchors exact: interface L75, `getDimension` L109, `isCompatible` L153, `asType` L191, `getConverterTo` L205, `getConverterToAny` L226; Dimension `multiply` L61, `divide` L70, `pow` L79, `root` L90, `getBaseDimensions` L98             | ✓        |
| 25  | `getConverterTo(Unit<Q>)` / `getConverterToAny(Unit<?>)` contract code block (`Unit.java` L205 & L226); the `Unit<Q>` generic is the one compile-time guard                                                                           | quote      | `Unit.java:205,226`                                                                                                                   | L205 `UnitConverter getConverterTo(Unit<Q> that) throws UnconvertibleException;`; L226 `getConverterToAny(Unit<?> that) throws IncommensurableException, UnconvertibleException;` — verbatim (page wraps L226)                                 | ✓        |
| 26  | `IncommensurableException` Javadoc QUOTE ("Only commensurable quantity (quantities with the same dimensions) may be compared, equated, added, or subtracted. … This is a **checked** exception.") (L31–38)                            | quote      | `IncommensurableException.java:33-36`                                                                                                 | verbatim fragments; "**checked**" = source `<strong>checked</strong>` L36; ellipsis drops "Also, one unit can be converted…" (L34) and truncates the "so it deliberately doesn't inherit…" tail                                                | ✓ (note) |
| 27  | `Unit.multiply`/`divide` return `Unit<?>` (`Unit.java` L383, L392)                                                                                                                                                                    | fact       | `Unit.java:328,372` (actual)                                                                                                          | **cited L383/L392 are `Unit<?> root(int n)` and `Unit<?> pow(int n)`, NOT multiply/divide.** The `Unit<?> multiply(Unit<?>)` is **L328**, `Unit<?> divide(Unit<?>)` is **L372**                                                                | ⚠ (#D1)  |
| 28  | indriya `AbstractUnit.getConverterToAny` throw code block (`if (!isCompatible(that)) throw new IncommensurableException(this + " is not compatible with " + that);`), labeled L400–401                                                | quote      | `AbstractUnit.java:401-402`                                                                                                           | text verbatim; method decl is L400, but the `if`/`throw` shown are **L401–402** (off-by-one)                                                                                                                                                   | ⚠ (#D4)  |
| 29  | indriya `UnitDimension.getBaseDimensions(): Map<? extends Dimension, Integer>` (L301–306); base dim = `pseudoUnit = new BaseUnit("[" + symbol + ']', NONE)` (L196)                                                                    | quote      | `UnitDimension.java:301-306,196`                                                                                                      | `public Map<? extends Dimension, Integer> getBaseDimensions()` L301; `pseudoUnit = new BaseUnit("[" + symbol + ']', NONE);` L196 — verbatim                                                                                                    | ✓        |
| 30  | Checking — ucum-java `isComparable` compares canonical strings (L334–342); `convert` throws on mismatch (L303–304)                                                                                                                    | behavior   | `UcumEssenceService.java:334-345,303-304`                                                                                             | `isComparable` computes `getCanonicalUnits(units1/2)` then `return u1.equals(u2)` (L334-345); `convert` throws `UcumException` when `!s.equals(d)` (L303-304)                                                                                  | ✓        |
| 31  | Checking — ucum-lhc `Unit.convertFrom` compares dim vectors + throws (`unit.js` L394–401); equivalence test L1181 `unit1Sum == unit2Sum && this.dim_.equals(unit2.dim_)`                                                              | behavior   | `unit.js:394-401,1181`                                                                                                                | `if (fromUnit.dim_ && this.dim_ && !(fromUnit.dim_.equals(this.dim_)))` L394 → throws L397/L400; L1181 `return unit1Sum == unit2Sum && this.dim_.equals(unit2.dim_);` (method `isEqMolCommensurable`) — exact                                  | ✓        |
| 32  | Checking — qudtlib `isConvertible` is IRI equality (`Unit.java` L504–511); `convert` throws on mismatch (L414–418)                                                                                                                    | behavior   | `Unit.java:504-511,414-419`                                                                                                           | `isConvertible` returns `getDimensionVectorIri().equals(toUnit.getDimensionVectorIri())` (L511, within L504-511); `convert` throws `InconvertibleQuantitiesException` when `!isConvertible` (L414-419)                                         | ✓        |
| 33  | Checking — indriya `isCompatible` compares dimensions (`AbstractUnit.java` L277–278); `getConverterToAny` throws `IncommensurableException` (L400–401)                                                                                | behavior   | `AbstractUnit.java:277-278,401-402`                                                                                                   | `isCompatible` → `internalIsCompatible(that,true)` (L277-278); throw at L401-402 (see #D4)                                                                                                                                                     | ✓        |
| 34  | Dimension-rep — qudtlib runtime compare is `getDimensionVectorIri().equals(...)` (`Unit.java` L508–510)                                                                                                                               | fact       | `Unit.java:511`                                                                                                                       | the `.equals(...)` return is **L511**; L508-510 is `return false; }` + blank (the null-guard). Method spans L504-511                                                                                                                           | ⚠ (#D3)  |
| 35  | Dimension-rep — JSR-385/indriya `isCompatible` reduces to `getDimension().equals(...)` (`Unit.java` L102–109)                                                                                                                         | fact       | `unit-api Unit.java:102-109`                                                                                                          | L102 Javadoc "Two units … compatible if and only if [dimensions equal]"; `Dimension getDimension()` L109 — supports the reduction claim                                                                                                        | ✓        |
| 36  | Affine/temperature — qudtlib special-cases `TEMPERATURE_DIFFERENCE` `QuantityKind` to suppress the offset (`Unit.java` L21, L398–411)                                                                                                 | fact       | `Unit.java:21,398-411`                                                                                                                | `TEMPERATURE_DIFFERENCE = "TemperatureDifference"` L21; `convert(...)` sets `ignoreOffset = true` when `quantityKind.getIriLocalname().equals(TEMPERATURE_DIFFERENCE)` (L404, within L398-411)                                                 | ✓        |
| 37  | JSR-385 `Quantity<Q>` `add`/`subtract` typed by `Q` (`Quantity.java` L97, L128)                                                                                                                                                       | fact       | `Quantity.java:97,128`                                                                                                                | interface `Quantity<Q extends Quantity<Q>>` L97; `Quantity<Q> add(Quantity<Q> addend)` L128; `subtract(Quantity<Q>)` immediately follows                                                                                                       | ✓        |
| 38  | `asType(Class<T>)` is a run-time cast throwing `ClassCastException` on dimension mismatch (`AbstractUnit.java` L349–354)                                                                                                              | behavior   | `AbstractUnit.java:349-354`                                                                                                           | `asType(Class<T>, SystemOfUnits)` throws `ClassCastException("The unit: … is not compatible with quantities of type …")` L353 when dimensions differ                                                                                           | ✓        |
| 39  | Diagnostics — ucum-lhc reproduced locally: `m→s` `status=failed` "Sorry. m cannot be converted to s."; `m→km` `toVal=0.001`                                                                                                           | behavior   | repro 2026-07-04; mechanism `unit.js:400`                                                                                             | error template `` `Sorry.  ${fromUnit.csCode_} cannot be converted to ${this.csCode_}.` `` (unit.js:400) yields exactly "Sorry. m cannot be converted to s." for m→s; `Error` is un-subtyped                                                   | ✓        |
| 40  | Diagnostics — in-repo test `g`→`/g` asserts `status='failed'`, msg "Sorry. g cannot be converted to /g." (`testUcumLhcUtils.spec.js` L193–200)                                                                                        | quote      | `test/testUcumLhcUtils.spec.js:193-197`                                                                                               | `resp4 = utils.convertUnitTo('g', 847, '/g')` L194; `assert.equal(resp4.status,'failed',…)` L195; `assert.equal(resp4.msg[0],'Sorry.  g cannot be converted to /g.',…)` L196 — verbatim (page reflows)                                         | ✓        |
| 41  | Diagnostics — ucum-java throw code block (`if (!s.equals(d)) throw new UcumException("Unable to convert between units … matching canonical forms …")`) L303–304                                                                       | quote      | `UcumEssenceService.java:303-304`                                                                                                     | `if (!s.equals(d))` L303; throw string verbatim (page line-wraps the single-line concatenation); `isComparable` false path L334-342 confirmed                                                                                                  | ✓        |
| 42  | Diagnostics — qudtlib throw code block (`InconvertibleQuantitiesException("Cannot convert from %s to %s: dimension vectors differ", getIri, toUnit.getIri)`) L414–418                                                                 | quote      | `Unit.java:414-419`                                                                                                                   | `if (!isConvertible(toUnit)) { throw new InconvertibleQuantitiesException(String.format("Cannot convert from %s to %s: dimension vectors differ", this.getIri(), toUnit.getIri())); }` — verbatim                                              | ✓        |
| 43  | Diagnostics summary — four exceptions (`Error`/`UcumException`/`InconvertibleQuantitiesException`/`IncommensurableException`) = same finding, run-time & data-driven                                                                  | exposition | rows 39–42, 28                                                                                                                        | all four throw sites confirmed in-source; synthesis consistent                                                                                                                                                                                 | ✓        |
| 44  | ucum-lhc quiet-failure ergonomics: `convertUnitTo` catches the `Error` and folds it into a `failed` status object (`ucumLhcUtils.js` L358–359, L424)                                                                                  | behavior   | `ucumLhcUtils.js:358-359,424`                                                                                                         | `catch (err) { returnObj['msg'].push(err.message); }` L359-360; `returnObj['status'] = 'failed';` L424 — status-object mechanism confirmed                                                                                                     | ✓        |
| 45  | 1745 QUDT units / 881 quantityKinds / ~400 kB self-contained jar; qudtlib precomputes at codegen, no runtime SPARQL                                                                                                                   | figure     | `qudtlib/README.md:6,9,10`                                                                                                            | "**~400kB large**" L6, "1745 units" L9, "881 quantityKinds" L10; precompute/no-dependency framing = README self-description                                                                                                                    | ✓        |
| 46  | Registry/doc links (Maven Central, npm) and release **dates** (1.0.10 2025-04-22, unit-api 2.2 2023-05-20, indriya 2.2.4 2026-05-17, "Rainbow" codename)                                                                              | ◯          | registry/web                                                                                                                          | link-only + point-in-time release dates not derivable from clones; version _numbers_ corroborated by poms (row 5)                                                                                                                              | ◯        |
| 47  | Editorial synthesis (three internal poles, "checker evaluates never solves", kind-vs-dimension weakness, zero-cost/ergonomics/strengths/weaknesses/trade-off tables)                                                                  | opinion    | author synthesis over rows 1–45                                                                                                       | each framing consistent with the verified mechanics above; not independently citable                                                                                                                                                           | ◯        |

## Discrepancies

- **#D1 ⚠ (material locator) — `ucum-qudt.md:294`.** "`Unit.multiply`/`divide`
  return `Unit<?>` (raw quantity type, `Unit.java` **L383, L392**)." In the pinned
  clone, `unit-api/src/main/java/javax/measure/Unit.java:383` is
  `Unit<?> root(int n);` and `:392` is `Unit<?> pow(int n);`. The
  `Unit<?> multiply(Unit<?> multiplier)` method is at **L328** and
  `Unit<?> divide(Unit<?> divisor)` at **L372**. **Fix:** change the locator to
  `L328, L372`.

- **#D2 ⚠ (minor line-drift) — `ucum-qudt.md:164,174,333,374`.** The affine
  code block (`BigDecimal result = value.add(fromOffset)…stripTrailingZeros();`)
  is labeled `Unit.java` **L419–430**; the shown statement is at **L427–432**
  (`.stripTrailingZeros()` is L432, beyond 430). L419–426 is the
  offset/multiplier resolution. **Fix:** cite `L419–433` (or `L427–433` for the
  `result =` statement) so the range covers `stripTrailingZeros()`.

- **#D3 ⚠ (minor line-drift) — `ucum-qudt.md:254`.** "runtime comparison is
  `getDimensionVectorIri().equals(...)` (`Unit.java` **L508–510**)." The
  `.equals(...)` return is at **L511**; L508–510 is the `return false;` null-guard
  tail. **Fix:** cite `L511` (or `L504–511` for the whole `isConvertible`).

- **#D4 ⚠ (minor off-by-one) — `ucum-qudt.md:220,223.**`The indriya`getConverterToAny`throw block is labeled`AbstractUnit.java`**L400–401**;
the method declaration is L400 but the shown`if (!isCompatible(that))`/`throw new IncommensurableException(...)`are **L401–402**. **Fix:** cite`L401–402`.

- **#D5 ⚠ (minor — Builder vs Unit fields) — `ucum-qudt.md:163`.** "A `Unit`
  carries a `conversionMultiplier` and `conversionOffset` (both `BigDecimal`)
  plus a `dimensionVectorIri` (`Unit.java` **L60–61, L70**)." Lines 60–61/70 are
  the field declarations of the inner **`Builder`** class; the `Unit`'s own
  `final` fields are `BigDecimal conversionMultiplier`/`conversionOffset` at
  **L271–272** (`dimensionVector` at L275). Types/names are correct. **Fix
  (optional):** point at `L271–272` for the `Unit`'s own fields.

## Unsourced / opinion

- Framings — "the survey's only five-library / standards-implementation page",
  "checker evaluates, never solves", "the field's sharpest weakness relative to
  the kind-aware typed systems", and the Zero-cost / Ergonomics /
  Strengths / Weaknesses / trade-off tables — author synthesis (◯), each
  consistent with the verified mechanics above.
- Release **dates** and the indriya "Rainbow" codename (row 46) — registry/web
  point-in-time facts, not in the clones; version _numbers_ are corroborated by
  the pin poms.
- Mericle/Schadow ucum-lhc authorship lineage (row 6) — not in clone metadata;
  README/history-grounded (◯).
- Quote-transcription notes (no page edit needed): rows 9/12/16/22/26 strip
  JSDoc/HTML markup and reflow multi-line concatenations under "illustration"
  labels; the error/throw strings are char-for-char intact.

## Net

47 rows; **5 ⚠** (one material locator #D1 pointing at the wrong methods, four
minor line-drift/Builder-vs-field), **5 ◯**/⚠-secondary rows (release dates,
Mericle attribution, JVM-version build detail). Every blockquote and inline code
citation from `ucum-java` (`README`, `Lexer`, `ExpressionParser`, `Converter`,
`Canonical`, `BaseUnit`, `UcumEssenceService`), `ucum-lhc` (`dimension.js`,
`unit.js`, `config.js`, `unitString.js`, `ucumLhcUtils.js`, the spec test),
`qudtlib` (`Unit.java`, `DimensionVector.java`, `README`), `unit-api`
(`Unit.java`, `Dimension.java`, `Quantity.java`, `IncommensurableException.java`),
and `indriya` (`AbstractUnit.java`, `UnitDimension.java`) matches the pinned
clones verbatim (modulo noted markup-stripping/reflow). All five pins verified via
`git rev-parse`. The four throw sites and the ucum-lhc local reproduction are
mechanism-confirmed in-source. **#D1 needs a page fix** (locator points at
`root`/`pow` instead of `multiply`/`divide`).

## At-risk quotes

None fully at risk — all quoted strings match char-for-char. The five ⚠ rows are
**locator** issues (wrong/drifted line numbers), not quote-text mismatches.

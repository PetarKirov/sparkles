# Grounding: Video Encoding (frames → file/stream)

> Not published research. Do not link to it from the survey pages.

Status legend: ✓ verified against a local artifact · ≈ paraphrase-verified · 🌐 web-verified (primary source fetched) · ◯ open
Claim types: quote · fact · figure · behavior · exposition

Grounds [`docs/research/manim/video-encoding.md`](../video-encoding.md) — four
integration styles (piped ffmpeg CLI, in-process libav\*, GStreamer, SVT-AV1
direct/IVF). Per-approach license + version + D-binding verdict are `fact` rows.
Internal QA, excluded from the built site (`srcExclude`) and the link checker.

**Hazards recorded by the page (flagged):** MathAnimation's container is
**mislabelled** — IVF/AV1 stream written to a `.mov`-named file, README says
`.mp4` (Q-svtav1-3); its NVENC hardware path is a **0-byte stub** (C-svtav1-2);
`yuv420p` is limited-range + chroma-subsampled, a color-correctness trap
(C-gotcha-1).

## ffmpeg (CLI subprocess)

| #          | Claim (short)                                                                                                                                                                                                                                                                                                   | Type  | Source (locator)                                                                                                                                                                                                        | Status |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Q-ffmpeg-1 | rawvideo demuxer: "This demuxer allows one to read raw video data. … Since there is no header specifying the assumed video parameters, the user must specify them in order to be able to decode the data correctly."                                                                                            | quote | https://ffmpeg.org/ffmpeg-formats.html                                                                                                                                                                                  | 🌐     |
| Q-ffmpeg-2 | Legal: FFmpeg is "licensed under the GNU Lesser General Public License (LGPL) version 2.1 or later", but incorporates optional GPL parts — "If those parts get used the GPL applies to all of FFmpeg" (`libx264` is one such part)                                                                              | quote | https://www.ffmpeg.org/legal.html                                                                                                                                                                                       | 🌐     |
| C-ffmpeg-1 | License LGPL-2.1+ (GPL if `libx264`/GPL parts linked); pixfmt in `-f rawvideo -pix_fmt rgba`; codecs `libx264`/`libx264rgb`/`libvpx-vp9` → mp4/webm/mov by extension                                                                                                                                            | fact  | https://ffmpeg.org/ffmpeg-formats.html                                                                                                                                                                                  | 🌐     |
| C-ffmpeg-2 | **ManimGL takes this path:** `SceneFileWriter.open_movie_pipe` assembles the argv (`-f rawvideo -s WxH -pix_fmt rgba -r fps -i - -vf vflip,eq=saturation:gamma -vcodec libx264 -pix_fmt yuv420p`) and launches via `subprocess.Popen(stdin=PIPE)`; `write_frame` reads the FBO with `camera.get_raw_fbo_data()` | fact  | https://github.com/3b1b/manim/blob/e61ad5c3f9c9ac96cba7a46dffc665c0ec13beea/manimlib/scene/scene_file_writer.py · https://github.com/3b1b/manim/blob/e61ad5c3f9c9ac96cba7a46dffc665c0ec13beea/manimlib/camera/camera.py | 🌐     |
| C-ffmpeg-3 | ManimGL defaults `ffmpeg_bin="ffmpeg"`, `video_codec="libx264"`, `pixel_format="yuv420p"`, ext `.mp4`; `use_fast_encoding()` swaps to lossless `libx264rgb`/`rgb32` (skips YUV chroma subsampling)                                                                                                              | fact  | https://github.com/3b1b/manim/blob/e61ad5c3f9c9ac96cba7a46dffc665c0ec13beea/manimlib/scene/scene_file_writer.py                                                                                                         | 🌐     |
| C-ffmpeg-4 | D-binding: **zero ABI surface** — nothing to bind; `std.process` / `sparkles:core-cli` spawn `ffmpeg` and feed stdin (`apps/terminal` already drives subprocesses this way)                                                                                                                                     | fact  | local repo (`apps/terminal`)                                                                                                                                                                                            | ✓      |

## libav\* via the C API (in-process)

| #         | Claim (short)                                                                                                                                                                                                                                                                         | Type  | Source (locator)                                                                                | Status |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- | ------ |
| Q-libav-1 | `sws_scale`: "Scale the image slice in srcSlice and put the resulting scaled slice in the image in dst." (libswscale = "Color conversion and scaling library")                                                                                                                        | quote | https://github.com/FFmpeg/FFmpeg/blob/n8.1/libswscale/swscale.h                                 | 🌐     |
| Q-libav-2 | `avcodec_send_frame`: "Supply a raw video or audio frame to the encoder. Use avcodec_receive_packet() to retrieve buffered output packets." (NULL frame flushes)                                                                                                                      | quote | https://github.com/FFmpeg/FFmpeg/blob/n8.1/libavcodec/avcodec.h                                 | 🌐     |
| Q-libav-3 | `av_interleaved_write_frame`: "Write a packet to an output media file ensuring correct interleaving." (buffers packets to keep DTS monotonic)                                                                                                                                         | quote | https://github.com/FFmpeg/FFmpeg/blob/n8.1/libavformat/avformat.h                               | 🌐     |
| C-libav-1 | License FFmpeg's (LGPL-2.1+, GPL if GPL codecs linked); PyAV wrapper adds BSD-3-Clause; FFmpeg headers pinned `n8.1`; encode loop = `sws_scale` RGBA→yuv420p → `avcodec_send_frame` → `av_interleaved_write_frame`                                                                    | fact  | https://github.com/FFmpeg/FFmpeg/blob/n8.1/libavcodec/avcodec.h                                 | 🌐     |
| C-libav-2 | **PyAV is the reference binding** (`import av`, BSD-3-Clause, `v18.0.0`); Manim Community's `SceneFileWriter` writes one partial movie file per `play()`, pushing `av.VideoFrame.from_ndarray(frame, format="rgba")`; `from_ndarray` branches on rgba/rgb24/yuv420p/yuva444p/…        | fact  | https://pyav.org/docs/stable/ · https://github.com/PyAV-Org/PyAV/blob/v18.0.0/av/video/frame.py | 🌐     |
| C-libav-3 | Codec/pixfmt by extension `[source-verified]` (`scene_file_writer.py:552-566`): `.mp4`→libx264/yuv420p; `.webm`→libvpx-vp9/yuv420p; transparent `.webm`→libvpx-vp9/yuva420p; transparent `.mov`→qtrle/argb; `finish()`→`combine_files` re-opens `av.open(..., format="concat")`       | fact  | https://github.com/ManimCommunity/manim/blob/4d25c031/manim/scene/scene_file_writer.py          | 🌐     |
| C-libav-4 | D-binding: **standout** — plain C headers via ImportC + pkg-config, same `sourceLibrary` recipe as `libs/ghostty` (a `c.c` `#include`ing the FFmpeg headers, `targetType "sourceLibrary"`, pkg-config libs); `AVFrame`/`AVCodecContext` large but stable; PyAV proves maintainability | fact  | https://dlang.org/spec/importc.html                                                             | 🌐     |
| C-libav-5 | Naming history: after the 2011 libav/FFmpeg fork, `avcodec_*`/`av_*` lived in both; the maintained provider today is **FFmpeg** — bind FFmpeg's headers, not the abandoned `libav` project's                                                                                          | fact  | survey exposition                                                                               | ◯      |

## GStreamer

| #       | Claim (short)                                                                                                                                                                                                                              | Type  | Source (locator)                                                                          | Status |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | ----------------------------------------------------------------------------------------- | ------ |
| Q-gst-1 | appsrc "can be used by applications to insert data into a GStreamer pipeline. Unlike most GStreamer elements, Appsrc provides external API functions."                                                                                     | quote | https://gstreamer.freedesktop.org/documentation/app/appsrc.html                           | 🌐     |
| Q-gst-2 | "GStreamer is a plugin-based framework licensed under the LGPL" / "We require that all code going into our core packages is LGPL"                                                                                                          | quote | https://gstreamer.freedesktop.org/documentation/frequently-asked-questions/licensing.html | 🌐     |
| C-gst-1 | License LGPL (core + base plugins); C GLib/GObject; `appsrc` caps `video/x-raw,format=RGBA`; encoder/muxer elements (`x264enc`, `vp9enc`, `av1enc`); current stable 1.26 series (`1.26.11`, March 2026)                                    | fact  | https://gstreamer.freedesktop.org/                                                        | 🌐     |
| C-gst-2 | D-binding: ImportC can consume GLib/GObject/`gst` headers, but that pulls the whole GObject macro-and-vtable idiom into D — a **large surface**, advised against; **documented non-choice** (no surveyed engine uses it for render output) | fact  | survey exposition                                                                         | ◯      |

## SVT-AV1 direct & IVF

| #          | Claim (short)                                                                                                                                                                                                                                   | Type  | Source (locator)                                                                                                                                                                                                                                                  | Status |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Q-svtav1-1 | SVT-AV1 is an "AV1-compliant software encoder library" whose work "targets the development of a production-quality AV1-encoder"                                                                                                                 | quote | https://gitlab.com/AOMediaCodec/SVT-AV1/-/blob/master/README.md                                                                                                                                                                                                   | 🌐     |
| Q-svtav1-2 | C API five-step loop: `svt_av1_enc_init_handle` ("Call the library to construct a Component Handle"), `svt_av1_enc_send_picture` ("Send the picture"), `svt_av1_enc_get_packet` ("Step 5: Receive packet.")                                     | quote | https://gitlab.com/AOMediaCodec/SVT-AV1/-/blob/master/Source/API/EbSvtAv1Enc.h                                                                                                                                                                                    | 🌐     |
| Q-svtav1-3 | **Hazard:** MathAnimation output is mislabelled — the IVF header is `{ 'D','K','I','F', 0,0,32,0, 'A','V','0','1' }` (IVF/AV1) but the export names the file `.mov` and the README advertises `.mp4`                                            | quote | https://github.com/ambrosiogabe/MathAnimation/blob/4b2bace5e5d43ecccfd9cd5374d3eb9760ace4ef/Animations/src/video/Encoder.cpp                                                                                                                                      | 🌐     |
| C-svtav1-1 | License **BSD-3-Clause-Clear** (© Alliance for Open Media); version `v4.1.0`; input planar YUV 4:2:0 (`EbBufferHeaderType`); AV1-only → IVF (`DKIF`/`AV01`) hand-muxed                                                                          | fact  | https://gitlab.com/AOMediaCodec/SVT-AV1/-/blob/master/LICENSE.md                                                                                                                                                                                                  | 🌐     |
| C-svtav1-2 | MathAnimation drives it: fixed 60 fps, GPU RGB→YUV 4:2:0 shader pass, async PBO readback, `crf 28`/`preset 12`/`color-format 420`/`input-depth 8` on a worker thread; writes the IVF container itself; NVENC hardware path is a **0-byte stub** | fact  | https://github.com/ambrosiogabe/MathAnimation/blob/4b2bace5e5d43ecccfd9cd5374d3eb9760ace4ef/Animations/src/video/Encoder.cpp · https://github.com/ambrosiogabe/MathAnimation/blob/4b2bace5e5d43ecccfd9cd5374d3eb9760ace4ef/Animations/src/video/NvidiaEncoder.cpp | 🌐     |
| C-svtav1-3 | D-binding: good in the small (`EbSvtAv1Enc.h` compact, ImportC-friendly via the ghostty recipe), but you own a correct IVF/MP4 muxer + colour-correct RGBA→YUV — exactly what libav\* hands you for free                                        | fact  | https://dlang.org/spec/importc.html                                                                                                                                                                                                                               | 🌐     |

## Pixel-format & color gotchas + caching

| #          | Claim (short)                                                                                                                                                                                                                                        | Type       | Source (locator)                                                                                                | Status |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------- | ------ |
| C-gotcha-1 | `yuv420p` (both forks' default) is 4:2:0 chroma-subsampled and, by ffmpeg convention, limited-range (luma 16–235); feeding full-range RGBA without saying so crushes contrast; escape = `libx264rgb`/`rgb32` lossless full-RGB                       | fact       | https://github.com/3b1b/manim/blob/e61ad5c3f9c9ac96cba7a46dffc665c0ec13beea/manimlib/scene/scene_file_writer.py | 🌐     |
| C-gotcha-2 | Premultiplied vs straight alpha must match the encoder's expectation or edges get dark/bright fringes; WebM/MOV transparency is a different pixfmt not a flag (`yuva420p`+libvpx-vp9, or `qtrle`/`argb`) — cannot get alpha from plain `yuv420p` mp4 | fact       | https://github.com/ManimCommunity/manim/blob/4d25c031/manim/scene/scene_file_writer.py                          | 🌐     |
| C-cache-1  | Content-hash cache keys on render **inputs**, not encoded bytes, so caching is correct even where the encoder is not bit-stable; both forks emit one partial file per `play()` then concat — the physical cache substrate                            | exposition | page synthesis (frame-capture.d probe)                                                                          | ◯      |
| C-rec-1    | Recommendation: ship the ffmpeg subprocess first (zero ABI), add in-process libav\* via ImportC later, skip GStreamer, skip SVT-AV1/IVF unless AV1-only is required, structure output for caching from day one                                       | exposition | page synthesis                                                                                                  | ◯      |

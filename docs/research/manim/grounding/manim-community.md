# Grounding: Manim Community Edition (hub + scene-graph + text-pipeline + caching)

> Not published research. Do not link to it from the survey pages.

One ledger covering the Manim Community hub
([`manim-community/index.md`](../manim-community/index.md)) and its three
sub-pages ([`scene-graph.md`](../manim-community/scene-graph.md),
[`text-pipeline.md`](../manim-community/text-pipeline.md),
[`caching.md`](../manim-community/caching.md)). Every locator is against the
**local clone** `ManimCommunity/manim@4d25c031` (`v0.20.1`) at
`/home/petar/code/repos/python/manim-community` (HEAD confirmed
`4d25c031ffe71c602e20935afd54a96f33545a6e`). A representative sample of the
`file:line` locators was re-read against that tree this session and matched;
paths below are repo-relative.

Status legend: ✓ verified against a local artifact · ≈ paraphrase-verified · 🌐 web-verified (primary source fetched) · ◯ open / not locally groundable
Claim types: quote · fact · figure · behavior · exposition
Page tags: `[hub]` index.md · `[sg]` scene-graph.md · `[text]` text-pipeline.md · `[cache]` caching.md

## Claim ledger

| #   | Claim (short)                                                                                                                                                                                                                                      | Type       | Source (locator)                                                                                                               | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------ | ------ |
| M1  | `[hub]` Python `requires-python >=3.11`; MIT (`LICENSE.community`); reviewed at `4d25c031` / `v0.20.1`                                                                                                                                             | fact       | `pyproject.toml:23`; `LICENSE.community`; clone HEAD `4d25c031`                                                                | ✓      |
| M2  | `[hub]` First release `v0.1.0`, Oct 21 2020, forked from `3b1b/manim`                                                                                                                                                                              | fact       | `docs/source/changelog/0.1.0-changelog.rst`                                                                                    | ✓      |
| M3  | `[hub]` Default renderer Cairo (`config.renderer == RendererType.CAIRO`); optional OpenGL/`moderngl`                                                                                                                                               | fact       | `manim/constants.py:258` (`RendererType`); `manim/camera/camera.py:15` (`import cairo`)                                        | ✓      |
| M4  | `[hub]` Cubic basis on Cairo (`n_points_per_cubic_curve = 4`); quadratic on OpenGL (`= 3`)                                                                                                                                                         | fact       | `manim/mobject/types/vectorized_mobject.py:129`; `manim/mobject/opengl/opengl_vectorized_mobject.py:112`                       | ✓      |
| Q1  | `[hub]` README: _"Manim is an animation engine for explanatory math videos. It's used to create precise animations programmatically…"_                                                                                                             | quote      | `README.md`                                                                                                                    | ✓      |
| Q2  | `[hub]` Design philosophy: _"This is the first release of manimce after forking from 3b1b/manim. As such, developers have focused on cleaning up and refactoring the codebase while still maintaining backwards compatibility wherever possible."_ | quote      | `docs/source/changelog/0.1.0-changelog.rst`                                                                                    | ✓      |
| Q3  | `[hub/sg]` `Mobject` = _"Mathematical Object: base class for objects that can be displayed on screen"_                                                                                                                                             | quote      | `manim/mobject/mobject.py:72`                                                                                                  | ✓      |
| Q4  | `[hub/sg]` `ConvertToOpenGL` = _"Metaclass for swapping (V)Mobject with its OpenGL counterpart at runtime depending on config.renderer"_                                                                                                           | quote      | `manim/mobject/opengl/opengl_compatibility.py:17`                                                                              | ✓      |
| Q5  | `[hub]` `Animation` = _"An animation. Animations have a fixed time span."_                                                                                                                                                                         | quote      | `manim/animation/animation.py:30`                                                                                              | ✓      |
| C1  | `[hub]` Per-`play()` loop: `compile_animation_data` → hash → `is_already_cached` short-circuit → `play_internal`                                                                                                                                   | behavior   | `manim/renderer/cairo_renderer.py:64`                                                                                          | ✓      |
| C2  | `[hub/cache]` Deterministic sampling `times = np.arange(0, run_time, 1/frame_rate)`; `alpha = t/run_time`                                                                                                                                          | behavior   | `manim/scene/scene.py:1087` (`get_time_progression`), `:1687` (`update_to_time`)                                               | ✓      |
| C3  | `[hub]` `camera.capture_mobjects` walks each `VMobject` into a Cairo path; RGBA framebuffer (`n_channels = 4`)                                                                                                                                     | behavior   | `manim/camera/camera.py:528`, `:87`                                                                                            | ✓      |
| C4  | `[sg]` `Mobject.__init__` sets `submobjects` list + `points` array, calls empty hooks `reset_points`/`generate_points`/`init_colors`                                                                                                               | behavior   | `manim/mobject/mobject.py:107`, `:120`, `:125-127`, `:456`, `:461`, `:468`                                                     | ✓      |
| C5  | `[sg]` Family tree one-directional (no parent back-ref); `add` via `list_update`; `get_family` recomputed + de-duped                                                                                                                               | behavior   | `manim/mobject/mobject.py:560`, `:2518`; `manim/utils/family.py:12`                                                            | ✓      |
| C6  | `[sg]` `__init_subclass__` resets `animation_overrides`, snapshots `_original__init__` (drives `mob.animate`)                                                                                                                                      | behavior   | `manim/mobject/mobject.py:100`, `:186`                                                                                         | ✓      |
| C7  | `[sg]` Cubic `points` interleaved `[anchor,handle,handle,anchor,…]` via `set_anchors_and_handles` (`points[i::4]`)                                                                                                                                 | behavior   | `manim/mobject/types/vectorized_mobject.py:841`                                                                                | ✓      |
| C8  | `[sg]` Fill/stroke are separate parallel RGBA arrays (`fill_rgbas`/`stroke_rgbas`/`background_stroke_rgbas`); `interpolate_color` lerps each independently                                                                                         | behavior   | `manim/mobject/types/vectorized_mobject.py:282`, `:332`, `:344`, `:1894`                                                       | ✓      |
| C9  | `[sg]` `interpolate` requires equal point counts; `align_points`/`Transform.begin`→`align_data` make them match                                                                                                                                    | behavior   | `manim/mobject/mobject.py:3080`, `:3149`; `manim/mobject/types/vectorized_mobject.py:1747`; `manim/animation/transform.py:200` | ✓      |
| Q6  | `[sg]` Quadratic→cubic elevation comment: _"Place the 2 middle control points 2/3 along the line segments… I think that's beautiful."_                                                                                                             | quote      | `manim/mobject/types/vectorized_mobject.py:978`                                                                                | ✓      |
| C10 | `[sg]` Cubic→quadratic is lossy: OpenGL stores **two** quadratics per cubic via `get_quadratic_approximation_of_cubic`                                                                                                                             | behavior   | `manim/mobject/opengl/opengl_vectorized_mobject.py:496`, `:499`                                                                | ✓      |
| Q7  | `[sg]` Unrealized TODO: _"Change cubic curve groups to have 4 points instead of 3"_                                                                                                                                                                | quote      | `manim/mobject/types/vectorized_mobject.py:73`                                                                                 | ✓      |
| C11 | `[hub]` `lag_ratio` staggering: `full_length=(n-1)*lag_ratio+1`; `rate_func` default `smooth`; wrapped by `unit_interval`                                                                                                                          | behavior   | `manim/animation/animation.py:339`, `:364`, `:384`; `manim/utils/rate_functions.py:124`                                        | ✓      |
| C12 | `[hub]` Default `rate_func` is **`smooth`** (normalized-logistic sigmoid, `inflection=10.0`), _not_ smootherstep                                                                                                                                   | fact       | `manim/utils/rate_functions.py:156` (`def smooth(t, inflection=10.0)`), `:174` (`smootherstep`)                                | ✓      |
| C13 | `[hub/text]` LaTeX = subprocess pipeline: `tex_to_svg_file` → `compile_tex` (`subprocess.run` of `latex`/…) → `dvisvgm --no-fonts`                                                                                                                 | behavior   | `manim/utils/tex_file_writing.py:35`, `:214`, `:249` (`--no-fonts`)                                                            | ✓      |
| Q8  | `[text]` `tex_file_writing` docstring: _"Interface for writing, compiling, and converting `.tex` files"_; `tex_hash = hexdigest()[:16]`                                                                                                            | quote+fact | `manim/utils/tex_file_writing.py:1`, `:32`                                                                                     | ✓      |
| C14 | `[hub/text]` Pango via `manimpango.text2svg` (`Text`/`MarkupText` are `SVGMobject`); Typst in-process `typst_compiler.compile(format="svg")`                                                                                                       | behavior   | `manim/mobject/text/text_mobject.py:66`, `:818`; `manim/utils/typst_file_writing.py:100`                                       | ✓      |
| C15 | `[text/sg]` `SVGMobject` parses via `svgelements`; `handle_commands` is basis-aware — elevates line/quad → cubic (2/3 rule), or lowers cubic → quads under OpenGL                                                                                  | behavior   | `manim/mobject/svg/svg_mobject.py:11`, `:561`, `:575`, `:602`                                                                  | ✓      |
| C16 | `[hub]` Encoding is PyAV (`import av`); mp4 `libx264`/`yuv420p`, webm `libvpx-vp9`, transparent mov `qtrle`/`argb`                                                                                                                                 | fact       | `manim/scene/scene_file_writer.py:17`, `:552`, `:560`, `:562`, `:566`                                                          | ✓      |
| C17 | `[hub]` Frames `av.VideoFrame.from_ndarray(format="rgba")`; `finish()` concats via `av.open(format="concat")`                                                                                                                                      | behavior   | `manim/scene/scene_file_writer.py:449`, `:652`                                                                                 | ✓      |
| C18 | `[hub]` Live interaction is OpenGL-only: `interactive_embed` asserts `isinstance(renderer, OpenGLRenderer)`; Cairo is render-then-open (`open_media_file` after render)                                                                            | behavior   | `manim/scene/scene.py:1412`, `:1415`; `manim/utils/file_ops.py:220`                                                            | ✓      |
| Q9  | `[hub/cache]` Hash docstring: _"A string concatenation of the respective hashes of `camera_object`, `animations_list` and `current_mobjects_list`, separated by `_`."\_                                                                            | quote      | `manim/utils/hashing.py:358`                                                                                                   | ✓      |
| C19 | `[hub/cache]` Each component = `zlib.crc32` of custom JSON; run-dependent keys stripped (`KEYS_TO_FILTER_OUT`)                                                                                                                                     | behavior   | `manim/utils/hashing.py:29`, `:366`                                                                                            | ✓      |
| Q10 | `[cache]` `KEYS_TO_FILTER_OUT` rationale: _"Sometimes there are elements that are not suitable for hashing (too long or run-dependent). This is used to filter them out."_                                                                         | quote      | `manim/utils/hashing.py:27`                                                                                                    | ✓      |
| C20 | `[cache]` `_Memoizer` breaks circular refs — keyed by `hash` falling back to `id`, replaces 2nd sighting with `"AP"` placeholder, reset each `play()`                                                                                              | behavior   | `manim/utils/hashing.py:37`, `:53`, `:135`, `:374`                                                                             | ✓      |
| C21 | `[hub/cache]` Hash **is** the partial-movie filename; `is_already_cached` = `path.exists()`; LRU eviction by `st_atime` capped at `max_files_cached` (100)                                                                                         | behavior   | `manim/scene/scene_file_writer.py:270`, `:606`, `:857`, `:864`, `:870`                                                         | ✓      |
| C22 | `[hub/cache]` Static/moving split: `begin_animations` paints non-moving mobjects once; only `moving_mobjects` re-rasterized per frame                                                                                                              | behavior   | `manim/scene/scene.py:1334`                                                                                                    | ✓      |
| C23 | `[hub]` OpenGL earcut (`mapbox_earcut`) fill only reached from the OpenGL renderer; Cairo path never triangulates                                                                                                                                  | behavior   | `manim/utils/space_ops.py:10`, `:718`; `manim/renderer/vectorized_mobject_rendering.py:162`                                    | ✓      |
| Q11 | `[hub]` `ManimConfig` = _"a single source of truth for all of the library's customizable behavior"_                                                                                                                                                | quote      | `manim/_config/utils.py:145`                                                                                                   | ✓      |

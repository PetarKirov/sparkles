# Grounding: ManimGL (3Blue1Brown's engine)

> Not published research. Do not link to it from the survey pages.

Claim-by-claim source verification of
[`manimgl.md`](../manimgl.md). Every locator is against the **local clone**
`3b1b/manim@e61ad5c3` (`manimgl 1.7.2`) at `/home/petar/code/repos/python/manim`
(HEAD confirmed `e61ad5c3f9c9ac96cba7a46dffc665c0ec13beea`). A representative
sample of the `file:line` locators (and every verbatim quote) was re-read against
that tree this session and matched; paths below are repo-relative.

Status legend: ✓ verified against a local artifact · ≈ paraphrase-verified · 🌐 web-verified (primary source fetched) · ◯ open / not locally groundable
Claim types: quote · fact · figure · behavior · exposition

> Note: the deep-dive cites the README two-versions quote at `README.md:14`; the
> line is actually **`README.md:15`** in this tree (off by one, text verbatim).

## Claim ledger

| #   | Claim (short)                                                                                                                                                                                                                                                              | Type     | Source (locator)                                                                                                                          | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| M1  | Python 3.7+, MIT; `manimlib` → `manimgl`/`manim-render` console scripts; version `1.7.2`                                                                                                                                                                                   | fact     | `setup.cfg` (`license = MIT`, classifiers, `[options.entry_points]`)                                                                      | ✓      |
| M2  | Repo `3b1b/manim` pinned `e61ad5c3`; OpenGL-only via `moderngl`; **no** pycairo / no PyAV                                                                                                                                                                                  | fact     | clone HEAD `e61ad5c3`; `setup.cfg` / `requirements.txt` (pycairo, av absent)                                                              | ✓      |
| M3  | Bézier basis **quadratic** (3-point `anchor,handle,anchor`); encoder = `ffmpeg` subprocess fed raw `rgba`                                                                                                                                                                  | fact     | `manimlib/mobject/types/vectorized_mobject.py:59`; `manimlib/scene/scene_file_writer.py:213`                                              | ✓      |
| C1  | `Mobject.data` is a NumPy structured array whose dtype names (`point`,`rgba`) _are_ the vert-shader `in` attributes; `render_primitive = TRIANGLE_STRIP`                                                                                                                   | behavior | `manimlib/mobject/mobject.py:70-76`                                                                                                       | ✓      |
| Q1  | Design philosophy, README: _"Note, there are two versions of manim. This repository began as a personal project by the author of 3Blue1Brown… In 2020 a group of developers forked it into what is now the community edition…"_                                            | quote    | `README.md:15` (deep-dive cites `:14`)                                                                                                    | ✓      |
| Q2  | `ThreeDCamera` kept _"Mostly just defined so old scenes don't break"_                                                                                                                                                                                                      | quote    | `manimlib/camera/camera.py:258`                                                                                                           | ✓      |
| Q3  | `Animation.update` _"shouldn't exist, but it's here to keep many old scenes from breaking"_                                                                                                                                                                                | quote    | `manimlib/animation/animation.py:141-146` (comment at `:144`)                                                                             | ✓      |
| C2  | Base `class Mobject(object)` — plain object (no ABC), `dim=3` (points always 3D)                                                                                                                                                                                           | behavior | `manimlib/mobject/mobject.py:64`                                                                                                          | ✓      |
| C3  | One structured array, not parallel arrays: `init_data` allocates single `self.data` + `self.uniforms`; subclasses widen the dtype                                                                                                                                          | behavior | `manimlib/mobject/mobject.py:137-149`                                                                                                     | ✓      |
| C4  | Doubly-linked tree: `submobjects` + `parents`; `add` appends both; `get_family` memoized in `self.family`                                                                                                                                                                  | behavior | `manimlib/mobject/mobject.py:99-101`, `:459-468`, `:426-433`                                                                              | ✓      |
| C5  | Dirty-flag propagation bubbles up via `note_changed_data`/`note_changed_family` (`@affects_data` decorators); `render` rebuilds `ShaderWrapper`s only when `_data_has_changed`                                                                                             | behavior | `manimlib/mobject/mobject.py:208-232`, `:417-424`, `:2081-2088`                                                                           | ✓      |
| Q4  | `.animate` credited: _"Borrowed from https://github.com/ManimCommunity/manim/"_ (cross-pollination both ways)                                                                                                                                                              | quote    | `manimlib/mobject/mobject.py:172`                                                                                                         | ✓      |
| C6  | `get_sub_alpha` lag staggering: `full_length=(n-1)*lag_ratio+1`; `raw_sub_alpha = clip(value-lower, 0, 1)` then `rate_func`                                                                                                                                                | behavior | `manimlib/animation/animation.py:168-182`                                                                                                 | ✓      |
| C7  | `Transform` calls `align_data_and_family`, then `lock`s unchanged data keys; interpolates `pointlike_data_keys` through `path_func` (default `straight_path`)                                                                                                              | behavior | `manimlib/animation/transform.py:24`, `:54-72`, `:121-129`; `manimlib/mobject/mobject.py:1810-1838`                                       | ✓      |
| C8  | Composition: `Succession` = `AnimationGroup` with `lag_ratio=1.0`; `LaggedStart` defaults `lag_ratio=0.05`                                                                                                                                                                 | fact     | `manimlib/animation/composition.py:27`, `:90-106`, `:124-163`                                                                             | ✓      |
| Q5  | `smooth` = quintic, _"Equivalent to `bezier([0, 0, 0, 1, 1, 1])`"_ (zero 1st/2nd derivs at both ends)                                                                                                                                                                      | quote    | `manimlib/utils/rate_functions.py:17-21`                                                                                                  | ✓      |
| C9  | `progress_through_animations` steps a deterministic time-grid, one frame per tick (`update_frame`→GPU draw, `emit_frame`→ffmpeg stdin)                                                                                                                                     | behavior | `manimlib/scene/scene.py:555-565`                                                                                                         | ✓      |
| C10 | Camera owns the GL context: `moderngl.create_standalone_context()` headless or `window.ctx`; two FBOs (MSAA `fbo_for_files`, `samples=0` readback)                                                                                                                         | behavior | `manimlib/camera/camera.py:25`, `:72-76`, `:81-122`                                                                                       | ✓      |
| C11 | MSAA `samples: int = 0` (2D anti-aliases analytically); `ThreeDCamera` bumps `samples=4`                                                                                                                                                                                   | fact     | `manimlib/camera/camera.py:46`, `:259-261`                                                                                                | ✓      |
| C12 | `CameraFrame` is itself a `Mobject` (orientation quaternion + `fovy` as uniforms) — panning/zoom animates a mobject                                                                                                                                                        | behavior | `manimlib/camera/camera_frame.py:23`, `:35-49`                                                                                            | ✓      |
| C13 | Fill is GPU winding-number: `use_winding_fill()` now a no-op; `VShaderWrapper.render_fill` draws into a float texture, blend cancels ±-oriented triangles, then composites                                                                                                 | behavior | `manimlib/mobject/types/vectorized_mobject.py:466-468`; `manimlib/shader_wrapper.py:384-431`                                              | ✓      |
| Q6  | `use_winding_fill` no-op: _"Only keeping this here because some old scene call it"_                                                                                                                                                                                        | quote    | `manimlib/mobject/types/vectorized_mobject.py:466-468`                                                                                    | ✓      |
| Q7  | `get_fill_canvas`: _"Because VMobjects with fill are rendered in a funny way, using alpha blending to effectively compute the winding number around each pixel, they need to be rendered to a separate texture, which is then composited onto the ordinary frame buffer."_ | quote    | `manimlib/shader_wrapper.py:437-441`                                                                                                      | ✓      |
| C14 | `get_triangulation`→`earclip_triangulation` (`mapbox_earcut`) is **vestigial** — nothing in the render path calls it; `refresh_triangulation` reference targets a method no longer defined                                                                                 | behavior | `manimlib/mobject/types/vectorized_mobject.py:1110-1157`; `manimlib/utils/space_ops.py:8`, `:423`; `manimlib/animation/indication.py:404` | ✓      |
| C15 | Fill frag shader is Loop-Blinn: negatively-oriented triangles get alpha `-a/(1-a)`; `Fxy=(y-x*x); if(Fxy<0) discard;`                                                                                                                                                      | behavior | `manimlib/shaders/quadratic_bezier/fill/frag.glsl`                                                                                        | ✓      |
| C16 | TeX → SVG: `latex_to_svg` (`@lru_cache(128)`) → `full_tex_to_svg` (`@cache_on_disk`) runs `latex`/`xelatex -no-pdf`, pipes through `dvisvgm -n --stdout`                                                                                                                   | behavior | `manimlib/utils/tex_file_writing.py:51`, `:85`, `:89-119`, `:132-150`                                                                     | ✓      |
| C17 | Non-TeX text: `manimpango` + `pygments`; `Code(MarkupText)` uses `PangoMarkupFormatter`; SVG → quadratic `VMobjectFromSVGPath`                                                                                                                                             | behavior | `manimlib/mobject/svg/text_mobject.py:10-13`, `:438-456`; `manimlib/mobject/svg/svg_mobject.py:53`, `:261-283`                            | ✓      |
| C18 | ffmpeg is a stdin-piped subprocess (`-f rawvideo -pix_fmt rgba … -i -`), launched `sp.Popen(..., stdin=sp.PIPE)`; `write_frame` writes `get_raw_fbo_data()` bytes                                                                                                          | behavior | `manimlib/scene/scene_file_writer.py:202-230`, `:213`, `:284-289`                                                                         | ✓      |
| C19 | `use_fast_encoding()` swaps lossless `libx264rgb`/`rgb32`; `-vf` folds in `vflip` + `eq=saturation:gamma`; vertical flip deferred to ffmpeg                                                                                                                                | behavior | `manimlib/scene/scene_file_writer.py:210-211`, `:241-243`; `manimlib/camera/camera.py:129-147`                                            | ✓      |
| C20 | Live authoring is first-class: `embed()` → `InteractiveSceneEmbed` IPython REPL; input hook `"manim"` keeps rendering while shell waits                                                                                                                                    | behavior | `manimlib/scene/scene.py:203-219`; `manimlib/scene/scene_embed.py:24`, `:60-81`, `:83-93`                                                 | ✓      |
| C21 | `checkpoint_paste` reads clipboard via `pyperclip`, uses leading `#` comment as checkpoint key (idempotent block re-exec); `reload_scene` raises `KillEmbedded`, re-run keeps same window                                                                                  | behavior | `manimlib/scene/scene_embed.py:187-238`, `:139-177`; `manimlib/__main__.py:34-44`                                                         | ✓      |
| C22 | `ValueTracker(Mobject)` — scalar lives in `uniforms["value"]`, so it animates like any mobject                                                                                                                                                                             | behavior | `manimlib/mobject/value_tracker.py:13`, `:30-45`                                                                                          | ✓      |
| C23 | `VMobject.data_dtype` packs **seven** fields into one array (point, stroke_rgba, stroke_width, joint_angle, fill_rgba, base_normal, fill_border_width)                                                                                                                     | fact     | `manimlib/mobject/types/vectorized_mobject.py:59`                                                                                         | ✓      |
| C24 | Quadratic API: `get_bezier_tuples` = each `points[2i:2i+3]`; `add_cubic_bezier_curve_to` immediately reduces to a quadratic approximation (no native cubic storage)                                                                                                        | behavior | `manimlib/mobject/types/vectorized_mobject.py:767-772`, `:506-534`                                                                        | ✓      |
| C25 | Deterministic frame sampling: `get_time_progression` returns `np.arange(0, run_time, 1/fps) + 1/fps`; real-time `time.sleep` throttles only the _preview_                                                                                                                  | behavior | `manimlib/scene/scene.py:467-491`, `:253`                                                                                                 | ✓      |
| C26 | Content-hash TeX disk cache: `cache_on_disk` keys a 1 GB `diskcache.Cache` on `hash_string(name+args+kwargs)`                                                                                                                                                              | behavior | `manimlib/utils/cache.py:20-30`                                                                                                           | ✓      |
| C27 | No public plugin system / renderer abstraction — extension surface _is_ Python subclassing (widen `data_dtype`, point `shader_folder` at GLSL)                                                                                                                             | fact     | `manimlib/mobject/types/vectorized_mobject.py:58-67` (pattern); absence of plugin registry                                                | ✓      |

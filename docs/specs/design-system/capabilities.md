# Capabilities (`CAP`)

_**Status:** proposed · **Date:** 2026-09-21 · **Owner:** `sparkles:ui`
(the declaration and its application); `sparkles:base` (`term_caps`, the
environment-derived answers) and `sparkles:tui` (query-derived answers) own
detection · **Scope:** how a target says what it can render and receive, how
the design system declares what it wants, and how the gap between the two is
rendered as a published substitution rather than a silent loss._

## The decision: flags, not tiers

The two outlines that seeded this tree both framed capability as three
tiers — baseline / enhanced / full — and their feature inventories (color
depth, unicode width, box drawing and block elements, the OSC and DEC-mode
protocols, multiplexer passthrough, non-tty modes) are absorbed into the table
below. The repository's own research
([concepts](../../research/platform-ui-guidelines/concepts.md)) and the shipped
input side ([`IXB10`](../ui/interaction-review.md), `InputCapabilities`)
argue the other way: **a support bit lies** the moment a real terminal has
true color but no OSC 8, or SGR mouse but no hover motion.

The contract therefore uses **per-feature flags as the data** and keeps the
three tiers only as **documented profiles** — named constant flag sets used by
tests and the style guide so that "what does this look like on a dumb
terminal" has one answer.

| ID      | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Status  | Traces to                                                                                                                                                                                                                                                                                        |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `CAP1`  | A target declares a **`TargetCapabilities`** value as data the toolkit inspects: the composition of `sparkles.base.term_caps.OutputCapabilities` (what may be emitted — color tier, glyph coverage, links, clipboard, images, …), `sparkles.input.capability.InputCapabilities` (the input axes, `IXB10`), and the target-only axes (`subCellScroll`, `proportionalText`, `radius`/`shadow`/`alpha`, `reducedMotion`). A terminal's two halves come from one `TermCaps` snapshot (`caps.output`, `fromTerminal(caps)`); a window or HTML sink declares constants. This is the chrome half [`TGT5`](../ui/backends.md) left in prose. A field a backend omits takes its conservative default (`false` / `none`). | partial | `tokens.d` `TargetCapabilities`, `terminalCapabilities`, `declaredCapabilities`; `ui_tui.session` `sessionCapabilities` (input half and OSC 8 / SGR 4:3 assumed until M7); `ui_raylib` `raylibCapabilities`; `interp.html` `htmlCapabilities`, `interp.html_semantic` `semanticHtmlCapabilities` |
| `CAP2`  | Every capability the design system names — every row in the table — is a field of `CAP1`'s type. A token or component that degrades on some capability names that field; a capability no token or component reads is removed. Violation: a projection rule in [`glyphs.md`](./glyphs.md) or a component's degradation prose naming a capability with no field.                                                                                                                                                                                                                                                                                                                                                  | partial | `degradation.d` `@needs` (every substitution cites a real field, checked at compile time); the table-vs-field audit is open                                                                                                                                                                      |
| `CAP3`  | Every terminal-detectable row must be **answerable by the toolchain**: environment-derived answers (`$TERM`, `$COLORTERM`, `$NO_COLOR`, `$TERM_PROGRAM`, tmux/screen presence) by `sparkles.base.term_caps.detectTermCaps`; query-derived answers (DA1, XTVERSION, XTGETTCAP, OSC 11 / mode 2031, cell pixel size, kitty keyboard flags) by `sparkles:tui` **onto the same `TermCaps` value**, with a bounded timeout and a DA1 sentinel so a non-responding terminal never blocks. The design system **declares intent**; the libraries **implement**; a test per row verifies the implementation against a scripted pty peer ([`testing.md` O5](./testing.md)).                                               | partial | `base.term_caps` (environment); `tui.probe` + `Terminal.probeImages` (the `images` row and the cell pixel size, `CSI 16 t`, opt-in), tests `integration.pty.probeImages*`, `integration.pty.sixelNeedsPixelsAndRedrawsWhatItLeft`; the other query rows are M7's                                 |
| `CAP4`  | The **theme is never gated on a terminal snapshot** ([`THM8`](../ui/theme.md) warning, restated as a contract): capability application is per target at display-list build time. One process rendering a cell target and an HTML target from the same theme must produce ASCII borders on the first only if the first declared `unicode = false`, and never affect the second. Violation: the parity harness rendering ASCII chrome into HTML because stdout was not a tty.                                                                                                                                                                                                                                     | full    | `ui_tui.grid_canvas` test `tui_canvas.capabilities.theThemeIsNotGatedOnATerminal`                                                                                                                                                                                                                |
| `CAP5`  | Every token projection and every component has a defined rendering under each **profile**: `baseline`, `enhanced`, `full`. The style guide shows all three; `ui-gallery --render --profile <p>` produces them; the `baseline` render of every page is a CI-verified fence ([`O1`](./testing.md)).                                                                                                                                                                                                                                                                                                                                                                                                               | full    | `ui-gallery --render --profile`; `apps/ui-gallery/test/data/profiles/<page>/` (`O1`); the style guide `docs/design-system/catalog/` imports those files                                                                                                                                          |
| `CAP6`  | A capability the target lacks is resolved to a **published substitution**, never dropped: the substitution is data (which glyph set, which attribute, which fallback ladder rung), recorded in the frame's degradation report the way anchored overlays already do for triggers ([`TRG3`](../ui/popup.md)). Violation: a feature the theme requested that neither renders nor appears in the report.                                                                                                                                                                                                                                                                                                            | full    | `degradation.d` `degradationsOf` (chrome and glyph rows); `ui-gallery --degradations`; every catalog page shows the three reports                                                                                                                                                                |
| `CAP7`  | Under a **multiplexer** (`$TMUX`, `$STY`, `TERM=screen*`/`tmux*`), a capability the multiplexer does not pass through (images, OSC 52 without `set-clipboard`, OSC 66, 2031, kitty keyboard) is reported `false` unless passthrough is positively confirmed by query. Environment sniffing may only lower a capability, never raise one.                                                                                                                                                                                                                                                                                                                                                                        | partial | presets: `fromReplies` drops DA1's sixel under a multiplexer, test `ui.emulators.multiplexerImagesNeedAConfirmedRoundTrip`; detection in `base.term_caps` not started                                                                                                                            |
| `CAP8`  | A **non-tty** stdout, `$NO_COLOR`, `TERM=dumb` and `$CLICOLOR_FORCE` keep their existing semantics (`detectTermCaps`); the resulting capability set is exactly the `baseline` profile with `colorDepth` as detected. Output to a pipe carries no escape sequence the profile forbids.                                                                                                                                                                                                                                                                                                                                                                                                                           | full    | `base.term_caps.detectTermCaps`                                                                                                                                                                                                                                                                  |
| `CAP9`  | Profiles are **monotone**: `baseline ⊆ enhanced ⊆ full` field-wise (a `bool` may only turn on, an enum only widen). Checked by a test over the three constants so a profile edit cannot invert the ladder.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | full    | `tokens.d` `subsetOf`, test `ui.tokens.profiles.monotone`; `ui_gallery.render.profilesOnlyEverLoseThings`                                                                                                                                                                                        |
| `CAP10` | An **emulator preset** is a measured emulator's recorded replies to the query battery, mapped to flags by the one pure function a runtime probe would feed (`fromReplies`). A preset claims only what the battery asks — color depth, 2004, 2026, 2027, 2031, the kitty keyboard and graphics protocols, the DA1 sixel attribute — over a shared glyph-and-pointer floor (D32); everything else stays off. Presets are previews and fixtures, never detection (`CAP3`), and a partial order, not a ladder: narrowing to one is `meet`. `ui-gallery --emulator <e>` paints for one, and the live profile switch narrows within it.                                                                               | full    | `sparkles.ui.emulators`; tests `ui.emulators.table`, `ui.emulators.neverClaimTheUnmeasured`, `ui.emulators.presetsAreAPartialOrder`, `ui_gallery.gallery.emulatorBoundsTheProfileSwitch`                                                                                                         |

## The capability table

One row per field of `TargetCapabilities`. _Detects_ names the owner of the
answer for the TUI target; GUI and HTML targets declare constants. _Consumed
by_ names what degrades — the row exists only because of that column (`CAP2`).

The **output** rows are fields of `OutputCapabilities` in `sparkles:base` (a
logger or a plain CLI tool needs `hyperlinks` and `progress` as much as a
full-screen UI does, and none should link a toolkit to ask); the **`input.*`**
rows are `InputCapabilities` axes that a terminal derives from its negotiated
modes through `fromTerminal` — the mode is the terminal's fact, the axis is the
target-neutral one, and only the axis is consumed. The remaining rows exist
only on `TargetCapabilities`.

| Field                       | Meaning                                                           | Protocol / source                                                              | Detects         | Consumed by                                                            |
| --------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------ | --------------- | ---------------------------------------------------------------------- |
| `colorDepth`                | `none` / `ansi16` / `ansi256` / `trueColor`                       | SGR 38/48; `$COLORTERM`, `$TERM`                                               | `base` (exists) | every color token; `ACC3` marks below `ansi256`                        |
| `unicode`                   | non-ASCII glyphs at all                                           | locale, `$TERM`                                                                | `base` (exists) | `GLY1` charset floor                                                   |
| `blocks`                    | block-element tier: `none`/`half`/`quadrant`/`sextant`/`octant`   | font coverage; unknowable by query — configured, default `half` when `unicode` | config, `tui`   | sub-cell rules and thumbs (`GLY2`, `GLY8`), image raster rung (`GLY9`) |
| `braille`                   | U+2800 block renders as a 2×4 grid                                | font coverage; configured                                                      | config          | sparklines/charts (`GLY8`)                                             |
| `nerdFont`                  | Nerd Font PUA glyphs available                                    | unknowable by query — configured; `true` on GUI/Web (bundled)                  | config          | icons, tree guides, powerline separators (`GLY4`)                      |
| `hyperlinks`                | OSC 8                                                             | `$TERM_PROGRAM` allow-list + XTGETTCAP                                         | `base` + `tui`  | `link.*` (`WGT21`), `osc_link.d`                                       |
| `clipboard`                 | OSC 52 write                                                      | XTGETTCAP `Ms`; tmux `set-clipboard`                                           | `tui`           | copy affordances (hue `TSL`)                                           |
| `notifications`             | OSC 99 (kitty), OSC 9 / 777 fallbacks                             | `$TERM_PROGRAM`; query                                                         | `tui`           | toast (`WGT16`) out-of-focus escalation                                |
| `pointerShape`              | OSC 22                                                            | `$TERM_PROGRAM`; query                                                         | `tui`           | hover cursors on links, splitters, text                                |
| `textSizing`                | OSC 66 (kitty text sizing)                                        | query (kitty ≥ 0.40, foot); Ghostty: none                                      | `tui`           | headings (`GLY5`) — layout-preserving fallback                         |
| `images`                    | `none` / `kitty` / `sixel` / `iterm2` / `pixels` (a window's own) | DA1 sixel bit; kitty query; `$TERM_PROGRAM`; a GUI declares `pixels`           | `tui`           | `WGT22` image ladder (`GLY9`); `pixels` is the top of the order (D34)  |
| `syncOutput`                | mode 2026 synchronized output                                     | DECRQM 2026                                                                    | `tui`           | frame emission (tear-free); required for smooth-scroll rungs           |
| `input.maxPointers`         | SGR 1006 button events ⇒ one pointer                              | `TermCaps.mouseSgr`                                                            | `tui`           | pointer tier (`INP`)                                                   |
| `input.hover`               | mode 1003 any-motion ⇒ hover                                      | `TermCaps.anyMotion` (DECRQM 1003)                                             | `tui`           | `hover` interaction state (`TOK4`)                                     |
| `input.precisePointer`      | mode 1016 pixel coordinates ⇒ sub-cell positions                  | `TermCaps.pixelMouse` (DECRQM 1016)                                            | `tui`           | sub-cell scrollbar grab                                                |
| `input.keyRelease`          | kitty keyboard protocol (CSI u) release/repeat                    | `TermCaps.kittyKeyboard` (`CSI ? u` query)                                     | `tui`           | held-key interactions (`INP16`)                                        |
| `input.focusEvents`         | mode 1004 ⇒ focus in/out events                                   | `TermCaps.focusReporting` (DECRQM 1004)                                        | `tui`           | blur dimming, `chromeFocused`                                          |
| `input.pasteEvents`         | mode 2004 ⇒ a paste is one event                                  | `TermCaps.bracketedPaste` (DECRQM 2004)                                        | `tui`           | text input (`WGT14`)                                                   |
| `colorSchemeNotify`         | mode 2031 / OSC 11 query                                          | DECRQM 2031; OSC 11 reply                                                      | `tui`           | scheme auto-select (deferred to `sparkles:appearance`)                 |
| `progress`                  | OSC 9;4 (ConEmu / Windows Terminal)                               | `$TERM_PROGRAM`, `$WT_SESSION`                                                 | `base`          | meter (`WGT19`) taskbar mirror                                         |
| `extendedUnderline`         | SGR 4:3 curly + 58 underline color                                | XTGETTCAP `Su`; `$TERM_PROGRAM`                                                | `tui`           | diagnostics undercurl, `link.underline`                                |
| `cellPixelSize`             | CSI 16 t answers                                                  | query                                                                          | `tui`           | image scaling, sub-cell geometry                                       |
| `graphemeClusters`          | mode 2027                                                         | DECRQM 2027                                                                    | `tui`           | width measurement policy (`GLY6`)                                      |
| `subCellScroll`             | scroll offsets finer than a cell                                  | GUI/Web constant                                                               | —               | `ScrollView` smooth scrolling                                          |
| `proportionalText`          | a non-monospace face for `FontRole.docs`                          | GUI/Web constant                                                               | —               | docs runs inside cell rects (`GLY7`)                                   |
| `radius`, `shadow`, `alpha` | sub-cell box chrome honoured                                      | GUI/Web constant                                                               | —               | `TOK7` honour-or-project declaration                                   |
| `input.tier`                | the interaction ladder                                            | a tty is `interactive`; a pipe `passive`                                       | `fromTerminal`  | unchanged                                                              |
| `reducedMotion`             | the user prefers no animation                                     | flag / env now; OS later                                                       | host            | `ACC5`                                                                 |

## Profiles

| Profile    | Intent                                                                | Flag values                                                                                                                                             |
| ---------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `baseline` | a pipe, `TERM=dumb`, a serial console: usable in monochrome and ASCII | `colorDepth = none`, `unicode = false`, everything else `false`/`none`; `tier = static`                                                                 |
| `enhanced` | any xterm-compatible emulator of the last decade                      | `ansi256`, `unicode`, `blocks = half`, `mouse`, `bracketedPaste`, `focusEvents`, `hyperlinks`; no images, no text sizing, no kitty keyboard             |
| `full`     | kitty / Ghostty / WezTerm / foot with a Nerd Font                     | `trueColor`, `blocks = octant`, `braille`, `nerdFont`, every OSC and mode row `true`, `images = kitty`, `syncOutput`, `keyRelease`, `extendedUnderline` |

## Emulator presets

Each preset is read by `fromReplies` (`CAP10`) from the rows recorded for it:
a row of the capability case study's
[empirical response matrix (§16)](https://github.com/PetarKirov/sparkles/blob/9ee7df44ef8870dea57925c057ca939a0764b502/docs/research/tui-libraries/capability-detection-case-study.md),
or a report of its probe checked in under `libs/ui/test/data/emulators/`
(foot, and tmux and zellij attached to a real emulator), which the tests
re-read against the rows. Every preset also has Unicode, the half
blocks and an SGR cell mouse with hover (D32); no preset has links, clipboard,
notifications, styled underlines, focus events, braille, octants or Nerd Font
icons, because no query answers them.

| Preset           | Measured                               | Color | Sync (2026) | Graphemes (2027) | Scheme (2031) | Images | Paste (2004) | Key release |
| ---------------- | -------------------------------------- | ----- | ----------- | ---------------- | ------------- | ------ | ------------ | ----------- |
| `xterm`          | XTerm 403, Linux                       | 24bit | —           | —                | —             | —      | ✓            | —           |
| `apple-terminal` | Apple Terminal, macOS 26.3             | 24bit | —           | —                | —             | —      | —            | —           |
| `iterm2`         | iTerm2 3.6.10                          | 24bit | ✓           | —                | ✓             | kitty  | ✓            | ✓           |
| `alacritty`      | Alacritty 0.16.1                       | 24bit | ✓           | —                | —             | —      | ✓            | ✓           |
| `foot`           | foot 1.25.0                            | 24bit | ✓           | ✓                | ✓             | sixel  | ✓            | ✓           |
| `wezterm`        | WezTerm 2025-10-14                     | 24bit | ✓           | ✓                | —             | kitty  | ✓            | —           |
| `kitty`          | kitty 0.44.0                           | 24bit | ✓           | —                | ✓             | kitty  | ✓            | ✓           |
| `ghostty`        | Ghostty 1.3.1, Linux, macOS            | 24bit | ✓           | ✓                | ✓             | kitty  | ✓            | ✓           |
| `tmux`           | tmux 3.6a: bare pty, foot, Ghostty     | 24bit | —           | —                | ✓             | —      | ✓            | —           |
| `zellij`         | zellij 0.45.1: bare pty, foot, Ghostty | 24bit | ✓           | —                | ✓             | —      | —            | ✓           |

A **multiplexer** answers for itself, and some of its answers follow the
terminal it is attached to (D33). tmux lists sixel in DA1 under every host —
Ghostty included, which draws no sixel — so under a multiplexer that attribute
is an advertisement, not a confirmed passthrough, and `CAP7` drops it. zellij
answers the kitty graphics query `OK` in Ghostty and refuses it in foot: a real
round trip, which counts. A multiplexer's preset is what holds under every host
it was measured in (their `meet`), so neither claims images.

Two matrix rows take no preset: the bare pty (no emulator — that is
`baseline`) and GNU screen 4.00.03, a 2006 build below the shared floor.
Windows Terminal and the Linux console are unmeasured; a new row is one
`query-probe.d --raw` run in the terminal.

GUI and HTML targets are not profiles; they declare their own constants
(`subCellScroll`, `proportionalText`, `radius`, `shadow`, `alpha` on; the
terminal-protocol rows meaningless and `false`).

→ [Overview](./index.md) · [Specification](./SPEC.md) · [Glyphs](./glyphs.md)

# Keyboard vocabulary (`KBD`)

_**Status:** proposed · **Date:** 2026-09-21 · **Owner:** `sparkles:ui`
(`keymap.d`), as one overlay table · **Scope:** the **minimal** set of
bindings every Sparkles application shares, so a user who learned one learns
all. Everything else stays application policy under [`KEY`](../ui/keymap.md)._

The machinery is [`KEY1`–`KEY12`](../ui/keymap.md); this page adds no
mechanism. It is one immutable table an application composes under its own
(`KEY12` overlay), and the rows below are the whole of it.

| ID     | Requirement                                                                                                                                                                                                                                                                                                                                                                                                           | Status      | Traces to                             |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------- |
| `KBD1` | The **universal rows** exist in every Sparkles application and mean the same thing: `q` / `Esc` close the innermost thing (overlay → pane → app, in that order); `?` opens the key guide (lantern); `Tab` / `Shift-Tab` cycle focus; `Enter` activates, `Space` toggles; `/` starts a search where one exists. An application may not rebind these to something else, only leave one unbound where it has no meaning. | partial     | `ui-gallery` `UGL8`/`UGL9`; hue `LMP` |
| `KBD2` | **Motion** rows: `h j k l` and the arrow keys are synonyms; `g g` / `G` go to start/end; `Ctrl-D` / `Ctrl-U` half-page; `Page Up/Down` whole page. A component that scrolls or moves a cursor honours all of them (`KEY9`: unnamed modifiers are ignored).                                                                                                                                                            | partial     | hue `NAV`; `ScrollView`               |
| `KBD3` | **Reserved keys are never bound** by a Sparkles application: `Ctrl-C` (interrupt — the host handles it), `Ctrl-Z`, `Ctrl-S`/`Ctrl-Q` (flow control), `Ctrl-\`. A table containing one of them fails the `KEY8`-style static check.                                                                                                                                                                                    | not started | `keymap.d` (proposed check)           |
| `KBD4` | **Footer hints** render bindings as `key description` pairs joined by `·`, keys in the guide's normalised spelling, in resolution order, and change with focus (`bindingsAt`). The format is the lantern's, so the footer and the guide never disagree.                                                                                                                                                               | partial     | `lantern.d`; hue status bar           |
| `KBD5` | **Text inputs** honour the Readline subset: `Ctrl-A`/`Ctrl-E` (line ends), `Ctrl-U`/`Ctrl-K` (kill to start/end), `Ctrl-W` (kill word), `Alt-B`/`Alt-F` (word motion). `Ctrl-I` and `Ctrl-M` are `Tab` and `Enter` unless `keyRelease` (kitty keyboard) disambiguates them.                                                                                                                                           | not started | `WGT14`; proposed `EDT`               |
| `KBD6` | A Sparkles application ships its **effective table** — universal rows, overlay, and its own — through one `--keys` (or equivalent) listing, so the guide, the docs and the binary agree by construction.                                                                                                                                                                                                              | not started | `bindingsAt`                          |

→ [Overview](./index.md) · [Keymap & lantern](../ui/keymap.md)
